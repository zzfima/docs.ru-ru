---
title: "Синхронизация потоков (C#)"
ms.custom: 
ms.date: 2015-07-20
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology:
- devlang-csharp
ms.topic: article
dev_langs:
- CSharp
ms.assetid: e42b1be6-c93c-479f-a148-be0759f1a4e1
caps.latest.revision: 3
author: BillWagner
ms.author: wiwagn
translation.priority.mt:
- cs-cz
- pl-pl
- pt-br
- tr-tr
ms.translationtype: HT
ms.sourcegitcommit: 306c608dc7f97594ef6f72ae0f5aaba596c936e1
ms.openlocfilehash: df4093d4bf777f904aa8ce376cd164ed822350a0
ms.contentlocale: ru-ru
ms.lasthandoff: 07/28/2017

---
# <a name="thread-synchronization-c"></a><span data-ttu-id="ebe95-102">Синхронизация потоков (C#)</span><span class="sxs-lookup"><span data-stu-id="ebe95-102">Thread Synchronization (C#)</span></span>
<span data-ttu-id="ebe95-103">В следующих разделах описаны функции и классы, которые можно использовать для синхронизации доступа к ресурсам в многопоточных приложениях.</span><span class="sxs-lookup"><span data-stu-id="ebe95-103">The following sections describe features and classes that can be used to synchronize access to resources in multithreaded applications.</span></span>  
  
 <span data-ttu-id="ebe95-104">Одним из преимуществ использования нескольких потоков в приложении является асинхронное выполнение каждого потока.</span><span class="sxs-lookup"><span data-stu-id="ebe95-104">One of the benefits of using multiple threads in an application is that each thread executes asynchronously.</span></span> <span data-ttu-id="ebe95-105">В приложениях Windows это позволяет выполнять длительные задачи в фоновом режиме, при этом окно приложения и элементы управления остаются активными.</span><span class="sxs-lookup"><span data-stu-id="ebe95-105">For Windows applications, this allows time-consuming tasks to be performed in the background while the application window and controls remain responsive.</span></span> <span data-ttu-id="ebe95-106">Для серверных приложений многопоточность обеспечивает возможность обработки каждого входящего запроса в отдельном потоке.</span><span class="sxs-lookup"><span data-stu-id="ebe95-106">For server applications, multithreading provides the ability to handle each incoming request with a different thread.</span></span> <span data-ttu-id="ebe95-107">В противном случае ни один новый запрос не будет обработан, пока не завершена обработка предыдущего запроса.</span><span class="sxs-lookup"><span data-stu-id="ebe95-107">Otherwise, each new request would not get serviced until the previous request had been fully satisfied.</span></span>  
  
 <span data-ttu-id="ebe95-108">Однако вследствие того, что потоки асинхронные, доступ к ресурсам, таким как дескрипторы файлов, сетевые подключения и память, должен быть скоординирован.</span><span class="sxs-lookup"><span data-stu-id="ebe95-108">However, the asynchronous nature of threads means that access to resources such as file handles, network connections, and memory must be coordinated.</span></span> <span data-ttu-id="ebe95-109">Иначе два или более потоков могут получить доступ к одному и тому же ресурсу одновременно, причем один поток не будет учитывать действия другого.</span><span class="sxs-lookup"><span data-stu-id="ebe95-109">Otherwise, two or more threads could access the same resource at the same time, each unaware of the other's actions.</span></span> <span data-ttu-id="ebe95-110">В результате данные могут быть повреждены непредсказуемым образом.</span><span class="sxs-lookup"><span data-stu-id="ebe95-110">The result is unpredictable data corruption.</span></span>  
  
 <span data-ttu-id="ebe95-111">Для простых операций над числовыми типами данных синхронизация потоков выполняется с помощью членов класса <xref:System.Threading.Interlocked>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-111">For simple operations on integral numeric data types, synchronizing threads can be accomplished with members of the <xref:System.Threading.Interlocked> class.</span></span> <span data-ttu-id="ebe95-112">Для прочих типов данных и других ресурсов, не являющихся потокобезопасными, многопоточность можно применять только с помощью структур, описываемых в этом разделе.</span><span class="sxs-lookup"><span data-stu-id="ebe95-112">For all other data types and non thread-safe resources, multithreading can only be safely performed using the constructs in this topic.</span></span>  
  
 <span data-ttu-id="ebe95-113">Дополнительные сведения о многопоточном программировании см. в разделах:</span><span class="sxs-lookup"><span data-stu-id="ebe95-113">For background information on multithreaded programming, see:</span></span>  
  
-   [<span data-ttu-id="ebe95-114">Основы управляемых потоков</span><span class="sxs-lookup"><span data-stu-id="ebe95-114">Managed Threading Basics</span></span>](../../../../standard/threading/managed-threading-basics.md)  
  
-   [<span data-ttu-id="ebe95-115">Использование потоков и работа с потоками</span><span class="sxs-lookup"><span data-stu-id="ebe95-115">Using Threads and Threading</span></span>](../../../../standard/threading/using-threads-and-threading.md)  
  
-   [<span data-ttu-id="ebe95-116">Рекомендации по работе с потоками</span><span class="sxs-lookup"><span data-stu-id="ebe95-116">Managed Threading Best Practices</span></span>](../../../../standard/threading/managed-threading-best-practices.md)  
  
## <a name="the-lock-keyword"></a><span data-ttu-id="ebe95-117">Ключевое слово lock</span><span class="sxs-lookup"><span data-stu-id="ebe95-117">The lock Keyword</span></span>  
 <span data-ttu-id="ebe95-118">Оператор C# `lock` используется для того, чтобы выполнение блока кода не прерывалось кодом, выполняемым в других потоках.</span><span class="sxs-lookup"><span data-stu-id="ebe95-118">The C# `lock` statement can be used to ensure that a block of code runs to completion without interruption by other threads.</span></span> <span data-ttu-id="ebe95-119">Для этого нужно получить взаимоисключающую блокировку для данного объекта на время длительности блока кода.</span><span class="sxs-lookup"><span data-stu-id="ebe95-119">This is accomplished by obtaining a mutual-exclusion lock for a given object for the duration of the code block.</span></span>  
  
 <span data-ttu-id="ebe95-120">Оператор `lock`получает объект в качестве аргумента, и за ним следует блок кода, который должен выполняться одновременно только в одном потоке.</span><span class="sxs-lookup"><span data-stu-id="ebe95-120">A `lock` statement is given an object as an argument, and is followed by a code block that is to be executed by only one thread at a time.</span></span> <span data-ttu-id="ebe95-121">Пример:</span><span class="sxs-lookup"><span data-stu-id="ebe95-121">For example:</span></span>  
  
```csharp  
public class TestThreading  
{  
    private System.Object lockThis = new System.Object();  
  
    public void Process()  
    {  
  
        lock (lockThis)  
        {  
            // Access thread-sensitive resources.  
        }  
    }  
  
}  
```  
  
 <span data-ttu-id="ebe95-122">Аргумент, предоставляемый ключевому слову `lock`, должен быть объектом на основе ссылочного типа; он используется для определения области блокировки.</span><span class="sxs-lookup"><span data-stu-id="ebe95-122">The argument provided to the `lock` keyword must be an object based on a reference type, and is used to define the scope of the lock.</span></span> <span data-ttu-id="ebe95-123">В приведенном выше примере область блокировки ограничена этой функцией, поскольку не существует ссылок на объект `lockThis` вне функции.</span><span class="sxs-lookup"><span data-stu-id="ebe95-123">In the example above, the lock scope is limited to this function because no references to the object `lockThis` exist outside the function.</span></span> <span data-ttu-id="ebe95-124">Если бы такая ссылка существовала, область блокировки включала бы этот объект.</span><span class="sxs-lookup"><span data-stu-id="ebe95-124">If such a reference did exist, lock scope would extend to that object.</span></span> <span data-ttu-id="ebe95-125">Строго говоря, предоставляемый объект используется только для того, чтобы уникальным образом определить ресурс, к которому предоставляется доступ для различных потоков, поэтому это может быть произвольный экземпляр класса.</span><span class="sxs-lookup"><span data-stu-id="ebe95-125">Strictly speaking, the object provided is used solely to uniquely identify the resource being shared among multiple threads, so it can be an arbitrary class instance.</span></span> <span data-ttu-id="ebe95-126">В действительности этот объект обычно представляет ресурс, для которого требуется синхронизация потоков.</span><span class="sxs-lookup"><span data-stu-id="ebe95-126">In practice, however, this object usually represents the resource for which thread synchronization is necessary.</span></span> <span data-ttu-id="ebe95-127">Например, если объект контейнера должен использоваться в нескольких потоках, то контейнер можно передать блокировке, а блок синхронизированного кода после блокировки должен получить доступ к контейнеру.</span><span class="sxs-lookup"><span data-stu-id="ebe95-127">For example, if a container object is to be used by multiple threads, then the container can be passed to lock, and the synchronized code block following the lock would access the container.</span></span> <span data-ttu-id="ebe95-128">Если другие потоки блокируются для того же контейнера перед доступом к нему, обеспечивается безопасная синхронизация доступа к объекту.</span><span class="sxs-lookup"><span data-stu-id="ebe95-128">As long as other threads locks on the same contain before accessing it, then access to the object is safely synchronized.</span></span>  
  
 <span data-ttu-id="ebe95-129">Как правило, рекомендуется избегать блокировки типа `public` или экземпляров объектов, которыми не управляет код вашего приложения.</span><span class="sxs-lookup"><span data-stu-id="ebe95-129">Generally, it is best to avoid locking on a `public` type, or on object instances beyond the control of your application.</span></span> <span data-ttu-id="ebe95-130">Например, использование `lock(this)` может привести к неполадкам, если к экземпляру разрешен открытый доступ, поскольку внешний код также может блокировать объект.</span><span class="sxs-lookup"><span data-stu-id="ebe95-130">For example, `lock(this)` can be problematic if the instance can be accessed publicly, because code beyond your control may lock on the object as well.</span></span> <span data-ttu-id="ebe95-131">Это может привести к созданию ситуаций взаимной блокировки, когда два или несколько потоков будут ожидать высвобождения одного и того же объекта.</span><span class="sxs-lookup"><span data-stu-id="ebe95-131">This could create deadlock situations where two or more threads wait for the release of the same object.</span></span> <span data-ttu-id="ebe95-132">По этой же причине блокировка открытого типа данных (в отличие от объектов) может привести к неполадкам.</span><span class="sxs-lookup"><span data-stu-id="ebe95-132">Locking on a public data type, as opposed to an object, can cause problems for the same reason.</span></span> <span data-ttu-id="ebe95-133">Блокировка строковых литералов наиболее опасна, поскольку строковые литералы *интернируются* средой CLR.</span><span class="sxs-lookup"><span data-stu-id="ebe95-133">Locking on literal strings is especially risky because literal strings are *interned* by the common language runtime (CLR).</span></span> <span data-ttu-id="ebe95-134">Это означает, что если во всей программе есть один экземпляр любого строкового литерала, точно такой же объект будет представлять литерал во всех запущенных доменах приложения и во всех потоках.</span><span class="sxs-lookup"><span data-stu-id="ebe95-134">This means that there is one instance of any given string literal for the entire program, the exact same object represents the literal in all running application domains, on all threads.</span></span> <span data-ttu-id="ebe95-135">В результате блокировка, включенная для строки с одинаковым содержимым во всем приложении, блокирует все экземпляры этой строки в приложении.</span><span class="sxs-lookup"><span data-stu-id="ebe95-135">As a result, a lock placed on a string with the same contents anywhere in the application process locks all instances of that string in the application.</span></span> <span data-ttu-id="ebe95-136">По этой причине лучше использовать блокировку закрытых или защищенных членов, для которых интернирование не применяется.</span><span class="sxs-lookup"><span data-stu-id="ebe95-136">As a result, it is best to lock a private or protected member that is not interned.</span></span> <span data-ttu-id="ebe95-137">В некоторых классах есть члены, специально предназначенные для блокировки.</span><span class="sxs-lookup"><span data-stu-id="ebe95-137">Some classes provide members specifically for locking.</span></span> <span data-ttu-id="ebe95-138">Например, тип <xref:System.Array> предоставляет <xref:System.Array.SyncRoot%2A>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-138">The <xref:System.Array> type, for example, provides <xref:System.Array.SyncRoot%2A>.</span></span> <span data-ttu-id="ebe95-139">Во многих типах коллекций есть член `SyncRoot`.</span><span class="sxs-lookup"><span data-stu-id="ebe95-139">Many collection types provide a `SyncRoot` member as well.</span></span>  
  
 <span data-ttu-id="ebe95-140">Дополнительные сведения об операторе `lock` см. в следующих разделах.</span><span class="sxs-lookup"><span data-stu-id="ebe95-140">For more information about the `lock` statement, see the following topics:</span></span>  
  
-   [<span data-ttu-id="ebe95-141">Оператор lock</span><span class="sxs-lookup"><span data-stu-id="ebe95-141">lock Statement</span></span>](../../../../csharp/language-reference/keywords/lock-statement.md)  
  
-   @System.Threading.Monitor  
  
## <a name="monitors"></a><span data-ttu-id="ebe95-142">Мониторы</span><span class="sxs-lookup"><span data-stu-id="ebe95-142">Monitors</span></span>  
 <span data-ttu-id="ebe95-143">Как и ключевое слово `lock`, мониторы не допускают одновременное выполнение несколькими потоками одних и тех же блоков кода.</span><span class="sxs-lookup"><span data-stu-id="ebe95-143">Like the `lock` keyword, monitors prevent blocks of code from simultaneous execution by multiple threads.</span></span> <span data-ttu-id="ebe95-144">Метод <xref:System.Threading.Monitor.Enter%2A> позволяет переходить к последующим операторам только одному потоку; все прочие потоки будут заблокированы, пока выполняемый поток не вызовет <xref:System.Threading.Monitor.Exit%2A>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-144">The <xref:System.Threading.Monitor.Enter%2A> method allows one and only one thread to proceed into the following statements; all other threads are blocked until the executing thread calls <xref:System.Threading.Monitor.Exit%2A>.</span></span> <span data-ttu-id="ebe95-145">Это аналогично использованию ключевого слова `lock`.</span><span class="sxs-lookup"><span data-stu-id="ebe95-145">This is just like using the `lock` keyword.</span></span> <span data-ttu-id="ebe95-146">Пример:</span><span class="sxs-lookup"><span data-stu-id="ebe95-146">For example:</span></span>  
  
```csharp  
lock (x)  
{  
    DoSomething();  
}  
```  
  
 <span data-ttu-id="ebe95-147">Это соответствует следующей записи:</span><span class="sxs-lookup"><span data-stu-id="ebe95-147">This is equivalent to:</span></span>  
  
```csharp  
System.Object obj = (System.Object)x;  
System.Threading.Monitor.Enter(obj);  
try  
{  
    DoSomething();  
}  
finally  
{  
    System.Threading.Monitor.Exit(obj);  
}  
```  
  
 <span data-ttu-id="ebe95-148">Использование ключевого слова `lock` предпочтительнее прямого использования класса <xref:System.Threading.Monitor>, поскольку `lock` лаконичнее, а также потому, что `lock` обеспечивает разблокировку основного монитора даже в том случае, если защищенный код вызывает исключение.</span><span class="sxs-lookup"><span data-stu-id="ebe95-148">Using the `lock` keyword is generally preferred over using the <xref:System.Threading.Monitor> class directly, both because `lock` is more concise, and because `lock` insures that the underlying monitor is released, even if the protected code throws an exception.</span></span> <span data-ttu-id="ebe95-149">Для этого применяется ключевое слово `finally`, которые выполняет свой блок кода вне зависимости от наличия исключений.</span><span class="sxs-lookup"><span data-stu-id="ebe95-149">This is accomplished with the `finally` keyword, which executes its associated code block regardless of whether an exception is thrown.</span></span>  
  
## <a name="synchronization-events-and-wait-handles"></a><span data-ttu-id="ebe95-150">События синхронизации и дескрипторы ожидания</span><span class="sxs-lookup"><span data-stu-id="ebe95-150">Synchronization Events and Wait Handles</span></span>  
 <span data-ttu-id="ebe95-151">Использование блокировки или монитора полезно для предотвращения одновременного выполнения блоков кода, но эти структуры не позволяют одному потоку передавать события в другой.</span><span class="sxs-lookup"><span data-stu-id="ebe95-151">Using a lock or monitor is useful for preventing the simultaneous execution of thread-sensitive blocks of code, but these constructs do not allow one thread to communicate an event to another.</span></span> <span data-ttu-id="ebe95-152">Для этого требуются *события синхронизации* — объекты, обладающие одним из двух состояний (с сигналом или без сигнала), применяющиеся для активации и приостановки потоков.</span><span class="sxs-lookup"><span data-stu-id="ebe95-152">This requires *synchronization events*, which are objects that have one of two states, signaled and un-signaled, that can be used to activate and suspend threads.</span></span> <span data-ttu-id="ebe95-153">Потоки можно приостанавливать, заставляя их ожидать события синхронизации без сигнала, и активировать, меняя состояние события на состояние с сигналом.</span><span class="sxs-lookup"><span data-stu-id="ebe95-153">Threads can be suspended by being made to wait on a synchronization event that is unsignaled, and can be activated by changing the event state to signaled.</span></span> <span data-ttu-id="ebe95-154">Если поток попытается ожидать события, для которого уже есть сигнал, то выполнение потока продолжится без задержки.</span><span class="sxs-lookup"><span data-stu-id="ebe95-154">If a thread attempts to wait on an event that is already signaled, then the thread continues to execute without delay.</span></span>  
  
 <span data-ttu-id="ebe95-155">События синхронизации делятся на два типа: <xref:System.Threading.AutoResetEvent> и <xref:System.Threading.ManualResetEvent>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-155">There are two kinds of synchronization events: <xref:System.Threading.AutoResetEvent>, and <xref:System.Threading.ManualResetEvent>.</span></span> <span data-ttu-id="ebe95-156">Отличие только одно: при каждой активации потока <xref:System.Threading.AutoResetEvent> автоматически переходит из состояния с сигналом в состояние без сигнала.</span><span class="sxs-lookup"><span data-stu-id="ebe95-156">They differ only in that <xref:System.Threading.AutoResetEvent> changes from signaled to unsignaled automatically any time it activates a thread.</span></span> <span data-ttu-id="ebe95-157"><xref:System.Threading.ManualResetEvent>, наоборот, допускает активацию любого количества потоков при переходе в состояние с сигналом и возвращается в состояние без сигнала только при вызове метода <xref:System.Threading.EventWaitHandle.Reset%2A>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-157">Conversely, a <xref:System.Threading.ManualResetEvent> allows any number of threads to be activated by its signaled state, and will only revert to an unsignaled state when its <xref:System.Threading.EventWaitHandle.Reset%2A> method is called.</span></span>  
  
 <span data-ttu-id="ebe95-158">Для того чтобы потоки ожидали событий, можно вызвать один из методов ожидания, таких как <xref:System.Threading.WaitHandle.WaitOne%2A>, <xref:System.Threading.WaitHandle.WaitAny%2A> или <xref:System.Threading.WaitHandle.WaitAll%2A>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-158">Threads can be made to wait on events by calling one of the wait methods, such as <xref:System.Threading.WaitHandle.WaitOne%2A>, <xref:System.Threading.WaitHandle.WaitAny%2A>, or <xref:System.Threading.WaitHandle.WaitAll%2A>.</span></span> <span data-ttu-id="ebe95-159">Метод <xref:System.Threading.WaitHandle.WaitOne%2A?displayProperty=fullName> заставляет поток дожидаться сигнала о единичном событии, метод <xref:System.Threading.WaitHandle.WaitAny%2A?displayProperty=fullName> блокирует поток до получения сигналов об одном или нескольких указанных событиях, а метод <xref:System.Threading.WaitHandle.WaitAll%2A?displayProperty=fullName> блокирует поток до тех пор, пока не будут получены сигналы обо всех указанных событиях.</span><span class="sxs-lookup"><span data-stu-id="ebe95-159"><xref:System.Threading.WaitHandle.WaitOne%2A?displayProperty=fullName> causes the thread to wait until a single event becomes signaled, <xref:System.Threading.WaitHandle.WaitAny%2A?displayProperty=fullName> blocks a thread until one or more indicated events become signaled, and <xref:System.Threading.WaitHandle.WaitAll%2A?displayProperty=fullName> blocks the thread until all of the indicated events become signaled.</span></span> <span data-ttu-id="ebe95-160">Сигнал о событии подается, когда вызывается его метод <xref:System.Threading.EventWaitHandle.Set%2A>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-160">An event becomes signaled when its <xref:System.Threading.EventWaitHandle.Set%2A> method is called.</span></span>  
  
 <span data-ttu-id="ebe95-161">В следующем примере поток создается и запускается функцией `Main`.</span><span class="sxs-lookup"><span data-stu-id="ebe95-161">In the following example, a thread is created and started by the `Main` function.</span></span> <span data-ttu-id="ebe95-162">Новый поток ожидает события за счет метода <xref:System.Threading.WaitHandle.WaitOne%2A>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-162">The new thread waits on an event using the <xref:System.Threading.WaitHandle.WaitOne%2A> method.</span></span> <span data-ttu-id="ebe95-163">Выполнение потока приостанавливается до получения сигнала от события основным потоком, выполняющим функцию `Main`.</span><span class="sxs-lookup"><span data-stu-id="ebe95-163">The thread is suspended until the event becomes signaled by the primary thread that is executing the `Main` function.</span></span> <span data-ttu-id="ebe95-164">После получения сигнала возвращается дополнительный поток.</span><span class="sxs-lookup"><span data-stu-id="ebe95-164">Once the event becomes signaled, the auxiliary thread returns.</span></span> <span data-ttu-id="ebe95-165">Поскольку в этом случае событие используется для активации только одного потока, можно использовать либо класс <xref:System.Threading.AutoResetEvent>, либо класс <xref:System.Threading.ManualResetEvent>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-165">In this case, because the event is only used for one thread activation, either the <xref:System.Threading.AutoResetEvent> or <xref:System.Threading.ManualResetEvent> classes could be used.</span></span>  
  
```csharp  
using System;  
using System.Threading;  
  
class ThreadingExample  
{  
    static AutoResetEvent autoEvent;  
  
    static void DoWork()  
    {  
        Console.WriteLine("   worker thread started, now waiting on event...");  
        autoEvent.WaitOne();  
        Console.WriteLine("   worker thread reactivated, now exiting...");  
    }  
  
    static void Main()  
    {  
        autoEvent = new AutoResetEvent(false);  
  
        Console.WriteLine("main thread starting worker thread...");  
        Thread t = new Thread(DoWork);  
        t.Start();  
  
        Console.WriteLine("main thread sleeping for 1 second...");  
        Thread.Sleep(1000);  
  
        Console.WriteLine("main thread signaling worker thread...");  
        autoEvent.Set();  
    }  
}  
```  
  
## <a name="mutex-object"></a><span data-ttu-id="ebe95-166">Мьютексные объекты</span><span class="sxs-lookup"><span data-stu-id="ebe95-166">Mutex Object</span></span>  
 <span data-ttu-id="ebe95-167">*Мьютекс* аналогичен монитору, он не допускает одновременного выполнения блока кода более чем из одного потока.</span><span class="sxs-lookup"><span data-stu-id="ebe95-167">A *mutex* is similar to a monitor; it prevents the simultaneous execution of a block of code by more than one thread at a time.</span></span> <span data-ttu-id="ebe95-168">Название "мьютекс" — сокращенная форма слова "взаимоисключающий" ("mutually exclusive" на английском языке).</span><span class="sxs-lookup"><span data-stu-id="ebe95-168">In fact, the name "mutex" is a shortened form of the term "mutually exclusive."</span></span> <span data-ttu-id="ebe95-169">Впрочем, в отличие от мониторов мьютексы можно использовать для синхронизации потоков по процессам.</span><span class="sxs-lookup"><span data-stu-id="ebe95-169">Unlike monitors, however, a mutex can be used to synchronize threads across processes.</span></span> <span data-ttu-id="ebe95-170">Мьютексы представляет класс <xref:System.Threading.Mutex>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-170">A mutex is represented by the <xref:System.Threading.Mutex> class.</span></span>  
  
 <span data-ttu-id="ebe95-171">При использовании для синхронизации между процессами мьютекс называется *именованным мьютексом*, поскольку он должен использоваться в другом приложении и к нему нельзя предоставить общий доступ с помощью глобальной или статической переменной.</span><span class="sxs-lookup"><span data-stu-id="ebe95-171">When used for inter-process synchronization, a mutex is called a *named mutex* because it is to be used in another application, and therefore it cannot be shared by means of a global or static variable.</span></span> <span data-ttu-id="ebe95-172">Ему нужно назначить имя, чтобы оба приложения могли получить доступ к одному и тому же объекту мьютекса.</span><span class="sxs-lookup"><span data-stu-id="ebe95-172">It must be given a name so that both applications can access the same mutex object.</span></span>  
  
 <span data-ttu-id="ebe95-173">Несмотря на то, что для синхронизации потоков внутри процесса можно использовать мьютекс, рекомендуется использовать <xref:System.Threading.Monitor>, поскольку мониторы были созданы специально для .NET Framework и более эффективно используют ресурсы.</span><span class="sxs-lookup"><span data-stu-id="ebe95-173">Although a mutex can be used for intra-process thread synchronization, using <xref:System.Threading.Monitor> is generally preferred, because monitors were designed specifically for the .NET Framework and therefore make better use of resources.</span></span> <span data-ttu-id="ebe95-174">Напротив, класс <xref:System.Threading.Mutex> является оболочкой для структуры Win32.</span><span class="sxs-lookup"><span data-stu-id="ebe95-174">In contrast, the <xref:System.Threading.Mutex> class is a wrapper to a Win32 construct.</span></span> <span data-ttu-id="ebe95-175">Мьютекс мощнее монитора, но для мьютекса требуются переходы взаимодействия, на которые затрачивается больше вычислительных ресурсов, чем на обработку класса <xref:System.Threading.Monitor>.</span><span class="sxs-lookup"><span data-stu-id="ebe95-175">While it is more powerful than a monitor, a mutex requires interop transitions that are more computationally expensive than those required by the <xref:System.Threading.Monitor> class.</span></span> <span data-ttu-id="ebe95-176">Пример использования мьютекса см. в разделе [Объекты Mutex](../../../../standard/threading/mutexes.md).</span><span class="sxs-lookup"><span data-stu-id="ebe95-176">For an example of using a mutex, see [Mutexes](../../../../standard/threading/mutexes.md).</span></span>  
  
## <a name="interlocked-class"></a><span data-ttu-id="ebe95-177">Класс Interlocked</span><span class="sxs-lookup"><span data-stu-id="ebe95-177">Interlocked Class</span></span>  
 <span data-ttu-id="ebe95-178">Методы класса <xref:System.Threading.Interlocked> можно использовать для предотвращения проблем, возникающих, когда несколько потоков пытаются обновить или сравнить какое-то значение одновременно.</span><span class="sxs-lookup"><span data-stu-id="ebe95-178">You can use the methods of the <xref:System.Threading.Interlocked> class to prevent problems that can occur when multiple threads attempt to simultaneously update or compare the same value.</span></span> <span data-ttu-id="ebe95-179">Методы этого класса позволяют безопасно увеличивать, уменьшать, заменять и сравнивать значения переменных из любого потока.</span><span class="sxs-lookup"><span data-stu-id="ebe95-179">The methods of this class let you safely increment, decrement, exchange, and compare values from any thread.</span></span>  
  
## <a name="readerwriter-locks"></a><span data-ttu-id="ebe95-180">Блокировки чтения и записи</span><span class="sxs-lookup"><span data-stu-id="ebe95-180">ReaderWriter Locks</span></span>  
 <span data-ttu-id="ebe95-181">В некоторых случаях может понадобиться блокировать ресурс только для записи данных и разрешить нескольким клиентам одновременно считывать данные, когда они не обновляются.</span><span class="sxs-lookup"><span data-stu-id="ebe95-181">In some cases, you may want to lock a resource only when data is being written and permit multiple clients to simultaneously read data when data is not being updated.</span></span> <span data-ttu-id="ebe95-182">Класс <xref:System.Threading.ReaderWriterLock> обеспечивает монопольный доступ к ресурсу на то время, в течение которого поток изменяет ресурс, но разрешает одновременно выполнять несколько операций чтения.</span><span class="sxs-lookup"><span data-stu-id="ebe95-182">The <xref:System.Threading.ReaderWriterLock> class enforces exclusive access to a resource while a thread is modifying the resource, but it allows non-exclusive access when reading the resource.</span></span> <span data-ttu-id="ebe95-183">Блокировки чтения и записи являются удобной альтернативой монопольным блокировкам, которые заставляют другие потоки находиться в состоянии ожидания, даже когда им не нужно обновлять данные.</span><span class="sxs-lookup"><span data-stu-id="ebe95-183">ReaderWriter locks are a useful alternative to exclusive locks, which cause other threads to wait, even when those threads do not need to update data.</span></span>  
  
## <a name="deadlocks"></a><span data-ttu-id="ebe95-184">Взаимоблокировки</span><span class="sxs-lookup"><span data-stu-id="ebe95-184">Deadlocks</span></span>  
 <span data-ttu-id="ebe95-185">В многопоточных приложениях не обойтись без синхронизации потоков, однако всегда существует опасность создания `deadlock`, когда несколько потоков ожидают друг друга, и приложение зависает.</span><span class="sxs-lookup"><span data-stu-id="ebe95-185">Thread synchronization is invaluable in multithreaded applications, but there is always the danger of creating a `deadlock`, where multiple threads are waiting for each other and the application comes to a halt.</span></span> <span data-ttu-id="ebe95-186">Взаимоблокировка аналогична ситуации, в которой автомобили останавливаются на перекрестке и каждый водитель ожидает, пока проедет другой.</span><span class="sxs-lookup"><span data-stu-id="ebe95-186">A deadlock is analogous to a situation in which cars are stopped at a four-way stop and each person is waiting for the other to go.</span></span> <span data-ttu-id="ebe95-187">Важно исключить возможность взаимоблокировок путем тщательного планирования.</span><span class="sxs-lookup"><span data-stu-id="ebe95-187">Avoiding deadlocks is important; the key is careful planning.</span></span> <span data-ttu-id="ebe95-188">Взаимоблокировки часто можно предвидеть еще до написания кода, построив диаграмму многопотокового приложения.</span><span class="sxs-lookup"><span data-stu-id="ebe95-188">You can often predict deadlock situations by diagramming multithreaded applications before you start coding.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ebe95-189">См. также</span><span class="sxs-lookup"><span data-stu-id="ebe95-189">See Also</span></span>  
 <span data-ttu-id="ebe95-190"><xref:System.Threading.Thread></span><span class="sxs-lookup"><span data-stu-id="ebe95-190"><xref:System.Threading.Thread></span></span>   
 <span data-ttu-id="ebe95-191"><xref:System.Threading.WaitHandle.WaitOne%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-191"><xref:System.Threading.WaitHandle.WaitOne%2A></span></span>   
 <span data-ttu-id="ebe95-192"><xref:System.Threading.WaitHandle.WaitAny%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-192"><xref:System.Threading.WaitHandle.WaitAny%2A></span></span>   
 <span data-ttu-id="ebe95-193"><xref:System.Threading.WaitHandle.WaitAll%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-193"><xref:System.Threading.WaitHandle.WaitAll%2A></span></span>   
 <span data-ttu-id="ebe95-194"><xref:System.Threading.Thread.Join%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-194"><xref:System.Threading.Thread.Join%2A></span></span>   
 <span data-ttu-id="ebe95-195"><xref:System.Threading.Thread.Start%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-195"><xref:System.Threading.Thread.Start%2A></span></span>   
 <span data-ttu-id="ebe95-196"><xref:System.Threading.Thread.Sleep%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-196"><xref:System.Threading.Thread.Sleep%2A></span></span>   
 <span data-ttu-id="ebe95-197"><xref:System.Threading.Monitor></span><span class="sxs-lookup"><span data-stu-id="ebe95-197"><xref:System.Threading.Monitor></span></span>   
 <span data-ttu-id="ebe95-198"><xref:System.Threading.Mutex></span><span class="sxs-lookup"><span data-stu-id="ebe95-198"><xref:System.Threading.Mutex></span></span>   
 <span data-ttu-id="ebe95-199"><xref:System.Threading.AutoResetEvent></span><span class="sxs-lookup"><span data-stu-id="ebe95-199"><xref:System.Threading.AutoResetEvent></span></span>   
 <span data-ttu-id="ebe95-200"><xref:System.Threading.ManualResetEvent></span><span class="sxs-lookup"><span data-stu-id="ebe95-200"><xref:System.Threading.ManualResetEvent></span></span>   
 <span data-ttu-id="ebe95-201"><xref:System.Threading.Interlocked></span><span class="sxs-lookup"><span data-stu-id="ebe95-201"><xref:System.Threading.Interlocked></span></span>   
 <span data-ttu-id="ebe95-202"><xref:System.Threading.WaitHandle></span><span class="sxs-lookup"><span data-stu-id="ebe95-202"><xref:System.Threading.WaitHandle></span></span>   
 <span data-ttu-id="ebe95-203"><xref:System.Threading.EventWaitHandle></span><span class="sxs-lookup"><span data-stu-id="ebe95-203"><xref:System.Threading.EventWaitHandle></span></span>   
 <span data-ttu-id="ebe95-204"><xref:System.Threading></span><span class="sxs-lookup"><span data-stu-id="ebe95-204"><xref:System.Threading></span></span>   
 <span data-ttu-id="ebe95-205"><xref:System.Threading.EventWaitHandle.Set%2A></span><span class="sxs-lookup"><span data-stu-id="ebe95-205"><xref:System.Threading.EventWaitHandle.Set%2A></span></span>   
 <span data-ttu-id="ebe95-206">[Многопоточные приложения(C#)](../../../../csharp/programming-guide/concepts/threading/multithreaded-applications.md) </span><span class="sxs-lookup"><span data-stu-id="ebe95-206">[Multithreaded Applications (C#)](../../../../csharp/programming-guide/concepts/threading/multithreaded-applications.md) </span></span>  
 <span data-ttu-id="ebe95-207">[Оператор lock](../../../../csharp/language-reference/keywords/lock-statement.md) </span><span class="sxs-lookup"><span data-stu-id="ebe95-207">[lock Statement](../../../../csharp/language-reference/keywords/lock-statement.md) </span></span>  
 <span data-ttu-id="ebe95-208">[Объекты Mutex](../../../../standard/threading/mutexes.md) </span><span class="sxs-lookup"><span data-stu-id="ebe95-208">[Mutexes](../../../../standard/threading/mutexes.md) </span></span>  
 <span data-ttu-id="ebe95-209">@System.Threading.Monitor</span><span class="sxs-lookup"><span data-stu-id="ebe95-209">@System.Threading.Monitor</span></span>   
 <span data-ttu-id="ebe95-210">[Блокируемые операции](../../../../standard/threading/interlocked-operations.md) </span><span class="sxs-lookup"><span data-stu-id="ebe95-210">[Interlocked Operations](../../../../standard/threading/interlocked-operations.md) </span></span>  
 <span data-ttu-id="ebe95-211">[AutoResetEvent](../../../../standard/threading/autoresetevent.md) </span><span class="sxs-lookup"><span data-stu-id="ebe95-211">[AutoResetEvent](../../../../standard/threading/autoresetevent.md) </span></span>  
 [<span data-ttu-id="ebe95-212">Синхронизация данных для многопоточности</span><span class="sxs-lookup"><span data-stu-id="ebe95-212">Synchronizing Data for Multithreading</span></span>](../../../../standard/threading/synchronizing-data-for-multithreading.md)

