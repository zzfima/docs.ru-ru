---
title: Предупреждение компилятора (уровень 3) CS1718
ms.date: 07/20/2015
f1_keywords:
- CS1718
helpviewer_keywords:
- CS1718
ms.assetid: 7c1c11fd-4f91-482d-b8f7-efe2a361634e
ms.openlocfilehash: e342fc809e12e6a71557b1ec6442d6eac6d790c4
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/12/2020
ms.locfileid: "79170804"
---
# <a name="compiler-warning-level-3-cs1718"></a><span data-ttu-id="d3864-102">Предупреждение компилятора (уровень 3) CS1718</span><span class="sxs-lookup"><span data-stu-id="d3864-102">Compiler Warning (level 3) CS1718</span></span>
<span data-ttu-id="d3864-103">Сравнение выполнено с той же переменной. Действительно следует выполнять такое сравнение?</span><span class="sxs-lookup"><span data-stu-id="d3864-103">Comparison made to same variable; did you mean to compare something else?</span></span>  
  
 <span data-ttu-id="d3864-104">Если предполагалось выполнить сравнение с другим элементом, следует просто исправить оператор.</span><span class="sxs-lookup"><span data-stu-id="d3864-104">If you meant to compare to something else, then you should simply correct the statement.</span></span>  
  
 <span data-ttu-id="d3864-105">Однако может быть и так, что вы выполняли проверку на возвращение значения true или false и для этого использовали такие операторы, как `if (a == a) (true)` или `if (a < a) (false)`.</span><span class="sxs-lookup"><span data-stu-id="d3864-105">But another possibility is that you were testing for true or false, and were doing so by statements such as `if (a == a) (true)` or `if (a < a) (false)`.</span></span> <span data-ttu-id="d3864-106">Удобнее использовать более простые операторы `if (true)` и `if (false)`.</span><span class="sxs-lookup"><span data-stu-id="d3864-106">It is better to simply say `if (true)` or `if (false)`.</span></span> <span data-ttu-id="d3864-107">Для этого имеются две причины:</span><span class="sxs-lookup"><span data-stu-id="d3864-107">There are two reasons for this:</span></span>  
  
- <span data-ttu-id="d3864-108">Последние операторы проще: всегда лучше написать ровно столько, сколько нужно.</span><span class="sxs-lookup"><span data-stu-id="d3864-108">It is simpler: it is always clearer to simply say what you mean.</span></span>  
  
- <span data-ttu-id="d3864-109">Использование этих операторов позволяет избежать неоднозначности: в версии C# 2.0 введена новая функция — типы значений, допускающие значение NULL, которые являются аналогом значения `null` в T-SQL — языке программирования, используемом в SQL Server.</span><span class="sxs-lookup"><span data-stu-id="d3864-109">It helps avoid confusion: a new feature of C# 2.0 is nullable value types, which are analogous to the value `null` in T-SQL, the programming language used by SQL Server.</span></span> <span data-ttu-id="d3864-110">Разработчики, знакомые с языком T-SQL, могут усмотреть проблему в использовании типов, допускающих значение NULL, в таких выражениях, как `if (a == a)`, из-за применения троичной логики в T-SQL.</span><span class="sxs-lookup"><span data-stu-id="d3864-110">Developers familiar with T-SQL might be concerned about the effect of nullable types on expressions such as `if (a == a)`, because of the use of ternary logic in T-SQL.</span></span> <span data-ttu-id="d3864-111">При использовании значения `true` или `false`можно устранить возможную неоднозначность.</span><span class="sxs-lookup"><span data-stu-id="d3864-111">If you use `true` or `false`, you avoid this possible confusion.</span></span>  
  
## <a name="example"></a><span data-ttu-id="d3864-112">Пример</span><span class="sxs-lookup"><span data-stu-id="d3864-112">Example</span></span>  
 <span data-ttu-id="d3864-113">Приведенный ниже пример кода приводит к возникновению предупреждения CS1718.</span><span class="sxs-lookup"><span data-stu-id="d3864-113">The following code generates warning CS1718.</span></span>  
  
```csharp  
// CS1718.cs  
using System;  
public class Tester
{  
    public static void Main()
    {
        int i = 0;  
        if (i == i) { // CS1718.cs  
        //if (true) {
            i++;  
        }  
    }  
}  
```
