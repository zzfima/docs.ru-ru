---
title: Ошибка компилятора CS1657
ms.date: 07/20/2015
f1_keywords:
- CS1657
helpviewer_keywords:
- CS1657
ms.assetid: 6f0aeebe-5c90-4d5b-981c-1795d2e8fbb9
ms.openlocfilehash: 2d3cfdc9e58a5acd9438e1e55fea7f2a3193f489
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/04/2018
ms.locfileid: "33313167"
---
# <a name="compiler-error-cs1657"></a><span data-ttu-id="2c8e0-102">Ошибка компилятора CS1657</span><span class="sxs-lookup"><span data-stu-id="2c8e0-102">Compiler Error CS1657</span></span>
<span data-ttu-id="2c8e0-103">Невозможно передать "параметр" как аргумент с ключевым словом ref или out, потому что "причина"</span><span class="sxs-lookup"><span data-stu-id="2c8e0-103">Cannot pass 'parameter' as a ref or out argument because 'reason''</span></span>  
  
 <span data-ttu-id="2c8e0-104">Эта ошибка возникает при передаче переменной как [ref](../../csharp/language-reference/keywords/ref.md) или [out](../../csharp/language-reference/keywords/out-parameter-modifier.md) аргумента в контексте, в котором переменная доступна только для чтения.</span><span class="sxs-lookup"><span data-stu-id="2c8e0-104">This error occurs when a variable is passed as a [ref](../../csharp/language-reference/keywords/ref.md) or [out](../../csharp/language-reference/keywords/out-parameter-modifier.md) argument in a context in which that variable is readonly.</span></span> <span data-ttu-id="2c8e0-105">Контексты только для чтения включают переменные итерации [foreach](../../csharp/language-reference/keywords/foreach-in.md) , переменные [using](../../csharp/language-reference/keywords/using-statement.md) и переменные `fixed` .</span><span class="sxs-lookup"><span data-stu-id="2c8e0-105">Readonly contexts include [foreach](../../csharp/language-reference/keywords/foreach-in.md) iteration variables, [using](../../csharp/language-reference/keywords/using-statement.md) variables, and `fixed` variables.</span></span> <span data-ttu-id="2c8e0-106">Чтобы устранить эту ошибку, не вызывайте функции, принимающие переменную `foreach`, `using` или `fixed` в качестве параметра `ref` или `out` в блоках `using` , операторах `foreach` и `fixed` .</span><span class="sxs-lookup"><span data-stu-id="2c8e0-106">To resolve this error, do not call functions that take the `foreach`, `using` or `fixed` variable as a `ref` or `out` parameter in `using` blocks, `foreach` statements, and `fixed` statements.</span></span>  
  
## <a name="example"></a><span data-ttu-id="2c8e0-107">Пример</span><span class="sxs-lookup"><span data-stu-id="2c8e0-107">Example</span></span>  
 <span data-ttu-id="2c8e0-108">Следующий пример приводит к возникновению ошибки CS1657:</span><span class="sxs-lookup"><span data-stu-id="2c8e0-108">The following sample generates CS1657:</span></span>  
  
```csharp  
// CS1657.cs  
using System;  
class C : IDisposable  
{  
    public int i;  
    public void Dispose() {}  
}  
  
class CMain  
{  
    static void f(ref C c)  
    {  
    }  
    static void Main()  
    {  
        using (C c = new C())  
        {  
            f(ref c);  // CS1657  
        }  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="2c8e0-109">Пример</span><span class="sxs-lookup"><span data-stu-id="2c8e0-109">Example</span></span>  
 <span data-ttu-id="2c8e0-110">Следующий код иллюстрирует ту же проблему в операторе `fixed` :</span><span class="sxs-lookup"><span data-stu-id="2c8e0-110">The following code illustrates the same problem in a `fixed` statement:</span></span>  
  
```csharp  
// CS1657b.cs  
// compile with: /unsafe  
unsafe class C  
{  
    static void F(ref int* p)  
    {  
    }  
  
    static void Main()  
    {  
        int[] a = new int[5];  
        fixed(int* p = a) F(ref p); // CS1657  
    }  
}  
```
