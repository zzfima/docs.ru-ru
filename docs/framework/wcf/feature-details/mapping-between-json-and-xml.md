---
title: Сопоставление JSON и XML.
ms.date: 03/30/2017
ms.assetid: 22ee1f52-c708-4024-bbf0-572e0dae64af
ms.openlocfilehash: 0dbe37a07024ae70e574b92582715d2d2ef52e5c
ms.sourcegitcommit: 09b4090b78f52fd09b0e430cd4b26576f1fdf96e
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/17/2020
ms.locfileid: "76212087"
---
# <a name="mapping-between-json-and-xml"></a><span data-ttu-id="7f490-102">Сопоставление JSON и XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-102">Mapping Between JSON and XML</span></span>
<span data-ttu-id="7f490-103">Модули чтения и записи, создаваемые фабрикой <xref:System.Runtime.Serialization.Json.JsonReaderWriterFactory>, обеспечивают интерфейс API XML к содержимому в формате JavaScript Object Notation (JSON, объектной нотации JavaScript).</span><span class="sxs-lookup"><span data-stu-id="7f490-103">The readers and writers produced by the <xref:System.Runtime.Serialization.Json.JsonReaderWriterFactory> provide an XML API over JavaScript Object Notation (JSON) content.</span></span> <span data-ttu-id="7f490-104">Формат JSON предусматривает кодирование данных с использованием подмножества объектных литералов JavaScript.</span><span class="sxs-lookup"><span data-stu-id="7f490-104">JSON encodes data using a subset of the object literals of JavaScript.</span></span> <span data-ttu-id="7f490-105">Читатели и модули записи, созданные этой фабрикой, также используются при отправке или получении содержимого JSON приложениями Windows Communication Foundation (WCF), использующими <xref:System.ServiceModel.Channels.WebMessageEncodingBindingElement> или <xref:System.ServiceModel.WebHttpBinding>.</span><span class="sxs-lookup"><span data-stu-id="7f490-105">The readers and writers produced by this factory are also used when JSON content is being sent or received by Windows Communication Foundation (WCF) applications using the <xref:System.ServiceModel.Channels.WebMessageEncodingBindingElement> or the <xref:System.ServiceModel.WebHttpBinding>.</span></span>

<span data-ttu-id="7f490-106">Модуль чтения JSON при инициализации JSON-содержимым ведет себя так же, как модуль чтения текстовых XML-данных при инициализации экземпляром XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-106">When initialized with JSON content, the JSON reader behaves in the same way that a textual XML reader does over an instance of XML.</span></span> <span data-ttu-id="7f490-107">Модуль записи JSON при получении последовательности вызовов, в результате которой модуль чтения текстовых XML-данных создает определенный экземпляр XML, записывает JSON-содержимое.</span><span class="sxs-lookup"><span data-stu-id="7f490-107">The JSON writer, when given a sequence of calls that on a textual XML reader produces a certain XML instance, writes out JSON content.</span></span> <span data-ttu-id="7f490-108">В этом разделе описано сопоставление между этим экземпляром XML-данных и JSON-содержимым для использования в сложных сценариях.</span><span class="sxs-lookup"><span data-stu-id="7f490-108">The mapping between this instance of XML and the JSON content is described in this topic for use in advanced scenarios.</span></span>

<span data-ttu-id="7f490-109">Внутренне JSON представляется в виде информационного набора XML при обработке WCF.</span><span class="sxs-lookup"><span data-stu-id="7f490-109">Internally, JSON is represented as an XML infoset when processed by WCF.</span></span> <span data-ttu-id="7f490-110">Обычно внутреннее представление не должно заботить разработчика, поскольку сопоставление является исключительно логическим: JSON обычно не преобразуется физически в XML в памяти, равно как и XML не преобразуется в JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-110">Normally you do not have to be concerned with this internal representation as the mapping is only a logical one: JSON is normally not physically converted to XML in memory or converted to JSON from XML.</span></span> <span data-ttu-id="7f490-111">Сопоставление означает, что для обращения к JSON-содержимому используются интерфейсы API XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-111">The mapping means that XML APIs are used to access JSON content.</span></span>

<span data-ttu-id="7f490-112">Когда WCF использует JSON, обычный сценарий заключается в том, что <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> автоматически подключается к <xref:System.ServiceModel.Description.WebScriptEnablingBehavior>ому поведению, или по <xref:System.ServiceModel.Description.WebHttpBehavior>ому поведению при необходимости.</span><span class="sxs-lookup"><span data-stu-id="7f490-112">When WCF uses JSON, the usual scenario is that the <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> is automatically plugged in by the <xref:System.ServiceModel.Description.WebScriptEnablingBehavior> behavior, or by the <xref:System.ServiceModel.Description.WebHttpBehavior> behavior when appropriate.</span></span> <span data-ttu-id="7f490-113">Сериализатор <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> понимает сопоставление между JSON и набором сведений XML и действует так, как будто работает непосредственно с JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-113">The <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> understands the mapping between JSON and the XML infoset and acts as if it is dealing with JSON directly.</span></span> <span data-ttu-id="7f490-114">(Можно использовать сериализатор <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> без какого-либо модуля чтения или записи XML, зная, что XML соответствует приведенному ниже сопоставлению).</span><span class="sxs-lookup"><span data-stu-id="7f490-114">(It is possible to use the <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer> with any XML reader or writer, with the understanding that the XML conforms to the following mapping.)</span></span>

<span data-ttu-id="7f490-115">В сложных сценариях может понадобиться непосредственно обратиться к приведенному ниже сопоставлению.</span><span class="sxs-lookup"><span data-stu-id="7f490-115">In advanced scenarios, it may become necessary to directly access the following mapping.</span></span> <span data-ttu-id="7f490-116">Такие сценарии имеют место, когда требуется сериализовать десериализовать JSON особыми способами, не полагаясь на <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer>, или при использовании типа <xref:System.ServiceModel.Channels.Message> непосредственно для сообщений, содержащих JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-116">These scenarios occur when you want to serialize and deserialize JSON in custom ways, without relying on the <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer>, or when dealing with the <xref:System.ServiceModel.Channels.Message> type directly for messages containing JSON.</span></span> <span data-ttu-id="7f490-117">Сопоставление JSON-XML также используется для ведения журнала сообщений.</span><span class="sxs-lookup"><span data-stu-id="7f490-117">The JSON-XML mapping is also used for message logging.</span></span> <span data-ttu-id="7f490-118">При использовании функции ведения журнала сообщений в WCF сообщения JSON регистрируются в виде XML-кода в соответствии с сопоставлением, описанным в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="7f490-118">When using the message logging feature in WCF, JSON messages is logged as XML according to the mapping described in the next section.</span></span>

<span data-ttu-id="7f490-119">Для пояснения принципов сопоставления ниже приведен пример JSON-документа.</span><span class="sxs-lookup"><span data-stu-id="7f490-119">To clarify the concept of a mapping, the following example is of a JSON document.</span></span>

```json
{"product":"pencil","price":12}
```

<span data-ttu-id="7f490-120">Для чтения этого JSON-документа с помощью одного из упомянутых выше модулей чтения используется та же последовательность вызовов класса <xref:System.Xml.XmlDictionaryReader>, что и для чтения следующего XML-документа.</span><span class="sxs-lookup"><span data-stu-id="7f490-120">To read this JSON document using one of the readers previously mentioned, use the same sequence of <xref:System.Xml.XmlDictionaryReader> calls as you would to read the following XML document.</span></span>

```xml
<root type="object">
    <product type="string">pencil</product>
    <price type="number">12</price>
</root>
```

<span data-ttu-id="7f490-121">Кроме того, если сообщение JSON в примере получено WCF и зарегистрировано, то в предыдущем журнале будет отображен фрагмент XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-121">Furthermore, if the JSON message in the example is received by WCF and logged, you would see the XML fragment in the preceding log.</span></span>

## <a name="mapping-between-json-and-the-xml-infoset"></a><span data-ttu-id="7f490-122">Сопоставление между JSON и набором сведений XML</span><span class="sxs-lookup"><span data-stu-id="7f490-122">Mapping Between JSON and the XML Infoset</span></span>

<span data-ttu-id="7f490-123">Формально сопоставление происходит между JSON, как описано в [документе RFC 4627](https://www.ietf.org/rfc/rfc4627.txt) (за исключением определенных ограничений, ослабленных и других добавленных ограничений) и информационного набора XML (а не текстового XML), как описано в [наборе данных XML](https://www.w3.org/TR/2004/REC-xml-infoset-20040204/).</span><span class="sxs-lookup"><span data-stu-id="7f490-123">Formally, the mapping is between JSON as described in [RFC 4627](https://www.ietf.org/rfc/rfc4627.txt) (except with certain restrictions relaxed and certain other restrictions added) and the XML infoset (and not textual XML) as described in [XML Information Set](https://www.w3.org/TR/2004/REC-xml-infoset-20040204/).</span></span> <span data-ttu-id="7f490-124">Определения *элементов информации* и полей в [квадратных скобках] см. в этом разделе.</span><span class="sxs-lookup"><span data-stu-id="7f490-124">See this topic for the definitions of *information items* and fields in [square brackets].</span></span>

<span data-ttu-id="7f490-125">Пустой документ JSON сопоставляется с пустым XML-документом, а пустой XML-документ сопоставляется с пустым документом JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-125">A blank JSON document maps to a blank XML document, and a blank XML document maps to a blank JSON document.</span></span> <span data-ttu-id="7f490-126">В сопоставлении XML с JSON, предшествующие пробелы и конечные пробелы после документа не допускаются.</span><span class="sxs-lookup"><span data-stu-id="7f490-126">On the XML to JSON mapping, preceding white space and trailing white space after the document are not allowed.</span></span>

<span data-ttu-id="7f490-127">Сопоставление определяется между информационной единицей документа (Document Information Item, DII) и информационной единицей элемента (Element Information Item, EII) и JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-127">The mapping is defined between either a Document Information Item (DII) or an Element Information Item (EII) and JSON.</span></span> <span data-ttu-id="7f490-128">Информационная единица элемента (или свойство [document element] информационной единицы документа) называется корневым элементом JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-128">The EII, or the DII’s [document element] property, is referred to as the Root JSON Element.</span></span> <span data-ttu-id="7f490-129">Обратите внимание, что фрагменты документов (XML-данные с несколькими корневыми элементами) в этом сопоставлении не поддерживаются.</span><span class="sxs-lookup"><span data-stu-id="7f490-129">Note that document fragments (XML with multiple root elements) are not supported in this mapping.</span></span>

<span data-ttu-id="7f490-130">Пример. Следующий документ</span><span class="sxs-lookup"><span data-stu-id="7f490-130">Example: The following document:</span></span>

```xml
<?xml version="1.0"?>
<root type="number">42</root>
```

<span data-ttu-id="7f490-131">и следующий элемент</span><span class="sxs-lookup"><span data-stu-id="7f490-131">And the following element:</span></span>

```xml
<root type="number">42</root>
```

<span data-ttu-id="7f490-132">оба могут быть сопоставлены JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-132">Both have a mapping to JSON.</span></span> <span data-ttu-id="7f490-133">Элемент >`root`< является корневым элементом JSON в обоих случаях.</span><span class="sxs-lookup"><span data-stu-id="7f490-133">The <`root`> element is the Root JSON Element in both cases.</span></span>

<span data-ttu-id="7f490-134">Кроме того, в случае DII необходимо иметь в виду следующее.</span><span class="sxs-lookup"><span data-stu-id="7f490-134">Furthermore, in the case of a DII, the following should be considered:</span></span>

- <span data-ttu-id="7f490-135">Некоторые элементы в списке [children] присутствовать не должны.</span><span class="sxs-lookup"><span data-stu-id="7f490-135">Some items in the [children] list must not be present.</span></span> <span data-ttu-id="7f490-136">Не следует полагаться на это при чтении XML-данных, полученных из JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-136">Do not rely on this fact when reading XML mapped from JSON.</span></span>

- <span data-ttu-id="7f490-137">Список [children] не содержит информационных единиц комментариев.</span><span class="sxs-lookup"><span data-stu-id="7f490-137">The [children] list holds no comment information items.</span></span>

- <span data-ttu-id="7f490-138">Список [children] не содержит информационных единиц DTD.</span><span class="sxs-lookup"><span data-stu-id="7f490-138">The [children] list holds no DTD information items.</span></span>

- <span data-ttu-id="7f490-139">Список [children] не содержит информационных единиц персональных данных (объявление `<?xml…>` не считается информационной единицей персональных данных).</span><span class="sxs-lookup"><span data-stu-id="7f490-139">The [children] list holds no personal Information (PI) information items (the `<?xml…>` declaration is not considered a PI information item)</span></span>

- <span data-ttu-id="7f490-140">Набор [notations] пуст.</span><span class="sxs-lookup"><span data-stu-id="7f490-140">The [notations] set is empty.</span></span>

- <span data-ttu-id="7f490-141">Набор [unparsed entities] пуст.</span><span class="sxs-lookup"><span data-stu-id="7f490-141">The [unparsed entities] set is empty.</span></span>

<span data-ttu-id="7f490-142">Пример. Следующий документ не может быть сопоставлен JSON, поскольку список [children] содержит персональные данные и комментарий.</span><span class="sxs-lookup"><span data-stu-id="7f490-142">Example: The following document has no mapping to JSON because [children] holds a PI and a comment.</span></span>

```xml
<?xml version="1.0"?>
<!--comment--><?pi?>
<root type="number">42</root>
```

<span data-ttu-id="7f490-143">EII для корневого элемента JSON имеет следующие характеристики.</span><span class="sxs-lookup"><span data-stu-id="7f490-143">The EII for the Root JSON Element has the following characteristics:</span></span>

- <span data-ttu-id="7f490-144">Свойство [local name] имеет значение "root".</span><span class="sxs-lookup"><span data-stu-id="7f490-144">[local name] has the value "root".</span></span>

- <span data-ttu-id="7f490-145">Свойство [namespace name] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-145">[namespace name] has no value.</span></span>

- <span data-ttu-id="7f490-146">Свойство [prefix] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-146">[prefix] has no value.</span></span>

- <span data-ttu-id="7f490-147">Список [children] может содержать информационные единицы элементов (которые представляют внутренние элементы; см. описание ниже) или информационные единицы символов (Character Information Item, CII; см. описание ниже) либо ни то, ни другое, но не то и другое одновременно.</span><span class="sxs-lookup"><span data-stu-id="7f490-147">[children] may either contain EIIs (which represent Inner Elements as described further) or CIIs (Character Information Items as described further) or none of these, but not both.</span></span>

- <span data-ttu-id="7f490-148">Набор [attributes] может содержать приведенные ниже необязательные информационные единицы атрибутов (Attribute Information Item, AII).</span><span class="sxs-lookup"><span data-stu-id="7f490-148">[attributes] may contain the following optional attribute information items (AIIs)</span></span>

- <span data-ttu-id="7f490-149">Атрибут типа JSON ("type") (см. описание ниже).</span><span class="sxs-lookup"><span data-stu-id="7f490-149">The JSON Type Attribute ("type") as described further.</span></span> <span data-ttu-id="7f490-150">Этот атрибут используется для сохранения типа JSON (string, number, boolean, object, array или null) в полученных в результате сопоставления XML-данных.</span><span class="sxs-lookup"><span data-stu-id="7f490-150">This attribute is used to preserve the JSON type (string, number, boolean, object, array or null) in the mapped XML.</span></span>

- <span data-ttu-id="7f490-151">Атрибут имени контракта данных ("\_тип \_"), как описано далее.</span><span class="sxs-lookup"><span data-stu-id="7f490-151">The Data Contract Name Attribute ("\_\_type") as described further.</span></span> <span data-ttu-id="7f490-152">Этот атрибут может присутствовать только при условии, что присутствует также атрибут типа JSON и его свойство [normalized value] имеет значение "object".</span><span class="sxs-lookup"><span data-stu-id="7f490-152">This attribute is can only be present if the JSON type attribute is also present and its [normalized value] is "object".</span></span> <span data-ttu-id="7f490-153">Этот атрибут используется сериализатором `DataContractJsonSerializer` для сохранения сведений о типе контракта данных - например, в случаях полиморфизма, где сериализуется производный тип и где ожидается базовый тип.</span><span class="sxs-lookup"><span data-stu-id="7f490-153">This attribute is used by the `DataContractJsonSerializer` to preserve data contract type information - for example, in polymorphic cases where a derived type is serialized and where a base type is expected.</span></span> <span data-ttu-id="7f490-154">Если используется не `DataContractJsonSerializer`, в большинстве случаев этот атрибут игнорируется.</span><span class="sxs-lookup"><span data-stu-id="7f490-154">If you are not working with the `DataContractJsonSerializer`, in most cases, this attribute is ignored.</span></span>

- <span data-ttu-id="7f490-155">[в пространствах имен в области] содержит привязку "XML" к `http://www.w3.org/XML/1998/namespace` в соответствии со спецификацией набора сведений.</span><span class="sxs-lookup"><span data-stu-id="7f490-155">[in-scope namespaces] contains the binding of "xml" to `http://www.w3.org/XML/1998/namespace` as mandated by the infoset specification.</span></span>

- <span data-ttu-id="7f490-156">Свойства [children], [attributes] и [in-scope namespaces] не должны содержать никаких единиц, кроме указанных выше, а свойство [namespace attributes] не должно иметь никаких членов; тем не менее, при чтении XML-данных, полученных из JSON, на это полагаться нельзя.</span><span class="sxs-lookup"><span data-stu-id="7f490-156">[children], [attributes] and [in-scope namespaces] must not have any items other than as specified previously and [namespace attributes] must have no members, but do not rely on these facts when reading XML mapped from JSON.</span></span>

<span data-ttu-id="7f490-157">Пример. Следующий документ не может быть сопоставлен JSON, поскольку набор [namespace attributes] не пуст.</span><span class="sxs-lookup"><span data-stu-id="7f490-157">Example: The following document has no mapping to JSON because [namespace attributes] is not empty.</span></span>

```xml
<?xml version="1.0"?>
<root xmlns:a="myattributevalue">42</root>
```

<span data-ttu-id="7f490-158">AII для атрибута типа JSON имеет следующие характеристики.</span><span class="sxs-lookup"><span data-stu-id="7f490-158">The AII for the JSON Type Attribute has the following characteristics:</span></span>

- <span data-ttu-id="7f490-159">Свойство [namespace name] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-159">[namespace name] has no value.</span></span>
- <span data-ttu-id="7f490-160">Свойство [prefix] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-160">[prefix] has no value.</span></span>
- <span data-ttu-id="7f490-161">Свойство [local name] имеет значение «type».</span><span class="sxs-lookup"><span data-stu-id="7f490-161">[local name] is "type".</span></span>
- <span data-ttu-id="7f490-162">Свойство [normalized value] имеет одно из возможных значений-типов, описанных в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="7f490-162">[normalized value] is one of the possible type values described in the following section.</span></span>
- <span data-ttu-id="7f490-163">Флаг [specified] имеет значение `true`.</span><span class="sxs-lookup"><span data-stu-id="7f490-163">[specified] is `true`.</span></span>
- <span data-ttu-id="7f490-164">Свойство [attribute type] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-164">[attribute type] has no value.</span></span>
- <span data-ttu-id="7f490-165">Свойство [references] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-165">[references] has no value.</span></span>

<span data-ttu-id="7f490-166">AII для атрибута имени контракта данных JSON имеет следующие характеристики.</span><span class="sxs-lookup"><span data-stu-id="7f490-166">The AII for the Data Contract Name Attribute has the following characteristics:</span></span>

- <span data-ttu-id="7f490-167">Свойство [namespace name] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-167">[namespace name] has no value.</span></span>
- <span data-ttu-id="7f490-168">Свойство [prefix] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-168">[prefix] has no value.</span></span>
- <span data-ttu-id="7f490-169">[local name] — "\_тип \_" (два символа подчеркивания, а затем "тип").</span><span class="sxs-lookup"><span data-stu-id="7f490-169">[local name] is "\_\_type" (two underscores and then "type").</span></span>
- <span data-ttu-id="7f490-170">Свойство [normalized value] равно любой строке Юникод - сопоставление этой строки с JSON описывается в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="7f490-170">[normalized value] is any valid Unicode string – the mapping of this string to JSON is described in the following section.</span></span>
- <span data-ttu-id="7f490-171">Флаг [specified] имеет значение `true`.</span><span class="sxs-lookup"><span data-stu-id="7f490-171">[specified] is `true`.</span></span>
- <span data-ttu-id="7f490-172">Свойство [attribute type] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-172">[attribute type] has no value.</span></span>
- <span data-ttu-id="7f490-173">Свойство [references] не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-173">[references] has no value.</span></span>

<span data-ttu-id="7f490-174">Внутренние элементы, содержащиеся в корневом элементе JSON или других внутренних элементах, имеют следующие характеристики.</span><span class="sxs-lookup"><span data-stu-id="7f490-174">Inner elements contained within the Root JSON Element or other inner elements have the following characteristics:</span></span>

- <span data-ttu-id="7f490-175">[локальное имя] может иметь любое значение, как описано далее.</span><span class="sxs-lookup"><span data-stu-id="7f490-175">[local name] may have any value as described further.</span></span>
- <span data-ttu-id="7f490-176">Свойства [namespace name], [prefix], [children], [attributes], [namespace attributes] и [in-scope namespaces] подчиняются тем же правилам, что и корневой элемент JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-176">[namespace name], [prefix], [children], [attributes], [namespace attributes], and [in-scope namespaces] are subject to the same rules as the Root JSON Element.</span></span>

<span data-ttu-id="7f490-177">И в корневом элементе JSON, и во внутренних элементах атрибут типа JSON определяет сопоставление с JSON, возможные дочерние информационные единицы ([children]) и их интерпретацию.</span><span class="sxs-lookup"><span data-stu-id="7f490-177">In both the Root JSON Element and the inner elements, the JSON Type Attribute defines the mapping to JSON and the possible [children] and their interpretation.</span></span> <span data-ttu-id="7f490-178">Атрибут [нормализованное значение] учитывает регистр, должен быть строчным и не может содержать пробелы.</span><span class="sxs-lookup"><span data-stu-id="7f490-178">The attribute’s [normalized value] is case-sensitive and must be lowercase, and cannot contain white space.</span></span>

|<span data-ttu-id="7f490-179">[нормализованное значение] все атрибута типа JSON</span><span class="sxs-lookup"><span data-stu-id="7f490-179">[normalized value] of JSON Type Attribute’s AII</span></span>|<span data-ttu-id="7f490-180">Допустимые дочерние информационные единицы соответствующей EII</span><span class="sxs-lookup"><span data-stu-id="7f490-180">Allowed [children] of the corresponding EII</span></span>|<span data-ttu-id="7f490-181">Соответствие в JSON</span><span class="sxs-lookup"><span data-stu-id="7f490-181">Mapping to JSON</span></span>|
|---------------------------------------------------------|---------------------------------------------------|---------------------|
|<span data-ttu-id="7f490-182">`string` (или отсутствие AII типа JSON)</span><span class="sxs-lookup"><span data-stu-id="7f490-182">`string` (or absence of the JSON type AII)</span></span><br /><br /> <span data-ttu-id="7f490-183">`string` и отсутствие AII типа JSON - одно и то же, поэтому `string` используется по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="7f490-183">A `string` and the absence of the JSON type AII are the same makes `string` the default.</span></span><br /><br /> <span data-ttu-id="7f490-184">Следовательно, `<root> string1</root>` соответствует `string` "string1" в JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-184">So, `<root> string1</root>` maps to the JSON `string` "string1".</span></span>|<span data-ttu-id="7f490-185">0 или более CII</span><span class="sxs-lookup"><span data-stu-id="7f490-185">0 or more CIIs</span></span>|<span data-ttu-id="7f490-186">Фрагмент JSON типа `string` (RFC по JSON, раздел 2.5).</span><span class="sxs-lookup"><span data-stu-id="7f490-186">A JSON `string` (JSON RFC, section 2.5).</span></span> <span data-ttu-id="7f490-187">Каждый фрагмент типа `char` - это символ, соответствующий свойству [character code] из CII.</span><span class="sxs-lookup"><span data-stu-id="7f490-187">Each `char` is a character that corresponds to the [character code] from the CII.</span></span> <span data-ttu-id="7f490-188">Если CII нет, он сопоставляется пустому фрагменту JSON типа `string`.</span><span class="sxs-lookup"><span data-stu-id="7f490-188">If there are no CIIs, it maps to an empty JSON `string`.</span></span><br /><br /> <span data-ttu-id="7f490-189">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-189">Example: The following element maps to a JSON fragment:</span></span><br /><br /> `<root type="string">42</root>`<br /><br /> <span data-ttu-id="7f490-190">Фрагмент JSON: "42".</span><span class="sxs-lookup"><span data-stu-id="7f490-190">The JSON fragment is "42".</span></span><br /><br /> <span data-ttu-id="7f490-191">При сопоставлении XML-JSON символы, которые должны быть снабжены escape-символом, сопоставляются символам с escape-символом, все остальные символы сопоставляются символам без escape-символа.</span><span class="sxs-lookup"><span data-stu-id="7f490-191">On XML to JSON mapping, characters that must be escaped map to escaped characters, all others map to characters that are not escaped.</span></span> <span data-ttu-id="7f490-192">Символ "/" является специальным — он экранирован, даже если он не должен быть (записан как "\\/").</span><span class="sxs-lookup"><span data-stu-id="7f490-192">The "/" character is special – it is escaped even though it does not have to be (written out as "\\/").</span></span><br /><br /> <span data-ttu-id="7f490-193">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-193">Example: The following element maps to a JSON fragment.</span></span><br /><br /> `<root type="string">the "da/ta"</root>`<br /><br /> <span data-ttu-id="7f490-194">Фрагмент JSON — это "\\Da\\/та\\" ".</span><span class="sxs-lookup"><span data-stu-id="7f490-194">The JSON fragment is "the \\"da\\/ta\\"".</span></span><br /><br /> <span data-ttu-id="7f490-195">При сопоставлении JSON-XML символы с escape-знаком и символы без escape-знака корректно сопоставляются соответствующему свойству [character code].</span><span class="sxs-lookup"><span data-stu-id="7f490-195">On JSON to XML mapping, any escaped characters and characters that are not escaped map correctly to the corresponding [character code].</span></span><br /><br /> <span data-ttu-id="7f490-196">Пример. Фрагмент JSON "\u0041BC" сопоставляется следующему XML-элементу:</span><span class="sxs-lookup"><span data-stu-id="7f490-196">Example: The JSON fragment "\u0041BC", maps to the following XML element.</span></span><br /><br /> `<root type="string">ABC</root>`<br /><br /> <span data-ttu-id="7f490-197">Строка может быть заключена в пробелы ("ws" в разделе 2 JSON RFC), которая не сопоставлена с XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-197">The string can be surrounded by white space ('ws' in section 2 of the JSON RFC) that does not get mapped to XML.</span></span><br /><br /> <span data-ttu-id="7f490-198">Пример. Фрагмент JSON           "ABC" (с пробелами перед первой двойной кавычкой) сопоставляется следующему XML-элементу:</span><span class="sxs-lookup"><span data-stu-id="7f490-198">Example: The JSON fragment           "ABC", (there are spaces before the first double quote), maps to the following XML element.</span></span><br /><br /> `<root type="string">ABC</root>`<br /><br /> <span data-ttu-id="7f490-199">Все пробелы в XML сопоставлены с пробелами в JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-199">Any white space in XML maps to white space in JSON.</span></span><br /><br /> <span data-ttu-id="7f490-200">Пример. Следующий XML-элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-200">Example: The following XML element maps to a JSON fragment.</span></span><br /><br /> `<root type="string">  A BC      </root>`<br /><br /> <span data-ttu-id="7f490-201">Фрагмент JSON: " A BC ".</span><span class="sxs-lookup"><span data-stu-id="7f490-201">The JSON fragment is " A BC ".</span></span>|
|`number`|<span data-ttu-id="7f490-202">1 или более CII</span><span class="sxs-lookup"><span data-stu-id="7f490-202">1 or more CIIs</span></span>|<span data-ttu-id="7f490-203">`number` JSON (JSON RFC, раздел 2,4), возможно, окружен пробелами.</span><span class="sxs-lookup"><span data-stu-id="7f490-203">A JSON `number` (JSON RFC, section 2.4), possibly surrounded by white space.</span></span> <span data-ttu-id="7f490-204">Каждый символ в сочетании чисел и пробелов является символом, который соответствует [коду символа] из ции.</span><span class="sxs-lookup"><span data-stu-id="7f490-204">Each character in the number/white space combination is a character that corresponds to the [character code] from the CII.</span></span><br /><br /> <span data-ttu-id="7f490-205">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-205">Example: The following element maps to a JSON fragment.</span></span><br /><br /> `<root type="number">    42</root>`<br /><br /> <span data-ttu-id="7f490-206">Фрагмент JSON:    42</span><span class="sxs-lookup"><span data-stu-id="7f490-206">The JSON fragment is    42</span></span><br /><br /> <span data-ttu-id="7f490-207">(Пробел сохраняется).</span><span class="sxs-lookup"><span data-stu-id="7f490-207">(White space is preserved).</span></span>|
|`boolean`|<span data-ttu-id="7f490-208">4 или 5 CII (что соответствует `true` или `false`), возможно, окружены дополнительными CII пробелами.</span><span class="sxs-lookup"><span data-stu-id="7f490-208">4 or 5 CIIs (which corresponds to `true` or `false`), possibly surrounded by additional white-space CIIs.</span></span>|<span data-ttu-id="7f490-209">Последовательность CII, соответствующая строке "true", сопоставляется литералу `true`, а последовательность CII, соответствующая строке "false", сопоставляется литералу `false`.</span><span class="sxs-lookup"><span data-stu-id="7f490-209">A CII sequence that corresponds to the string "true" is mapped to the literal `true`, and a CII sequence that corresponds to the string "false" is mapped to the literal `false`.</span></span> <span data-ttu-id="7f490-210">Окружающие пробелы сохраняются.</span><span class="sxs-lookup"><span data-stu-id="7f490-210">Surrounding white space is preserved.</span></span><br /><br /> <span data-ttu-id="7f490-211">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-211">Example: The following element maps to a JSON fragment.</span></span><br /><br /> `<root type="boolean"> false</root>`<br /><br /> <span data-ttu-id="7f490-212">Фрагмент JSON: `false`.</span><span class="sxs-lookup"><span data-stu-id="7f490-212">The JSON fragment is `false`.</span></span>|
|`null`|<span data-ttu-id="7f490-213">Не допускается ни одного.</span><span class="sxs-lookup"><span data-stu-id="7f490-213">None allowed.</span></span>|<span data-ttu-id="7f490-214">Литерал `null`.</span><span class="sxs-lookup"><span data-stu-id="7f490-214">The literal `null`.</span></span> <span data-ttu-id="7f490-215">Для сопоставления JSON с XML `null` может быть заключено в пробел ("ws" в разделе 2), который не сопоставлен с XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-215">On JSON to XML mapping, the `null` may be surrounded by white space (‘ws’ in section 2) that does not get mapped to XML.</span></span><br /><br /> <span data-ttu-id="7f490-216">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-216">Example: The following element maps to a JSON fragment.</span></span><br /><br /> `<root type="null"/>`<br /><br /> <span data-ttu-id="7f490-217">или</span><span class="sxs-lookup"><span data-stu-id="7f490-217">or</span></span><br /><br /> `<root type="null"></root>`<br /><br /> <span data-ttu-id="7f490-218">.</span><span class="sxs-lookup"><span data-stu-id="7f490-218">:</span></span><br /><br /> <span data-ttu-id="7f490-219">Фрагмент JSON в обоих случаях - `Null`.</span><span class="sxs-lookup"><span data-stu-id="7f490-219">The JSON fragment in both cases is `Null`.</span></span>|
|`object`|<span data-ttu-id="7f490-220">0 или более EII.</span><span class="sxs-lookup"><span data-stu-id="7f490-220">0 or more EIIs.</span></span>|<span data-ttu-id="7f490-221">Фрагмент `begin-object` (левая фигурная скобка), согласно разделу 2.2 RFC по JSON, после которой идет запись-член для каждой EII, как описано ниже.</span><span class="sxs-lookup"><span data-stu-id="7f490-221">A `begin-object` (left curly brace) as in section 2.2 of the JSON RFC, followed by a member record for each EII as described further.</span></span> <span data-ttu-id="7f490-222">Если EII больше одной, между записями-членами ставятся разделители значений (запятые).</span><span class="sxs-lookup"><span data-stu-id="7f490-222">If there is more than one EII, there are value-separators (commas) between the member records.</span></span> <span data-ttu-id="7f490-223">После этого идет фрагмент end-object (правая фигурная скобка).</span><span class="sxs-lookup"><span data-stu-id="7f490-223">All this is followed by an end-object (right curly brace).</span></span><br /><br /> <span data-ttu-id="7f490-224">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-224">Example: The following element maps to the JSON fragment.</span></span><br /><br /> `<root type="object">`<br /><br /> `<type1 type="string">aaa\</type1>`<br /><br /> `<type2 type="string">bbb\</type2>`<br /><br /> `</root >`<br /><br /> <span data-ttu-id="7f490-225">Фрагмент JSON: `{"type1":"aaa","type2":"bbb"}`.</span><span class="sxs-lookup"><span data-stu-id="7f490-225">The JSON fragment is `{"type1":"aaa","type2":"bbb"}`.</span></span><br /><br /> <span data-ttu-id="7f490-226">Если в сопоставлении XML-JSON присутствует атрибут типа контракта данных, в начале вставляется дополнительная запись-член.</span><span class="sxs-lookup"><span data-stu-id="7f490-226">If the Data Contract Type Attribute is present on XML to JSON mapping, then an additional Member Record is inserted at the beginning.</span></span> <span data-ttu-id="7f490-227">Его имя является [локальным именем] атрибута типа контракта данных ("\_\_тип"), а его значением является [нормализованное значение] атрибута.</span><span class="sxs-lookup"><span data-stu-id="7f490-227">Its name is the [local name] of the Data Contract Type Attribute ("\_\_type"), and its value is the attribute's [normalized value].</span></span> <span data-ttu-id="7f490-228">И наоборот, при сопоставлении JSON с XML, если имя первой записи члена является [локальным именем] атрибута типа контракта данных (то есть "\_\_тип"), соответствующий атрибут типа контракта данных содержится в сопоставленном XML, но соответствующий EII отсутствует.</span><span class="sxs-lookup"><span data-stu-id="7f490-228">Conversely, on JSON to XML mapping, if the first member-record’s name is the [local name] of the Data Contract Type Attribute (that is, "\_\_type"), a corresponding Data Contract Type Attribute is present in the mapped XML, but a corresponding EII is not present.</span></span> <span data-ttu-id="7f490-229">Обратите внимание, что для применения этого особого сопоставления эта запись-член должна идти первой в объекте JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-229">Note that this member record must occur first in the JSON object for this special mapping to apply.</span></span> <span data-ttu-id="7f490-230">Это отход от обычной обработки JSON, где порядок записей-членов не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="7f490-230">This represents a departure from usual JSON processing, where the order of member records is not significant.</span></span><br /><br /> <span data-ttu-id="7f490-231">Пример:</span><span class="sxs-lookup"><span data-stu-id="7f490-231">Example:</span></span><br /><br /> <span data-ttu-id="7f490-232">Следующий фрагмент JSON сопоставляется XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-232">The following JSON fragment maps to XML.</span></span><br /><br /> `{"__type":"Person","name":"John"}`<br /><br /> <span data-ttu-id="7f490-233">XML представляет собой следующий код.</span><span class="sxs-lookup"><span data-stu-id="7f490-233">The XML is the following code.</span></span><br /><br /> `<root type="object" __type="Person">   <name type="string">John</name> </root>`<br /><br /> <span data-ttu-id="7f490-234">Обратите внимание, что \_тип \_все присутствует, но \_\_типа EII отсутствует.</span><span class="sxs-lookup"><span data-stu-id="7f490-234">Notice that the \_\_type AII is present, but there is no \_\_type EII.</span></span><br /><br /> <span data-ttu-id="7f490-235">Однако если порядок в JSON будет обратным, как показано в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="7f490-235">However, if the order in the JSON is reversed as shown in the following example.</span></span><br /><br /> `{"name":"John","\_\_type":"Person"}`<br /><br /> <span data-ttu-id="7f490-236">соответствующий XML-код будет выглядеть так:</span><span class="sxs-lookup"><span data-stu-id="7f490-236">The corresponding XML is shown.</span></span><br /><br /> `<root type="object">   <name type="string">John</name>   <__type type="string">Person</__type> </root>`<br /><br /> <span data-ttu-id="7f490-237">То есть \__type прекращает иметь особое значение и сопоставляется с EII, как обычно, а не все.</span><span class="sxs-lookup"><span data-stu-id="7f490-237">That is, \__type ceases to have special meaning and maps to an EII as usual, not AII.</span></span><br /><br /> <span data-ttu-id="7f490-238">Правила добавления/удаления escape-знаков для свойства [normalized value] AII при сопоставлении значению JSON такие же, как и для строк JSON (см. строку "string" выше в таблице).</span><span class="sxs-lookup"><span data-stu-id="7f490-238">Escaping/unescaping rules for the AII’s [normalized value] when mapped to a JSON value are the same as for JSON strings, specified in the "string" row of this table.</span></span><br /><br /> <span data-ttu-id="7f490-239">Пример:</span><span class="sxs-lookup"><span data-stu-id="7f490-239">Example:</span></span><br /><br /> `<root type="object" __type="\abc" />`<br /><br /> <span data-ttu-id="7f490-240">Предыдущий пример может быть сопоставлен следующему фрагменту JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-240">to the previous example can be mapped to the following JSON.</span></span><br /><br /> `{"__type":"\\abc"}`<br /><br /> <span data-ttu-id="7f490-241">При сопоставлении XML с JSON первый EII [локальное имя] не должен быть "\_тип \_".</span><span class="sxs-lookup"><span data-stu-id="7f490-241">On an XML to JSON mapping, the first EII’s [local name] must not be "\_\_type".</span></span><br /><br /> <span data-ttu-id="7f490-242">Пробел (`ws`) никогда не создается при сопоставлении XML с JSON для объектов и игнорируется в сопоставлении JSON с XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-242">White space (`ws`) is never generated on XML to JSON mapping for objects and is ignored on JSON to XML mapping.</span></span><br /><br /> <span data-ttu-id="7f490-243">Пример. Следующий фрагмент JSON сопоставляется XML-элементу:</span><span class="sxs-lookup"><span data-stu-id="7f490-243">Example: The following JSON fragment maps to an XML element.</span></span><br /><br /> `{ "ccc" : "aaa", "ddd" :"bbb"}`<br /><br /> <span data-ttu-id="7f490-244">XML-элемент показан в следующем коде.</span><span class="sxs-lookup"><span data-stu-id="7f490-244">The XML element is shown in the following code.</span></span><br /><br /> `<root type="object">    <ccc type="string">aaa</ccc>    <ddd type="string">bbb</bar> </root >`|
|<span data-ttu-id="7f490-245">Массив .</span><span class="sxs-lookup"><span data-stu-id="7f490-245">array</span></span>|<span data-ttu-id="7f490-246">0 или более EII</span><span class="sxs-lookup"><span data-stu-id="7f490-246">0 or more EIIs</span></span>|<span data-ttu-id="7f490-247">Фрагмент begin-array (левая квадратная скобка), согласно разделу 2.3 RFC по JSON, после которой идет запись-массив для каждой EII, как описано ниже.</span><span class="sxs-lookup"><span data-stu-id="7f490-247">A begin-array (left square bracket) as in section 2.3 of the JSON RFC, followed by an array record for each EII as described further.</span></span> <span data-ttu-id="7f490-248">Если EII больше одной, между записями-массивами ставятся разделители значений (запятые).</span><span class="sxs-lookup"><span data-stu-id="7f490-248">If there is more than one EII, there are value-separators (commas) between the array records.</span></span> <span data-ttu-id="7f490-249">После этого идет фрагмент end-array.</span><span class="sxs-lookup"><span data-stu-id="7f490-249">All this is followed by an end-array.</span></span><br /><br /> <span data-ttu-id="7f490-250">Пример. Следующий XML-элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-250">Example: The following XML element maps to a JSON fragment.</span></span><br /><br /> `<root type="array"/>    <item type="string">aaa</item>    <item type="string">bbb</item> </root >`<br /><br /> <span data-ttu-id="7f490-251">Фрагмент JSON `["aaa","bbb"]`</span><span class="sxs-lookup"><span data-stu-id="7f490-251">The JSON fragment is `["aaa","bbb"]`</span></span><br /><br /> <span data-ttu-id="7f490-252">Пробел (`ws`) никогда не создается в сопоставлении XML с JSON для массивов и игнорируется в сопоставлении JSON с XML.</span><span class="sxs-lookup"><span data-stu-id="7f490-252">White space (`ws`) is never generated on XML to JSON mapping for arrays and is ignored on JSON to XML mapping.</span></span><br /><br /> <span data-ttu-id="7f490-253">Пример. фрагмент JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-253">Example: A JSON fragment.</span></span><br /><br />`["aaa", "bbb"]`<br /><br /> <span data-ttu-id="7f490-254">XML-элемент, которому он сопоставляется:</span><span class="sxs-lookup"><span data-stu-id="7f490-254">The XML element that it maps to.</span></span><br /><br /> `<root type="array"/>    <item type="string">aaa</item>    <item type="string">bbb</item> </root >`|

<span data-ttu-id="7f490-255">Записи-члены работают следующим образом.</span><span class="sxs-lookup"><span data-stu-id="7f490-255">Member Records work as follows:</span></span>

- <span data-ttu-id="7f490-256">Свойство [local name] внутреннего элемента сопоставляется `string`-части фрагмента `member`, согласно 2.2 RFC по JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-256">Inner element’s [local name] maps to the `string` part of the `member` as defined in section 2.2 of the JSON RFC.</span></span>

<span data-ttu-id="7f490-257">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-257">Example: The following element maps to a JSON fragment.</span></span>

```xml
<root type="object"/>
<myLocalName type="string">aaa</myLocalName>
</root >
```

<span data-ttu-id="7f490-258">Получается следующий фрагмент JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-258">The following JSON fragment is displayed.</span></span>

```json
{"myLocalName":"aaa"}
```

- <span data-ttu-id="7f490-259">При сопоставлении XML-JSON символы, которые в JSON должны быть снабжены escape-знаком, предваряются escape-знаком, остальные символы - нет.</span><span class="sxs-lookup"><span data-stu-id="7f490-259">On the XML to JSON mapping, the characters that must be escaped in JSON are escaped, and the others are not escaped.</span></span> <span data-ttu-id="7f490-260">Символ "/", хотя и не является требующим escape-знака символом, тем не менее предваряется escape-знаком (при сопоставлении JSON-XML предварять escape-знаком его не нужно).</span><span class="sxs-lookup"><span data-stu-id="7f490-260">The "/" character, even though it is not a character that must be escaped, is escaped nevertheless (it does not have to be escaped on JSON to XML mapping).</span></span> <span data-ttu-id="7f490-261">Это необходимо для поддержки формата AJAX ASP.NET для данных типа `DateTime` в JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-261">This is required to support the ASP.NET AJAX format for `DateTime` data in JSON.</span></span>

- <span data-ttu-id="7f490-262">При сопоставлении JSON-XML все символы (включая символы без escape-знака, если необходимо) используются для формирования фрагмента типа `string`, который представляет собой значение свойства [local name].</span><span class="sxs-lookup"><span data-stu-id="7f490-262">On the JSON to XML mapping, all characters (including the not escaped characters, if necessary) are taken to form a `string` that produces a [local name].</span></span>

- <span data-ttu-id="7f490-263">Внутренние элементы (список [children]) сопоставляются значению в разделе 2.2, в соответствии с атрибутом типа JSON (`JSON Type Attribute`), аналогично корневому элементу JSON (`Root JSON Element`).</span><span class="sxs-lookup"><span data-stu-id="7f490-263">Inner elements [children] map to the value in section 2.2, according to the `JSON Type Attribute` just like for the `Root JSON Element`.</span></span> <span data-ttu-id="7f490-264">Несколько уровней вложения EII (включая вложения в массивах) допустимы.</span><span class="sxs-lookup"><span data-stu-id="7f490-264">Multiple levels of nesting of EIIs (including nesting within arrays) are allowed.</span></span>

<span data-ttu-id="7f490-265">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-265">Example: The following element maps to a JSON fragment.</span></span>

```xml
<root type="object">
    <myLocalName1 type="string">myValue1</myLocalName1>
    <myLocalName2 type="number">2</myLocalName2>
    <myLocalName3 type="object">
        <myNestedName1 type="boolean">true</myNestedName1>
        <myNestedName2 type="null"/>
    </myLocalName3>
</root >
```

<span data-ttu-id="7f490-266">В результате получается следующий фрагмент JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-266">The following JSON fragment is what it maps to.</span></span>

```json
{"myLocalName1":"myValue1","myLocalName2":2,"myLocalName3":{"myNestedName1":true,"myNestedName2":null}}
```

> [!NOTE]
> <span data-ttu-id="7f490-267">В показанном выше сопоставлении отсутствует этап XML-кодирования.</span><span class="sxs-lookup"><span data-stu-id="7f490-267">There is no XML encoding step in the preceding mapping.</span></span> <span data-ttu-id="7f490-268">Поэтому WCF поддерживает только документы JSON, в которых все символы в именах ключей являются допустимыми символами в именах XML-элементов.</span><span class="sxs-lookup"><span data-stu-id="7f490-268">Therefore, WCF only supports JSON documents where all characters in key names are valid characters in XML element names.</span></span> <span data-ttu-id="7f490-269">Например, документ JSON {"<": "a"} не поддерживается, так как < не является допустимым именем для XML-элемента.</span><span class="sxs-lookup"><span data-stu-id="7f490-269">For example, the JSON document {"<":"a"} is not supported because < is not a valid name for an XML element.</span></span>

<span data-ttu-id="7f490-270">Обратная ситуация (символы, допустимые в XML, но недопустимые в JSON) никаких проблем не вызывает, поскольку описанное выше сопоставление предусматривает добавление или удаление escape-символов JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-270">The reverse situation (characters valid in XML but not in JSON) does not cause any problems because the preceding mapping includes JSON escaping/unescaping steps.</span></span>

<span data-ttu-id="7f490-271">Записи-массивы работают следующим образом.</span><span class="sxs-lookup"><span data-stu-id="7f490-271">Array Records work as follows:</span></span>

- <span data-ttu-id="7f490-272">Свойство [local name] внутреннего элемента имеет значение "item".</span><span class="sxs-lookup"><span data-stu-id="7f490-272">Inner element’s [local name] is "item".</span></span>

- <span data-ttu-id="7f490-273">Список [children] внутреннего элемента сопоставляется значению в разделе 2.3, в соответствии с атрибутом типа JSON, как и для корневого элемента JSON.</span><span class="sxs-lookup"><span data-stu-id="7f490-273">Inner element’s [children] map to the value in section 2.3, according to the JSON Type Attribute as is does for the Root JSON Element.</span></span> <span data-ttu-id="7f490-274">Несколько уровней вложения EII (включая вложения в объектах) допустимы.</span><span class="sxs-lookup"><span data-stu-id="7f490-274">Multiple levels of nesting of EIIs (including nesting within objects) are allowed.</span></span>

<span data-ttu-id="7f490-275">Пример. Следующий элемент сопоставляется фрагменту JSON:</span><span class="sxs-lookup"><span data-stu-id="7f490-275">Example: The following element maps to a JSON fragment.</span></span>

```xml
<root type="array"/>
    <item type="string">myValue1</item>
    <item type="number">2</item>
    <item type="array">
    <item type="boolean">true</item>
    <item type="null"/></item>
</root >
```

<span data-ttu-id="7f490-276">Фрагмент JSON выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="7f490-276">The following is the JSON fragment.</span></span>

```json
["myValue1",2,[true,null]]
```

## <a name="see-also"></a><span data-ttu-id="7f490-277">См. также:</span><span class="sxs-lookup"><span data-stu-id="7f490-277">See also</span></span>

- <xref:System.Runtime.Serialization.Json.JsonReaderWriterFactory>
- <xref:System.Runtime.Serialization.Json.DataContractJsonSerializer>
- [<span data-ttu-id="7f490-278">Автономная сериализация JSON</span><span class="sxs-lookup"><span data-stu-id="7f490-278">Stand-Alone JSON Serialization</span></span>](stand-alone-json-serialization.md)
