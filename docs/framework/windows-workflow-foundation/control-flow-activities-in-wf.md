---
title: Действия потока управления потока в WF
ms.custom: ''
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
ms.assetid: 6892885b-f7c5-4aea-8f5e-28863fb4ae75
caps.latest.revision: 16
author: dotnet-bot
ms.author: dotnetcontent
manager: wpickett
ms.workload:
- dotnet
ms.openlocfilehash: 91fb4e18d753709ab973730300ffef5a952c56d6
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/26/2018
---
# <a name="control-flow-activities-in-wf"></a><span data-ttu-id="74d2a-102">Действия потока управления потока в WF</span><span class="sxs-lookup"><span data-stu-id="74d2a-102">Control Flow Activities in WF</span></span>
<span data-ttu-id="74d2a-103">[!INCLUDE[netfx_current_long](../../../includes/netfx-current-long-md.md)] содержит несколько действий для управления потоком выполнения внутри рабочего процесса.</span><span class="sxs-lookup"><span data-stu-id="74d2a-103">The [!INCLUDE[netfx_current_long](../../../includes/netfx-current-long-md.md)] provides several activities for controlling flow of execution within a workflow.</span></span> <span data-ttu-id="74d2a-104">Некоторые из этих действий (таких как `Switch` и `If`) реализуют структуры управления потоком, аналогичные доступным в средах как Visual C#, тогда как другие программирования (такие как `Pick`) моделируют новые структуры программирования.</span><span class="sxs-lookup"><span data-stu-id="74d2a-104">Some of these activities (such as `Switch` and `If`) implement flow control structures similar to those in programming environments such as Visual C#, while others (such as `Pick`) model new programming structures.</span></span>  
  
 <span data-ttu-id="74d2a-105">Следует отметить, что, хотя такие действия, как `Parallel` и `ParallelForEach`, планируют одновременное выполнение нескольких дочерних действий, в рабочем процессе применяется лишь один поток.</span><span class="sxs-lookup"><span data-stu-id="74d2a-105">Note that while activities such as the `Parallel` and `ParallelForEach` activities schedule multiple child activities for execution simultaneously, only a single thread is used for a workflow.</span></span> <span data-ttu-id="74d2a-106">Каждое дочернее действие данных действий выполняется последовательно; последующее действие не выполняется, пока не завершатся или не перейдут в состояние бездействия все предыдущие действия.</span><span class="sxs-lookup"><span data-stu-id="74d2a-106">Each child activity of these activities executes sequentially and successive activities do not execute until previous activities either complete or go idle.</span></span> <span data-ttu-id="74d2a-107">В результате эти действия больше всего полезны в приложениях, в которых несколько потенциально блокирующих друг друга действий должны выполняться поочередно.</span><span class="sxs-lookup"><span data-stu-id="74d2a-107">As a result, these activities are most useful for applications in which several potentially blocking activities must execute in an interleaved fashion.</span></span> <span data-ttu-id="74d2a-108">Если дочерние действия этих действий не переходят в состояние бездействия, действие `Parallel` выполняется аналогично действию `Sequence`, а действие `ParallelForEach` выполняется аналогично действию `ForEach`.</span><span class="sxs-lookup"><span data-stu-id="74d2a-108">If none of the child activities of these activities go idle, a `Parallel` activity executes just like a `Sequence` activity, and a `ParallelForEach` activity executes just like a `ForEach` activity.</span></span> <span data-ttu-id="74d2a-109">Однако, если применяются асинхронные действия (например, действия, производные от <xref:System.Activities.AsyncCodeActivity>) или действия обмена сообщениями, элемент управления передает управление следующей ветви, пока дочернее действие ожидает получения своего сообщения или завершения выполнения асинхронной операции.</span><span class="sxs-lookup"><span data-stu-id="74d2a-109">If, however, asynchronous activities (such as activities that derive from <xref:System.Activities.AsyncCodeActivity>) or messaging activities are used, control will pass to the next branch while the child activity waits for its message to be received or its asynchronous work to be completed.</span></span>  
  
## <a name="flow-control-activities"></a><span data-ttu-id="74d2a-110">Действия управления потоком</span><span class="sxs-lookup"><span data-stu-id="74d2a-110">Flow control activities</span></span>  
  
|<span data-ttu-id="74d2a-111">Действие</span><span class="sxs-lookup"><span data-stu-id="74d2a-111">Activity</span></span>|<span data-ttu-id="74d2a-112">Описание</span><span class="sxs-lookup"><span data-stu-id="74d2a-112">Description</span></span>|  
|--------------|-----------------|  
|<xref:System.Activities.Statements.DoWhile>|<span data-ttu-id="74d2a-113">Выполняет содержащиеся в нем действия один раз, затем продолжает выполнять их, пока условие равно `true`.</span><span class="sxs-lookup"><span data-stu-id="74d2a-113">Executes the contained activities once and continues to do so while a condition is `true`.</span></span>|  
|<xref:System.Activities.Statements.ForEach%601>|<span data-ttu-id="74d2a-114">Последовательно выполняет внедренную инструкцию в последовательность для каждого из элементов коллекции.</span><span class="sxs-lookup"><span data-stu-id="74d2a-114">Executes an embedded statement in sequence for each element in a collection.</span></span> <span data-ttu-id="74d2a-115">Объект <xref:System.Activities.Statements.ForEach%601> аналогичен ключевому слову `foreach`, но реализуется как действие, а не как инструкция языка.</span><span class="sxs-lookup"><span data-stu-id="74d2a-115"><xref:System.Activities.Statements.ForEach%601> is similar to the keyword `foreach`, but is implemented as an activity rather than a language statement.</span></span>|  
|<xref:System.Activities.Statements.If>|<span data-ttu-id="74d2a-116">Выполняет содержащиеся действия в случае, если условие равно `true`; может выполнять действия, содержащиеся в свойстве <xref:System.Activities.Statements.If.Else%2A>, если условие равно `false`.</span><span class="sxs-lookup"><span data-stu-id="74d2a-116">Executes contained activities if a condition is `true`, and can execute activities contained in the <xref:System.Activities.Statements.If.Else%2A> property if the condition is `false`.</span></span>|  
|<xref:System.Activities.Statements.Parallel>|<span data-ttu-id="74d2a-117">Параллельно выполняет содержащиеся действия.</span><span class="sxs-lookup"><span data-stu-id="74d2a-117">Executes contained activities in parallel.</span></span>|  
|<xref:System.Activities.Statements.ParallelForEach%601>|<span data-ttu-id="74d2a-118">Выполняет внедренную инструкцию параллельно для каждого из элементов коллекции.</span><span class="sxs-lookup"><span data-stu-id="74d2a-118">Executes an embedded statement in parallel for each element in a collection.</span></span>|  
|<xref:System.Activities.Statements.Pick>|<span data-ttu-id="74d2a-119">Предоставляет моделирование потока управления на основе событий.</span><span class="sxs-lookup"><span data-stu-id="74d2a-119">Provides event-based control flow modeling.</span></span>|  
|<xref:System.Activities.Statements.PickBranch>|<span data-ttu-id="74d2a-120">Представляет потенциальный путь выполнения в действии <xref:System.Activities.Statements.Pick>.</span><span class="sxs-lookup"><span data-stu-id="74d2a-120">Represents a potential path of execution in a <xref:System.Activities.Statements.Pick> activity.</span></span>|  
|<xref:System.Activities.Statements.Sequence>|<span data-ttu-id="74d2a-121">Выполняет содержащиеся действия последовательно.</span><span class="sxs-lookup"><span data-stu-id="74d2a-121">Executes contained activities in sequence.</span></span>|  
|<xref:System.Activities.Statements.Switch%601>|<span data-ttu-id="74d2a-122">Выбирает для выполнения одно действие из нескольких в зависимости от значения указанного выражения.</span><span class="sxs-lookup"><span data-stu-id="74d2a-122">Selects one choice from a number of activities to execute, based on the value of a given expression.</span></span>|  
|<xref:System.Activities.Statements.While>|<span data-ttu-id="74d2a-123">Выполняет содержащиеся действия, если условие равно `true`.</span><span class="sxs-lookup"><span data-stu-id="74d2a-123">Executes contained activities while a condition is `true`.</span></span>|
