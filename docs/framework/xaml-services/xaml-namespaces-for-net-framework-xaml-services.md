---
title: Пространства имен языка XAML для служб .NET Framework XAML
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 11bf5d112c64fb0b942decf7635f02b90a98bdeb
ms.sourcegitcommit: a4f9b754059f0210e29ae0578363a27b9ba84b64
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 12/05/2019
ms.locfileid: "74837172"
---
# <a name="xaml-namespaces-for-net-framework-xaml-services"></a><span data-ttu-id="e057f-102">Пространства имен языка XAML для служб .NET Framework XAML</span><span class="sxs-lookup"><span data-stu-id="e057f-102">XAML Namespaces for .NET Framework XAML Services</span></span>
<span data-ttu-id="e057f-103">Пространство имен XAML — это концепция, которая расширяет определение пространства имен XML.</span><span class="sxs-lookup"><span data-stu-id="e057f-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="e057f-104">Аналогично пространству имен XML, можно определить пространство имен XAML с помощью атрибута `xmlns` в разметке.</span><span class="sxs-lookup"><span data-stu-id="e057f-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="e057f-105">Пространства имен XAML также представлены в потоке узлов XAML и других API-интерфейсах служб XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="e057f-106">В этом разделе определяется понятие пространства имен XAML и описываются способы определения пространств имен XAML и использования контекстов схемы XAML и других аспектов .NET Framework служб XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET Framework XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="e057f-107">Пространство имен XML и пространство имен XAML</span><span class="sxs-lookup"><span data-stu-id="e057f-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="e057f-108">Пространство имен XAML — это специализированное пространство имен XML, так же как XAML является специализированной формой XML и использует базовую форму XML для его разметки.</span><span class="sxs-lookup"><span data-stu-id="e057f-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="e057f-109">В разметке пространство имен XAML и его сопоставление объявляются с помощью атрибута `xmlns`, применяемого к элементу.</span><span class="sxs-lookup"><span data-stu-id="e057f-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="e057f-110">Объявление `xmlns` может быть выполнено с тем же элементом, что и пространство имен XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="e057f-111">Объявление пространства имен XAML, выполненное для элемента, допустимо для этого элемента, всех атрибутов этого элемента и всех дочерних элементов этого элемента.</span><span class="sxs-lookup"><span data-stu-id="e057f-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="e057f-112">Атрибуты могут использовать пространства имен XAML, которые не совпадают с элементом, содержащим атрибут, при условии, что само имя атрибута ссылается на префикс как часть имени атрибута в разметке.</span><span class="sxs-lookup"><span data-stu-id="e057f-112">Attributes can use a XAML namespaces that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="e057f-113">Различие между пространством имен XAML и пространством имен XML заключается в том, что пространство имен XML может использоваться для ссылки на схему или может использоваться для простого различения сущностей.</span><span class="sxs-lookup"><span data-stu-id="e057f-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema, or might be used to simply differentiate entities.</span></span> <span data-ttu-id="e057f-114">Для XAML типы и члены, используемые в XAML, в конечном итоге должны быть разрешены для резервных типов, а понятия схемы XML не применимы к этой возможности.</span><span class="sxs-lookup"><span data-stu-id="e057f-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="e057f-115">Пространство имен XAML содержит сведения, которые должны быть доступны контексту схемы XAML для выполнения этого сопоставления типов.</span><span class="sxs-lookup"><span data-stu-id="e057f-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="e057f-116">Компоненты пространства имен XAML</span><span class="sxs-lookup"><span data-stu-id="e057f-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="e057f-117">Определение пространства имен XAML содержит два компонента: префикс и идентификатор.</span><span class="sxs-lookup"><span data-stu-id="e057f-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="e057f-118">Каждый из этих компонентов имеется в том случае, если пространство имен XAML объявлено в разметке или определено в системе типов XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-118">Each of these components are present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="e057f-119">Префикс может быть любой строкой, как это разрешено [пространствами имен W3C в спецификации XML 1,0](https://www.w3.org/TR/REC-xml-names/) .</span><span class="sxs-lookup"><span data-stu-id="e057f-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/) .</span></span> <span data-ttu-id="e057f-120">По соглашению префиксы обычно являются короткими строками, так как префикс повторяется много раз в обычном файле разметки.</span><span class="sxs-lookup"><span data-stu-id="e057f-120">By convention, the prefixes are typically very short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="e057f-121">Некоторые пространства имен XAML, предназначенные для использования в нескольких реализациях XAML, используют определенные традиционные префиксы.</span><span class="sxs-lookup"><span data-stu-id="e057f-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="e057f-122">Например, пространство имен XAML языка XAML обычно сопоставляется с префиксом `x`.</span><span class="sxs-lookup"><span data-stu-id="e057f-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="e057f-123">Можно определить пространство имен XAML по умолчанию, где префикс не задан в определении, но представлен как пустая строка, если определен или запрашивался API служб XAML by.NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e057f-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET Framework XAML Services API.</span></span> <span data-ttu-id="e057f-124">Как правило, пространство имен XAML по умолчанию намеренно выбирается для повышения развернутого в префиксе разметки с помощью технологии, реализующей XAML, и ее сценариев и словари.</span><span class="sxs-lookup"><span data-stu-id="e057f-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="e057f-125">Идентификатор может быть любой строкой, как это разрешено [пространствами имен W3C в спецификации XML 1,0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="e057f-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="e057f-126">По соглашению идентификаторы для пространств имен XML или пространств имен XAML часто задаются в виде URI, обычно в виде абсолютного URI с указанием протокола.</span><span class="sxs-lookup"><span data-stu-id="e057f-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="e057f-127">Часто сведения о версии, определяющие конкретный словарь XAML, подразумеваются как часть строки пути.</span><span class="sxs-lookup"><span data-stu-id="e057f-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="e057f-128">Пространства имен XAML добавляют дополнительное соглашение об идентификаторах после соглашения об URI XML.</span><span class="sxs-lookup"><span data-stu-id="e057f-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="e057f-129">Для пространств имен XAML идентификатор передает сведения, необходимые контексту схемы XAML, для разрешения типов, указанных в качестве элементов в этом пространстве имен XAML, или для разрешения атрибутов для членов.</span><span class="sxs-lookup"><span data-stu-id="e057f-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="e057f-130">В целях передачи информации в контекст схемы XAML идентификатор пространства имен XAML может по-прежнему быть в формате URI.</span><span class="sxs-lookup"><span data-stu-id="e057f-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="e057f-131">Однако в этом случае URI также объявляется как соответствующий идентификатор в определенной сборке или списке сборок.</span><span class="sxs-lookup"><span data-stu-id="e057f-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="e057f-132">Это делается в сборках путем присвоения сборке <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span><span class="sxs-lookup"><span data-stu-id="e057f-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="e057f-133">Этот метод определения пространства имен XAML и поддержки поведения разрешения типов на основе среды CLR в сборке с атрибутами поддерживается контекстом схемы XAML по умолчанию в .NET Framework службах XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET Framework XAML Services.</span></span> <span data-ttu-id="e057f-134">В общем случае это соглашение можно использовать в случаях, когда контекст схемы XAML включает среду CLR или основан на контексте схемы XAML по умолчанию, который необходим для чтения атрибутов CLR из сборок CLR.</span><span class="sxs-lookup"><span data-stu-id="e057f-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="e057f-135">Пространства имен XAML также можно идентифицировать по соглашению, которое передает пространство имен CLR и сборку, определяющую тип.</span><span class="sxs-lookup"><span data-stu-id="e057f-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="e057f-136">Это соглашение используется в случаях, когда в сборках, содержащих типы, не существует <xref:System.Windows.Markup.XmlnsDefinitionAttribute> соотнесений.</span><span class="sxs-lookup"><span data-stu-id="e057f-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="e057f-137">Это соглашение потенциально сложнее, чем соглашение об универсальном коде ресурса (URI), а также может привести к неоднозначности и дублированию, поскольку существует несколько способов обращения к сборке.</span><span class="sxs-lookup"><span data-stu-id="e057f-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="e057f-138">Самая простая форма идентификатора, использующего пространство имен CLR и соглашение о сборке, выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="e057f-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="e057f-139">`clr-namespace:` *клрнснаме* `; assembly=` *ассемблишортнаме*</span><span class="sxs-lookup"><span data-stu-id="e057f-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span></span>  
  
 <span data-ttu-id="e057f-140">`clr-namespace:` и `; assembly=` являются литеральными компонентами синтаксиса.</span><span class="sxs-lookup"><span data-stu-id="e057f-140">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="e057f-141">*клрнснаме* — это строковое имя, идентифицирующее пространство имен CLR.</span><span class="sxs-lookup"><span data-stu-id="e057f-141">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="e057f-142">Это имя строки включает все внутренние символы-точки (.), которые предоставляют указания о пространстве имен CLR и его связи с другими пространствами имен CLR.</span><span class="sxs-lookup"><span data-stu-id="e057f-142">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>  
  
 <span data-ttu-id="e057f-143">*ассемблишортнаме* — это строковое имя сборки, определяющей типы, которые ПОЛЕЗНЫ в XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-143">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="e057f-144">Типы, к которым осуществляется доступ через объявленное пространство имен XAML, должны быть определены сборкой и должны быть объявлены в пространстве имен CLR, заданном параметром *клрнснаме*.</span><span class="sxs-lookup"><span data-stu-id="e057f-144">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be specifically declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="e057f-145">Это строковое имя обычно параллельно с данными, сообщаемыми <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e057f-145">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="e057f-146">Более полное определение соглашения о пространстве имен CLR и сборке выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="e057f-146">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="e057f-147">`clr-namespace:` *клрнснаме* `; assembly=` *AssemblyName*</span><span class="sxs-lookup"><span data-stu-id="e057f-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span></span>  
  
 <span data-ttu-id="e057f-148">*AssemblyName* представляет любую строку, которая является допустимой <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> входными данными.</span><span class="sxs-lookup"><span data-stu-id="e057f-148">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="e057f-149">Эта строка может включать сведения о культуре, открытом ключе или версии (определения этих концепций определены в справочном разделе по <xref:System.Reflection.Assembly>).</span><span class="sxs-lookup"><span data-stu-id="e057f-149">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="e057f-150">Формат COFF и свидетельство (используемые другими перегрузками <xref:System.Reflection.Assembly.Load%2A>) не применимы для загрузки сборок XAML. все сведения о нагрузке должны быть представлены в виде строки.</span><span class="sxs-lookup"><span data-stu-id="e057f-150">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="e057f-151">Указание открытого ключа для сборки является полезным методом для безопасности XAML или для удаления возможной неоднозначности, которая может существовать, если сборки загружаются простым именем или уже существуют в кэше или домене приложения.</span><span class="sxs-lookup"><span data-stu-id="e057f-151">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="e057f-152">Дополнительные сведения см. в разделе [вопросы безопасности XAML](xaml-security-considerations.md).</span><span class="sxs-lookup"><span data-stu-id="e057f-152">For more information, see [XAML Security Considerations](xaml-security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="e057f-153">Объявления пространств имен XAML в API служб XAML</span><span class="sxs-lookup"><span data-stu-id="e057f-153">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="e057f-154">В API служб XAML объявление пространства имен XAML представлено <xref:System.Xaml.NamespaceDeclaration> объектом.</span><span class="sxs-lookup"><span data-stu-id="e057f-154">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="e057f-155">При объявлении пространства имен XAML в коде вызывается конструктор <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29>.</span><span class="sxs-lookup"><span data-stu-id="e057f-155">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="e057f-156">Параметры `ns` и `prefix` указываются как строки, а входные данные для этих параметров соответствуют определению идентификатора пространства имен XAML и префиксу пространства имен XAML, как указано выше в этом разделе.</span><span class="sxs-lookup"><span data-stu-id="e057f-156">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="e057f-157">При проверке сведений о пространстве имен XAML в виде части потока узлов XAML или при помощи другого доступа к системе типов XAML <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> сообщает идентификатор пространства имен XAML, а <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> сообщает префикс пространства имен XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-157">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="e057f-158">В потоке узлов XAML сведения о пространстве имен XAML могут отображаться в виде узла XAML, который предшествует сущности, к которой он применяется.</span><span class="sxs-lookup"><span data-stu-id="e057f-158">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="e057f-159">Сюда входят случаи, когда сведения о пространстве имен XAML предшествуют `StartObject` корневого элемента XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-159">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="e057f-160">Для получения дополнительной информации см. [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span><span class="sxs-lookup"><span data-stu-id="e057f-160">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="e057f-161">Во многих сценариях, использующих API служб .NET Framework XAML, ожидается существование хотя бы одного объявления пространства имен XAML, и объявление должно либо содержать, либо ссылаться на сведения, необходимые контексту схемы XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-161">For many scenarios that use .NET Framework XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="e057f-162">Пространства имен XAML должны указывать сборки для загрузки или обрабатывать определенные типы в пространствах имен и сборках, которые уже загружены или известны контексту схемы XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-162">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="e057f-163">Чтобы создать поток узлов XAML, необходимо получить сведения о типе XAML с помощью контекста схемы XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-163">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="e057f-164">Не удается определить сведения о типе XAML без предварительного определения соответствующего пространства имен XAML для каждого создаваемого узла.</span><span class="sxs-lookup"><span data-stu-id="e057f-164">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="e057f-165">На этом этапе экземпляры типов еще не созданы, но контексту схемы XAML может потребоваться выполнить поиск информации из определяющей сборки и резервного типа.</span><span class="sxs-lookup"><span data-stu-id="e057f-165">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="e057f-166">Например, чтобы обработать разметку `<Party><PartyFavor/></Party>`, контекст схемы XAML должен иметь возможность определить имя и тип `ContentProperty` `Party`и, таким образом, также должен узнать сведения о пространстве имен XAML для `Party` и `PartyFavor`.</span><span class="sxs-lookup"><span data-stu-id="e057f-166">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="e057f-167">В случае контекста схемы XAML по умолчанию статическое отражение сообщает большую часть сведений о системе типов XAML, необходимых для создания узлов типов XAML в потоке узлов.</span><span class="sxs-lookup"><span data-stu-id="e057f-167">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="e057f-168">Чтобы создать граф объекта из потока узлов XAML, должны существовать объявления пространств имен XAML для каждого префикса XAML, используемого в исходной разметке и записанного в потоке узлов XAML.</span><span class="sxs-lookup"><span data-stu-id="e057f-168">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="e057f-169">На этом этапе создаются экземпляры, и происходит истинное поведение сопоставления типов.</span><span class="sxs-lookup"><span data-stu-id="e057f-169">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="e057f-170">Если необходимо предварительно заполнить сведения о пространстве имен XAML, то в тех случаях, когда пространство имен XAML, которое предполагается использовать для контекста схемы XAML, не определено в разметке, один из методов, которые можно использовать, — объявить объявления пространств имен XML в <xref:System.Xml.XmlParserContext> для <xref:System.Xml.XmlReader>.</span><span class="sxs-lookup"><span data-stu-id="e057f-170">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="e057f-171">Затем используйте этот <xref:System.Xml.XmlReader> в качестве входных данных для конструктора средства чтения XAML или <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e057f-171">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="e057f-172">Еще два других API, относящиеся к обработке пространства имен XAML в .NET Framework службах XAML, являются атрибутами <xref:System.Windows.Markup.XmlnsDefinitionAttribute> и <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span><span class="sxs-lookup"><span data-stu-id="e057f-172">Two other API that are relevant for XAML namespace handling in .NET Framework XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="e057f-173">Эти атрибуты применяются к сборкам.</span><span class="sxs-lookup"><span data-stu-id="e057f-173">These attributes apply to assemblies.</span></span> <span data-ttu-id="e057f-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> используется контекстом схемы XAML для интерпретации любого объявления пространства имен XAML, включающего универсальный код ресурса (URI).</span><span class="sxs-lookup"><span data-stu-id="e057f-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="e057f-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> используется средствами, которые создают XAML, чтобы конкретное пространство имен XAML можно было сериализовать с помощью прогнозируемого префикса.</span><span class="sxs-lookup"><span data-stu-id="e057f-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="e057f-176">Дополнительные сведения см. в разделе [АТРИБУТЫ CLR, связанные с XAML, для пользовательских типов и библиотек](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span><span class="sxs-lookup"><span data-stu-id="e057f-176">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e057f-177">См. также:</span><span class="sxs-lookup"><span data-stu-id="e057f-177">See also</span></span>

- [<span data-ttu-id="e057f-178">Общее представление о понятиях и структурах потока узлов XAML</span><span class="sxs-lookup"><span data-stu-id="e057f-178">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
