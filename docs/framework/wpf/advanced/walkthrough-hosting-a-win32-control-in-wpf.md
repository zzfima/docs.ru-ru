---
title: Проведение управления Win32 в WPF
titleSuffix: ''
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- hosting Win32 control in WPF [WPF]
- Win32 code [WPF], WPF interoperation
ms.assetid: a676b1eb-fc55-4355-93ab-df840c41cea0
ms.openlocfilehash: 5185e60640c652b79bd105db54830ac3acc57129
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/12/2020
ms.locfileid: "79186739"
---
# <a name="walkthrough-host-a-win32-control-in-wpf"></a><span data-ttu-id="cc444-102">Прохождение: Хозяин Win32 Контроль в WPF</span><span class="sxs-lookup"><span data-stu-id="cc444-102">Walkthrough: Host a Win32 Control in WPF</span></span>
<span data-ttu-id="cc444-103">Фонд презентации Windows (WPF) предоставляет богатую среду для создания приложений.</span><span class="sxs-lookup"><span data-stu-id="cc444-103">Windows Presentation Foundation (WPF) provides a rich environment for creating applications.</span></span> <span data-ttu-id="cc444-104">Однако, если у вас есть значительные инвестиции в код Win32, может быть более эффективным повторно использовать хотя бы часть этого кода в приложении WPF, а не переписывать его полностью.</span><span class="sxs-lookup"><span data-stu-id="cc444-104">However, when you have a substantial investment in Win32 code, it may be more effective to reuse at least some of that code in your WPF application rather than rewrite it completely.</span></span> <span data-ttu-id="cc444-105">WPF предоставляет простой механизм размещения окна Win32 на странице WPF.</span><span class="sxs-lookup"><span data-stu-id="cc444-105">WPF provides a straightforward mechanism for hosting a Win32 window, on a WPF page.</span></span>  
  
 <span data-ttu-id="cc444-106">Эта тема прогулки вас через приложение, [Хостинг Win32 ListBox управления в WPF образец](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), который хостов Win32 список поле управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-106">This topic walks you through an application, [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control), that hosts a Win32 list box control.</span></span> <span data-ttu-id="cc444-107">Эта общая процедура может быть распространена на хостинг любого окна Win32.</span><span class="sxs-lookup"><span data-stu-id="cc444-107">This general procedure can be extended to hosting any Win32 window.</span></span>  

<a name="requirements"></a>
## <a name="requirements"></a><span data-ttu-id="cc444-108">Требования</span><span class="sxs-lookup"><span data-stu-id="cc444-108">Requirements</span></span>  
 <span data-ttu-id="cc444-109">Эта тема предполагает базовое знакомство с программированием WPF и Windows API.</span><span class="sxs-lookup"><span data-stu-id="cc444-109">This topic assumes a basic familiarity with both WPF and Windows API programming.</span></span> <span data-ttu-id="cc444-110">Для базового введения в программирование WPF [см.](../getting-started/index.md)</span><span class="sxs-lookup"><span data-stu-id="cc444-110">For a basic introduction to WPF programming, see [Getting Started](../getting-started/index.md).</span></span> <span data-ttu-id="cc444-111">Для введения в программирование API Windows, см. *Programming Windows*</span><span class="sxs-lookup"><span data-stu-id="cc444-111">For an introduction to Windows API programming, see any of the numerous books on the subject, in particular *Programming Windows* by Charles Petzold.</span></span>  
  
 <span data-ttu-id="cc444-112">Поскольку образец, сопровождающий эту тему, реализован в СЗ, он использует службы вызова платформы (PInvoke) для доступа к API Windows.</span><span class="sxs-lookup"><span data-stu-id="cc444-112">Because the sample that accompanies this topic is implemented in C#, it makes use of Platform Invocation Services (PInvoke) to access the Windows API.</span></span> <span data-ttu-id="cc444-113">Некоторое знакомство с PInvoke полезно, но не является существенным.</span><span class="sxs-lookup"><span data-stu-id="cc444-113">Some familiarity with PInvoke is helpful but not essential.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="cc444-114">Этот учебник включает ряд примеров кода из связанного примера.</span><span class="sxs-lookup"><span data-stu-id="cc444-114">This topic includes a number of code examples from the associated sample.</span></span> <span data-ttu-id="cc444-115">Однако для удобства чтения он не содержит полный пример кода.</span><span class="sxs-lookup"><span data-stu-id="cc444-115">However, for readability, it does not include the complete sample code.</span></span> <span data-ttu-id="cc444-116">Вы можете получить или просмотреть полный код от [хостинга Win32 ListBox Control в образце WPF.](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control)</span><span class="sxs-lookup"><span data-stu-id="cc444-116">You can obtain or view complete code from [Hosting a Win32 ListBox Control in WPF Sample](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control).</span></span>  
  
<a name="basic_procedure"></a>
## <a name="the-basic-procedure"></a><span data-ttu-id="cc444-117">Основная процедура</span><span class="sxs-lookup"><span data-stu-id="cc444-117">The Basic Procedure</span></span>  
 <span data-ttu-id="cc444-118">В этом разделе излагается основная процедура размещения окна Win32 на странице WPF.</span><span class="sxs-lookup"><span data-stu-id="cc444-118">This section outlines the basic procedure for hosting a Win32 window on a WPF page.</span></span> <span data-ttu-id="cc444-119">В остальных разделах подробно описывается каждый шаг.</span><span class="sxs-lookup"><span data-stu-id="cc444-119">The remaining sections go through the details of each step.</span></span>  
  
 <span data-ttu-id="cc444-120">Основная процедура размещения</span><span class="sxs-lookup"><span data-stu-id="cc444-120">The basic hosting procedure is:</span></span>  
  
1. <span data-ttu-id="cc444-121">Реализация страницы WPF для размещения окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-121">Implement a WPF page to host the window.</span></span> <span data-ttu-id="cc444-122">Один из методов <xref:System.Windows.Controls.Border> заключается в создании элемента для резервирования раздела страницы для размещенного окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-122">One technique is to create a <xref:System.Windows.Controls.Border> element to reserve a section of the page for the hosted window.</span></span>  
  
2. <span data-ttu-id="cc444-123">Реализация класса для размещения элемента <xref:System.Windows.Interop.HwndHost>управления, наследуетемого от .</span><span class="sxs-lookup"><span data-stu-id="cc444-123">Implement a class to host the control that inherits from <xref:System.Windows.Interop.HwndHost>.</span></span>  
  
3. <span data-ttu-id="cc444-124">В этом классе переопределить <xref:System.Windows.Interop.HwndHost> <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>участника класса.</span><span class="sxs-lookup"><span data-stu-id="cc444-124">In that class, override the <xref:System.Windows.Interop.HwndHost> class member <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>.</span></span>  
  
4. <span data-ttu-id="cc444-125">Создайте размещенное окно в качестве ребенка окна, содержащего страницу WPF.</span><span class="sxs-lookup"><span data-stu-id="cc444-125">Create the hosted window as a child of the window that contains the WPF page.</span></span> <span data-ttu-id="cc444-126">Хотя обычное программирование WPF не обязательно использовать его, страница хостинга представляет собой окно с ручкой (HWND).</span><span class="sxs-lookup"><span data-stu-id="cc444-126">Although conventional WPF programming does not need to explicitly make use of it, the hosting page is a window with a handle (HWND).</span></span> <span data-ttu-id="cc444-127">Вы получаете страницу HWND по `hwndParent` параметру метода. <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A></span><span class="sxs-lookup"><span data-stu-id="cc444-127">You receive the page HWND through the `hwndParent` parameter of the <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A> method.</span></span> <span data-ttu-id="cc444-128">Размещаемое окно необходимо создать в качестве дочернего объекта этого окна HWND.</span><span class="sxs-lookup"><span data-stu-id="cc444-128">The hosted window should be created as a child of this HWND.</span></span>  
  
5. <span data-ttu-id="cc444-129">Создав главное окно, верните HWND размещаемого окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-129">Once you have created the host window, return the HWND of the hosted window.</span></span> <span data-ttu-id="cc444-130">Если вы хотите разместить один или несколько элементов управления Win32, вы обычно создаете окно хоста в качестве ребенка HWND и создаете элементы управления в этом окне хоста.</span><span class="sxs-lookup"><span data-stu-id="cc444-130">If you want to host one or more Win32 controls, you typically create a host window as a child of the HWND and make the controls children of that host window.</span></span> <span data-ttu-id="cc444-131">Упаковка элементов управления в окне хоста обеспечивает простой способ получения уведомлений от элементов управления, которые касались некоторых конкретных проблем Win32 с уведомлениями через границу HWND.</span><span class="sxs-lookup"><span data-stu-id="cc444-131">Wrapping the controls in a host window provides a simple way for your WPF page to receive notifications from the controls, which deals with some particular Win32 issues with notifications across the HWND boundary.</span></span>  
  
6. <span data-ttu-id="cc444-132">Обрабатывайте некоторые сообщения, которые отправляются в главное окно, например уведомления от дочерних элементов управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-132">Handle selected messages sent to the host window, such as notifications from child controls.</span></span> <span data-ttu-id="cc444-133">Это можно сделать двумя способами.</span><span class="sxs-lookup"><span data-stu-id="cc444-133">There are two ways to do this.</span></span>  
  
    - <span data-ttu-id="cc444-134">Если вы предпочитаете <xref:System.Windows.Interop.HwndHost.WndProc%2A> обрабатывать сообщения в классе хостинга, переопределить метод <xref:System.Windows.Interop.HwndHost> класса.</span><span class="sxs-lookup"><span data-stu-id="cc444-134">If you prefer to handle messages in your hosting class, override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> method of the <xref:System.Windows.Interop.HwndHost> class.</span></span>  
  
    - <span data-ttu-id="cc444-135">Если вы предпочитаете, чтобы WPF обрабатывал сообщения, обрабатывают событие <xref:System.Windows.Interop.HwndHost> класса <xref:System.Windows.Interop.HwndHost.MessageHook> в коде за ним.</span><span class="sxs-lookup"><span data-stu-id="cc444-135">If you prefer to have the WPF handle the messages, handle the <xref:System.Windows.Interop.HwndHost> class <xref:System.Windows.Interop.HwndHost.MessageHook> event in your code-behind.</span></span> <span data-ttu-id="cc444-136">Это событие возникает для каждого сообщения, получаемого размещаемым окном.</span><span class="sxs-lookup"><span data-stu-id="cc444-136">This event occurs for every message that is received by the hosted window.</span></span> <span data-ttu-id="cc444-137">Если вы выбираете этот вариант, <xref:System.Windows.Interop.HwndHost.WndProc%2A>вы все равно должны переопределить, но вам нужна только минимальная реализация.</span><span class="sxs-lookup"><span data-stu-id="cc444-137">If you choose this option, you must still override <xref:System.Windows.Interop.HwndHost.WndProc%2A>, but you only need a minimal implementation.</span></span>  
  
7. <span data-ttu-id="cc444-138">Переопределить <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> и <xref:System.Windows.Interop.HwndHost.WndProc%2A> методы <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="cc444-138">Override the <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> and <xref:System.Windows.Interop.HwndHost.WndProc%2A> methods of <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="cc444-139">Вы должны переопределить эти методы, чтобы удовлетворить <xref:System.Windows.Interop.HwndHost> контракт, но вам может потребоваться только обеспечить минимальную реализацию.</span><span class="sxs-lookup"><span data-stu-id="cc444-139">You must override these methods to satisfy the <xref:System.Windows.Interop.HwndHost> contract, but you may only need to provide a minimal implementation.</span></span>  
  
8. <span data-ttu-id="cc444-140">В файле с кодом создайте экземпляр класса хостинга управления <xref:System.Windows.Controls.Border> и сделайте его ребенком элемента, предназначенного для размещения окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-140">In your code-behind file, create an instance of the control hosting class and make it a child of the <xref:System.Windows.Controls.Border> element that is intended to host the window.</span></span>  
  
9. <span data-ttu-id="cc444-141">Общайтесь с размещенным окном, отправляя ему сообщения Microsoft Windows и обрабатывая сообщения из своих детских окон, такие как уведомления, отправленные элементами управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-141">Communicate with the hosted window by sending it Microsoft Windows messages and handling messages from its child windows, such as notifications sent by controls.</span></span>  
  
<a name="page_layout"></a>
## <a name="implement-the-page-layout"></a><span data-ttu-id="cc444-142">Реализация макета страницы</span><span class="sxs-lookup"><span data-stu-id="cc444-142">Implement the Page Layout</span></span>  
 <span data-ttu-id="cc444-143">Макет для страницы WPF, на котором размещает сяврей ListBox, состоит из двух регионов.</span><span class="sxs-lookup"><span data-stu-id="cc444-143">The layout for the WPF page that hosts the ListBox Control consists of two regions.</span></span> <span data-ttu-id="cc444-144">В левой части страницы находится несколько [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] элементов управления WPF, которые обеспечивают, что позволяет манипулировать управлением Win32.</span><span class="sxs-lookup"><span data-stu-id="cc444-144">The left side of the page hosts several WPF controls that provide a [!INCLUDE[TLA#tla_ui](../../../../includes/tlasharptla-ui-md.md)] that allows you to manipulate the Win32 control.</span></span> <span data-ttu-id="cc444-145">В правом верхнем углу страницы имеется квадратная область для размещенного элемента управления ListBox.</span><span class="sxs-lookup"><span data-stu-id="cc444-145">The upper right corner of the page has a square region for the hosted ListBox Control.</span></span>  
  
 <span data-ttu-id="cc444-146">Код для реализации этого макета довольно прост.</span><span class="sxs-lookup"><span data-stu-id="cc444-146">The code to implement this layout is quite simple.</span></span> <span data-ttu-id="cc444-147">Корневой элемент <xref:System.Windows.Controls.DockPanel> имеет два элемента ребенка.</span><span class="sxs-lookup"><span data-stu-id="cc444-147">The root element is a <xref:System.Windows.Controls.DockPanel> that has two child elements.</span></span> <span data-ttu-id="cc444-148">Первый элемент, <xref:System.Windows.Controls.Border> который размещает контроль ListBox.</span><span class="sxs-lookup"><span data-stu-id="cc444-148">The first is a <xref:System.Windows.Controls.Border> element that hosts the ListBox Control.</span></span> <span data-ttu-id="cc444-149">Он занимает квадрат размером 200 x 200 в верхнем правом углу страницы.</span><span class="sxs-lookup"><span data-stu-id="cc444-149">It occupies a 200x200 square in the upper right corner of the page.</span></span> <span data-ttu-id="cc444-150">Второй <xref:System.Windows.Controls.StackPanel> элемент, который содержит набор элементов управления WPF, которые отображают информацию и позволяют манипулировать Control ListBox, установив открытые свойства интероперации.</span><span class="sxs-lookup"><span data-stu-id="cc444-150">The second is a <xref:System.Windows.Controls.StackPanel> element that contains a set of WPF controls that display information and allow you to manipulate the ListBox Control by setting exposed interoperation properties.</span></span> <span data-ttu-id="cc444-151">Для каждого из <xref:System.Windows.Controls.StackPanel>элементов, которые являются детьми, см. справочный материал для различных элементов, используемых для получения подробной информации о том, что эти элементы или что они делают, они перечислены в примере кода ниже, но не будут объяснены здесь (основная модель интероперации не требует любого из них, они предоставляются, чтобы добавить некоторую интерактивность в образец).</span><span class="sxs-lookup"><span data-stu-id="cc444-151">For each of the elements that are children of the <xref:System.Windows.Controls.StackPanel>, see the reference material for the various elements used for details on what these elements are or what they do, these are listed in the example code below but will not be explained here (the basic interoperation model does not require any of them, they are provided to add some interactivity to the sample).</span></span>  
  
 [!code-xaml[WPFHostingWin32Control#WPFUI](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml#wpfui)]  
  
<a name="host_class"></a>
## <a name="implement-a-class-to-host-the-microsoft-win32-control"></a><span data-ttu-id="cc444-152">Реализация размещения элемента управления Microsoft Win32 в классе</span><span class="sxs-lookup"><span data-stu-id="cc444-152">Implement a Class to Host the Microsoft Win32 Control</span></span>  
 <span data-ttu-id="cc444-153">Ядром этого образца является класс, который фактически размещает в себе элемент управления, ControlHost.cs.</span><span class="sxs-lookup"><span data-stu-id="cc444-153">The core of this sample is the class that actually hosts the control, ControlHost.cs.</span></span> <span data-ttu-id="cc444-154">Он наследует от <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="cc444-154">It inherits from <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="cc444-155">Конструктор принимает два параметра, высоту и ширину, которые <xref:System.Windows.Controls.Border> соответствуют высоте и ширине элемента, который размещает элемент ListBox.</span><span class="sxs-lookup"><span data-stu-id="cc444-155">The constructor takes two parameters, height and width, which correspond to the height and width of the <xref:System.Windows.Controls.Border> element that hosts the ListBox control.</span></span> <span data-ttu-id="cc444-156">Эти значения используются позже для обеспечения соответствия размера элемента элементу. <xref:System.Windows.Controls.Border></span><span class="sxs-lookup"><span data-stu-id="cc444-156">These values are used later to ensure that the size of the control matches the <xref:System.Windows.Controls.Border> element.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostclass)]
 [!code-vb[WPFHostingWin32Control#ControlHostClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostclass)]  
  
 <span data-ttu-id="cc444-157">Кроме того, имеется набор констант.</span><span class="sxs-lookup"><span data-stu-id="cc444-157">There is also a set of constants.</span></span> <span data-ttu-id="cc444-158">Эти константы в значительной степени взяты из Winuser.h, и позволяют использовать обычные имена при вызове функций Win32.</span><span class="sxs-lookup"><span data-stu-id="cc444-158">These constants are largely taken from Winuser.h, and allow you to use conventional names when calling Win32 functions.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#controlhostconstants)]
 [!code-vb[WPFHostingWin32Control#ControlHostConstants](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#controlhostconstants)]  
  
<a name="buildwindowcore"></a>
### <a name="override-buildwindowcore-to-create-the-microsoft-win32-window"></a><span data-ttu-id="cc444-159">Переопределение класса BuildWindowCore для создания окна Microsoft Win32</span><span class="sxs-lookup"><span data-stu-id="cc444-159">Override BuildWindowCore to Create the Microsoft Win32 Window</span></span>  
 <span data-ttu-id="cc444-160">Вы переопределить этот метод, чтобы создать окно Win32, которое будет размещено на странице, и сделать соединение между окном и страницей.</span><span class="sxs-lookup"><span data-stu-id="cc444-160">You override this method to create the Win32 window that will be hosted by the page, and make the connection between the window and the page.</span></span> <span data-ttu-id="cc444-161">Поскольку в этом примере также показано размещение элемента управления ListBox, создаются два окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-161">Because this sample involves hosting a ListBox Control, two windows are created.</span></span> <span data-ttu-id="cc444-162">Во-первых, это окно, которое фактически размещено на странице WPF.</span><span class="sxs-lookup"><span data-stu-id="cc444-162">The first is the window that is actually hosted by the WPF page.</span></span> <span data-ttu-id="cc444-163">Элемент управления ListBox создается в качестве дочернего элемента этого окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-163">The ListBox Control is created as a child of that window.</span></span>  
  
 <span data-ttu-id="cc444-164">Это делается для того, чтобы упростить получение уведомлений от элемента управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-164">The reason for this approach is to simplify the process of receiving notifications from the control.</span></span> <span data-ttu-id="cc444-165">Класс <xref:System.Windows.Interop.HwndHost> позволяет обрабатывать сообщения, отправленные в окно, которое он размещает.</span><span class="sxs-lookup"><span data-stu-id="cc444-165">The <xref:System.Windows.Interop.HwndHost> class allows you to process messages sent to the window that it is hosting.</span></span> <span data-ttu-id="cc444-166">Если вы размещаете элемент управления Win32 напрямую, вы получаете сообщения, отправленные во внутренний цикл сообщения элемента управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-166">If you host a Win32 control directly, you receive the messages sent to the internal message loop of the control.</span></span> <span data-ttu-id="cc444-167">Можно отображать элемент управления и отправлять ему сообщения, но не получать уведомления, которые элемент управления отправляет своему родительскому окну.</span><span class="sxs-lookup"><span data-stu-id="cc444-167">You can display the control and send it messages, but you do not receive the notifications that the control sends to its parent window.</span></span> <span data-ttu-id="cc444-168">Это означает, среди прочего, что нет способа обнаружения взаимодействия пользователя с элементом управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-168">This means, among other things, that you have no way of detecting when the user interacts with the control.</span></span> <span data-ttu-id="cc444-169">Вместо этого следует создать главное окно и сделать элемент управления дочерним элементом этого окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-169">Instead, create a host window and make the control a child of that window.</span></span> <span data-ttu-id="cc444-170">Это позволит обрабатывать сообщения для главного окна, включая уведомления, отправляемые элементом управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-170">This allows you to process the messages for the host window including the notifications sent to it by the control.</span></span> <span data-ttu-id="cc444-171">Для удобства, поскольку главное окно — это всего лишь программа-оболочка для элемента управления, мы будем называть этот пакет "элементом управления ListBox".</span><span class="sxs-lookup"><span data-stu-id="cc444-171">For convenience, since the host window is little more than a simple wrapper for the control, the package will be referred to as a ListBox control.</span></span>  
  
<a name="create_the_window_and_listbox"></a>
#### <a name="create-the-host-window-and-listbox-control"></a><span data-ttu-id="cc444-172">Создание главного окна и элемента управления ListBox</span><span class="sxs-lookup"><span data-stu-id="cc444-172">Create the Host Window and ListBox Control</span></span>  
 <span data-ttu-id="cc444-173">Можно использовать PInvoke для создания окна-хозяина для управления путем создания и регистрации класса окон и так далее.</span><span class="sxs-lookup"><span data-stu-id="cc444-173">You can use PInvoke to create a host window for the control by creating and registering a window class, and so on.</span></span> <span data-ttu-id="cc444-174">Однако гораздо проще создать окно с помощью готового "статического" класса окон.</span><span class="sxs-lookup"><span data-stu-id="cc444-174">However, a much simpler approach is to create a window with the predefined "static" window class.</span></span> <span data-ttu-id="cc444-175">Это позволит реализовать в окне процедуру, необходимую для получения уведомлений из элемента управления, и требует минимальной работы с кодом.</span><span class="sxs-lookup"><span data-stu-id="cc444-175">This provides you with the window procedure you need in order to receive notifications from the control, and requires minimal coding.</span></span>  
  
 <span data-ttu-id="cc444-176">HWND элемента управления предоставляется через доступное только для чтения свойство, чтобы главная страница могла использовать его для отправки сообщения элементу управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-176">The HWND of the control is exposed through a read-only property, such that the host page can use it to send messages to the control.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#intptrproperty)]
 [!code-vb[WPFHostingWin32Control#IntPtrProperty](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#intptrproperty)]  
  
 <span data-ttu-id="cc444-177">Элемент управления ListBox создается в качестве дочернего элемента главного окна.</span><span class="sxs-lookup"><span data-stu-id="cc444-177">The ListBox control is created as a child of the host window.</span></span> <span data-ttu-id="cc444-178">Высота и ширина обоих окон задается с помощью значений, передаваемых конструктору (см. выше).</span><span class="sxs-lookup"><span data-stu-id="cc444-178">The height and width of both windows are set to the values passed to the constructor, discussed above.</span></span> <span data-ttu-id="cc444-179">Это гарантирует, что размер главного окна и элемента управления идентичен размеру отведенной для них области на странице.</span><span class="sxs-lookup"><span data-stu-id="cc444-179">This ensures that the size of the host window and control is identical to the reserved area on the page.</span></span>  <span data-ttu-id="cc444-180">После создания окон образец возвращает <xref:System.Runtime.InteropServices.HandleRef> объект, содержащий HWND окна хоста.</span><span class="sxs-lookup"><span data-stu-id="cc444-180">After the windows are created, the sample returns a <xref:System.Runtime.InteropServices.HandleRef> object that contains the HWND of the host window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcore)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCore](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcore)]  
  
 [!code-csharp[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#buildwindowcorehelper)]
 [!code-vb[WPFHostingWin32Control#BuildWindowCoreHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#buildwindowcorehelper)]  
  
<a name="destroywindow_wndproc"></a>
### <a name="implement-destroywindow-and-wndproc"></a><span data-ttu-id="cc444-181">Реализация классов DestroyWindow и WndProc</span><span class="sxs-lookup"><span data-stu-id="cc444-181">Implement DestroyWindow and WndProc</span></span>  
 <span data-ttu-id="cc444-182">В дополнение <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>к , Вы <xref:System.Windows.Interop.HwndHost.WndProc%2A> также <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> должны переопределить и методы <xref:System.Windows.Interop.HwndHost>.</span><span class="sxs-lookup"><span data-stu-id="cc444-182">In addition to <xref:System.Windows.Interop.HwndHost.BuildWindowCore%2A>, you must also override the <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> methods of the <xref:System.Windows.Interop.HwndHost>.</span></span> <span data-ttu-id="cc444-183">В этом примере сообщения для управления обрабатываются <xref:System.Windows.Interop.HwndHost.MessageHook> обработчиком, таким образом, реализация <xref:System.Windows.Interop.HwndHost.WndProc%2A> и <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> минимальна.</span><span class="sxs-lookup"><span data-stu-id="cc444-183">In this example, the messages for the control are handled by the <xref:System.Windows.Interop.HwndHost.MessageHook> handler, thus the implementation of <xref:System.Windows.Interop.HwndHost.WndProc%2A> and <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A> is minimal.</span></span> <span data-ttu-id="cc444-184">В <xref:System.Windows.Interop.HwndHost.WndProc%2A>случае, установленный `handled` для `false` указания на то, что сообщение не было обработано и возврат 0.</span><span class="sxs-lookup"><span data-stu-id="cc444-184">In the case of <xref:System.Windows.Interop.HwndHost.WndProc%2A>, set `handled` to `false` to indicate that the  message was not handled and return 0.</span></span> <span data-ttu-id="cc444-185">Ибо, <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>просто уничтожить окно.</span><span class="sxs-lookup"><span data-stu-id="cc444-185">For <xref:System.Windows.Interop.HwndHost.DestroyWindowCore%2A>, simply destroy the window.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroy)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroy](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroy)]  
  
 [!code-csharp[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/ControlHost.cs#wndprocdestroyhelper)]
 [!code-vb[WPFHostingWin32Control#WndProcDestroyHelper](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/ControlHost.vb#wndprocdestroyhelper)]  
  
<a name="host_the_control"></a>
## <a name="host-the-control-on-the-page"></a><span data-ttu-id="cc444-186">Размещение элемента управления на странице</span><span class="sxs-lookup"><span data-stu-id="cc444-186">Host the Control on the Page</span></span>  
 <span data-ttu-id="cc444-187">Чтобы разместить элемент управления на странице, сначала создайте новый экземпляр `ControlHost` класса.</span><span class="sxs-lookup"><span data-stu-id="cc444-187">To host the control on the page, you first create a new instance of the `ControlHost` class.</span></span> <span data-ttu-id="cc444-188">Передайте высоту и ширину элемента`ControlHostElement`границы, `ControlHost` содержащего элемент управления () конструктору.</span><span class="sxs-lookup"><span data-stu-id="cc444-188">Pass the height and width of the border element that contains the control (`ControlHostElement`) to the `ControlHost` constructor.</span></span> <span data-ttu-id="cc444-189">Это позволит гарантировать, что ListBox имеет правильный размер.</span><span class="sxs-lookup"><span data-stu-id="cc444-189">This ensures that the ListBox is sized correctly.</span></span> <span data-ttu-id="cc444-190">Затем элемент управления размещается на `ControlHost` странице, <xref:System.Windows.Controls.Decorator.Child%2A> назначая <xref:System.Windows.Controls.Border>объект свойству узла.</span><span class="sxs-lookup"><span data-stu-id="cc444-190">You then host the control on the page by assigning the `ControlHost` object to the <xref:System.Windows.Controls.Decorator.Child%2A> property of the host <xref:System.Windows.Controls.Border>.</span></span>  
  
 <span data-ttu-id="cc444-191">Образец прикрепляет обработчика `ControlHost` к событию <xref:System.Windows.Interop.HwndHost.MessageHook> получения сообщений от элемента управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-191">The sample attaches a handler to the <xref:System.Windows.Interop.HwndHost.MessageHook> event of the `ControlHost` to receive messages from the control.</span></span> <span data-ttu-id="cc444-192">Это событие вызывается для каждого сообщения, отправляемого в главное окно.</span><span class="sxs-lookup"><span data-stu-id="cc444-192">This event is raised for every message sent to the hosted window.</span></span> <span data-ttu-id="cc444-193">В данном случае это сообщения, отправленные в окно, которое служит оболочкой текущего элемента управления ListBox, включая уведомления от элемента управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-193">In this case, these are the messages sent to window that wraps the actual ListBox control, including notifications from the control.</span></span> <span data-ttu-id="cc444-194">В этом примере вызывается метод SendMessage, чтобы получать информацию от элемента управления и менять его содержимое.</span><span class="sxs-lookup"><span data-stu-id="cc444-194">The sample calls SendMessage to obtain information from the control and modify its contents.</span></span> <span data-ttu-id="cc444-195">Подробные сведения о том, как страница обменивается данными с элементом управления, рассматриваются в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="cc444-195">The details of how the page communicates with the control are discussed in the next section.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="cc444-196">Обратите внимание, что для SendMessage есть две декларации PInvoke.</span><span class="sxs-lookup"><span data-stu-id="cc444-196">Notice that there are two PInvoke declarations for SendMessage.</span></span> <span data-ttu-id="cc444-197">Это необходимо, потому `wParam` что один использует параметр для прохождения строки, а другой использует его для прохождения рядов.</span><span class="sxs-lookup"><span data-stu-id="cc444-197">This is necessary because one uses the `wParam` parameter to pass a string and the other uses it to pass an integer.</span></span> <span data-ttu-id="cc444-198">Необходимо отдельно объявить каждую сигнатуру, чтобы обеспечить правильный маршалинг данных.</span><span class="sxs-lookup"><span data-stu-id="cc444-198">You need a separate declaration for each signature to ensure that the data is marshaled correctly.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#hostwindowclass)]
 [!code-vb[WPFHostingWin32Control#HostWindowClass](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#hostwindowclass)]  
  
 [!code-csharp[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#controlmsgfiltersendmessage)]
 [!code-vb[WPFHostingWin32Control#ControlMsgFilterSendMessage](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#controlmsgfiltersendmessage)]  
  
<a name="communication"></a>
## <a name="implement-communication-between-the-control-and-the-page"></a><span data-ttu-id="cc444-199">Обмен данными между элементом управления и страницей</span><span class="sxs-lookup"><span data-stu-id="cc444-199">Implement Communication Between the Control and the Page</span></span>  
 <span data-ttu-id="cc444-200">Вы манипулируете управлением, отправляя ему сообщения Windows.</span><span class="sxs-lookup"><span data-stu-id="cc444-200">You manipulate the control by sending it Windows messages.</span></span> <span data-ttu-id="cc444-201">Элемент управления уведомляет вас, когда пользователь взаимодействует с ним, отправляя уведомления в главное окно.</span><span class="sxs-lookup"><span data-stu-id="cc444-201">The control notifies you when the user interacts with it by sending notifications to its host window.</span></span> <span data-ttu-id="cc444-202">[Хостинг Win32 ListBox Control в](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) выборке WPF включает в себя uI, который предоставляет несколько примеров того, как это работает:</span><span class="sxs-lookup"><span data-stu-id="cc444-202">The [Hosting a Win32 ListBox Control in WPF](https://github.com/Microsoft/WPF-Samples/tree/master/Migration%20and%20Interoperability/WPFHostingWin32Control) sample includes a UI that provides several examples of how this works:</span></span>  
  
- <span data-ttu-id="cc444-203">Добавить элемент в список</span><span class="sxs-lookup"><span data-stu-id="cc444-203">Append an item to the list.</span></span>  
  
- <span data-ttu-id="cc444-204">Удалить выбранный элемент из списка</span><span class="sxs-lookup"><span data-stu-id="cc444-204">Delete the selected item from the list</span></span>  
  
- <span data-ttu-id="cc444-205">Отобразить текст выбранного в настоящее время элемента</span><span class="sxs-lookup"><span data-stu-id="cc444-205">Display the text of the currently selected item.</span></span>  
  
- <span data-ttu-id="cc444-206">Отобразить число элементов в списке</span><span class="sxs-lookup"><span data-stu-id="cc444-206">Display the number of items in the list.</span></span>  
  
 <span data-ttu-id="cc444-207">Пользователь также может выбрать элемент в поле списка, нажав на него, так же, как они будут для обычного приложения Win32.</span><span class="sxs-lookup"><span data-stu-id="cc444-207">The user can also select an item in the list box by clicking on it, just as they would for a conventional Win32 application.</span></span> <span data-ttu-id="cc444-208">Отображаемые данные обновляются каждый раз, когда пользователь меняет состояние списка, выбирая, добавляя или изменяя документы.</span><span class="sxs-lookup"><span data-stu-id="cc444-208">The displayed data is updated each time the user changes the state of the list box by selecting, adding, or appending an item.</span></span>  
  
 <span data-ttu-id="cc444-209">Чтобы присвоить элементы, отправьте в поле списка [ `LB_ADDSTRING` сообщение.](/windows/desktop/Controls/lb-addstring)</span><span class="sxs-lookup"><span data-stu-id="cc444-209">To append items, send the list box an [`LB_ADDSTRING` message](/windows/desktop/Controls/lb-addstring).</span></span> <span data-ttu-id="cc444-210">Чтобы удалить элементы, отправьте, [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) чтобы получить [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) индекс текущего выбора, а затем удалить элемент.</span><span class="sxs-lookup"><span data-stu-id="cc444-210">To delete items, send [`LB_GETCURSEL`](/windows/desktop/Controls/lb-getcursel) to get the index of the current selection and then [`LB_DELETESTRING`](/windows/desktop/Controls/lb-deletestring) to delete the item.</span></span> <span data-ttu-id="cc444-211">Образец также [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount)отправляет и использует возвращенное значение для обновления дисплея, отображая количество элементов.</span><span class="sxs-lookup"><span data-stu-id="cc444-211">The sample also sends [`LB_GETCOUNT`](/windows/desktop/Controls/lb-getcount), and uses the returned value to update the display that shows the number of items.</span></span> <span data-ttu-id="cc444-212">Оба эти примера [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) использования одной из деклараций PInvoke обсуждались в предыдущем разделе.</span><span class="sxs-lookup"><span data-stu-id="cc444-212">Both these instances of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) use one of the PInvoke declarations discussed in the previous section.</span></span>  
  
 [!code-csharp[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/csharp/VS_Snippets_Wpf/WPFHostingWin32Control/CSharp/Page1.xaml.cs#appenddeletetext)]
 [!code-vb[WPFHostingWin32Control#AppendDeleteText](~/samples/snippets/visualbasic/VS_Snippets_Wpf/WPFHostingWin32Control/VisualBasic/Page1.xaml.vb#appenddeletetext)]  
  
 <span data-ttu-id="cc444-213">Когда пользователь выбирает элемент или изменяет свой выбор, элемент уведомляет окно узла, отправляя ему [ `WM_COMMAND` сообщение,](/windows/desktop/menurc/wm-command)которое поднимает <xref:System.Windows.Interop.HwndHost.MessageHook> событие для страницы.</span><span class="sxs-lookup"><span data-stu-id="cc444-213">When the user selects an item or changes their selection, the control notifies the host window by sending it a [`WM_COMMAND` message](/windows/desktop/menurc/wm-command), which raises the <xref:System.Windows.Interop.HwndHost.MessageHook> event for the page.</span></span> <span data-ttu-id="cc444-214">Обработчик получает те же сведения, что и процедура главного окна в главном окне.</span><span class="sxs-lookup"><span data-stu-id="cc444-214">The handler receives the same information as the main window procedure of the host window.</span></span> <span data-ttu-id="cc444-215">Он также передает ссылку на `handled`значение Boolean, .</span><span class="sxs-lookup"><span data-stu-id="cc444-215">It also passes a reference to a Boolean value, `handled`.</span></span> <span data-ttu-id="cc444-216">Вы `handled` устанавливаете, чтобы `true` указать, что вы обрабатываются сообщение и никакой дальнейшей обработки не требуется.</span><span class="sxs-lookup"><span data-stu-id="cc444-216">You set `handled` to `true` to indicate that you have handled the message and no further processing is needed.</span></span>  
  
 <span data-ttu-id="cc444-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command)отправляется по разным причинам, поэтому необходимо изучить идентификатор уведомления, чтобы определить, является ли это событием, которым вы хотите справиться.</span><span class="sxs-lookup"><span data-stu-id="cc444-217">[`WM_COMMAND`](/windows/desktop/menurc/wm-command) is sent for a variety of reasons, so you must examine the notification ID to determine whether it is an event that you wish to handle.</span></span> <span data-ttu-id="cc444-218">Идентификатор содержится в `wParam` высоком слове параметра.</span><span class="sxs-lookup"><span data-stu-id="cc444-218">The ID is contained in the high word of the `wParam` parameter.</span></span> <span data-ttu-id="cc444-219">В образце используются более битые операторы для извлечения идентификатора.</span><span class="sxs-lookup"><span data-stu-id="cc444-219">The sample uses bitwise operators to extract the ID.</span></span> <span data-ttu-id="cc444-220">Если пользователь сделал или изменил свой выбор, [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange)идентификатор будет .</span><span class="sxs-lookup"><span data-stu-id="cc444-220">If the user has made or changed their selection, the ID will be [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange).</span></span>  
  
 <span data-ttu-id="cc444-221">При [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) получении образец получает индекс выбранного элемента, отправив [ `LB_GETCURSEL` элемент элемента.](/windows/desktop/Controls/lb-getcursel)</span><span class="sxs-lookup"><span data-stu-id="cc444-221">When [`LBN_SELCHANGE`](/windows/desktop/Controls/lbn-selchange) is received, the sample gets the index of the selected item by sending the control a [`LB_GETCURSEL` message](/windows/desktop/Controls/lb-getcursel).</span></span> <span data-ttu-id="cc444-222">Чтобы получить текст, вы <xref:System.Text.StringBuilder>сначала создать .</span><span class="sxs-lookup"><span data-stu-id="cc444-222">To get the text, you first create a <xref:System.Text.StringBuilder>.</span></span> <span data-ttu-id="cc444-223">Затем вы отправляете [ `LB_GETTEXT` сообщение](/windows/desktop/Controls/lb-gettext)управления.</span><span class="sxs-lookup"><span data-stu-id="cc444-223">You then send the control an [`LB_GETTEXT` message](/windows/desktop/Controls/lb-gettext).</span></span> <span data-ttu-id="cc444-224">Передайте <xref:System.Text.StringBuilder> пустой `wParam` объект в качестве параметра.</span><span class="sxs-lookup"><span data-stu-id="cc444-224">Pass the empty <xref:System.Text.StringBuilder> object as the `wParam` parameter.</span></span> <span data-ttu-id="cc444-225">При [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) возврате <xref:System.Text.StringBuilder> будет содержаться текст выбранного элемента.</span><span class="sxs-lookup"><span data-stu-id="cc444-225">When [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) returns, the <xref:System.Text.StringBuilder> will contain the text of the selected item.</span></span> <span data-ttu-id="cc444-226">Такое [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) использование требует еще одной декларации PInvoke.</span><span class="sxs-lookup"><span data-stu-id="cc444-226">This use of [`SendMessage`](/windows/desktop/api/winuser/nf-winuser-sendmessage) requires yet another PInvoke declaration.</span></span>  
  
 <span data-ttu-id="cc444-227">Наконец, `handled` `true` установите, чтобы указать, что сообщение было обработано.</span><span class="sxs-lookup"><span data-stu-id="cc444-227">Finally, set `handled` to `true` to indicate that the message has been handled.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cc444-228">См. также раздел</span><span class="sxs-lookup"><span data-stu-id="cc444-228">See also</span></span>

- <xref:System.Windows.Interop.HwndHost>
- [<span data-ttu-id="cc444-229">Взаимодействие WPF и Win32</span><span class="sxs-lookup"><span data-stu-id="cc444-229">WPF and Win32 Interoperation</span></span>](wpf-and-win32-interoperation.md)
- [<span data-ttu-id="cc444-230">Пошаговое руководство. Создание первого классического приложения WPF</span><span class="sxs-lookup"><span data-stu-id="cc444-230">Walkthrough: My first WPF desktop application</span></span>](../getting-started/walkthrough-my-first-wpf-desktop-application.md)
