---
title: Метод ICorProfilerInfo2::DoStackSnapshot
ms.date: 03/30/2017
api_name:
- ICorProfilerInfo2.DoStackSnapshot
api_location:
- mscorwks.dll
api_type:
- COM
f1_keywords:
- ICorProfilerInfo2::DoStackSnapshot
helpviewer_keywords:
- ICorProfilerInfo2::DoStackSnapshot method [.NET Framework profiling]
- DoStackSnapshot method [.NET Framework profiling]
ms.assetid: 287b11e9-7c52-4a13-ba97-751203fa97f4
topic_type:
- apiref
author: mairaw
ms.author: mairaw
ms.openlocfilehash: 12ef215253ca02048a5a3fc2c7c682823233929f
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/23/2019
ms.locfileid: "61779825"
---
# <a name="icorprofilerinfo2dostacksnapshot-method"></a><span data-ttu-id="b4238-102">Метод ICorProfilerInfo2::DoStackSnapshot</span><span class="sxs-lookup"><span data-stu-id="b4238-102">ICorProfilerInfo2::DoStackSnapshot Method</span></span>
<span data-ttu-id="b4238-103">Пошаговое описание управляемые фреймы в стеке для указанного потока и отправляет сведения профилировщику посредством обратного вызова.</span><span class="sxs-lookup"><span data-stu-id="b4238-103">Walks the managed frames on the stack for the specified thread, and sends information to the profiler through a callback.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="b4238-104">Синтаксис</span><span class="sxs-lookup"><span data-stu-id="b4238-104">Syntax</span></span>  
  
```  
HRESULT DoStackSnapshot(  
    [in] ThreadID thread,  
    [in] StackSnapshotCallback *callback,  
    [in] ULONG32 infoFlags,  
    [in] void *clientData,  
    [in, size_is(contextSize), length_is(contextSize)] BYTE context[],  
    [in] ULONG32 contextSize);  
```  
  
## <a name="parameters"></a><span data-ttu-id="b4238-105">Параметры</span><span class="sxs-lookup"><span data-stu-id="b4238-105">Parameters</span></span>  
 `thread`  
 <span data-ttu-id="b4238-106">[in] Идентификатор целевой поток.</span><span class="sxs-lookup"><span data-stu-id="b4238-106">[in] The ID of the target thread.</span></span>  
  
 <span data-ttu-id="b4238-107">Передача значения null в `thread` создается моментальный снимок текущего потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-107">Passing null in `thread` yields a snapshot of the current thread.</span></span> <span data-ttu-id="b4238-108">Если `ThreadID` из другого потока, передается, общеязыковой среды выполнения (CLR) приостанавливает работу потока, делает моментальный снимок и возобновляет работу.</span><span class="sxs-lookup"><span data-stu-id="b4238-108">If a `ThreadID` of a different thread is passed, the common language runtime (CLR) suspends that thread, performs the snapshot, and resumes.</span></span>  
  
 `callback`  
 <span data-ttu-id="b4238-109">[in] Указатель на реализацию [StackSnapshotCallback](../../../../docs/framework/unmanaged-api/profiling/stacksnapshotcallback-function.md) метод, который вызывается средой CLR для предоставления информации для каждого управляемого кадра и каждом запуске неуправляемых фреймов профилировщика.</span><span class="sxs-lookup"><span data-stu-id="b4238-109">[in] A pointer to the implementation of the [StackSnapshotCallback](../../../../docs/framework/unmanaged-api/profiling/stacksnapshotcallback-function.md) method, which is called by the CLR to provide the profiler with information on each managed frame and each run of unmanaged frames.</span></span>  
  
 <span data-ttu-id="b4238-110">`StackSnapshotCallback` Метод реализуется разработчиком профилировщика.</span><span class="sxs-lookup"><span data-stu-id="b4238-110">The `StackSnapshotCallback` method is implemented by the profiler writer.</span></span>  
  
 `infoFlags`  
 <span data-ttu-id="b4238-111">[in] Значение [COR_PRF_SNAPSHOT_INFO](../../../../docs/framework/unmanaged-api/profiling/cor-prf-snapshot-info-enumeration.md) перечисления, который определяет объем данных будет передан обратно для каждого кадра с `StackSnapshotCallback`.</span><span class="sxs-lookup"><span data-stu-id="b4238-111">[in] A value of the [COR_PRF_SNAPSHOT_INFO](../../../../docs/framework/unmanaged-api/profiling/cor-prf-snapshot-info-enumeration.md) enumeration, which specifies the amount of data to be passed back for each frame by `StackSnapshotCallback`.</span></span>  
  
 `clientData`  
 <span data-ttu-id="b4238-112">[in] Указатель на данные клиента, который передается напрямую через `StackSnapshotCallback` функцию обратного вызова.</span><span class="sxs-lookup"><span data-stu-id="b4238-112">[in] A pointer to the client data, which is passed straight through to the `StackSnapshotCallback` callback function.</span></span>  
  
 `context`  
 <span data-ttu-id="b4238-113">[in] Указатель на Win32 `CONTEXT` структуру, которая используется для задания начального значения стека.</span><span class="sxs-lookup"><span data-stu-id="b4238-113">[in] A pointer to a Win32 `CONTEXT` structure, which is used to seed the stack walk.</span></span> <span data-ttu-id="b4238-114">Win32 `CONTEXT` структура содержит значения для регистров ЦП и отражает состояние ЦП в определенный момент времени.</span><span class="sxs-lookup"><span data-stu-id="b4238-114">The Win32 `CONTEXT` structure contains values of the CPU registers and represents the state of the CPU at a particular moment in time.</span></span>  
  
 <span data-ttu-id="b4238-115">Начальное значение позволяет среде CLR определить, откуда следует начинать анализ стека, в верхней части стека в случае вспомогательный неуправляемый код; в противном случае — значение начального заполнения игнорируется.</span><span class="sxs-lookup"><span data-stu-id="b4238-115">The seed helps the CLR determine where to begin the stack walk, if the top of the stack is unmanaged helper code; otherwise, the seed is ignored.</span></span> <span data-ttu-id="b4238-116">Для асинхронного прохода необходимо указать начальное значение.</span><span class="sxs-lookup"><span data-stu-id="b4238-116">A seed must be supplied for an asynchronous walk.</span></span> <span data-ttu-id="b4238-117">Если вы выполняете синхронный анализ стека, начальное значение не требуется.</span><span class="sxs-lookup"><span data-stu-id="b4238-117">If you are doing a synchronous walk, no seed is necessary.</span></span>  
  
 <span data-ttu-id="b4238-118">`context` Параметр допустим только в том случае, если был передан флаг COR_PRF_SNAPSHOT_CONTEXT `infoFlags` параметра.</span><span class="sxs-lookup"><span data-stu-id="b4238-118">The `context` parameter is valid only if the COR_PRF_SNAPSHOT_CONTEXT flag was passed in the `infoFlags` parameter.</span></span>  
  
 `contextSize`  
 <span data-ttu-id="b4238-119">[in] Размер `CONTEXT` структуру, которая ссылается `context` параметра.</span><span class="sxs-lookup"><span data-stu-id="b4238-119">[in] The size of the `CONTEXT` structure, which is referenced by the `context` parameter.</span></span>  
  
## <a name="remarks"></a><span data-ttu-id="b4238-120">Примечания</span><span class="sxs-lookup"><span data-stu-id="b4238-120">Remarks</span></span>  
 <span data-ttu-id="b4238-121">Передача значения null для `thread` создается моментальный снимок текущего потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-121">Passing null for `thread` yields a snapshot of the current thread.</span></span> <span data-ttu-id="b4238-122">Моментальные снимки могут быть взяты из других потоков, только в том случае, если целевой поток приостановлен во время.</span><span class="sxs-lookup"><span data-stu-id="b4238-122">Snapshots can be taken of other threads only if the target thread is suspended at the time.</span></span>  
  
 <span data-ttu-id="b4238-123">Когда профилировщик хочет пройти по стеку, он вызывает `DoStackSnapshot`.</span><span class="sxs-lookup"><span data-stu-id="b4238-123">When the profiler wants to walk the stack, it calls `DoStackSnapshot`.</span></span> <span data-ttu-id="b4238-124">Прежде чем среда CLR возвращает из этого вызова, он вызывает ваш `StackSnapshotCallback` несколько раз, один раз для каждого управляемого фрейма (или неуправляемых фреймов) в стеке.</span><span class="sxs-lookup"><span data-stu-id="b4238-124">Before the CLR returns from that call, it calls your `StackSnapshotCallback` several times, once for each managed frame (or run of unmanaged frames) on the stack.</span></span> <span data-ttu-id="b4238-125">При возникновении неуправляемых фреймов, вы должны пройти их самостоятельно.</span><span class="sxs-lookup"><span data-stu-id="b4238-125">When unmanaged frames are encountered, you must walk them yourself.</span></span>  
  
 <span data-ttu-id="b4238-126">Порядок, в котором выполняется обход стека является обратной по отношению как кадры были переданы в стек: последнего конечные (отправить в последней) во-первых, основной (первый переданный) фрейма.</span><span class="sxs-lookup"><span data-stu-id="b4238-126">The order in which the stack is walked is the reverse of how the frames were pushed onto the stack: leaf (last-pushed) frame first, main (first-pushed) frame last.</span></span>  
  
 <span data-ttu-id="b4238-127">Дополнительные сведения о программировании профилировщика для обхода управляемых стеков см. в разделе [Profiler, анализ стека в .NET Framework 2.0: Основы и за его пределами](https://go.microsoft.com/fwlink/?LinkId=73638).</span><span class="sxs-lookup"><span data-stu-id="b4238-127">For more information about how to program the profiler to walk managed stacks, see [Profiler Stack Walking in the .NET Framework 2.0: Basics and Beyond](https://go.microsoft.com/fwlink/?LinkId=73638).</span></span>  
  
 <span data-ttu-id="b4238-128">Обход стека может быть синхронным или асинхронным, как описано в следующих разделах.</span><span class="sxs-lookup"><span data-stu-id="b4238-128">A stack walk can be synchronous or asynchronous, as explained in the following sections.</span></span>  
  
## <a name="synchronous-stack-walk"></a><span data-ttu-id="b4238-129">Синхронные стека</span><span class="sxs-lookup"><span data-stu-id="b4238-129">Synchronous Stack Walk</span></span>  
 <span data-ttu-id="b4238-130">Синхронные стека включает в себя анализ стека текущего потока в ответ на обратный вызов.</span><span class="sxs-lookup"><span data-stu-id="b4238-130">A synchronous stack walk involves walking the stack of the current thread in response to a callback.</span></span> <span data-ttu-id="b4238-131">Он не требует заполнения или приостановить действие.</span><span class="sxs-lookup"><span data-stu-id="b4238-131">It does not require seeding or suspending.</span></span>  
  
 <span data-ttu-id="b4238-132">Внесенные синхронной вызывается, когда, в ответ на CLR, вызвав один из вашего профилировщика [ICorProfilerCallback](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-interface.md) (или [ICorProfilerCallback2](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback2-interface.md)) методы, вызов `DoStackSnapshot` для анализа стека текущий поток.</span><span class="sxs-lookup"><span data-stu-id="b4238-132">You make a synchronous call when, in response to the CLR calling one of your profiler's [ICorProfilerCallback](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-interface.md) (or [ICorProfilerCallback2](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback2-interface.md)) methods, you call `DoStackSnapshot` to walk the stack of the current thread.</span></span> <span data-ttu-id="b4238-133">Это полезно, если вы хотите увидеть стек как выглядят на уведомление о таких как [ICorProfilerCallback::ObjectAllocated](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-objectallocated-method.md).</span><span class="sxs-lookup"><span data-stu-id="b4238-133">This is useful when you want to see what the stack looks like at a notification such as [ICorProfilerCallback::ObjectAllocated](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-objectallocated-method.md).</span></span> <span data-ttu-id="b4238-134">Нужно просто вызвать `DoStackSnapshot` изнутри вашей `ICorProfilerCallback` , передавая значение null в `context` и `thread` параметров.</span><span class="sxs-lookup"><span data-stu-id="b4238-134">You just call `DoStackSnapshot` from within your `ICorProfilerCallback` method, passing null in the `context` and `thread` parameters.</span></span>  
  
## <a name="asynchronous-stack-walk"></a><span data-ttu-id="b4238-135">Асинхронный проход стека</span><span class="sxs-lookup"><span data-stu-id="b4238-135">Asynchronous Stack Walk</span></span>  
 <span data-ttu-id="b4238-136">Влечет за собой Асинхронный проход стека, анализ стека другого потока или анализ стека текущего потока не в ответ на обратный вызов, а также путем перехвата указателя инструкций для текущего потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-136">An asynchronous stack walk entails walking the stack of a different thread, or walking the stack of the current thread, not in response to a callback, but by hijacking the current thread's instruction pointer.</span></span> <span data-ttu-id="b4238-137">Для асинхронного прохода требуется исходное значение, если неуправляемый код, который не является частью платформы находится вверху стека вызова (PInvoke) или вызов COM, а вспомогательным кодом самой CLR.</span><span class="sxs-lookup"><span data-stu-id="b4238-137">An asynchronous walk requires a seed if the top of the stack is unmanaged code that is not part of a platform invoke (PInvoke) or COM call, but helper code in the CLR itself.</span></span> <span data-ttu-id="b4238-138">Например код, который собирает just-in-time (JIT) компиляцию или сбор мусора является вспомогательным кодом.</span><span class="sxs-lookup"><span data-stu-id="b4238-138">For example, code that does just-in-time (JIT) compiling or garbage collection is helper code.</span></span>  
  
 <span data-ttu-id="b4238-139">Получить начальное значение, непосредственно приостановки анализируемого потока и пройти по стеку самостоятельно, пока не найдете самый верхний управляемый фрейм.</span><span class="sxs-lookup"><span data-stu-id="b4238-139">You obtain a seed by directly suspending the target thread and walking its stack yourself, until you find the topmost managed frame.</span></span> <span data-ttu-id="b4238-140">После целевой поток приостанавливается и получите текущий контекст регистра анализируемого потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-140">After the target thread is suspended, get the target thread's current register context.</span></span> <span data-ttu-id="b4238-141">Проверьте, указывает ли регистров на неуправляемый код, вызвав [ICorProfilerInfo::GetFunctionFromIP](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-getfunctionfromip-method.md) , если он возвращает `FunctionID` равен нулю, кадр является неуправляемым кодом.</span><span class="sxs-lookup"><span data-stu-id="b4238-141">Next, determine whether the register context points to unmanaged code by calling [ICorProfilerInfo::GetFunctionFromIP](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-getfunctionfromip-method.md) — if it returns a `FunctionID` equal to zero, the frame is unmanaged code.</span></span> <span data-ttu-id="b4238-142">Теперь обход стека, пока не достигнете первого управляемого фрейма, а затем вычислить контекст начального значения, исходя из контекста регистра для этого кадра.</span><span class="sxs-lookup"><span data-stu-id="b4238-142">Now, walk the stack until you reach the first managed frame, and then calculate the seed context based on the register context for that frame.</span></span>  
  
 <span data-ttu-id="b4238-143">Вызовите `DoStackSnapshot` с контекстом направления, чтобы начать Асинхронный проход стека.</span><span class="sxs-lookup"><span data-stu-id="b4238-143">Call `DoStackSnapshot` with your seed context to begin the asynchronous stack walk.</span></span> <span data-ttu-id="b4238-144">Если не указать начальное значение, `DoStackSnapshot` может пропустить управляемые фреймы на вершине стека и, следовательно, предоставит неполный проход стека.</span><span class="sxs-lookup"><span data-stu-id="b4238-144">If you do not supply a seed, `DoStackSnapshot` might skip managed frames at the top of the stack and, consequently, will give you an incomplete stack walk.</span></span> <span data-ttu-id="b4238-145">Если указать начальное значение, оно должно указывать на JIT-компиляции или собственного генератором образов (Ngen.exe)-сформированного кода; в противном случае `DoStackSnapshot` возвращает код ошибки, CORPROF_E_STACKSNAPSHOT_UNMANAGED_CTX.</span><span class="sxs-lookup"><span data-stu-id="b4238-145">If you do supply a seed, it must point to JIT-compiled or Native Image Generator (Ngen.exe)-generated code; otherwise, `DoStackSnapshot` returns the failure code, CORPROF_E_STACKSNAPSHOT_UNMANAGED_CTX.</span></span>  
  
 <span data-ttu-id="b4238-146">Стека может легко привести к возникновению взаимоблокировки или нарушение прав доступа, если не следовать рекомендациям:</span><span class="sxs-lookup"><span data-stu-id="b4238-146">Asynchronous stack walks can easily cause deadlocks or access violations, unless you follow these guidelines:</span></span>  
  
- <span data-ttu-id="b4238-147">При приостановке непосредственно потоков, помните, что только поток, ни никогда не выполняет управляемый код может приостановить другой поток.</span><span class="sxs-lookup"><span data-stu-id="b4238-147">When you directly suspend threads, remember that only a thread that has never run managed code can suspend another thread.</span></span>  
  
- <span data-ttu-id="b4238-148">Всегда блока в вашей [ICorProfilerCallback::ThreadDestroyed](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-threaddestroyed-method.md) обратного вызова до завершения анализа стека данного потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-148">Always block in your [ICorProfilerCallback::ThreadDestroyed](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-threaddestroyed-method.md) callback until that thread's stack walk is complete.</span></span>  
  
- <span data-ttu-id="b4238-149">Не блокируйте период обращения вашего профилировщика к функции CLR, способной запустить сбор мусора.</span><span class="sxs-lookup"><span data-stu-id="b4238-149">Do not hold a lock while your profiler calls into a CLR function that can trigger a garbage collection.</span></span> <span data-ttu-id="b4238-150">То есть не удерживать блокировку, если собственный поток может осуществить вызов, который запускает сборку мусора.</span><span class="sxs-lookup"><span data-stu-id="b4238-150">That is, do not hold a lock if the owning thread might make a call that triggers a garbage collection.</span></span>  
  
 <span data-ttu-id="b4238-151">Есть также риск взаимоблокировки при вызове метода `DoStackSnapshot` из потока, который создал ваш профилировщик, таким образом, чтобы вы для прохода по стеку анализируемого потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-151">There is also a risk of deadlock if you call `DoStackSnapshot` from a thread that your profiler has created so that you can walk the stack of a separate target thread.</span></span> <span data-ttu-id="b4238-152">Первый раз вы создали поток входит в определенных `ICorProfilerInfo*` методы (включая `DoStackSnapshot`), среда CLR будет выполнять на уровне потока, инициализации среды CLR в этом потоке.</span><span class="sxs-lookup"><span data-stu-id="b4238-152">The first time the thread you created enters certain `ICorProfilerInfo*` methods (including `DoStackSnapshot`), the CLR will perform per-thread, CLR-specific initialization on that thread.</span></span> <span data-ttu-id="b4238-153">Если профилировщик приостановил анализируемый поток, стек которого вы пытаетесь получить пошаговые инструкции, и если этот целевой поток является владельцем блокировки, необходимой для выполнения инициализации каждого потока, произойдет взаимоблокировка.</span><span class="sxs-lookup"><span data-stu-id="b4238-153">If your profiler has suspended the target thread whose stack you are trying to walk, and if that target thread happened to own a lock necessary for performing this per-thread initialization, a deadlock will occur.</span></span> <span data-ttu-id="b4238-154">Чтобы избежать этой взаимоблокировки, выполните исходный вызов в `DoStackSnapshot` из вашего потока, созданного профилировщика для обхода предназначены потока отдельно, но не приостанавливаете анализируемый поток сначала.</span><span class="sxs-lookup"><span data-stu-id="b4238-154">To avoid this deadlock, make an initial call into `DoStackSnapshot` from your profiler-created thread to walk a separate target thread, but do not suspend the target thread first.</span></span> <span data-ttu-id="b4238-155">Исходный вызов гарантирует, что инициализация отдельных потоков может быть завершена без взаимоблокировки.</span><span class="sxs-lookup"><span data-stu-id="b4238-155">This initial call ensures that the per-thread initialization can complete without deadlock.</span></span> <span data-ttu-id="b4238-156">Если `DoStackSnapshot` выполняется успешно, а также отчеты по крайней мере один кадр после этой точки, будут иметь безопасный для этого потока, созданный профилировщиком приостановить любой целевой поток и вызов `DoStackSnapshot` для анализа стека данного целевого потока.</span><span class="sxs-lookup"><span data-stu-id="b4238-156">If `DoStackSnapshot` succeeds and reports at least one frame, after that point, it will be safe for that profiler-created thread to suspend any target thread and call `DoStackSnapshot` to walk the stack of that target thread.</span></span>  
  
## <a name="requirements"></a><span data-ttu-id="b4238-157">Требования</span><span class="sxs-lookup"><span data-stu-id="b4238-157">Requirements</span></span>  
 <span data-ttu-id="b4238-158">**Платформы:** См. раздел [Требования к системе](../../../../docs/framework/get-started/system-requirements.md).</span><span class="sxs-lookup"><span data-stu-id="b4238-158">**Platforms:** See [System Requirements](../../../../docs/framework/get-started/system-requirements.md).</span></span>  
  
 <span data-ttu-id="b4238-159">**Заголовок.** CorProf.idl, CorProf.h</span><span class="sxs-lookup"><span data-stu-id="b4238-159">**Header:** CorProf.idl, CorProf.h</span></span>  
  
 <span data-ttu-id="b4238-160">**Библиотека:** CorGuids.lib</span><span class="sxs-lookup"><span data-stu-id="b4238-160">**Library:** CorGuids.lib</span></span>  
  
 <span data-ttu-id="b4238-161">**Версии платформы .NET Framework:** [!INCLUDE[net_current_v20plus](../../../../includes/net-current-v20plus-md.md)]</span><span class="sxs-lookup"><span data-stu-id="b4238-161">**.NET Framework Versions:** [!INCLUDE[net_current_v20plus](../../../../includes/net-current-v20plus-md.md)]</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="b4238-162">См. также</span><span class="sxs-lookup"><span data-stu-id="b4238-162">See also</span></span>

- [<span data-ttu-id="b4238-163">Интерфейс ICorProfilerInfo</span><span class="sxs-lookup"><span data-stu-id="b4238-163">ICorProfilerInfo Interface</span></span>](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-interface.md)
- [<span data-ttu-id="b4238-164">Интерфейс ICorProfilerInfo2</span><span class="sxs-lookup"><span data-stu-id="b4238-164">ICorProfilerInfo2 Interface</span></span>](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo2-interface.md)
