---
title: Типы языка F#
description: 'Дополнительные сведения о типах, которые используются в языке F # и как с именем и описаны типы F #.'
ms.date: 05/16/2016
ms.openlocfilehash: bdbb89dc751970ac31fe102df009f0bff6388e52
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/04/2018
---
# <a name="f-types"></a>Типы языка F#

В этом разделе описываются типы, которые используются в языке F # и как с именем и описаны типы F #.


## <a name="summary-of-f-types"></a>Сводка типов F #
Некоторые типы считаются *типов-примитивов*, такие как тип Boolean `bool` и типы целочисленные и с плавающей запятой различных размеров, в том числе типы для байтов и символов. Эти типы описаны в [типов-примитивов](primitive-types.md).

Другие типы, встроенные в язык, включают кортежи, списки, массивы, последовательности, записи и размеченные объединения. Если есть опыт работы с другими языками .NET и вы изучаете F #, следует изучить разделы для каждого из этих типов. Ссылки на дополнительные сведения об этих типах включаются в [щелкните ссылку](https://msdn.microsoft.com/library/#rel) этого раздела. Эти F #-определенные типы поддерживают стили программирования, которые являются общими для языков функционального программирования. Многие из этих типов имеют связанные модули в библиотеке F #, которые поддерживают распространенные операции с этими типами.

Тип функции включает сведения о типах параметров и тип возвращаемого значения.

.NET Framework является источником типов объектов, типы интерфейсов, типы делегатов и другим пользователям. Можно определить собственные типы объектов, так же, как и в любом другом языке .NET.

Кроме того, F # позволяет определять псевдонимы, которые называются *аббревиатуры типов*, которые альтернативными именами типов. Аббревиатуры типов можно использовать, когда тип может измениться в будущем, и вы хотите избежать изменения кода, который зависит от типа. Или можно использовать как понятное имя для типа, который может сделать код проще читать и понимать сокращенная форма типа.

F # предоставляет полезные коллекцию типов, которые разработаны с функционального программирования в виду. С помощью этих типах коллекций помогает писать код, который является более функциональным в стиле. Дополнительные сведения см. в разделе [типы коллекций F #](fsharp-collection-types.md).


## <a name="syntax-for-types"></a>Синтаксис для типов
В коде F # часто приходится писать имена типов. Каждый тип имеет синтаксическую форму, и использовать эти синтаксические формы в аннотации типов, объявлениях абстрактных методов, объявления делегата, подписи и другие конструкции. При объявлении новой программной конструкции в интерпретатор интерпретатор напечатает имя конструкции и синтаксис для его типа. Этот синтаксис может быть всего лишь идентификатором для определяемого пользователем типа или встроенным идентификатором, например `int` или `string`, но для более сложных типов синтаксис более сложный.

В следующей таблице показаны аспекты синтаксиса типов для типов F #.



|Тип|Синтаксис типа|Примеры|
|----|-----------|--------|
|тип-примитив|*Имя типа*|`int`<br /><br />`float`<br /><br />`string`|
|Агрегатный тип (класс, структура, объединение, запись, перечисления и т. д)|*Имя типа*|`System.DateTime`<br /><br />`Color`|
|сокращенная форма типа|*Имя типа-сокращенная форма*|`bigint`|
|Полное имя типа|*Имя Namespaces.Type*<br /><br />или<br /><br />*Имя Modules.Type*<br /><br />или<br /><br />*Имя Namespaces.Modules.Type*|`System.IO.StreamWriter`|
|array|*Имя типа*[] или<br /><br />*Имя типа* массива|`int[]`<br /><br />`array<int>`<br /><br />`int array`|
|Двумерный массив|*Имя типа*[,]|`int[,]`<br /><br />`float[,]`|
|Трехмерный массив|*Имя типа*[,]|`float[,,]`|
|tuple|*Тип name1* &#42; *тип name2* ...|Например `(1,'b',3)` имеет тип `int * char * int`|
|универсальный тип|*параметр типа* *универсальный имя типа*<br /><br />или<br /><br />*универсальный тип имени*&lt;*список параметров типа*&gt;|`'a list`<br /><br />`list<'a>`<br /><br />`Dictionary<'key, 'value>`|
|сконструированный тип (универсальный тип с определенным аргументом типа)|*Аргумент типа* *универсальный имя типа*<br /><br />или<br /><br />*универсальный тип имени*&lt;*список аргументов типа*&gt;|`int option`<br /><br />`string list`<br /><br />`int ref`<br /><br />`option<int>`<br /><br />`list<string>`<br /><br />`ref<int>`<br /><br />`Dictionary<int, string>`|
|тип функции, которая принимает один параметр|*параметр тип1*  - &gt; *типа возвращаемого значения*|Функция, которая принимает `int` и возвращает `string` имеет тип `int -> string`|
|тип функции, которая имеет несколько параметров|*параметр тип1*  - &gt; *параметр тип2*  - &gt; ... -&gt; *типа возвращаемого значения*|Функция, которая принимает `int` и `float` и возвращает `string` имеет тип `int -> float -> string`|
|функция высшего порядка как параметр|(*тип функции*)|`List.map` имеет тип `('a -> 'b) -> 'a list -> 'b list`|
|делегат|делегат типа *тип функции*|`delegate of unit -> int`|
|гибкий тип|#*Имя типа*|`#System.Windows.Forms.Control`<br /><br />`#seq<int>`|

## <a name="related-topics"></a>См. также


|Раздел|Описание|
|-----|-----------|
|[Типы-примитивы](primitive-types.md)|Описывает встроенные простые типы, такие как целочисленные типы, логический тип и символьные типы.|
|[Тип единиц](unit-type.md)|Описывает `unit` , тип, который имеет одно значение и обозначением (); эквивалент `void` в C# и `Nothing` в Visual Basic.|
|[Кортежи](tuples.md)|Описывает тип кортежа, тип, состоящий из связанных значений любого типа, объединенных в пары, тройки, quadruples и т. д.|
|[Параметры](options.md)|Описывает тип параметра, тип, который может иметь значение или быть пустым.|
|[Списки](lists.md)|Описывает списки, которые являются упорядоченная, неизменная серия элементов одного типа.|
|[Массивы](arrays.md)|Содержит описание массивов, упорядоченные наборы изменяемый элементов того же типа, которые занимают определенный блок памяти и имеют фиксированный размер.|
|[Последовательности](sequences.md)|Описывает тип последовательности, который представляет логический ряд значений; отдельные значения вычисляются только в случае необходимости.|
|[Записи](records.md)|Описывает тип записи, небольшой набор именованных значений.|
|[Размеченные объединения](discriminated-unions.md)|Описывает тип размеченного объединения, тип, значения которого может быть любым из набора возможных типов.|
|[Функции](functions/index.md)|Описание значения функции.|
|[Классы](classes.md)|Описывает тип класса, тип объекта, который соответствует ссылочным типом .NET. Типы класса могут содержать члены, свойства, реализованные интерфейсы и базовый тип.|
|[Структуры](structures.md)|Описывает `struct` тип, тип объекта, который соответствует типу значения .NET. `struct` Тип обычно представляет небольшой набор данных.|
|[Интерфейсы](interfaces.md)|Описывает типы интерфейса, которые представляют набор элементов, предоставляющих определенную функциональность, но не содержат данных. Тип интерфейса должен быть реализован тип объекта, чтобы быть полезным.|
|[Делегаты](delegates.md)|Описывает тип делегата, который представляет функцию как объект.|
|[Перечисления](enumerations.md)|Описывает типы перечисления, значения которого относятся набор именованных значений.|
|[Атрибуты](attributes.md)|Описывает атрибуты, которые используются для определения метаданных для другого типа.|
|[Типы исключения](exception-handling/exception-types.md)|Описывает исключения, которые задают сведения об ошибке.|
