---
title: Цитирование кода (F#)
description: 'Сведения о F # Цитирование кода — функцию языка, позволяет создавать и работать с помощью выражения кода F #, программными средствами.'
ms.date: 05/16/2016
ms.openlocfilehash: 27e9cf1d99e2b5955cc6359653fc87bdbe824cc7
ms.sourcegitcommit: a885cc8c3e444ca6471348893d5373c6e9e49a47
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 09/07/2018
ms.locfileid: "44070073"
---
# <a name="code-quotations"></a><span data-ttu-id="14300-103">Цитирование кода</span><span class="sxs-lookup"><span data-stu-id="14300-103">Code Quotations</span></span>

> [!NOTE]
<span data-ttu-id="14300-104">Ссылка на справочник по API ведет на сайт MSDN.</span><span class="sxs-lookup"><span data-stu-id="14300-104">The API reference link will take you to MSDN.</span></span>  <span data-ttu-id="14300-105">Работа над справочником по API docs.microsoft.com не завершена.</span><span class="sxs-lookup"><span data-stu-id="14300-105">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="14300-106">В этом разделе описывается *кавычки кода*, — функцию языка, позволяет создавать и работать с помощью выражения кода F #, программными средствами.</span><span class="sxs-lookup"><span data-stu-id="14300-106">This topic describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="14300-107">Эта функция позволяет создавать дерево абстрактного синтаксиса, которое представляет код F #.</span><span class="sxs-lookup"><span data-stu-id="14300-107">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="14300-108">Дерево абстрактного синтаксиса можно просматривать и обрабатываются в соответствии с требованиями приложения.</span><span class="sxs-lookup"><span data-stu-id="14300-108">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="14300-109">Например дерева можно использовать для создания кода на F # или создания кода на другом языке.</span><span class="sxs-lookup"><span data-stu-id="14300-109">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>

## <a name="quoted-expressions"></a><span data-ttu-id="14300-110">Цитируемые выражения</span><span class="sxs-lookup"><span data-stu-id="14300-110">Quoted Expressions</span></span>

<span data-ttu-id="14300-111">Объект *заключено в кавычки выражение* — это выражение F # в коде, в качестве разделителя используется таким образом, что оно не компилируется как часть программы, а компилируется в объект, представляющий выражение языка F #.</span><span class="sxs-lookup"><span data-stu-id="14300-111">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="14300-112">Вы можете пометить заключенные в кавычки выражения в одном из двух способов: с помощью сведений о типе или без сведений о типе.</span><span class="sxs-lookup"><span data-stu-id="14300-112">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="14300-113">Если вы хотите включить сведения о типе, можно использовать символы `<@` и `@>` для разделения заключенные в кавычки выражения.</span><span class="sxs-lookup"><span data-stu-id="14300-113">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="14300-114">Если сведения о типе не требуется, можно использовать символы `<@@` и `@@>`.</span><span class="sxs-lookup"><span data-stu-id="14300-114">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="14300-115">В следующем коде показано типизированные и нетипизированные цитаты.</span><span class="sxs-lookup"><span data-stu-id="14300-115">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="14300-116">Обход большого дерева выражения выполняется быстрее, если не включать сведения о типе.</span><span class="sxs-lookup"><span data-stu-id="14300-116">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="14300-117">Результирующий тип выражения в кавычках символами является `Expr<'T>`, где параметр типа имеет тип выражения определяется алгоритм определения типов компилятор F #.</span><span class="sxs-lookup"><span data-stu-id="14300-117">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="14300-118">При использовании Цитирование кода без сведений о типе, тип указанного выражения — неуниверсального типа [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span><span class="sxs-lookup"><span data-stu-id="14300-118">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span></span> <span data-ttu-id="14300-119">Можно вызвать [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) свойство типизированного `Expr` для получения нетипизированное `Expr` объекта.</span><span class="sxs-lookup"><span data-stu-id="14300-119">You can call the [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="14300-120">Существуют разнообразные статические методы, которые позволяют создавать объектов выражений F # программно с помощью `Expr` выражения, содержащие класс без использования.</span><span class="sxs-lookup"><span data-stu-id="14300-120">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="14300-121">Обратите внимание на то, что цитату кода должен включать полное выражение.</span><span class="sxs-lookup"><span data-stu-id="14300-121">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="14300-122">Для `let` привязки, например, требуется как определение имени привязки и дополнительное выражение, которое использует эту привязку.</span><span class="sxs-lookup"><span data-stu-id="14300-122">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="14300-123">В подробном синтаксисе это выражение, следующее за `in` ключевое слово.</span><span class="sxs-lookup"><span data-stu-id="14300-123">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="14300-124">На верхнем уровне в модуле это просто следующее выражение в модуле, но в предложение, это явно не требуется.</span><span class="sxs-lookup"><span data-stu-id="14300-124">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="14300-125">Таким образом следующее выражение не является допустимым.</span><span class="sxs-lookup"><span data-stu-id="14300-125">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="14300-126">Но следующие выражения являются допустимыми.</span><span class="sxs-lookup"><span data-stu-id="14300-126">But the following expressions are valid.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="14300-127">Чтобы использовать Цитирование кода, необходимо добавить объявление импорта (с помощью `open` ключевое слово), открывает [Microsoft.FSharp.Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) пространства имен.</span><span class="sxs-lookup"><span data-stu-id="14300-127">To use code quotations, you must add an import declaration (by using the `open` keyword) that opens the [Microsoft.FSharp.Quotations](https://msdn.microsoft.com/library/e9ce8a3a-e00c-4190-bad5-cce52ee089b2) namespace.</span></span>

<span data-ttu-id="14300-128">Пакете PowerPack языка F # обеспечивает поддержку для вычисления и выполнения объектов выражений F #.</span><span class="sxs-lookup"><span data-stu-id="14300-128">The F# PowerPack provides support for evaluating and executing F# expression objects.</span></span>

## <a name="expr-type"></a><span data-ttu-id="14300-129">Тип expr</span><span class="sxs-lookup"><span data-stu-id="14300-129">Expr Type</span></span>

<span data-ttu-id="14300-130">Экземпляр `Expr` тип представляет выражения F #.</span><span class="sxs-lookup"><span data-stu-id="14300-130">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="14300-131">Универсальные и неуниверсальные `Expr` типы описаны в документации по библиотеке F #.</span><span class="sxs-lookup"><span data-stu-id="14300-131">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="14300-132">Дополнительные сведения см. в разделе [имен Microsoft.FSharp.Quotations](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) и [класс Quotations.Expr](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="14300-132">For more information, see [Microsoft.FSharp.Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) and [Quotations.Expr Class](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span></span>

## <a name="splicing-operators"></a><span data-ttu-id="14300-133">Операторы объединения</span><span class="sxs-lookup"><span data-stu-id="14300-133">Splicing Operators</span></span>

<span data-ttu-id="14300-134">Объединение позволяет объединять литеральные цитаты кода с выражениями, которые вы создали программным способом или из другого цитату кода.</span><span class="sxs-lookup"><span data-stu-id="14300-134">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="14300-135">`%` И `%%` операторы позволяют добавлять объект выражения языка F # в цитату кода.</span><span class="sxs-lookup"><span data-stu-id="14300-135">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="14300-136">Использовании `%` оператор для вставки объекта типизированного выражения в типизированную цитату; вам использовать `%%` оператор для вставки объекта нетипизированного выражения в нетипизированную цитату.</span><span class="sxs-lookup"><span data-stu-id="14300-136">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="14300-137">Оба оператора, префикс унарные операторы.</span><span class="sxs-lookup"><span data-stu-id="14300-137">Both operators are unary prefix operators.</span></span> <span data-ttu-id="14300-138">Поэтому если `expr` является нетипизированным выражением типа `Expr`, следующий код является допустимым.</span><span class="sxs-lookup"><span data-stu-id="14300-138">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="14300-139">И если `expr` является типизированным предложения типа `Expr<int>`, следующий код является допустимым.</span><span class="sxs-lookup"><span data-stu-id="14300-139">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="14300-140">Пример</span><span class="sxs-lookup"><span data-stu-id="14300-140">Example</span></span>

### <a name="description"></a><span data-ttu-id="14300-141">Описание</span><span class="sxs-lookup"><span data-stu-id="14300-141">Description</span></span>

<span data-ttu-id="14300-142">Следующий пример иллюстрирует использование Цитирование кода поместите код F # в объект выражения и затем распечатать код F #, который представляет выражение.</span><span class="sxs-lookup"><span data-stu-id="14300-142">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="14300-143">Функция `println` определяется, содержащий рекурсивной функции `print` , отображающий объект выражения языка F # (типа `Expr`) в удобном формате.</span><span class="sxs-lookup"><span data-stu-id="14300-143">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="14300-144">Существует несколько активных шаблонов в [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) и [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) модули, которые могут быть использованы для анализа объектов выражений.</span><span class="sxs-lookup"><span data-stu-id="14300-144">There are several active patterns in the [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) and [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="14300-145">В этом примере не включает все возможные шаблоны, которые могут отображаться в выражении F #.</span><span class="sxs-lookup"><span data-stu-id="14300-145">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="14300-146">Любой нераспознанный шаблон вызывает сопоставления Шаблон подстановочного знака (`_`) и обрабатывается с помощью `ToString` метод, который, в `Expr` введите позволяет узнать активный шаблон для добавления в выражение match.</span><span class="sxs-lookup"><span data-stu-id="14300-146">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>

### <a name="code"></a><span data-ttu-id="14300-147">Код</span><span class="sxs-lookup"><span data-stu-id="14300-147">Code</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet601.fs)]

### <a name="output"></a><span data-ttu-id="14300-148">Вывод</span><span class="sxs-lookup"><span data-stu-id="14300-148">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="14300-149">Пример</span><span class="sxs-lookup"><span data-stu-id="14300-149">Example</span></span>

### <a name="description"></a><span data-ttu-id="14300-150">Описание</span><span class="sxs-lookup"><span data-stu-id="14300-150">Description</span></span>

<span data-ttu-id="14300-151">Также можно использовать три активные шаблоны в [ExprShape-модуль](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) для обхода деревьев выражений с меньшим количеством активных шаблонов.</span><span class="sxs-lookup"><span data-stu-id="14300-151">You can also use the three active patterns in the [ExprShape module](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="14300-152">Эти активные шаблоны можно использовать, когда требуется переход по дереву, но не обязательно вся информация содержится в большинство узлов.</span><span class="sxs-lookup"><span data-stu-id="14300-152">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="14300-153">При использовании этих шаблонов, все выражения языка F # совпадает с одним из следующих трех шаблонов: `ShapeVar` Если выражение является переменной, `ShapeLambda` Если выражение является лямбда-выражения, или `ShapeCombination` Если выражение является что-нибудь еще.</span><span class="sxs-lookup"><span data-stu-id="14300-153">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="14300-154">При обходе дерева выражения с помощью активные шаблоны как в предыдущем примере кода, необходимо использовать многие другие шаблоны для обработки всех возможных типов выражений F #, и ваш код будет более сложной.</span><span class="sxs-lookup"><span data-stu-id="14300-154">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="14300-155">Дополнительные сведения см. в разделе [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination-активный шаблон](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="14300-155">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span></span>

<span data-ttu-id="14300-156">В следующем примере кода можно использовать в качестве основы для более сложных обходов.</span><span class="sxs-lookup"><span data-stu-id="14300-156">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="14300-157">В этом коде создается дерево выражений для выражения, содержащего вызов функции `add`.</span><span class="sxs-lookup"><span data-stu-id="14300-157">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="14300-158">[SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) активный шаблон используется для обнаружения всех вызовов функции `add` в дереве выражения.</span><span class="sxs-lookup"><span data-stu-id="14300-158">The [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="14300-159">Это активный шаблон присваивает аргументы вызова `exprList` значение.</span><span class="sxs-lookup"><span data-stu-id="14300-159">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="14300-160">В этом случае существует только два, поэтому они извлекаются и функция вызывается рекурсивно на аргументы.</span><span class="sxs-lookup"><span data-stu-id="14300-160">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="14300-161">Результаты вставляются в цитату кода, который представляет вызов `mul` с помощью оператора объединения (`%%`).</span><span class="sxs-lookup"><span data-stu-id="14300-161">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="14300-162">`println` Функция из предыдущего примера используется для отображения результатов.</span><span class="sxs-lookup"><span data-stu-id="14300-162">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="14300-163">Код в других ветвях активный шаблон просто заново создает это же дерево выражения, поэтому единственное изменение в результирующем выражении является замена `add` для `mul`.</span><span class="sxs-lookup"><span data-stu-id="14300-163">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>

### <a name="code"></a><span data-ttu-id="14300-164">Код</span><span class="sxs-lookup"><span data-stu-id="14300-164">Code</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-3/snippet701.fs)]

### <a name="output"></a><span data-ttu-id="14300-165">Вывод</span><span class="sxs-lookup"><span data-stu-id="14300-165">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="14300-166">См. также</span><span class="sxs-lookup"><span data-stu-id="14300-166">See also</span></span>

- [<span data-ttu-id="14300-167">Справочник по языку F#</span><span class="sxs-lookup"><span data-stu-id="14300-167">F# Language Reference</span></span>](index.md)
