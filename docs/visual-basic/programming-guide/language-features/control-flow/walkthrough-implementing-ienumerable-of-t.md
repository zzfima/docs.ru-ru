---
title: Реализация IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 4151a680050f234d450d8de5e67a767c54e8df68
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266915"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="937c8-102">Пошаговое руководство. Реализация IEnumerable(Of T) в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="937c8-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="937c8-103">Интерфейс <xref:System.Collections.Generic.IEnumerable%601> реализован классами, которые могут возвращать последовательность значений по одному элементу за раз.</span><span class="sxs-lookup"><span data-stu-id="937c8-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="937c8-104">Преимущество возврата данных по одному элементу за один раз заключается в том, что вам не нужно загружать полный набор данных в память, чтобы работать с ним.</span><span class="sxs-lookup"><span data-stu-id="937c8-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="937c8-105">Для загрузки одного элемента из данных необходимо использовать достаточную память.</span><span class="sxs-lookup"><span data-stu-id="937c8-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="937c8-106">Классы, `IEnumerable(T)` реализующие `For Each` интерфейс, могут использоваться с помощью циклов или запросов LIN.</span><span class="sxs-lookup"><span data-stu-id="937c8-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="937c8-107">Например, рассмотрим приложение, которое должно прочитать большой текстовый файл и вернуть каждую строку из файла, которое соответствует определенным критериям поиска.</span><span class="sxs-lookup"><span data-stu-id="937c8-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="937c8-108">Приложение использует запрос LIN'а для возврата строк из файла, которые соответствуют указанным критериям.</span><span class="sxs-lookup"><span data-stu-id="937c8-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="937c8-109">Чтобы задать запрос на содержимое файла с помощью запроса LIN,, приложение может загрузить содержимое файла в массив или коллекцию.</span><span class="sxs-lookup"><span data-stu-id="937c8-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="937c8-110">Однако загрузка всего файла в массив или коллекцию потребует гораздо больше памяти, чем требуется.</span><span class="sxs-lookup"><span data-stu-id="937c8-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="937c8-111">Вместо этого запрос НАИВный запрос может задать запрос содержимого файла с помощью перечисленного класса, вернув только значения, которые соответствуют критериям поиска.</span><span class="sxs-lookup"><span data-stu-id="937c8-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="937c8-112">Запросы, возвращающие лишь несколько соответствующих значений, потребляют гораздо меньше памяти.</span><span class="sxs-lookup"><span data-stu-id="937c8-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="937c8-113">Можно создать класс, который <xref:System.Collections.Generic.IEnumerable%601> реализует интерфейс для обнажая исходные данные в качестве перечисленных данных.</span><span class="sxs-lookup"><span data-stu-id="937c8-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="937c8-114">Классу, реализующего `IEnumerable(T)` интерфейс, потребуется <xref:System.Collections.Generic.IEnumerator%601> другой класс, который реализует интерфейс для итерации через исходные данные.</span><span class="sxs-lookup"><span data-stu-id="937c8-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="937c8-115">Эти два класса позволяют возвращать элементы данных последовательно в качестве определенного типа.</span><span class="sxs-lookup"><span data-stu-id="937c8-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="937c8-116">В этом пошаговом шаге вы `IEnumerable(Of String)` создадите класс, который `IEnumerator(Of String)` реализует интерфейс и класс, который реализует интерфейс для чтения одной строки текста за раз.</span><span class="sxs-lookup"><span data-stu-id="937c8-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="937c8-117">Создание подавиваемого класса</span><span class="sxs-lookup"><span data-stu-id="937c8-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="937c8-118">**Создание проекта, поддававшегося пересчету класса**</span><span class="sxs-lookup"><span data-stu-id="937c8-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="937c8-119">В Visual Basic, в меню **файла,** укажите на **новый,** а затем нажмите **Project**.</span><span class="sxs-lookup"><span data-stu-id="937c8-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="937c8-120">Убедитесь, что в диалоговом окне **Создание проекта** в области **Типы проектов** выбран пункт **Windows**.</span><span class="sxs-lookup"><span data-stu-id="937c8-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="937c8-121">Выберите **Библиотеки классов** в области **Шаблоны**.</span><span class="sxs-lookup"><span data-stu-id="937c8-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="937c8-122">В поле **Имя** введите `StreamReaderEnumerable` и нажмите кнопку **ОК**.</span><span class="sxs-lookup"><span data-stu-id="937c8-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="937c8-123">Отображается новый проект.</span><span class="sxs-lookup"><span data-stu-id="937c8-123">The new project is displayed.</span></span>

1. <span data-ttu-id="937c8-124">В **Solution Explorer**, правой кнопкой мыши на class1.vb файл и нажмите **Переименовать**.</span><span class="sxs-lookup"><span data-stu-id="937c8-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="937c8-125">Измените имя файла на `StreamReaderEnumerable.vb` и нажмите клавишу ВВОД.</span><span class="sxs-lookup"><span data-stu-id="937c8-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="937c8-126">При переименовании файла класс также будет переименован в `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="937c8-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="937c8-127">Этот класс реализует интерфейс `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="937c8-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="937c8-128">Право нажмите на StreamReaderEnumable проекта, указать, чтобы **добавить,** а затем нажмите **Новый пункт**.</span><span class="sxs-lookup"><span data-stu-id="937c8-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="937c8-129">Выберите шаблон **класса.**</span><span class="sxs-lookup"><span data-stu-id="937c8-129">Select the **Class** template.</span></span> <span data-ttu-id="937c8-130">В поле **Имя** введите `StreamReaderEnumerator.vb`, а затем нажмите кнопку **ОК**.</span><span class="sxs-lookup"><span data-stu-id="937c8-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="937c8-131">Первым классом в этом проекте является перечисленный `IEnumerable(Of String)` класс и будет реализован интерфейс.</span><span class="sxs-lookup"><span data-stu-id="937c8-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="937c8-132">Этот общий интерфейс <xref:System.Collections.IEnumerable> реализует интерфейс и гарантирует, что потребители этого `String`класса могут получить доступ к значениям, набранным как.</span><span class="sxs-lookup"><span data-stu-id="937c8-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="937c8-133">**Добавление кода для реализации IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="937c8-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="937c8-134">Откройте файл StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="937c8-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="937c8-135">На линии `Public Class StreamReaderEnumerable`после , введите следующее и нажмите ENTER.</span><span class="sxs-lookup"><span data-stu-id="937c8-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="937c8-136">Visual Basic автоматически заполняет класс членами, `IEnumerable(Of String)` которые требуются интерфейсу.</span><span class="sxs-lookup"><span data-stu-id="937c8-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="937c8-137">Этот перечисленный класс будет читать строки из текстового файла по одной строке за раз.</span><span class="sxs-lookup"><span data-stu-id="937c8-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="937c8-138">Добавьте следующий код в класс, чтобы разоблачить общедоступный конструктор, который использует путь файла в качестве параметра ввода.</span><span class="sxs-lookup"><span data-stu-id="937c8-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="937c8-139">Реализация <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> метода `IEnumerable(Of String)` интерфейса вернет новый экземпляр `StreamReaderEnumerator` класса.</span><span class="sxs-lookup"><span data-stu-id="937c8-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="937c8-140">Реализация `GetEnumerator` метода интерфейса `IEnumerable` может быть `Private`сделана, потому что `IEnumerable(Of String)` вы должны подвергать только члены интерфейса.</span><span class="sxs-lookup"><span data-stu-id="937c8-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="937c8-141">Замените код, созданный `GetEnumerator` Visual Basic для методов, следующим кодом.</span><span class="sxs-lookup"><span data-stu-id="937c8-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="937c8-142">**Добавить код для реализации IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="937c8-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="937c8-143">Откройте файл StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="937c8-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="937c8-144">На линии `Public Class StreamReaderEnumerator`после , введите следующее и нажмите ENTER.</span><span class="sxs-lookup"><span data-stu-id="937c8-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="937c8-145">Visual Basic автоматически заполняет класс членами, `IEnumerator(Of String)` которые требуются интерфейсу.</span><span class="sxs-lookup"><span data-stu-id="937c8-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="937c8-146">Класс enumerator открывает текстовый файл и выполняет ввоза файла/от, чтобы прочитать строки из файла.</span><span class="sxs-lookup"><span data-stu-id="937c8-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="937c8-147">Добавьте следующий код в класс, чтобы разоблачить общедоступный конструктор, который использует путь файла в качестве параметра ввода, и откройте текстовый файл для чтения.</span><span class="sxs-lookup"><span data-stu-id="937c8-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="937c8-148">Свойства `Current` как для `IEnumerator(Of String)` `IEnumerator` интерфейсов, так и для интерфейсов `String`возвращают текущий элемент из текстового файла в виде.</span><span class="sxs-lookup"><span data-stu-id="937c8-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="937c8-149">Реализация `Current` свойства `IEnumerator` интерфейса может быть `Private`сделана, потому что вы `IEnumerator(Of String)` должны подвергать только члены интерфейса.</span><span class="sxs-lookup"><span data-stu-id="937c8-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="937c8-150">Замените код, созданный `Current` Visual Basic для свойств, следующим кодом.</span><span class="sxs-lookup"><span data-stu-id="937c8-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="937c8-151">Метод `MoveNext` `IEnumerator` интерфейса переходит к следующему элементу в текстовом файле `Current` и обновляет значение, возвращаемое свойством.</span><span class="sxs-lookup"><span data-stu-id="937c8-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="937c8-152">Если нет больше элементов для `MoveNext` чтения, метод возвращается; `False` в `MoveNext` противном `True`случае метод возвращается.</span><span class="sxs-lookup"><span data-stu-id="937c8-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="937c8-153">Добавьте в метод `MoveNext` следующий код.</span><span class="sxs-lookup"><span data-stu-id="937c8-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="937c8-154">Метод `Reset` `IEnumerator` интерфейса направляет итератору указывать на начало текстового файла и очищает текущее значение элемента.</span><span class="sxs-lookup"><span data-stu-id="937c8-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="937c8-155">Добавьте в метод `Reset` следующий код.</span><span class="sxs-lookup"><span data-stu-id="937c8-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="937c8-156">Метод `Dispose` `IEnumerator` интерфейса гарантирует, что все неуправляемые ресурсы будут выпущены до уничтожения итератора.</span><span class="sxs-lookup"><span data-stu-id="937c8-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="937c8-157">Ручка файла, используемая `StreamReader` объектом, является неуправляемым ресурсом и должна быть закрыта до того, как экземпляр итератора будет уничтожен.</span><span class="sxs-lookup"><span data-stu-id="937c8-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="937c8-158">Замените код, созданный `Dispose` Visual Basic для метода, следующим кодом.</span><span class="sxs-lookup"><span data-stu-id="937c8-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="937c8-159">Использование образца Итератора</span><span class="sxs-lookup"><span data-stu-id="937c8-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="937c8-160">В коде можно использовать перечисленный класс вместе со структурами управления, `IEnumerable`требующими `For Next` реализации объекта, например цикла или запроса НАИС.</span><span class="sxs-lookup"><span data-stu-id="937c8-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="937c8-161">В следующем примере `StreamReaderEnumerable` показан запрос LIN'а.</span><span class="sxs-lookup"><span data-stu-id="937c8-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="937c8-162">См. также раздел</span><span class="sxs-lookup"><span data-stu-id="937c8-162">See also</span></span>

- <span data-ttu-id="937c8-163">[Introduction to LINQ in Visual Basic](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md) (Знакомство с LINQ в Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="937c8-163">[Introduction to LINQ in Visual Basic](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)</span></span>
- [<span data-ttu-id="937c8-164">Поток управления</span><span class="sxs-lookup"><span data-stu-id="937c8-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="937c8-165">Циклические структуры</span><span class="sxs-lookup"><span data-stu-id="937c8-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="937c8-166">Оператор For Each...Next</span><span class="sxs-lookup"><span data-stu-id="937c8-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
