---
title: Кортежи
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: e0310f31d7becb1f79bb023a277bd565421b44fb
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/22/2019
ms.locfileid: "74350502"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="82622-102">Кортежи (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="82622-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="82622-103">Начиная с Visual Basic 2017, язык Visual Basic предлагает встроенную поддержку кортежей, которые упрощают создание кортежей и доступ к элементам кортежей.</span><span class="sxs-lookup"><span data-stu-id="82622-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="82622-104">Кортеж — это упрощенная структура данных, которая имеет определенное число и последовательность значений.</span><span class="sxs-lookup"><span data-stu-id="82622-104">A tuple is a lightweight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="82622-105">При создании экземпляра кортежа вы определяете число и тип данных каждого значения (или элемента).</span><span class="sxs-lookup"><span data-stu-id="82622-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="82622-106">Например, 2 кортежа (или пара) имеет два элемента.</span><span class="sxs-lookup"><span data-stu-id="82622-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="82622-107">Первый может быть `Boolean` значением, а второй — `String`.</span><span class="sxs-lookup"><span data-stu-id="82622-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="82622-108">Поскольку кортежи упрощают хранение нескольких значений в одном объекте, они часто используются в качестве упрощенного способа возврата нескольких значений из метода.</span><span class="sxs-lookup"><span data-stu-id="82622-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="82622-109">Для поддержки кортежей требуется тип <xref:System.ValueTuple>.</span><span class="sxs-lookup"><span data-stu-id="82622-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="82622-110">Если .NET Framework 4,7 не установлен, необходимо добавить пакет NuGet `System.ValueTuple`, который доступен в коллекции NuGet.</span><span class="sxs-lookup"><span data-stu-id="82622-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="82622-111">Без этого пакета может возникнуть ошибка компиляции, аналогичная "предопределенный тип ValueTuple (Of,,,)" не определен или не импортирован ".</span><span class="sxs-lookup"><span data-stu-id="82622-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="82622-112">Создание экземпляров и использование кортежа</span><span class="sxs-lookup"><span data-stu-id="82622-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="82622-113">Вы создаете экземпляр кортежа, заключая в круглые скобки значения, разделенные запятыми.</span><span class="sxs-lookup"><span data-stu-id="82622-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="82622-114">Каждое из этих значений затем превращается в поле кортежа.</span><span class="sxs-lookup"><span data-stu-id="82622-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="82622-115">Например, следующий код определяет тройной (или 3-кортежный) элемент с `Date` в качестве первого значения, `String` в качестве второго, а `Boolean` в качестве третьего.</span><span class="sxs-lookup"><span data-stu-id="82622-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="82622-116">По умолчанию имя каждого поля в кортеже состоит из строки `Item` вместе с позицией поля, основанной на единице в кортеже.</span><span class="sxs-lookup"><span data-stu-id="82622-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="82622-117">В этом трехмерном кортеже поле `Date` `Item1`, поле `String` `Item2`, а `Boolean` поле — `Item3`.</span><span class="sxs-lookup"><span data-stu-id="82622-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="82622-118">В следующем примере выводятся значения полей кортежа, созданного в предыдущей строке кода.</span><span class="sxs-lookup"><span data-stu-id="82622-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="82622-119">Поля Visual Basic кортежа доступны для чтения и записи; После создания экземпляра кортежа можно изменить его значения.</span><span class="sxs-lookup"><span data-stu-id="82622-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="82622-120">В следующем примере изменяются два из трех полей кортежа, созданного в предыдущем примере, и отображается результат.</span><span class="sxs-lookup"><span data-stu-id="82622-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="82622-121">Создание экземпляров и использование именованного кортежа</span><span class="sxs-lookup"><span data-stu-id="82622-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="82622-122">Вместо использования имен по умолчанию для полей кортежа можно создать экземпляр *именованного кортежа* , назначив собственные имена элементам кортежа.</span><span class="sxs-lookup"><span data-stu-id="82622-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="82622-123">Затем к полям кортежа можно получить доступ по их назначенным именам *или* по именам по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="82622-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="82622-124">В следующем примере создается экземпляр того же трех кортежей, как и ранее, за исключением того, что он явно именует первое поле `EventDate`, второй `Name`и третий `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="82622-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="82622-125">Затем он отображает значения полей, изменяет их и снова отображает значения полей.</span><span class="sxs-lookup"><span data-stu-id="82622-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="82622-126">Выводимые имена элементов кортежа</span><span class="sxs-lookup"><span data-stu-id="82622-126">Inferred tuple element names</span></span>

<span data-ttu-id="82622-127">Начиная с Visual Basic 15,3, Visual Basic может определять имена элементов кортежа. их не нужно назначать явным образом.</span><span class="sxs-lookup"><span data-stu-id="82622-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="82622-128">Выводимые имена кортежей полезны при инициализации кортежа из набора переменных и если имя элемента кортежа должно совпадать с именем переменной.</span><span class="sxs-lookup"><span data-stu-id="82622-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span>

<span data-ttu-id="82622-129">В следующем примере создается `stateInfo` кортеж, содержащий три явно именованных элемента, `state`, `stateName`и `capital`.</span><span class="sxs-lookup"><span data-stu-id="82622-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="82622-130">Обратите внимание, что при именовании элементов оператор инициализации кортежа просто присваивает именованным элементам значения переменных с одинаковыми именами.</span><span class="sxs-lookup"><span data-stu-id="82622-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]

<span data-ttu-id="82622-131">Поскольку элементы и переменные имеют одинаковое имя, компилятор Visual Basic может вывести имена полей, как показано в следующем примере.</span><span class="sxs-lookup"><span data-stu-id="82622-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="82622-132">Чтобы включить выводимые имена элементов кортежа, необходимо определить версию компилятора Visual Basic для использования в файле проекта Visual Basic (\*. vbproj):</span><span class="sxs-lookup"><span data-stu-id="82622-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span>

```xml
<PropertyGroup>
  <LangVersion>15.3</LangVersion>
</PropertyGroup>
```

<span data-ttu-id="82622-133">Номер версии может быть любой версией компилятора Visual Basic, начиная с 15,3.</span><span class="sxs-lookup"><span data-stu-id="82622-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="82622-134">Вместо того чтобы жестко программировать конкретную версию компилятора, можно также указать "latest" в качестве значения `LangVersion` для компиляции с последней версией компилятора Visual Basic, установленного в системе.</span><span class="sxs-lookup"><span data-stu-id="82622-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="82622-135">Дополнительные сведения см. [в разделе Задание версии языка Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="82622-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="82622-136">В некоторых случаях компилятор Visual Basic не может определить имя элемента кортежа из имени кандидата, а на поле кортежа можно ссылаться только по имени по умолчанию, например `Item1`, `Item2`и т. д. К ним относятся:</span><span class="sxs-lookup"><span data-stu-id="82622-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="82622-137">Имя кандидата совпадает с именем элемента кортежа, например `Item3`, `Rest`или `ToString`.</span><span class="sxs-lookup"><span data-stu-id="82622-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="82622-138">Имя кандидата дублируется в кортеже.</span><span class="sxs-lookup"><span data-stu-id="82622-138">The candidate name is duplicated in the tuple.</span></span>

<span data-ttu-id="82622-139">При сбое определения имени поля Visual Basic не создает ошибку компилятора, а во время выполнения не создается исключение.</span><span class="sxs-lookup"><span data-stu-id="82622-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="82622-140">Вместо этого на поля кортежа должны ссылаться их предопределенные имена, такие как `Item1` и `Item2`.</span><span class="sxs-lookup"><span data-stu-id="82622-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span>

## <a name="tuples-versus-structures"></a><span data-ttu-id="82622-141">Кортежи и структуры</span><span class="sxs-lookup"><span data-stu-id="82622-141">Tuples versus structures</span></span>

<span data-ttu-id="82622-142">Visual Basic кортеж — это тип значения, который является экземпляром одного из универсальных типов **System. ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="82622-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="82622-143">Например, `holiday` кортеж, определенный в предыдущем примере, является экземпляром структуры <xref:System.ValueTuple%603>.</span><span class="sxs-lookup"><span data-stu-id="82622-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="82622-144">Она разработана как облегченный контейнер для данных.</span><span class="sxs-lookup"><span data-stu-id="82622-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="82622-145">Поскольку кортеж предназначен для упрощения создания объекта с несколькими элементами данных, в нем отсутствуют некоторые функции, которые могут иметься в пользовательской структуре.</span><span class="sxs-lookup"><span data-stu-id="82622-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="82622-146">К ним относятся следующие методы.</span><span class="sxs-lookup"><span data-stu-id="82622-146">These include:</span></span>

- <span data-ttu-id="82622-147">Пользовательские элементы.</span><span class="sxs-lookup"><span data-stu-id="82622-147">Custom members.</span></span> <span data-ttu-id="82622-148">Нельзя определить собственные свойства, методы или события для кортежа.</span><span class="sxs-lookup"><span data-stu-id="82622-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="82622-149">Проверка.</span><span class="sxs-lookup"><span data-stu-id="82622-149">Validation.</span></span> <span data-ttu-id="82622-150">Невозможно проверить данные, назначенные полям.</span><span class="sxs-lookup"><span data-stu-id="82622-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="82622-151">Неизменяемости.</span><span class="sxs-lookup"><span data-stu-id="82622-151">Immutability.</span></span> <span data-ttu-id="82622-152">Visual Basic кортежи являются изменяемыми.</span><span class="sxs-lookup"><span data-stu-id="82622-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="82622-153">В отличие от этого, пользовательская структура позволяет контролировать, является ли экземпляр изменяемым или неизменяемым.</span><span class="sxs-lookup"><span data-stu-id="82622-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="82622-154">Если важны пользовательские члены, проверка свойств и полей или неизменность, следует использовать инструкцию Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) для определения пользовательского типа значения.</span><span class="sxs-lookup"><span data-stu-id="82622-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="82622-155">Visual Basicный кортеж наследует члены его типа **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="82622-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="82622-156">Помимо полей, в их число входят следующие методы.</span><span class="sxs-lookup"><span data-stu-id="82622-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="82622-157">Член</span><span class="sxs-lookup"><span data-stu-id="82622-157">Member</span></span> | <span data-ttu-id="82622-158">Описание</span><span class="sxs-lookup"><span data-stu-id="82622-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="82622-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="82622-159">CompareTo</span></span> | <span data-ttu-id="82622-160">Сравнивает текущий кортеж с другим кортежем с тем же количеством элементов.</span><span class="sxs-lookup"><span data-stu-id="82622-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="82622-161">Равно</span><span class="sxs-lookup"><span data-stu-id="82622-161">Equals</span></span> | <span data-ttu-id="82622-162">Определяет, равен ли текущий кортеж другому кортежу или объекту.</span><span class="sxs-lookup"><span data-stu-id="82622-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="82622-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="82622-163">GetHashCode</span></span> | <span data-ttu-id="82622-164">Вычисляет хэш-код для текущего экземпляра.</span><span class="sxs-lookup"><span data-stu-id="82622-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="82622-165">ToString</span><span class="sxs-lookup"><span data-stu-id="82622-165">ToString</span></span> | <span data-ttu-id="82622-166">Возвращает строковое представление этого кортежа, которое принимает форму `(Item1, Item2...)`, где `Item1` и `Item2` представляют значения полей кортежа.</span><span class="sxs-lookup"><span data-stu-id="82622-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="82622-167">Кроме того, типы **ValueTuple** реализуют интерфейсы <xref:System.Collections.IStructuralComparable> и <xref:System.Collections.IStructuralEquatable>, которые позволяют определять компараторы клиентов.</span><span class="sxs-lookup"><span data-stu-id="82622-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="82622-168">Назначение и кортежи</span><span class="sxs-lookup"><span data-stu-id="82622-168">Assignment and tuples</span></span>

<span data-ttu-id="82622-169">Visual Basic поддерживает присваивание между типами кортежей, которые имеют одинаковое число полей.</span><span class="sxs-lookup"><span data-stu-id="82622-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="82622-170">Типы полей можно преобразовать, если выполняется одно из следующих условий.</span><span class="sxs-lookup"><span data-stu-id="82622-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="82622-171">Исходное и целевое поля имеют один и тот же тип.</span><span class="sxs-lookup"><span data-stu-id="82622-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="82622-172">Определено расширяющее (или неявное) преобразование исходного типа в целевой тип.</span><span class="sxs-lookup"><span data-stu-id="82622-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span>

- <span data-ttu-id="82622-173">`Option Strict` `On`и определено суженное (или явное) преобразование исходного типа в целевой тип.</span><span class="sxs-lookup"><span data-stu-id="82622-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="82622-174">Это преобразование может вызвать исключение, если исходное значение находится за пределами диапазона целевого типа.</span><span class="sxs-lookup"><span data-stu-id="82622-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="82622-175">Другие преобразования в контексте назначений не учитываются.</span><span class="sxs-lookup"><span data-stu-id="82622-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="82622-176">Рассмотрим возможные виды назначений между типами кортежей.</span><span class="sxs-lookup"><span data-stu-id="82622-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="82622-177">В приведенных ниже примерах можно использовать указанные переменные:</span><span class="sxs-lookup"><span data-stu-id="82622-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="82622-178">Первые две переменные, `unnamed` и `anonymous`, не имеют семантических имен, предоставленных для полей.</span><span class="sxs-lookup"><span data-stu-id="82622-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="82622-179">Их имена полей являются `Item1` по умолчанию и `Item2`.</span><span class="sxs-lookup"><span data-stu-id="82622-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="82622-180">Последние две переменные, `named` и `differentName` имеют семантические имена полей.</span><span class="sxs-lookup"><span data-stu-id="82622-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="82622-181">Обратите внимание на то, что поля в этих двух кортежах называются по-разному.</span><span class="sxs-lookup"><span data-stu-id="82622-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="82622-182">Все четыре из этих кортежей имеют одинаковое число полей (называемое арностью), а типы этих полей идентичны.</span><span class="sxs-lookup"><span data-stu-id="82622-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="82622-183">Таким образом, все эти назначения работают:</span><span class="sxs-lookup"><span data-stu-id="82622-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="82622-184">Обратите внимание на то, что имена кортежей не назначаются.</span><span class="sxs-lookup"><span data-stu-id="82622-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="82622-185">Значения полей назначаются в соответствии с порядком полей в кортеже.</span><span class="sxs-lookup"><span data-stu-id="82622-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="82622-186">Наконец, обратите внимание, что можно присвоить `named` кортежу `conversion`ному кортежу, несмотря на то, что первое поле `named` — это `Integer`, а первое поле `conversion` — `Long`.</span><span class="sxs-lookup"><span data-stu-id="82622-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="82622-187">Это назначение выполнено, так как преобразование `Integer` в `Long` является расширяющим преобразованием.</span><span class="sxs-lookup"><span data-stu-id="82622-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="82622-188">Кортежи с разными числами полей не могут быть назначены:</span><span class="sxs-lookup"><span data-stu-id="82622-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="82622-189">Кортежи как возвращаемые значения методов</span><span class="sxs-lookup"><span data-stu-id="82622-189">Tuples as method return values</span></span>

<span data-ttu-id="82622-190">Метод может возвращать только одно значение.</span><span class="sxs-lookup"><span data-stu-id="82622-190">A method can return only a single value.</span></span> <span data-ttu-id="82622-191">Однако часто требуется, чтобы вызов метода возвращал несколько значений.</span><span class="sxs-lookup"><span data-stu-id="82622-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="82622-192">Существует несколько способов обойти это ограничение:</span><span class="sxs-lookup"><span data-stu-id="82622-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="82622-193">Можно создать пользовательский класс или структуру, свойства или поля которой представляют значения, возвращаемые методом.</span><span class="sxs-lookup"><span data-stu-id="82622-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="82622-194">Таким решением является высокоплотное решение; для этого необходимо определить пользовательский тип, предназначенный только для извлечения значений из вызова метода.</span><span class="sxs-lookup"><span data-stu-id="82622-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="82622-195">Можно вернуть одно значение из метода и вернуть оставшиеся значения, передав их по ссылке к методу.</span><span class="sxs-lookup"><span data-stu-id="82622-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="82622-196">Это включает дополнительные издержки при создании экземпляра переменной и риск непреднамеренной перезаписи значения переменной, которую вы передаете по ссылке.</span><span class="sxs-lookup"><span data-stu-id="82622-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="82622-197">Можно использовать кортеж, который предоставляет упрощенное решение для извлечения нескольких возвращаемых значений.</span><span class="sxs-lookup"><span data-stu-id="82622-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="82622-198">Например, методы **TryParse** в .NET возвращают `Boolean` значение, указывающее, была ли операция анализа успешной.</span><span class="sxs-lookup"><span data-stu-id="82622-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="82622-199">Результат операции синтаксического анализа возвращается в переменную, передаваемой по ссылке на метод.</span><span class="sxs-lookup"><span data-stu-id="82622-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="82622-200">Как правило, вызов метода синтаксического анализа, например <xref:System.Int32.TryParse%2A?displayProperty=nameWithType>, выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="82622-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="82622-201">Мы можем вернуть кортеж из операции синтаксического анализа, если заключить вызов метода <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> в собственный метод.</span><span class="sxs-lookup"><span data-stu-id="82622-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="82622-202">В следующем примере `NumericLibrary.ParseInteger` вызывает метод <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> и возвращает именованный кортеж с двумя элементами.</span><span class="sxs-lookup"><span data-stu-id="82622-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="82622-203">Затем можно вызвать метод с помощью следующего кода:</span><span class="sxs-lookup"><span data-stu-id="82622-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="82622-204">Visual Basic кортежи и кортежи в .NET Framework</span><span class="sxs-lookup"><span data-stu-id="82622-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="82622-205">Visual Basic кортеж — это экземпляр одного из универсальных типов **System. ValueTuple** , которые появились в .NET Framework 4,7.</span><span class="sxs-lookup"><span data-stu-id="82622-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="82622-206">.NET Framework также включает набор универсальных классов **System. Tuple** .</span><span class="sxs-lookup"><span data-stu-id="82622-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="82622-207">Однако эти классы отличаются от Visual Basic кортежей и универсальных типов **System. ValueTuple** несколькими способами:</span><span class="sxs-lookup"><span data-stu-id="82622-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="82622-208">Элементы классов **кортежей** — это свойства с именами `Item1`, `Item2`и т. д.</span><span class="sxs-lookup"><span data-stu-id="82622-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="82622-209">В Visual Basic кортежах и типах **ValueTuple** элементы кортежа являются полями.</span><span class="sxs-lookup"><span data-stu-id="82622-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="82622-210">Нельзя присваивать осмысленные имена элементам экземпляра **кортежа** или экземпляра **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="82622-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="82622-211">Visual Basic позволяет назначать имена, которые сообщают значение полей.</span><span class="sxs-lookup"><span data-stu-id="82622-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="82622-212">Свойства экземпляра **кортежа** доступны только для чтения; кортежи являются неизменяемыми.</span><span class="sxs-lookup"><span data-stu-id="82622-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="82622-213">В Visual Basic кортежах и типах **ValueTuple** поля кортежей доступны для чтения и записи. кортежи являются изменяемыми.</span><span class="sxs-lookup"><span data-stu-id="82622-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="82622-214">Универсальные типы **кортежей** являются ссылочными типами.</span><span class="sxs-lookup"><span data-stu-id="82622-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="82622-215">Использование этих типов **кортежей** означает выделение объектов.</span><span class="sxs-lookup"><span data-stu-id="82622-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="82622-216">В критических путях это может заметно влиять на производительность приложения.</span><span class="sxs-lookup"><span data-stu-id="82622-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="82622-217">Visual Basic кортежи и типы **ValueTuple** являются типами значений.</span><span class="sxs-lookup"><span data-stu-id="82622-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="82622-218">Методы расширения в классе <xref:System.TupleExtensions> упрощают преобразование между Visual Basicными кортежами и объектами **кортежей** .NET.</span><span class="sxs-lookup"><span data-stu-id="82622-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="82622-219">Метод **тотупле** преобразует кортеж Visual Basic в объект **кортежа** .NET, а метод **товалуетупле** преобразует объект **кортежа** .NET в кортеж Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="82622-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="82622-220">В следующем примере создается кортеж, преобразуется в объект **кортежа** .NET и преобразуется обратно в Visual Basic кортеж.</span><span class="sxs-lookup"><span data-stu-id="82622-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="82622-221">Затем в примере сравнивается этот кортеж с исходным, чтобы убедиться, что он равен.</span><span class="sxs-lookup"><span data-stu-id="82622-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="82622-222">См. также</span><span class="sxs-lookup"><span data-stu-id="82622-222">See also</span></span>

- [<span data-ttu-id="82622-223">Справочник по языку Visual Basic</span><span class="sxs-lookup"><span data-stu-id="82622-223">Visual Basic Language Reference</span></span>](index.md)
