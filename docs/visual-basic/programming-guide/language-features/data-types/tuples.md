---
title: "Кортежи в Visual Basic"
ms.custom: 
ms.date: 04/23/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology:
- devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: bf26b7ce58c1e20fbbe5043cbd2acfd5712837fa
ms.sourcegitcommit: d95a91d685565f4d95c8773b558752864a6a3d7e
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/12/2018
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="9ff09-102">Кортежи (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="9ff09-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="9ff09-103">Начиная с Visual Basic 2017 г., в языке Visual Basic предлагает встроенную поддержку кортежей, который делает создание кортежей и доступ к элементам проще кортежей.</span><span class="sxs-lookup"><span data-stu-id="9ff09-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="9ff09-104">Кортеж — это структура данных недоступно, с определенным номером и последовательность значений.</span><span class="sxs-lookup"><span data-stu-id="9ff09-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="9ff09-105">При создании экземпляра кортежа, следует определить, количество и тип данных каждого значения (или элемент).</span><span class="sxs-lookup"><span data-stu-id="9ff09-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="9ff09-106">Например кортеж (или пары) состоит из двух элементов.</span><span class="sxs-lookup"><span data-stu-id="9ff09-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="9ff09-107">Возможно, первый `Boolean` значение, а второй — `String`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="9ff09-108">Поскольку кортежей упростить процесс сохранения нескольких значений в одном объекте, они часто используются как упрощенный способ возвращать несколько значений из метода.</span><span class="sxs-lookup"><span data-stu-id="9ff09-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="9ff09-109">Поддержка кортежа требует <xref:System.ValueTuple> типа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="9ff09-110">Если 4.7 .NET Framework не установлена, необходимо добавить пакет NuGet `System.ValueTuple`, который можно найти в коллекции NuGet.</span><span class="sxs-lookup"><span data-stu-id="9ff09-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="9ff09-111">Без этого пакета может получить ошибку компиляции аналогично «Предопределенный тип «ValueTuple(Of,,,)» не определен и не импортирован.»</span><span class="sxs-lookup"><span data-stu-id="9ff09-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="9ff09-112">Создание и использование кортежа</span><span class="sxs-lookup"><span data-stu-id="9ff09-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="9ff09-113">Создать кортеж, заключив его круглые скобки обмена мгновенными сообщениями значений с разделителями запятыми.</span><span class="sxs-lookup"><span data-stu-id="9ff09-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="9ff09-114">Затем каждый из этих значений становится полем кортежа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="9ff09-115">Например, следующий код определяет triple (или кортежа 3) с помощью `Date` как значение его первого, `String` его вторым и `Boolean` как третьего.</span><span class="sxs-lookup"><span data-stu-id="9ff09-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="9ff09-116">По умолчанию имя каждого поля в кортеж включает строку `Item` вместе с положением на основе одного поля в кортеже.</span><span class="sxs-lookup"><span data-stu-id="9ff09-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="9ff09-117">Для 3-кортежа `Date` поле является `Item1`, `String` поле является `Item2`и `Boolean` поле является `Item3`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="9ff09-118">Следующий пример отображает значения полей кортежа, созданный в предыдущей строке кода</span><span class="sxs-lookup"><span data-stu-id="9ff09-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="9ff09-119">Поля кортеж Visual Basic, чтения и записи; После создания экземпляра кортеж его значения можно изменить.</span><span class="sxs-lookup"><span data-stu-id="9ff09-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="9ff09-120">Следующий пример изменяет две из трех полей кортежа, созданные в предыдущем примере и отображает результат.</span><span class="sxs-lookup"><span data-stu-id="9ff09-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="9ff09-121">Создание экземпляра и использование именованных кортежа</span><span class="sxs-lookup"><span data-stu-id="9ff09-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="9ff09-122">Вместо того чтобы использовать имена по умолчанию для полей кортежа, можно создать экземпляр *кортеж с именем* , назначив собственные имена элементов кортежа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="9ff09-123">Поля кортежа может осуществляться по именам и назначенный *или* , их имена по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="9ff09-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="9ff09-124">В следующем примере создается же кортеж 3 как ранее, за исключением того, что она явно указана первое поле `EventDate`, второй `Name`, а в третьем `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="9ff09-125">Затем он отображает значения полей, изменяет их и отображает значения полей еще раз.</span><span class="sxs-lookup"><span data-stu-id="9ff09-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="9ff09-126">Имена элементов выводимых кортежа</span><span class="sxs-lookup"><span data-stu-id="9ff09-126">Inferred tuple element names</span></span>

<span data-ttu-id="9ff09-127">Начиная с 15,3 Visual Basic, Visual Basic могут выводить имена элементов кортежа; необходимо назначить их явно.</span><span class="sxs-lookup"><span data-stu-id="9ff09-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="9ff09-128">Выводимый кортежа имена полезны в тех случаях, когда инициализировать кортежа из набора переменных, и требуется имя элемента кортежа должен совпадать с именем переменной.</span><span class="sxs-lookup"><span data-stu-id="9ff09-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="9ff09-129">В следующем примере создается `stateInfo` кортеж, содержащий три явно именованные элементы, `state`, `stateName`, и `capital`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="9ff09-130">Обратите внимание, что при именовании элементы, оператор инициализации кортежа просто назначает именованных элементов значения переменных таким же именем.</span><span class="sxs-lookup"><span data-stu-id="9ff09-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="9ff09-131">Поскольку элементы и переменные имеют то же имя, компилятор Visual Basic может определить имена полей, как показано в следующем примере.</span><span class="sxs-lookup"><span data-stu-id="9ff09-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="9ff09-132">Чтобы включить имена элементов interred кортеж, необходимо определить версии компилятора Visual Basic для использования в проекте Visual Basic (\*.vbproj) файла:</span><span class="sxs-lookup"><span data-stu-id="9ff09-132">To enable interred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="9ff09-133">Номер версии может быть любой версии компилятора Visual Basic, начиная с 15,3.</span><span class="sxs-lookup"><span data-stu-id="9ff09-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="9ff09-134">Вместо того чтобы жестко запрограммированные конкретной версии компилятора, можно указать «Последней» в качестве значения `LangVersion` компилироваться в последней версии компилятора Visual Basic, установленного в системе.</span><span class="sxs-lookup"><span data-stu-id="9ff09-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="9ff09-135">В некоторых случаях компилятор Visual Basic не может определить имя элемента кортежа из допустимое имя и поля кортеж может ссылаться только с помощью его имя по умолчанию, например `Item1`, `Item2`и т. д. Сюда входит следующее.</span><span class="sxs-lookup"><span data-stu-id="9ff09-135">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="9ff09-136">Допустимое имя совпадает имя элемента кортежа, таких как `Item3`, `Rest`, или `ToString`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-136">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="9ff09-137">Имя кандидата дублируется в кортеже.</span><span class="sxs-lookup"><span data-stu-id="9ff09-137">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="9ff09-138">Если не удается определить имя поля, Visual Basic не создает ошибку компилятора, а также является исключение, создаваемое во время выполнения.</span><span class="sxs-lookup"><span data-stu-id="9ff09-138">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="9ff09-139">Вместо этого поля кортежа должна ссылаться на стандартные имена, такие как `Item1` и `Item2`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-139">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="9ff09-140">Кортежи и структур</span><span class="sxs-lookup"><span data-stu-id="9ff09-140">Tuples versus structures</span></span>

<span data-ttu-id="9ff09-141">Кортеж Visual Basic — это тип значения, который представляет собой экземпляр одного из **System.ValueTuple** универсальных типов.</span><span class="sxs-lookup"><span data-stu-id="9ff09-141">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="9ff09-142">Например `holiday` кортежа, определенных в предыдущем примере является экземпляром класса <xref:System.ValueTuple%603> структуры.</span><span class="sxs-lookup"><span data-stu-id="9ff09-142">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="9ff09-143">Она должна быть упрощенный контейнер для данных.</span><span class="sxs-lookup"><span data-stu-id="9ff09-143">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="9ff09-144">Поскольку кортежа стремится упростить процесс создания объекта с несколькими элементами данных, он не имеет некоторые возможности, которые может иметь пользовательскую структуру.</span><span class="sxs-lookup"><span data-stu-id="9ff09-144">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="9ff09-145">Сюда входит следующее.</span><span class="sxs-lookup"><span data-stu-id="9ff09-145">These include:</span></span>

- <span data-ttu-id="9ff09-146">Члены клиента.</span><span class="sxs-lookup"><span data-stu-id="9ff09-146">Customer members.</span></span> <span data-ttu-id="9ff09-147">Не удается определить собственные свойства, методы или события для кортеж.</span><span class="sxs-lookup"><span data-stu-id="9ff09-147">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="9ff09-148">Проверка.</span><span class="sxs-lookup"><span data-stu-id="9ff09-148">Validation.</span></span> <span data-ttu-id="9ff09-149">Не удалось проверить данные, присвоенные поля.</span><span class="sxs-lookup"><span data-stu-id="9ff09-149">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="9ff09-150">Неизменность.</span><span class="sxs-lookup"><span data-stu-id="9ff09-150">Immutability.</span></span> <span data-ttu-id="9ff09-151">Кортежи Visual Basic может изменяться.</span><span class="sxs-lookup"><span data-stu-id="9ff09-151">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="9ff09-152">В отличие от этого пользовательская структура позволяет вам управлять ли экземпляр изменяемым или неизменяемым.</span><span class="sxs-lookup"><span data-stu-id="9ff09-152">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="9ff09-153">Если пользовательские элементы, свойства и проверка полей или неизменность важны, следует использовать в Visual Basic [структуры](../../../language-reference/statements/structure-statement.md) инструкцию, чтобы определить тип пользовательского значения.</span><span class="sxs-lookup"><span data-stu-id="9ff09-153">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="9ff09-154">Кортеж Visual Basic наследуют элементы его **ValueTuple** типа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-154">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="9ff09-155">В дополнение к его полям к ним относятся следующие методы:</span><span class="sxs-lookup"><span data-stu-id="9ff09-155">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="9ff09-156">Член</span><span class="sxs-lookup"><span data-stu-id="9ff09-156">Member</span></span> | <span data-ttu-id="9ff09-157">Описание:</span><span class="sxs-lookup"><span data-stu-id="9ff09-157">Description</span></span> |
| ---|---|
| <span data-ttu-id="9ff09-158">CompareTo</span><span class="sxs-lookup"><span data-stu-id="9ff09-158">CompareTo</span></span> | <span data-ttu-id="9ff09-159">Сравнивает текущий кортеж другой кортежу с одинаковым числом элементов.</span><span class="sxs-lookup"><span data-stu-id="9ff09-159">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="9ff09-160">Равно</span><span class="sxs-lookup"><span data-stu-id="9ff09-160">Equals</span></span> | <span data-ttu-id="9ff09-161">Определяет, равен ли текущий кортеж другой кортеж или объектом.</span><span class="sxs-lookup"><span data-stu-id="9ff09-161">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="9ff09-162">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="9ff09-162">GetHashCode</span></span> | <span data-ttu-id="9ff09-163">Вычисляет хэш-код для текущего экземпляра.</span><span class="sxs-lookup"><span data-stu-id="9ff09-163">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="9ff09-164">ToString</span><span class="sxs-lookup"><span data-stu-id="9ff09-164">ToString</span></span> | <span data-ttu-id="9ff09-165">Возвращает строковое представление в виде кортежа `(Item1, Item2...)`, где `Item1` и `Item2` представляют значения полей кортежа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-165">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="9ff09-166">Кроме того **ValueTuple** типы реализуют <xref:System.Collections.IStructuralComparable> и <xref:System.Collections.IStructuralEquatable> интерфейсы, которые позволяют определить функции сравнения клиента.</span><span class="sxs-lookup"><span data-stu-id="9ff09-166">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="9ff09-167">Назначение и кортежи</span><span class="sxs-lookup"><span data-stu-id="9ff09-167">Assignment and tuples</span></span>

<span data-ttu-id="9ff09-168">Visual Basic поддерживает назначение между типами кортежа, имеющих одинаковое число полей.</span><span class="sxs-lookup"><span data-stu-id="9ff09-168">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="9ff09-169">Можно преобразовать типы полей, если выполняется одно из следующих условий:</span><span class="sxs-lookup"><span data-stu-id="9ff09-169">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="9ff09-170">Поле источника и целевой, того же типа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-170">The source and target field are of the same type.</span></span>

- <span data-ttu-id="9ff09-171">Расширяющее (или неявными) преобразование исходного типа в конечный тип определяется.</span><span class="sxs-lookup"><span data-stu-id="9ff09-171">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="9ff09-172">`Option Strict` — `On`, и определена, сужающего диапазон значений (или явных) преобразования типа источника к целевому типу.</span><span class="sxs-lookup"><span data-stu-id="9ff09-172">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="9ff09-173">Это преобразование может создать исключение, если исходное значение выходит за пределы типа целевого объекта.</span><span class="sxs-lookup"><span data-stu-id="9ff09-173">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="9ff09-174">Другие преобразования в контексте назначений не учитываются.</span><span class="sxs-lookup"><span data-stu-id="9ff09-174">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="9ff09-175">Рассмотрим возможные виды назначений между типами кортежей.</span><span class="sxs-lookup"><span data-stu-id="9ff09-175">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="9ff09-176">В приведенных ниже примерах можно использовать указанные переменные:</span><span class="sxs-lookup"><span data-stu-id="9ff09-176">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="9ff09-177">Первые две переменные `unnamed` и `anonymous`, не имеют имен семантики, указанное для поля.</span><span class="sxs-lookup"><span data-stu-id="9ff09-177">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="9ff09-178">По умолчанию используются соответствующие имена полей `Item1` и `Item2`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-178">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="9ff09-179">Последние два переменные `named` и `differentName` имеют имена полей семантики.</span><span class="sxs-lookup"><span data-stu-id="9ff09-179">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="9ff09-180">Обратите внимание на то, что поля в этих двух кортежах называются по-разному.</span><span class="sxs-lookup"><span data-stu-id="9ff09-180">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="9ff09-181">Все четыре эти кортежей, имеют одинаковый номер полей (так называемый «арность») и типы эти поля идентичны.</span><span class="sxs-lookup"><span data-stu-id="9ff09-181">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="9ff09-182">Таким образом, все эти назначения работают:</span><span class="sxs-lookup"><span data-stu-id="9ff09-182">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="9ff09-183">Обратите внимание на то, что имена кортежей не назначаются.</span><span class="sxs-lookup"><span data-stu-id="9ff09-183">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="9ff09-184">Значения полей назначаются в соответствии с порядком полей в кортеже.</span><span class="sxs-lookup"><span data-stu-id="9ff09-184">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="9ff09-185">И, наконец, обратите внимание, что можно назначить `named` кортеж `conversion` кортежа, несмотря на то что в первое поле `named` — `Integer`и в первое поле `conversion` — `Long`.</span><span class="sxs-lookup"><span data-stu-id="9ff09-185">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="9ff09-186">Это назначение завершается успешно, поскольку преобразование `Integer` для `Long` расширяющие преобразования.</span><span class="sxs-lookup"><span data-stu-id="9ff09-186">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="9ff09-187">Кортежи с различным количеством поля не может быть назначен:</span><span class="sxs-lookup"><span data-stu-id="9ff09-187">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="9ff09-188">Кортежи как возвращаемые значения методов</span><span class="sxs-lookup"><span data-stu-id="9ff09-188">Tuples as method return values</span></span>

<span data-ttu-id="9ff09-189">Метод может возвращать только одно значение.</span><span class="sxs-lookup"><span data-stu-id="9ff09-189">A method can return only a single value.</span></span> <span data-ttu-id="9ff09-190">Часто хотя вам бы хотелось вызов метода для возврата нескольких значений.</span><span class="sxs-lookup"><span data-stu-id="9ff09-190">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="9ff09-191">Чтобы обойти это ограничение несколькими способами:</span><span class="sxs-lookup"><span data-stu-id="9ff09-191">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="9ff09-192">Можно создать пользовательский класс или структуру, свойства или поля представляют значения, возвращаемого методом.</span><span class="sxs-lookup"><span data-stu-id="9ff09-192">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="9ff09-193">Таким образом — это решение высокой плотности; необходимо определить пользовательский тип которого предназначен только для извлечения значений из вызова метода.</span><span class="sxs-lookup"><span data-stu-id="9ff09-193">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="9ff09-194">Возвращает одно значение из метода и возврата оставшиеся значения, передавая их по ссылке в метод.</span><span class="sxs-lookup"><span data-stu-id="9ff09-194">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="9ff09-195">Это включает в себя дополнительную нагрузку, связанную с создания экземпляра переменной и риски при перезаписи значение переменной, которая передается по ссылке.</span><span class="sxs-lookup"><span data-stu-id="9ff09-195">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="9ff09-196">Можно использовать кортеж, который предоставляет простое решение для извлечения с несколькими возвращаемыми значениями.</span><span class="sxs-lookup"><span data-stu-id="9ff09-196">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="9ff09-197">Например **TryParse** методы возврата .NET `Boolean` значение, указывающее, успешно ли выполнена операция синтаксического анализа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-197">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="9ff09-198">Результат операции анализа возвращается в переменную, передаваемый по ссылке в метод.</span><span class="sxs-lookup"><span data-stu-id="9ff09-198">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="9ff09-199">Как правило, вызов метода анализа, такие как <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="9ff09-199">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="9ff09-200">Мы можем вернуться кортеж из операции анализа, если мы программу-оболочку вызова <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> в нашей методе.</span><span class="sxs-lookup"><span data-stu-id="9ff09-200">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="9ff09-201">В следующем примере `NumericLibrary.ParseInteger` вызовы <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> метод и возвращает кортеж, именованный с двумя элементами.</span><span class="sxs-lookup"><span data-stu-id="9ff09-201">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="9ff09-202">Затем можно вызвать метод с помощью следующего кода:</span><span class="sxs-lookup"><span data-stu-id="9ff09-202">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="9ff09-203">Visual Basic кортежи и кортежи в .NET Framework</span><span class="sxs-lookup"><span data-stu-id="9ff09-203">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="9ff09-204">Visual Basic кортеж представляет собой экземпляр одного из **System.ValueTuple** универсальных типов, которые впервые появились в .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="9ff09-204">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="9ff09-205">Платформа .NET Framework также включает набор универсального **System.Tuple** классы.</span><span class="sxs-lookup"><span data-stu-id="9ff09-205">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="9ff09-206">Эти классы тем не менее, отличаются от кортежей Visual Basic и **System.ValueTuple** универсальных типов в следующих случаях:</span><span class="sxs-lookup"><span data-stu-id="9ff09-206">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="9ff09-207">Элементы **кортежа** классы являются свойства с именем `Item1`, `Item2`, и т. д.</span><span class="sxs-lookup"><span data-stu-id="9ff09-207">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="9ff09-208">В Visual Basic кортежей и **ValueTuple** поля, типы элементов кортежа.</span><span class="sxs-lookup"><span data-stu-id="9ff09-208">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="9ff09-209">Элементы нельзя назначить значимые имена **кортежа** экземпляра или **ValueTuple** экземпляра.</span><span class="sxs-lookup"><span data-stu-id="9ff09-209">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="9ff09-210">Visual Basic позволяет присваивать имена, которые смысловое значение поля.</span><span class="sxs-lookup"><span data-stu-id="9ff09-210">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="9ff09-211">Свойства **кортежа** экземпляра доступны только для чтения; кортежи являются неизменяемыми.</span><span class="sxs-lookup"><span data-stu-id="9ff09-211">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="9ff09-212">В Visual Basic кортежей и **ValueTuple** типы, поля кортежа, чтения и записи; кортежи может изменяться.</span><span class="sxs-lookup"><span data-stu-id="9ff09-212">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="9ff09-213">Универсальный **кортежа** типы являются ссылочными типами.</span><span class="sxs-lookup"><span data-stu-id="9ff09-213">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="9ff09-214">Используя эти **кортежа** типы означает выделение объектов.</span><span class="sxs-lookup"><span data-stu-id="9ff09-214">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="9ff09-215">В критических путях это может заметно влиять на производительность приложения.</span><span class="sxs-lookup"><span data-stu-id="9ff09-215">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="9ff09-216">Visual Basic кортежей и **ValueTuple** типы являются типами значений.</span><span class="sxs-lookup"><span data-stu-id="9ff09-216">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="9ff09-217">Методы расширения в <xref:System.TupleExtensions> класс облегчить преобразование между кортежи Visual Basic и .NET **кортежа** объектов.</span><span class="sxs-lookup"><span data-stu-id="9ff09-217">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="9ff09-218">**ToTuple** метод преобразует кортеж Visual Basic .NET **кортежа** объекта и **ToValueTuple** метод преобразует .NET **кортежа** Объект кортежа Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="9ff09-218">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="9ff09-219">Следующий пример создает кортеж, преобразует его в .NET **кортежа** объекта и преобразует его обратно кортеж Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="9ff09-219">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="9ff09-220">Затем в примере сравнивается этот кортеж исходной версией, чтобы убедиться, что они равны.</span><span class="sxs-lookup"><span data-stu-id="9ff09-220">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="9ff09-221">См. также</span><span class="sxs-lookup"><span data-stu-id="9ff09-221">See also</span></span>

[<span data-ttu-id="9ff09-222">Справочник по языку Visual Basic</span><span class="sxs-lookup"><span data-stu-id="9ff09-222">Visual Basic Language Reference</span></span>](index.md)  
