---
title: Кортежи в Visual Basic
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: 146e9c2360cea153d2f487769d5b983516861e8d
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/23/2019
ms.locfileid: "61663301"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="bd963-102">Кортежи (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="bd963-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="bd963-103">Начиная с Visual Basic 2017, язык Visual Basic предлагает встроенную поддержку для кортежей, которая делает создание кортежей и обращение к элементов кортежей, проще.</span><span class="sxs-lookup"><span data-stu-id="bd963-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="bd963-104">Кортеж — это структура данных нетребовательный к ресурсам, с определенным номером и последовательность значений.</span><span class="sxs-lookup"><span data-stu-id="bd963-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="bd963-105">При создании экземпляра кортежа, вы определяете, номер и тип данных каждого значения (или элемент).</span><span class="sxs-lookup"><span data-stu-id="bd963-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="bd963-106">Например 2 кортежей (или пара) состоит из двух элементов.</span><span class="sxs-lookup"><span data-stu-id="bd963-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="bd963-107">Возможно, первый `Boolean` значения, второй — `String`.</span><span class="sxs-lookup"><span data-stu-id="bd963-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="bd963-108">Поскольку кортежи позволяют легко для сохранения нескольких значений в одном объекте, они часто используются как простой способ возврата нескольких значений из метода.</span><span class="sxs-lookup"><span data-stu-id="bd963-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="bd963-109">Требуется поддержка кортежей <xref:System.ValueTuple> типа.</span><span class="sxs-lookup"><span data-stu-id="bd963-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="bd963-110">Если .NET Framework 4.7 не установлена, необходимо добавить пакет NuGet `System.ValueTuple`, который можно найти в коллекции NuGet.</span><span class="sxs-lookup"><span data-stu-id="bd963-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="bd963-111">Без этого пакета может появиться ошибка компиляции аналогичную «Предопределенный тип «ValueTuple(Of,,,)» не определен и не импортирован.»</span><span class="sxs-lookup"><span data-stu-id="bd963-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="bd963-112">Создание экземпляра и использование кортежа</span><span class="sxs-lookup"><span data-stu-id="bd963-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="bd963-113">Создать кортеж, заключив его круглые скобки обмена мгновенными сообщениями значений с разделителями запятыми.</span><span class="sxs-lookup"><span data-stu-id="bd963-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="bd963-114">Затем каждое из этих значений становится полем элемента кортежа.</span><span class="sxs-lookup"><span data-stu-id="bd963-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="bd963-115">Например, следующий код определяет triple (или трех кортежей) с `Date` как его первое значение `String` как второй и `Boolean` как третьего.</span><span class="sxs-lookup"><span data-stu-id="bd963-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="bd963-116">По умолчанию имя каждого поля в кортеже включает строку `Item` вместе с положением от единицы поля в кортеже.</span><span class="sxs-lookup"><span data-stu-id="bd963-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="bd963-117">Для этого 3 кортежа `Date` поле является `Item1`, `String` поле является `Item2`и `Boolean` поле является `Item3`.</span><span class="sxs-lookup"><span data-stu-id="bd963-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="bd963-118">В следующем примере отображаются значения полей кортежа, созданный в предыдущей строке кода</span><span class="sxs-lookup"><span data-stu-id="bd963-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="bd963-119">Полей кортежа Visual Basic доступны для чтения и записи; После создания экземпляра кортежа его значения можно изменить.</span><span class="sxs-lookup"><span data-stu-id="bd963-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="bd963-120">В следующем примере два из трех полей кортежа, созданные в предыдущем примере изменяет и отображает результат.</span><span class="sxs-lookup"><span data-stu-id="bd963-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="bd963-121">Создание экземпляра и использование именованного кортежа</span><span class="sxs-lookup"><span data-stu-id="bd963-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="bd963-122">Вместо того чтобы использовать имена по умолчанию для полей кортежа, можно создать экземпляр *именованный кортеж* путем назначения собственные имена элементов кортежа.</span><span class="sxs-lookup"><span data-stu-id="bd963-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="bd963-123">Поля кортежа может осуществляться по именам назначенных *или* по их именам по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="bd963-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="bd963-124">В следующем примере создается же трем кортежам как ранее, за исключением того, что она явно указана первое поле `EventDate`, второй `Name`, а третий `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="bd963-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="bd963-125">Затем он отображает значения полей, изменяет их и повторно отображает значения полей.</span><span class="sxs-lookup"><span data-stu-id="bd963-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="bd963-126">Выводимые имена элементов кортежа</span><span class="sxs-lookup"><span data-stu-id="bd963-126">Inferred tuple element names</span></span>

<span data-ttu-id="bd963-127">Начиная с версии 15.3 Visual Basic, Visual Basic может вывести имена элементов кортежа; необходимо назначить их явно.</span><span class="sxs-lookup"><span data-stu-id="bd963-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="bd963-128">Кортеж выводимые имена полезны при инициализации кортежа из набора переменных, и вы хотите использовать имя элемента кортежа должен совпадать с именем переменной.</span><span class="sxs-lookup"><span data-stu-id="bd963-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="bd963-129">В следующем примере создается `stateInfo` кортеж, содержащий три явно именованные элементы, `state`, `stateName`, и `capital`.</span><span class="sxs-lookup"><span data-stu-id="bd963-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="bd963-130">Обратите внимание, что в именах элементов, оператора инициализации кортежа просто присваивает именованных элементов значения переменных с одинаковыми именами.</span><span class="sxs-lookup"><span data-stu-id="bd963-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="bd963-131">Так как элементы и переменные имеют тем же именем, компилятор Visual Basic может вывести имена полей, как показано в следующем примере.</span><span class="sxs-lookup"><span data-stu-id="bd963-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="bd963-132">Чтобы включить выводимые имена элементов кортежа, необходимо определить версии компилятора Visual Basic для использования в проекте Visual Basic (\*.vbproj) файла:</span><span class="sxs-lookup"><span data-stu-id="bd963-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="bd963-133">Номер версии может быть любой версии компилятора Visual Basic, начиная с версии 15.3.</span><span class="sxs-lookup"><span data-stu-id="bd963-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="bd963-134">Вместо того чтобы жестко программировать определенной версией компилятора, можно также указать «Latest» для параметра `LangVersion` для компиляции с самой последней версии компилятора Visual Basic, установленного на компьютере.</span><span class="sxs-lookup"><span data-stu-id="bd963-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="bd963-135">Дополнительные сведения см. в разделе [параметр версии языка Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="bd963-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="bd963-136">В некоторых случаях компилятор Visual Basic не может определить имя элемента кортежа из имя кандидата, и поле кортежа может ссылаться только с помощью его имя по умолчанию, например `Item1`, `Item2`и т. д. Сюда входит следующее.</span><span class="sxs-lookup"><span data-stu-id="bd963-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="bd963-137">Имя кандидата является таким же, как имя элемента кортежа, такие как `Item3`, `Rest`, или `ToString`.</span><span class="sxs-lookup"><span data-stu-id="bd963-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="bd963-138">Имя кандидата дублируется в кортеже.</span><span class="sxs-lookup"><span data-stu-id="bd963-138">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="bd963-139">Если не удается вывести имя поля, Visual Basic не создает ошибку компилятора, а также является исключение, возникающее во время выполнения.</span><span class="sxs-lookup"><span data-stu-id="bd963-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="bd963-140">Вместо этого поля кортежа должны иметь двухкомпонентные их предопределенные имена, такие как `Item1` и `Item2`.</span><span class="sxs-lookup"><span data-stu-id="bd963-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="bd963-141">Кортежи и структур</span><span class="sxs-lookup"><span data-stu-id="bd963-141">Tuples versus structures</span></span>

<span data-ttu-id="bd963-142">Кортеж Visual Basic — это тип значения, который является экземпляром любого **System.ValueTuple** универсальных типов.</span><span class="sxs-lookup"><span data-stu-id="bd963-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="bd963-143">Например `holiday` кортеж, определяемый в предыдущем примере является экземпляром класса <xref:System.ValueTuple%603> структуры.</span><span class="sxs-lookup"><span data-stu-id="bd963-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="bd963-144">Он должен быть упрощенный контейнер для данных.</span><span class="sxs-lookup"><span data-stu-id="bd963-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="bd963-145">Так как кортеж стремится упростить процесс для создания объекта с несколькими элементами данных, у него нет некоторых функций, которые могут иметь пользовательская структура.</span><span class="sxs-lookup"><span data-stu-id="bd963-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="bd963-146">Сюда входит следующее.</span><span class="sxs-lookup"><span data-stu-id="bd963-146">These include:</span></span>

- <span data-ttu-id="bd963-147">Пользовательские элементы.</span><span class="sxs-lookup"><span data-stu-id="bd963-147">Custom members.</span></span> <span data-ttu-id="bd963-148">Нельзя определить свои собственные свойства, методы или события для кортежа.</span><span class="sxs-lookup"><span data-stu-id="bd963-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="bd963-149">Проверка.</span><span class="sxs-lookup"><span data-stu-id="bd963-149">Validation.</span></span> <span data-ttu-id="bd963-150">Невозможно проверить данные, назначенных полей.</span><span class="sxs-lookup"><span data-stu-id="bd963-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="bd963-151">Неизменность.</span><span class="sxs-lookup"><span data-stu-id="bd963-151">Immutability.</span></span> <span data-ttu-id="bd963-152">Кортежи Visual Basic являются изменяемыми.</span><span class="sxs-lookup"><span data-stu-id="bd963-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="bd963-153">Напротив пользовательская структура позволяет контролировать, имеет ли экземпляр изменяемым или неизменяемым.</span><span class="sxs-lookup"><span data-stu-id="bd963-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="bd963-154">Если пользовательские элементы, свойства и проверка полей или неизменность важны, следует использовать Visual Basic [структуры](../../../language-reference/statements/structure-statement.md) инструкцию, чтобы определить тип пользовательское значение.</span><span class="sxs-lookup"><span data-stu-id="bd963-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="bd963-155">Кортеж Visual Basic наследовать члены его **ValueTuple** типа.</span><span class="sxs-lookup"><span data-stu-id="bd963-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="bd963-156">В дополнение к его поля к ним относятся следующие методы:</span><span class="sxs-lookup"><span data-stu-id="bd963-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="bd963-157">Член</span><span class="sxs-lookup"><span data-stu-id="bd963-157">Member</span></span> | <span data-ttu-id="bd963-158">Описание</span><span class="sxs-lookup"><span data-stu-id="bd963-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="bd963-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="bd963-159">CompareTo</span></span> | <span data-ttu-id="bd963-160">Сравнивает текущий кортеж для другой кортеж с одинаковое количество элементов.</span><span class="sxs-lookup"><span data-stu-id="bd963-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="bd963-161">Равно</span><span class="sxs-lookup"><span data-stu-id="bd963-161">Equals</span></span> | <span data-ttu-id="bd963-162">Определяет, равен ли текущий кортеж с другой кортеж или объектом.</span><span class="sxs-lookup"><span data-stu-id="bd963-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="bd963-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="bd963-163">GetHashCode</span></span> | <span data-ttu-id="bd963-164">Вычисляет хэш-код для текущего экземпляра.</span><span class="sxs-lookup"><span data-stu-id="bd963-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="bd963-165">ToString</span><span class="sxs-lookup"><span data-stu-id="bd963-165">ToString</span></span> | <span data-ttu-id="bd963-166">Возвращает строковое представление этого кортежа, который принимает форму `(Item1, Item2...)`, где `Item1` и `Item2` представляют значения полей кортежа.</span><span class="sxs-lookup"><span data-stu-id="bd963-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="bd963-167">Кроме того **ValueTuple** типы реализуют <xref:System.Collections.IStructuralComparable> и <xref:System.Collections.IStructuralEquatable> интерфейсы, которые позволяют определять компараторы клиента.</span><span class="sxs-lookup"><span data-stu-id="bd963-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="bd963-168">Назначение и кортежи</span><span class="sxs-lookup"><span data-stu-id="bd963-168">Assignment and tuples</span></span>

<span data-ttu-id="bd963-169">Visual Basic поддерживает назначения между типами кортежей с одинаковым количеством полей.</span><span class="sxs-lookup"><span data-stu-id="bd963-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="bd963-170">Можно преобразовать типы полей, если выполняется одно из следующих:</span><span class="sxs-lookup"><span data-stu-id="bd963-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="bd963-171">Исходные и целевые поля имеют тот же тип.</span><span class="sxs-lookup"><span data-stu-id="bd963-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="bd963-172">Расширяющее (или неявное) преобразования типа источника в целевой тип определен.</span><span class="sxs-lookup"><span data-stu-id="bd963-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="bd963-173">`Option Strict` является `On`, и определяется сужением (или неявного) преобразования типа источника в целевой тип.</span><span class="sxs-lookup"><span data-stu-id="bd963-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="bd963-174">Это преобразование может создавать исключения, если исходное значение находится вне диапазона типа целевого объекта.</span><span class="sxs-lookup"><span data-stu-id="bd963-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="bd963-175">Другие преобразования в контексте назначений не учитываются.</span><span class="sxs-lookup"><span data-stu-id="bd963-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="bd963-176">Рассмотрим возможные виды назначений между типами кортежей.</span><span class="sxs-lookup"><span data-stu-id="bd963-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="bd963-177">В приведенных ниже примерах можно использовать указанные переменные:</span><span class="sxs-lookup"><span data-stu-id="bd963-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="bd963-178">Первые две переменные, `unnamed` и `anonymous`, не имеют предоставленных полям семантические имена.</span><span class="sxs-lookup"><span data-stu-id="bd963-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="bd963-179">По умолчанию используются соответствующие имена полей `Item1` и `Item2`.</span><span class="sxs-lookup"><span data-stu-id="bd963-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="bd963-180">Последние две переменные, `named` и `differentName` иметь имена полей семантики.</span><span class="sxs-lookup"><span data-stu-id="bd963-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="bd963-181">Обратите внимание на то, что поля в этих двух кортежах называются по-разному.</span><span class="sxs-lookup"><span data-stu-id="bd963-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="bd963-182">Все четыре этих кортежа имеют одинаковое количество полей (называется «арность»), а типы этих полей идентичны.</span><span class="sxs-lookup"><span data-stu-id="bd963-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="bd963-183">Таким образом, все эти назначения работают:</span><span class="sxs-lookup"><span data-stu-id="bd963-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="bd963-184">Обратите внимание на то, что имена кортежей не назначаются.</span><span class="sxs-lookup"><span data-stu-id="bd963-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="bd963-185">Значения полей назначаются в соответствии с порядком полей в кортеже.</span><span class="sxs-lookup"><span data-stu-id="bd963-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="bd963-186">Наконец, обратите внимание, что можно назначить `named` кортеж `conversion` кортежа, несмотря на то что в первое поле `named` — `Integer`и в первое поле `conversion` является `Long`.</span><span class="sxs-lookup"><span data-stu-id="bd963-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="bd963-187">Это назначение завершается успешно, поскольку преобразование `Integer` для `Long` расширяющие преобразования.</span><span class="sxs-lookup"><span data-stu-id="bd963-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="bd963-188">Кортежи с различным количеством полей не назначаются:</span><span class="sxs-lookup"><span data-stu-id="bd963-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="bd963-189">Кортежи как возвращаемые значения методов</span><span class="sxs-lookup"><span data-stu-id="bd963-189">Tuples as method return values</span></span>

<span data-ttu-id="bd963-190">Метод может возвращать только одно значение.</span><span class="sxs-lookup"><span data-stu-id="bd963-190">A method can return only a single value.</span></span> <span data-ttu-id="bd963-191">Часто Однако требуется вызов метода для возврата нескольких значений.</span><span class="sxs-lookup"><span data-stu-id="bd963-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="bd963-192">Существует несколько способов обойти это ограничение:</span><span class="sxs-lookup"><span data-stu-id="bd963-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="bd963-193">Можно создать пользовательский класс или структуру, свойства или поля представляют значения, возвращаемые этим методом.</span><span class="sxs-lookup"><span data-stu-id="bd963-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="bd963-194">Таким образом — это трудоемкое решение; его необходимо определить пользовательский тип, единственным назначением которого является для извлечения значений из вызова метода.</span><span class="sxs-lookup"><span data-stu-id="bd963-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="bd963-195">Вам могут возвращать одиночное значение из метода и остальные значения путем их передачи по ссылке в метод.</span><span class="sxs-lookup"><span data-stu-id="bd963-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="bd963-196">Это включает в себя затраты на создание экземпляров переменной и риски, случайно перезаписать значение переменной, которая передается по ссылке.</span><span class="sxs-lookup"><span data-stu-id="bd963-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="bd963-197">Можно использовать кортеж, который предоставляет упрощенного решения для извлечения нескольких возвращаемых значений.</span><span class="sxs-lookup"><span data-stu-id="bd963-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="bd963-198">Например **TryParse** методы возврата .NET `Boolean` значение, указывающее, успешно ли выполнена операция синтаксического анализа.</span><span class="sxs-lookup"><span data-stu-id="bd963-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="bd963-199">Результат операции анализа возвращается в переменную, переданную по ссылке в метод.</span><span class="sxs-lookup"><span data-stu-id="bd963-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="bd963-200">Как правило, вызов метода синтаксического анализа, такие как <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="bd963-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="bd963-201">Мы может возвращать кортеж из операции анализа, если мы Подведем краткие вызов <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> в нашем собственном методе.</span><span class="sxs-lookup"><span data-stu-id="bd963-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="bd963-202">В следующем примере `NumericLibrary.ParseInteger` вызовы <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> метод и возвращает именованный кортеж с двумя элементами.</span><span class="sxs-lookup"><span data-stu-id="bd963-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="bd963-203">Затем можно вызвать метод с помощью следующего кода:</span><span class="sxs-lookup"><span data-stu-id="bd963-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="bd963-204">Visual Basic кортежи и кортежи, в .NET Framework</span><span class="sxs-lookup"><span data-stu-id="bd963-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="bd963-205">Кортеж Visual Basic — это экземпляр одного из **System.ValueTuple** универсальных типов, которые впервые появились в .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="bd963-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="bd963-206">.NET Framework также включает набор универсальных **System.Tuple** классы.</span><span class="sxs-lookup"><span data-stu-id="bd963-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="bd963-207">Эти классы, тем не менее, отличаются от кортежей Visual Basic и **System.ValueTuple** универсальных типов в несколькими способами:</span><span class="sxs-lookup"><span data-stu-id="bd963-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="bd963-208">Элементы **кортежа** классы являются свойства с именем `Item1`, `Item2`, и т. д.</span><span class="sxs-lookup"><span data-stu-id="bd963-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="bd963-209">В Visual Basic кортежей и **ValueTuple** типы, элементы кортежа: поля.</span><span class="sxs-lookup"><span data-stu-id="bd963-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="bd963-210">Элементы нельзя назначить значимые имена **кортежа** экземпляра или **ValueTuple** экземпляра.</span><span class="sxs-lookup"><span data-stu-id="bd963-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="bd963-211">Visual Basic позволяет присваивать имена, которые обмениваются данными значение поля.</span><span class="sxs-lookup"><span data-stu-id="bd963-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="bd963-212">Свойства **кортежа** экземпляра доступны только для чтения; кортежи неизменны.</span><span class="sxs-lookup"><span data-stu-id="bd963-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="bd963-213">В Visual Basic кортежей и **ValueTuple** типов, полей кортежа доступны для чтения и записи; кортежи являются изменяемыми.</span><span class="sxs-lookup"><span data-stu-id="bd963-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="bd963-214">Универсальный **кортежа** типы являются ссылочными типами.</span><span class="sxs-lookup"><span data-stu-id="bd963-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="bd963-215">Используя эти **кортежа** типы происходит распределение объектов.</span><span class="sxs-lookup"><span data-stu-id="bd963-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="bd963-216">В критических путях это может заметно влиять на производительность приложения.</span><span class="sxs-lookup"><span data-stu-id="bd963-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="bd963-217">Кортежи Visual Basic и **ValueTuple** типы являются типами значений.</span><span class="sxs-lookup"><span data-stu-id="bd963-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="bd963-218">Методы расширения в <xref:System.TupleExtensions> класс упрощают преобразование кортежей Visual Basic и .NET **кортежа** объектов.</span><span class="sxs-lookup"><span data-stu-id="bd963-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="bd963-219">**ToTuple** метод преобразует кортеж Visual Basic .NET **кортежа** объекта и **ToValueTuple** метод преобразует .NET **кортежа** Объект, в кортеж Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="bd963-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="bd963-220">Следующий пример создает кортеж, преобразует его в .NET **кортежа** объекта и преобразует его обратно в кортеж Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="bd963-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="bd963-221">Затем в примере сравнивается этого кортежа с исходной схемы, чтобы убедиться, что они равны.</span><span class="sxs-lookup"><span data-stu-id="bd963-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="bd963-222">См. также</span><span class="sxs-lookup"><span data-stu-id="bd963-222">See also</span></span>

- [<span data-ttu-id="bd963-223">Справочник по языку Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bd963-223">Visual Basic Language Reference</span></span>](index.md)
