---
title: Логические и побитовые операторы
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 55a246c0d56501a409ebbc7d0d0aa39ae9fa1770
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/22/2019
ms.locfileid: "74343590"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="1cf93-102">Логические и побитовые операторы в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="1cf93-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="1cf93-103">Логические операторы сравнивают `Boolean` выражения и возвращают `Boolean` результат.</span><span class="sxs-lookup"><span data-stu-id="1cf93-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="1cf93-104">Операторы `And`, `Or`, `AndAlso`, `OrElse`и `Xor` являются *двоичными* , так как они принимают два операнда, а оператор `Not` — *унарный* , так как он принимает один операнд.</span><span class="sxs-lookup"><span data-stu-id="1cf93-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="1cf93-105">Некоторые из этих операторов также могут выполнять побитовые логические операции над целочисленными значениями.</span><span class="sxs-lookup"><span data-stu-id="1cf93-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="1cf93-106">Унарный логический оператор</span><span class="sxs-lookup"><span data-stu-id="1cf93-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="1cf93-107">[Оператор not](../../../../visual-basic/language-reference/operators/not-operator.md) выполняет логическое *отрицание* в выражении `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="1cf93-108">Он возвращает логическое противоположное значение операнда.</span><span class="sxs-lookup"><span data-stu-id="1cf93-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="1cf93-109">Если выражение принимает значение `True`, `Not` возвращает `False`; Если выражение принимает значение `False`, `Not` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="1cf93-110">Это показано в следующем примере.</span><span class="sxs-lookup"><span data-stu-id="1cf93-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="1cf93-111">Бинарные логические операторы</span><span class="sxs-lookup"><span data-stu-id="1cf93-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="1cf93-112">[Оператор and](../../../../visual-basic/language-reference/operators/and-operator.md) выполняет логическое *умножение* двух выражений `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="1cf93-113">Если оба выражения оцениваются как `True`, `And` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="1cf93-114">Если хотя бы одно из выражений имеет значение `False`, `And` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="1cf93-115">[Оператор OR](../../../../visual-basic/language-reference/operators/or-operator.md) выполняет логическое *сложение* или *включение* в двух выражениях `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="1cf93-116">Если любое из выражений принимает значение `True`или оба значения имеют значение `True`, то `Or` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="1cf93-117">Если ни одно из выражений не имеет значение `True`, `Or` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="1cf93-118">[Оператор XOR](../../../../visual-basic/language-reference/operators/xor-operator.md) выполняет логическое *исключение* в двух выражениях `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="1cf93-119">Если только одно выражение имеет значение `True`, но не оба, `Xor` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="1cf93-120">Если оба выражения оцениваются как `True` или оба имеют `False`, `Xor` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="1cf93-121">В следующем примере показаны операторы `And`, `Or`и `Xor`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="1cf93-122">Сокращенное вычисление логических операций</span><span class="sxs-lookup"><span data-stu-id="1cf93-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="1cf93-123">[Оператор AndAlso](../../../../visual-basic/language-reference/operators/andalso-operator.md) очень похож на оператор `And` в том, что он также выполняет логическое умножение двух `Boolean`ных выражений.</span><span class="sxs-lookup"><span data-stu-id="1cf93-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="1cf93-124">Основное различие между ними состоит в том, что `AndAlso` приводит к *сокращению* поведения.</span><span class="sxs-lookup"><span data-stu-id="1cf93-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="1cf93-125">Если первое выражение в `AndAlso` выражении имеет значение `False`, второе выражение не вычисляется, так как оно не может изменить окончательный результат, а `AndAlso` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="1cf93-126">Аналогичным образом [оператор OrElse](../../../../visual-basic/language-reference/operators/orelse-operator.md) выполняет сокращенное логическое сложение двух `Boolean` выражений.</span><span class="sxs-lookup"><span data-stu-id="1cf93-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="1cf93-127">Если первое выражение в `OrElse` выражении имеет значение `True`, второе выражение не вычисляется, так как оно не может изменить окончательный результат, а `OrElse` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="1cf93-128">Сокращенное вычисление компромиссов</span><span class="sxs-lookup"><span data-stu-id="1cf93-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="1cf93-129">Сокращенное вычисление может повысить производительность, не вычисляя выражение, которое не может изменить результат логической операции.</span><span class="sxs-lookup"><span data-stu-id="1cf93-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="1cf93-130">Однако если это выражение выполняет дополнительные действия, сокращенное вычисление пропускает эти действия.</span><span class="sxs-lookup"><span data-stu-id="1cf93-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="1cf93-131">Например, если выражение содержит вызов `Function` процедуры, эта процедура не вызывается, если выражение сокращено, и любой дополнительный код, содержащийся в `Function`, не выполняется.</span><span class="sxs-lookup"><span data-stu-id="1cf93-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="1cf93-132">Таким образом, функция может выполняться только иногда и может быть некорректно проверена.</span><span class="sxs-lookup"><span data-stu-id="1cf93-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="1cf93-133">Или логика программы может зависеть от кода в `Function`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="1cf93-134">В следующем примере показана разница между `And`, `Or`и их сокращенными аналогами.</span><span class="sxs-lookup"><span data-stu-id="1cf93-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="1cf93-135">Обратите внимание, что в предыдущем примере некоторый важный код в `checkIfValid()` не выполняется при сокращенном вызове.</span><span class="sxs-lookup"><span data-stu-id="1cf93-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="1cf93-136">Первая инструкция `If` вызывает `checkIfValid()` несмотря на то, что `12 > 45` возвращает `False`, так как `And` не является сокращенной схемой.</span><span class="sxs-lookup"><span data-stu-id="1cf93-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="1cf93-137">Вторая инструкция `If` не вызывает `checkIfValid()`, так как когда `12 > 45` возвращает `False`, `AndAlso` сокращенное значение второго выражения.</span><span class="sxs-lookup"><span data-stu-id="1cf93-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="1cf93-138">Третья `If`ная инструкция вызывает `checkIfValid()` несмотря на то, что `12 < 45` возвращает `True`, так как `Or` не является сокращенной схемой.</span><span class="sxs-lookup"><span data-stu-id="1cf93-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="1cf93-139">Четвертый оператор `If` не вызывает `checkIfValid()`, так как когда `12 < 45` возвращает `True`, `OrElse` сокращенное значение второго выражения.</span><span class="sxs-lookup"><span data-stu-id="1cf93-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="1cf93-140">Битовые операции</span><span class="sxs-lookup"><span data-stu-id="1cf93-140">Bitwise Operations</span></span>  
 <span data-ttu-id="1cf93-141">Побитовые операции оценивают два целочисленных значения в форме binary (основание 2).</span><span class="sxs-lookup"><span data-stu-id="1cf93-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="1cf93-142">Они сравнивают биты в соответствующих позициях и затем присваивают значения на основе сравнения.</span><span class="sxs-lookup"><span data-stu-id="1cf93-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="1cf93-143">В следующем примере показан оператор `And`.</span><span class="sxs-lookup"><span data-stu-id="1cf93-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="1cf93-144">В предыдущем примере значение `x` задается равным 1.</span><span class="sxs-lookup"><span data-stu-id="1cf93-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="1cf93-145">Это происходит по следующим причинам.</span><span class="sxs-lookup"><span data-stu-id="1cf93-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="1cf93-146">Значения рассматриваются как двоичные:</span><span class="sxs-lookup"><span data-stu-id="1cf93-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="1cf93-147">3 в двоичном формате = 011</span><span class="sxs-lookup"><span data-stu-id="1cf93-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="1cf93-148">5 в двоичном формате = 101</span><span class="sxs-lookup"><span data-stu-id="1cf93-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="1cf93-149">Оператор `And` сравнивает двоичные представления, по одной двоичной позиции (бит) за раз.</span><span class="sxs-lookup"><span data-stu-id="1cf93-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="1cf93-150">Если оба бита в заданной позиции равны 1, то в результате в эту точку помещается 1.</span><span class="sxs-lookup"><span data-stu-id="1cf93-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="1cf93-151">Если любой из битов равен 0, то в результат помещается 0.</span><span class="sxs-lookup"><span data-stu-id="1cf93-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="1cf93-152">В предыдущем примере это работает следующим образом:</span><span class="sxs-lookup"><span data-stu-id="1cf93-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="1cf93-153">011 (3 в двоичной форме)</span><span class="sxs-lookup"><span data-stu-id="1cf93-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="1cf93-154">101 (5 в двоичной форме)</span><span class="sxs-lookup"><span data-stu-id="1cf93-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="1cf93-155">001 (результат в двоичной форме)</span><span class="sxs-lookup"><span data-stu-id="1cf93-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="1cf93-156">Результат считается десятичным.</span><span class="sxs-lookup"><span data-stu-id="1cf93-156">The result is treated as decimal.</span></span> <span data-ttu-id="1cf93-157">Значение 001 является двоичным представлением 1, поэтому `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="1cf93-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="1cf93-158">Побитовая операция `Or` похожа на, за исключением того, что значение 1 назначается биту результата, если один или оба сравниваемых бита равны 1.</span><span class="sxs-lookup"><span data-stu-id="1cf93-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="1cf93-159">`Xor` присваивает значение 1 результирующему биту, если ровно один из сравниваемых битов (не оба) равен 1.</span><span class="sxs-lookup"><span data-stu-id="1cf93-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="1cf93-160">`Not` принимает один операнд и инвертирует все биты, включая бит знака, и присваивает это значение результату.</span><span class="sxs-lookup"><span data-stu-id="1cf93-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="1cf93-161">Это означает, что для положительных чисел со знаком `Not` всегда возвращает отрицательное значение, а для отрицательных чисел `Not` всегда возвращает положительное или нулевое значение.</span><span class="sxs-lookup"><span data-stu-id="1cf93-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="1cf93-162">Операторы `AndAlso` и `OrElse` не поддерживают побитовые операции.</span><span class="sxs-lookup"><span data-stu-id="1cf93-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="1cf93-163">Битовые операции могут выполняться только с целыми типами.</span><span class="sxs-lookup"><span data-stu-id="1cf93-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="1cf93-164">Перед выполнением побитовой операции значения с плавающей запятой необходимо преобразовать в целочисленные типы.</span><span class="sxs-lookup"><span data-stu-id="1cf93-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="1cf93-165">См. также</span><span class="sxs-lookup"><span data-stu-id="1cf93-165">See also</span></span>

- [<span data-ttu-id="1cf93-166">Логические и битовые операторы (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="1cf93-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="1cf93-167">Логические выражения</span><span class="sxs-lookup"><span data-stu-id="1cf93-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="1cf93-168">Арифметические операторы в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="1cf93-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="1cf93-169">Операторы сравнения в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="1cf93-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="1cf93-170">Операторы объединения в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="1cf93-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="1cf93-171">Эффективное сочетание операторов</span><span class="sxs-lookup"><span data-stu-id="1cf93-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
