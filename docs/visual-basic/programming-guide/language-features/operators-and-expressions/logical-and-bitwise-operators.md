---
title: Логические и побитовые операторы в Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: ac47b6d7fa4861d18646a23f442caccc4062852f
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/18/2019
ms.locfileid: "58819311"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="a0422-102">Логические и побитовые операторы в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a0422-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="a0422-103">Логические операторы сравнения `Boolean` выражения и возвращают `Boolean` результат.</span><span class="sxs-lookup"><span data-stu-id="a0422-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="a0422-104">`And`, `Or`, `AndAlso`, `OrElse`, И `Xor` операторы *двоичных* так, как они принимают два операнда, тогда как `Not` оператор *унарный* так как он принимает один операнд.</span><span class="sxs-lookup"><span data-stu-id="a0422-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="a0422-105">Некоторые из этих операторов можно также выполнять побитовые логические операции над целыми значениями.</span><span class="sxs-lookup"><span data-stu-id="a0422-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="a0422-106">Унарный логический оператор</span><span class="sxs-lookup"><span data-stu-id="a0422-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="a0422-107">[Оператор Not](../../../../visual-basic/language-reference/operators/not-operator.md) выполняет логические *отрицания* на `Boolean` выражение.</span><span class="sxs-lookup"><span data-stu-id="a0422-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="a0422-108">Он возвращает логическое отрицание операнда.</span><span class="sxs-lookup"><span data-stu-id="a0422-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="a0422-109">Если выражение, результатом которого является `True`, затем `Not` возвращает `False`; Если выражение, результатом которого является `False`, затем `Not` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="a0422-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="a0422-110">Это показано в следующем примере.</span><span class="sxs-lookup"><span data-stu-id="a0422-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="a0422-111">Двоичный логические операторы</span><span class="sxs-lookup"><span data-stu-id="a0422-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="a0422-112">[И оператором](../../../../visual-basic/language-reference/operators/and-operator.md) выполняет логические *совместно* двух `Boolean` выражения.</span><span class="sxs-lookup"><span data-stu-id="a0422-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="a0422-113">Если оба выражения имеют значение `True`, затем `And` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="a0422-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="a0422-114">Если по крайней мере одно из выражений имеет значение `False`, затем `And` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="a0422-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="a0422-115">[Или оператор](../../../../visual-basic/language-reference/operators/or-operator.md) выполняет логические *дизъюнкция* или *включения* двух `Boolean` выражения.</span><span class="sxs-lookup"><span data-stu-id="a0422-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="a0422-116">Если любое из выражений, результатом которого является `True`, или оба `True`, затем `Or` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="a0422-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="a0422-117">Если ни одно из выражений, результатом которого является `True`, `Or` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="a0422-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="a0422-118">[Оператор Xor](../../../../visual-basic/language-reference/operators/xor-operator.md) выполняет логические *исключения* двух `Boolean` выражения.</span><span class="sxs-lookup"><span data-stu-id="a0422-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="a0422-119">Если только одно выражение, результатом которого является `True`, но не оба `Xor` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="a0422-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="a0422-120">Если оба выражения имеют значение `True` или оба `False`, `Xor` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="a0422-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="a0422-121">В следующем примере показано `And`, `Or`, и `Xor` операторы.</span><span class="sxs-lookup"><span data-stu-id="a0422-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="a0422-122">Логические операторы сокращенного вычисления</span><span class="sxs-lookup"><span data-stu-id="a0422-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="a0422-123">[AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) очень похожа на `And` оператор, в том, что он также выполняет логическое умножение двух `Boolean` выражения.</span><span class="sxs-lookup"><span data-stu-id="a0422-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="a0422-124">Это ключевое различие между этими двумя `AndAlso` характеризуется *сокращенного вычисления* поведение.</span><span class="sxs-lookup"><span data-stu-id="a0422-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="a0422-125">Если первое выражение в `AndAlso` выражение, результатом которого является `False`, то второе выражение не вычисляется, так как его невозможно изменить конечный результат и `AndAlso` возвращает `False`.</span><span class="sxs-lookup"><span data-stu-id="a0422-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="a0422-126">Аналогичным образом [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) выполняет сокращенное вычисление логического сложения двух `Boolean` выражения.</span><span class="sxs-lookup"><span data-stu-id="a0422-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="a0422-127">Если первое выражение в `OrElse` выражение, результатом которого является `True`, то второе выражение не вычисляется, так как его невозможно изменить конечный результат и `OrElse` возвращает `True`.</span><span class="sxs-lookup"><span data-stu-id="a0422-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="a0422-128">Сокращенных</span><span class="sxs-lookup"><span data-stu-id="a0422-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="a0422-129">Сокращенное вычисление можно повысить производительность, не вычисления выражений, которые невозможно изменить результат логической операции.</span><span class="sxs-lookup"><span data-stu-id="a0422-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="a0422-130">Тем не менее если выражение выполняет дополнительные действия, сокращенного вычисления пропускает эти действия.</span><span class="sxs-lookup"><span data-stu-id="a0422-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="a0422-131">Например, если выражение включает вызов `Function` процедуры, что процедура не вызывается, если выражение является выполнение этого кода отключено, и любой дополнительный код, содержащийся в `Function` не выполняется.</span><span class="sxs-lookup"><span data-stu-id="a0422-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="a0422-132">Таким образом функция может выполняться лишь время от времени и не может быть проверен должным образом.</span><span class="sxs-lookup"><span data-stu-id="a0422-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="a0422-133">Или логику программы могут опираться на код в `Function`.</span><span class="sxs-lookup"><span data-stu-id="a0422-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="a0422-134">В следующем примере показано различие между `And`, `Or`и их сокращенными аналогами.</span><span class="sxs-lookup"><span data-stu-id="a0422-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="a0422-135">В предыдущем примере обратите внимание, что некоторые важные коды внутри `checkIfValid()` не выполняется при сокращенном вызов.</span><span class="sxs-lookup"><span data-stu-id="a0422-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="a0422-136">Первый `If` инструкция вызывает `checkIfValid()` несмотря на то что `12 > 45` возвращает `False`, так как `And` сокращенный.</span><span class="sxs-lookup"><span data-stu-id="a0422-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="a0422-137">Второй `If` инструкция не вызывает `checkIfValid()`, так как при `12 > 45` возвращает `False`, `AndAlso` игнорирует второе выражение.</span><span class="sxs-lookup"><span data-stu-id="a0422-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="a0422-138">Третий `If` инструкция вызывает `checkIfValid()` несмотря на то что `12 < 45` возвращает `True`, так как `Or` сокращенный.</span><span class="sxs-lookup"><span data-stu-id="a0422-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="a0422-139">Четвертый `If` инструкция не вызывает `checkIfValid()`, так как при `12 < 45` возвращает `True`, `OrElse` игнорирует второе выражение.</span><span class="sxs-lookup"><span data-stu-id="a0422-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="a0422-140">Поразрядные операции</span><span class="sxs-lookup"><span data-stu-id="a0422-140">Bitwise Operations</span></span>  
 <span data-ttu-id="a0422-141">Побитовые операции оценки двух целочисленных значений в виде двоичного файла (по основанию 2).</span><span class="sxs-lookup"><span data-stu-id="a0422-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="a0422-142">Они сравнения биты в соответствующих позициях, а затем назначьте на основе сравнения значений.</span><span class="sxs-lookup"><span data-stu-id="a0422-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="a0422-143">В следующем примере показано `And` оператор.</span><span class="sxs-lookup"><span data-stu-id="a0422-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="a0422-144">Предыдущий пример задает значение `x` 1.</span><span class="sxs-lookup"><span data-stu-id="a0422-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="a0422-145">Это происходит по следующим причинам:</span><span class="sxs-lookup"><span data-stu-id="a0422-145">This happens for the following reasons:</span></span>  
  
-   <span data-ttu-id="a0422-146">Значения обрабатываются как двоичные:</span><span class="sxs-lookup"><span data-stu-id="a0422-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="a0422-147">3 в двоичной форме = 011</span><span class="sxs-lookup"><span data-stu-id="a0422-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="a0422-148">5 в двоичной форме = 101</span><span class="sxs-lookup"><span data-stu-id="a0422-148">5 in binary form = 101</span></span>  
  
-   <span data-ttu-id="a0422-149">`And` Оператор сравнивает двоичное представление, один двоичный позиции (бит) за раз.</span><span class="sxs-lookup"><span data-stu-id="a0422-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="a0422-150">Если оба бита в заданной позиции равны 1, 1 помещается в этой позиции, в результате.</span><span class="sxs-lookup"><span data-stu-id="a0422-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="a0422-151">Если оба бита равны 0, 0 помещается в этой позиции, в результате.</span><span class="sxs-lookup"><span data-stu-id="a0422-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="a0422-152">В приведенном выше примере это происходит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="a0422-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="a0422-153">011 (3 в двоичной форме)</span><span class="sxs-lookup"><span data-stu-id="a0422-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="a0422-154">101 (5 в двоичной форме)</span><span class="sxs-lookup"><span data-stu-id="a0422-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="a0422-155">001 (результат в двоичной форме)</span><span class="sxs-lookup"><span data-stu-id="a0422-155">001 (The result, in binary form)</span></span>  
  
-   <span data-ttu-id="a0422-156">Результат рассматривается как десятичное число.</span><span class="sxs-lookup"><span data-stu-id="a0422-156">The result is treated as decimal.</span></span> <span data-ttu-id="a0422-157">Значение 001 является двоичным представлением 1, поэтому `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="a0422-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="a0422-158">Побитовое `Or` операции аналогичен предыдущему, за исключением того, что бит результата присваивается значение 1, если оба сравниваемых бита равны 1.</span><span class="sxs-lookup"><span data-stu-id="a0422-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="a0422-159">`Xor` назначает 1 бит результата, если только один из сравниваемых битов (а не оба) равен 1.</span><span class="sxs-lookup"><span data-stu-id="a0422-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="a0422-160">`Not` принимает один операнд и инвертирует все биты, включая бит знака и присваивает это значение к результату.</span><span class="sxs-lookup"><span data-stu-id="a0422-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="a0422-161">Это означает, что для положительных чисел со знаком, `Not` всегда возвращает отрицательное значение, а также для отрицательных чисел `Not` всегда возвращает положительное или нулевое значение.</span><span class="sxs-lookup"><span data-stu-id="a0422-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="a0422-162">`AndAlso` И `OrElse` операторы не поддерживают побитовых операций.</span><span class="sxs-lookup"><span data-stu-id="a0422-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="a0422-163">Побитовые операции могут выполняться только над целыми типами.</span><span class="sxs-lookup"><span data-stu-id="a0422-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="a0422-164">Значения с плавающей запятой должны преобразовываться в целочисленные типы, прежде чем будет продолжена побитовой операции.</span><span class="sxs-lookup"><span data-stu-id="a0422-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a0422-165">См. также</span><span class="sxs-lookup"><span data-stu-id="a0422-165">See also</span></span>

- [<span data-ttu-id="a0422-166">Логические (побитовые) операторы (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="a0422-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="a0422-167">Логические выражения</span><span class="sxs-lookup"><span data-stu-id="a0422-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="a0422-168">Арифметические операторы в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a0422-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="a0422-169">Операторы сравнения в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a0422-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="a0422-170">Операторы объединения в Visual Basic</span><span class="sxs-lookup"><span data-stu-id="a0422-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="a0422-171">Эффективное сочетание операторов</span><span class="sxs-lookup"><span data-stu-id="a0422-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
