---
title: Устранение неполадок взаимодействия
ms.date: 07/20/2015
helpviewer_keywords:
- interop, deploying assemblies
- assemblies [Visual Basic]
- interop, installing assemblies that share components
- COM objects, troubleshooting
- interop, sharing components
- troubleshooting interoperability [Visual Basic]
- interoperability, troubleshooting
- COM interop [Visual Basic], troubleshooting
- assemblies [Visual Basic], deploying
- troubleshooting Visual Basic, interoperability
- interop assemblies
- interoperability, sharing components
- shared components, using with assemblies
ms.assetid: b324cc1e-b03c-4f39-aea6-6a6d5bfd0e37
ms.openlocfilehash: 344c180cf0b9426898e17b45db768a337fd45beb
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/22/2019
ms.locfileid: "74338683"
---
# <a name="troubleshooting-interoperability-visual-basic"></a><span data-ttu-id="f145e-102">Устранение неполадок взаимодействия (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="f145e-102">Troubleshooting Interoperability (Visual Basic)</span></span>
<span data-ttu-id="f145e-103">При взаимодействии между COM и управляемым кодом .NET Framework может возникнуть одна или несколько из следующих распространенных проблем.</span><span class="sxs-lookup"><span data-stu-id="f145e-103">When you interoperate between COM and the managed code of the .NET Framework, you may encounter one or more of the following common issues.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor1"></a><span data-ttu-id="f145e-104">Маршалинг взаимодействия</span><span class="sxs-lookup"><span data-stu-id="f145e-104">Interop Marshaling</span></span>  
 <span data-ttu-id="f145e-105">Иногда может потребоваться использовать типы данных, которые не являются частью .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="f145e-105">At times, you may have to use data types that are not part of the .NET Framework.</span></span> <span data-ttu-id="f145e-106">Сборки взаимодействия обрабатывают большую часть работы для COM-объектов, но может потребоваться управлять типами данных, которые используются, когда управляемые объекты доступны для COM.</span><span class="sxs-lookup"><span data-stu-id="f145e-106">Interop assemblies handle most of the work for COM objects, but you may have to control the data types that are used when managed objects are exposed to COM.</span></span> <span data-ttu-id="f145e-107">Например, структуры в библиотеках классов должны указывать `BStr` неуправляемый тип в строках, отправляемых в COM-объекты, созданные с помощью Visual Basic 6,0 и более ранних версий.</span><span class="sxs-lookup"><span data-stu-id="f145e-107">For example, structures in class libraries must specify the `BStr` unmanaged type on strings sent to COM objects created by Visual Basic 6.0 and earlier versions.</span></span> <span data-ttu-id="f145e-108">В таких случаях можно использовать атрибут <xref:System.Runtime.InteropServices.MarshalAsAttribute>, чтобы управляемые типы предоставлялись как неуправляемые типы.</span><span class="sxs-lookup"><span data-stu-id="f145e-108">In such cases, you can use the <xref:System.Runtime.InteropServices.MarshalAsAttribute> attribute to cause managed types to be exposed as unmanaged types.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor2"></a><span data-ttu-id="f145e-109">Экспорт строк фиксированной длины в неуправляемый код</span><span class="sxs-lookup"><span data-stu-id="f145e-109">Exporting Fixed-Length Strings to Unmanaged Code</span></span>  
 <span data-ttu-id="f145e-110">В Visual Basic 6,0 и более ранних версиях строки экспортируются в COM-объекты в виде последовательности байтов без завершающего символа null.</span><span class="sxs-lookup"><span data-stu-id="f145e-110">In Visual Basic 6.0 and earlier versions, strings are exported to COM objects as sequences of bytes without a null termination character.</span></span> <span data-ttu-id="f145e-111">Для совместимости с другими языками Visual Basic .NET содержит символ завершения при экспорте строк.</span><span class="sxs-lookup"><span data-stu-id="f145e-111">For compatibility with other languages, Visual Basic .NET includes a termination character when exporting strings.</span></span> <span data-ttu-id="f145e-112">Лучший способ устранения этой несовместимости заключается в экспорте строк, в которых отсутствует завершающий символ, в виде массивов `Byte` или `Char`.</span><span class="sxs-lookup"><span data-stu-id="f145e-112">The best way to address this incompatibility is to export strings that lack the termination character as arrays of `Byte` or `Char`.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor3"></a><span data-ttu-id="f145e-113">Экспорт иерархий наследования</span><span class="sxs-lookup"><span data-stu-id="f145e-113">Exporting Inheritance Hierarchies</span></span>  
 <span data-ttu-id="f145e-114">Иерархии управляемых классов выравниваются при предоставлении в виде COM-объектов.</span><span class="sxs-lookup"><span data-stu-id="f145e-114">Managed class hierarchies flatten out when exposed as COM objects.</span></span> <span data-ttu-id="f145e-115">Например, если определить базовый класс с элементом, а затем наследовать базовый класс в производном классе, предоставляемом в виде COM-объекта, клиенты, использующие производный класс в COM-объекте, не смогут использовать наследуемые члены.</span><span class="sxs-lookup"><span data-stu-id="f145e-115">For example, if you define a base class with a member, and then inherit the base class in a derived class that is exposed as a COM object, clients that use the derived class in the COM object will not be able to use the inherited members.</span></span> <span data-ttu-id="f145e-116">Доступ к членам базового класса возможен из COM-объектов только в качестве экземпляров базового класса, а затем только в том случае, если базовый класс также создается как COM-объект.</span><span class="sxs-lookup"><span data-stu-id="f145e-116">Base class members can be accessed from COM objects only as instances of a base class, and then only if the base class is also created as a COM object.</span></span>  
  
## <a name="overloaded-methods"></a><span data-ttu-id="f145e-117">Перегруженные методы</span><span class="sxs-lookup"><span data-stu-id="f145e-117">Overloaded Methods</span></span>  
 <span data-ttu-id="f145e-118">Хотя можно создавать перегруженные методы с Visual Basic, они не поддерживаются COM.</span><span class="sxs-lookup"><span data-stu-id="f145e-118">Although you can create overloaded methods with Visual Basic, they are not supported by COM.</span></span> <span data-ttu-id="f145e-119">Если класс, содержащий перегруженные методы, предоставляется в виде COM-объекта, для перегруженных методов создаются новые имена методов.</span><span class="sxs-lookup"><span data-stu-id="f145e-119">When a class that contains overloaded methods is exposed as a COM object, new method names are generated for the overloaded methods.</span></span>  
  
 <span data-ttu-id="f145e-120">Например, рассмотрим класс с двумя перегрузками метода `Synch`.</span><span class="sxs-lookup"><span data-stu-id="f145e-120">For example, consider a class that has two overloads of the `Synch` method.</span></span> <span data-ttu-id="f145e-121">Если класс представлен в виде COM-объекта, то новые имена создаваемых методов могут быть `Synch` и `Synch_2`.</span><span class="sxs-lookup"><span data-stu-id="f145e-121">When the class is exposed as a COM object, the new generated method names could be `Synch` and `Synch_2`.</span></span>  
  
 <span data-ttu-id="f145e-122">Переименование может привести к двум проблемам для потребителей COM-объекта.</span><span class="sxs-lookup"><span data-stu-id="f145e-122">The renaming can cause two problems for consumers of the COM object.</span></span>  
  
1. <span data-ttu-id="f145e-123">Клиенты могут не ожидать имен создаваемых методов.</span><span class="sxs-lookup"><span data-stu-id="f145e-123">Clients might not expect the generated method names.</span></span>  
  
2. <span data-ttu-id="f145e-124">Созданные имена методов в классе, доступном в виде COM-объекта, могут изменяться при добавлении новых перегрузок в класс или его базовый класс.</span><span class="sxs-lookup"><span data-stu-id="f145e-124">The generated method names in the class exposed as a COM object can change when new overloads are added to the class or its base class.</span></span> <span data-ttu-id="f145e-125">Это может вызвать проблемы с версиями.</span><span class="sxs-lookup"><span data-stu-id="f145e-125">This can cause versioning problems.</span></span>  
  
 <span data-ttu-id="f145e-126">Чтобы решить обе проблемы, присвойте каждому методу уникальное имя, а не использование перегрузки при разработке объектов, которые будут предоставлены в виде COM-объектов.</span><span class="sxs-lookup"><span data-stu-id="f145e-126">To solve both problems, give each method a unique name, instead of using overloading, when you develop objects that will be exposed as COM objects.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor4"></a><span data-ttu-id="f145e-127">Использование COM-объектов с помощью сборок взаимодействия</span><span class="sxs-lookup"><span data-stu-id="f145e-127">Use of COM Objects Through Interop Assemblies</span></span>  
 <span data-ttu-id="f145e-128">Сборки взаимодействия используются почти так же, как если бы они находились под управляемым кодом для объектов COM, которые они представляют.</span><span class="sxs-lookup"><span data-stu-id="f145e-128">You use interop assemblies almost as if they are managed code replacements for the COM objects they represent.</span></span> <span data-ttu-id="f145e-129">Однако поскольку они являются оболочками и не являются реальными COM-объектами, между использованием сборок взаимодействия и стандартных сборок существуют некоторые различия.</span><span class="sxs-lookup"><span data-stu-id="f145e-129">However, because they are wrappers and not actual COM objects, there are some differences between using interop assemblies and standard assemblies.</span></span> <span data-ttu-id="f145e-130">Эти области различий включают раскрытие классов и типы данных для параметров и возвращаемых значений.</span><span class="sxs-lookup"><span data-stu-id="f145e-130">These areas of difference include the exposure of classes, and data types for parameters and return values.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor5"></a><span data-ttu-id="f145e-131">Классы, предоставляемые как интерфейсы и классы</span><span class="sxs-lookup"><span data-stu-id="f145e-131">Classes Exposed as Both Interfaces and Classes</span></span>  
 <span data-ttu-id="f145e-132">В отличие от классов в стандартных сборках, классы COM доступны в сборках взаимодействия как интерфейс и класс, представляющий COM-класс.</span><span class="sxs-lookup"><span data-stu-id="f145e-132">Unlike classes in standard assemblies, COM classes are exposed in interop assemblies as both an interface and a class that represents the COM class.</span></span> <span data-ttu-id="f145e-133">Имя интерфейса идентично имени класса COM.</span><span class="sxs-lookup"><span data-stu-id="f145e-133">The interface's name is identical to that of the COM class.</span></span> <span data-ttu-id="f145e-134">Имя класса взаимодействия совпадает с именем исходного класса COM, но с добавленным словом «class».</span><span class="sxs-lookup"><span data-stu-id="f145e-134">The name of the interop class is the same as that of the original COM class, but with the word "Class" appended.</span></span> <span data-ttu-id="f145e-135">Например, предположим, что имеется проект со ссылкой на сборку взаимодействия для COM-объекта.</span><span class="sxs-lookup"><span data-stu-id="f145e-135">For example, suppose you have a project with a reference to an interop assembly for a COM object.</span></span> <span data-ttu-id="f145e-136">Если класс COM называется `MyComClass`, IntelliSense и обозреватель объектов отображают интерфейс с именем `MyComClass` и класс с именем `MyComClassClass`.</span><span class="sxs-lookup"><span data-stu-id="f145e-136">If the COM class is named `MyComClass`, IntelliSense and the Object Browser show an interface named `MyComClass` and a class named `MyComClassClass`.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor6"></a><span data-ttu-id="f145e-137">Создание экземпляров класса .NET Framework</span><span class="sxs-lookup"><span data-stu-id="f145e-137">Creating Instances of a .NET Framework Class</span></span>  
 <span data-ttu-id="f145e-138">Как правило, экземпляр класса .NET Framework создается с помощью инструкции `New` с именем класса.</span><span class="sxs-lookup"><span data-stu-id="f145e-138">Generally, you create an instance of a .NET Framework class using the `New` statement with a class name.</span></span> <span data-ttu-id="f145e-139">Наличие класса COM, представленного сборкой взаимодействия, — это один из вариантов, в котором можно использовать оператор `New` с интерфейсом.</span><span class="sxs-lookup"><span data-stu-id="f145e-139">Having a COM class represented by an interop assembly is the one case in which you can use the `New` statement with an interface.</span></span> <span data-ttu-id="f145e-140">Если класс COM не используется с оператором `Inherits`, можно использовать интерфейс точно так же, как и класс.</span><span class="sxs-lookup"><span data-stu-id="f145e-140">Unless you are using the COM class with an `Inherits` statement, you can use the interface just as you would a class.</span></span> <span data-ttu-id="f145e-141">В следующем коде показано, как создать объект `Command` в проекте, который содержит ссылку на COM-объект библиотеки Microsoft объекты данных ActiveX 2,8:</span><span class="sxs-lookup"><span data-stu-id="f145e-141">The following code demonstrates how to create a `Command` object in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object:</span></span>  
  
 [!code-vb[VbVbalrInterop#20](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#20)]  
  
 <span data-ttu-id="f145e-142">Однако при использовании класса COM в качестве основы для производного класса необходимо использовать класс взаимодействия, представляющий COM-класс, как показано в следующем коде:</span><span class="sxs-lookup"><span data-stu-id="f145e-142">However, if you are using the COM class as the base for a derived class, you must use the interop class that represents the COM class, as in the following code:</span></span>  
  
 [!code-vb[VbVbalrInterop#21](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#21)]  
  
> [!NOTE]
> <span data-ttu-id="f145e-143">Сборки взаимодействия неявно реализуют интерфейсы, представляющие COM-классы.</span><span class="sxs-lookup"><span data-stu-id="f145e-143">Interop assemblies implicitly implement interfaces that represent COM classes.</span></span> <span data-ttu-id="f145e-144">Не пытайтесь использовать оператор `Implements` для реализации этих интерфейсов, иначе возникнет ошибка.</span><span class="sxs-lookup"><span data-stu-id="f145e-144">You should not try to use the `Implements` statement to implement these interfaces or an error will result.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor7"></a><span data-ttu-id="f145e-145">Типы данных для параметров и возвращаемых значений</span><span class="sxs-lookup"><span data-stu-id="f145e-145">Data Types for Parameters and Return Values</span></span>  
 <span data-ttu-id="f145e-146">В отличие от членов стандартных сборок, члены сборки взаимодействия могут иметь типы данных, отличные от тех, которые используются в объявлении исходного объекта.</span><span class="sxs-lookup"><span data-stu-id="f145e-146">Unlike members of standard assemblies, interop assembly members may have data types that differ from those used in the  original object declaration.</span></span> <span data-ttu-id="f145e-147">Несмотря на то, что сборки взаимодействия неявно преобразуют типы COM в совместимые типы среды CLR, следует обратить внимание на типы данных, используемые обеими сторонами для предотвращения ошибок во время выполнения.</span><span class="sxs-lookup"><span data-stu-id="f145e-147">Although interop assemblies implicitly convert COM types to compatible common language runtime types, you should pay attention to the data types that are used by both sides to prevent runtime errors.</span></span> <span data-ttu-id="f145e-148">Например, в COM-объектах, созданных в Visual Basic 6,0 и более ранних версиях, значения типа `Integer` предполагают .NET Framework эквивалентный тип `Short`.</span><span class="sxs-lookup"><span data-stu-id="f145e-148">For example, in COM objects created in Visual Basic 6.0 and earlier versions, values of type `Integer` assume the .NET Framework equivalent type, `Short`.</span></span> <span data-ttu-id="f145e-149">Рекомендуется использовать обозреватель объектов для проверки характеристик импортированных элементов перед их использованием.</span><span class="sxs-lookup"><span data-stu-id="f145e-149">It is recommended that you use the Object Browser to examine the characteristics of imported members before you use them.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor8"></a><span data-ttu-id="f145e-150">Методы COM уровня модуля</span><span class="sxs-lookup"><span data-stu-id="f145e-150">Module level COM methods</span></span>  
 <span data-ttu-id="f145e-151">Большинство COM-объектов используются путем создания экземпляра класса COM с помощью ключевого слова `New` и последующего вызова методов объекта.</span><span class="sxs-lookup"><span data-stu-id="f145e-151">Most COM objects are used by creating an instance of a COM class using the `New` keyword and then calling methods of the object.</span></span> <span data-ttu-id="f145e-152">Единственным исключением из этого правила является COM-объекты, содержащие `AppObj` или `GlobalMultiUse` классы COM.</span><span class="sxs-lookup"><span data-stu-id="f145e-152">One exception to this rule involves COM objects that contain `AppObj` or `GlobalMultiUse` COM classes.</span></span> <span data-ttu-id="f145e-153">Такие классы похожи на методы уровня модуля в Visual Basic классов .NET.</span><span class="sxs-lookup"><span data-stu-id="f145e-153">Such classes resemble module level methods in Visual Basic .NET classes.</span></span> <span data-ttu-id="f145e-154">Visual Basic 6,0 и более ранних версий неявно создают экземпляры таких объектов при первом вызове одного из их методов.</span><span class="sxs-lookup"><span data-stu-id="f145e-154">Visual Basic 6.0 and earlier versions implicitly create instances of such objects for you the first time that you call one of their methods.</span></span> <span data-ttu-id="f145e-155">Например, в Visual Basic 6,0 можно добавить ссылку на библиотеку объектов Microsoft DAO 3,6 и вызвать метод `DBEngine` без предварительного создания экземпляра:</span><span class="sxs-lookup"><span data-stu-id="f145e-155">For example, in Visual Basic 6.0 you can add a reference to the Microsoft DAO 3.6 Object Library and call the `DBEngine` method without first creating an instance:</span></span>  
  
```vb  
Dim db As DAO.Database  
' Open the database.  
Set db = DBEngine.OpenDatabase("C:\nwind.mdb")  
' Use the database object.  
```  
  
 <span data-ttu-id="f145e-156">Visual Basic .NET требует, чтобы всегда были созданы экземпляры COM-объектов, прежде чем можно будет использовать их методы.</span><span class="sxs-lookup"><span data-stu-id="f145e-156">Visual Basic .NET requires that you always create instances of COM objects before you can use their methods.</span></span> <span data-ttu-id="f145e-157">Чтобы использовать эти методы в Visual Basic, объявите переменную нужного класса и используйте ключевое слово New, чтобы присвоить объект переменной объекта.</span><span class="sxs-lookup"><span data-stu-id="f145e-157">To use these methods in Visual Basic, declare a variable of the desired class and use the new keyword to assign the object to the object variable.</span></span> <span data-ttu-id="f145e-158">Ключевое слово `Shared` можно использовать, если необходимо убедиться, что создан только один экземпляр класса.</span><span class="sxs-lookup"><span data-stu-id="f145e-158">The `Shared` keyword can be used when you want to make sure that only one instance of the class is created.</span></span>  
  
 [!code-vb[VbVbalrInterop#23](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#23)]  
  
## <a name="vbconinteroperabilitymarshalinganchor9"></a><span data-ttu-id="f145e-159">Необработанные ошибки в обработчиках событий</span><span class="sxs-lookup"><span data-stu-id="f145e-159">Unhandled Errors in Event Handlers</span></span>  
 <span data-ttu-id="f145e-160">Одна из распространенных проблем взаимодействия включает в себя ошибки обработчиков событий, которые обрабатывали события, вызванные COM-объектами.</span><span class="sxs-lookup"><span data-stu-id="f145e-160">One common interop problem involves errors in event handlers that handle events raised by COM objects.</span></span> <span data-ttu-id="f145e-161">Такие ошибки игнорируются, если только вы не проверите наличие ошибок с помощью инструкций `On Error` или `Try...Catch...Finally`.</span><span class="sxs-lookup"><span data-stu-id="f145e-161">Such errors are ignored unless you specifically check for errors using `On Error` or `Try...Catch...Finally` statements.</span></span> <span data-ttu-id="f145e-162">Например, следующий пример относится к Visual Basic проекту .NET, имеющему ссылку на COM-объект библиотеки Microsoft объекты данных ActiveX 2,8.</span><span class="sxs-lookup"><span data-stu-id="f145e-162">For example, the following example is from a Visual Basic .NET project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object.</span></span>  
  
 [!code-vb[VbVbalrInterop#24](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#24)]  
  
 <span data-ttu-id="f145e-163">В этом примере возникает ошибка, как ожидалось.</span><span class="sxs-lookup"><span data-stu-id="f145e-163">This example raises an error as expected.</span></span> <span data-ttu-id="f145e-164">Однако при попытке выполнить тот же пример без блока `Try...Catch...Finally` ошибка игнорируется, как если бы использовалась инструкция `OnError Resume Next`.</span><span class="sxs-lookup"><span data-stu-id="f145e-164">However, if you try the same example without the `Try...Catch...Finally` block, the error is ignored as if you used the `OnError Resume Next` statement.</span></span> <span data-ttu-id="f145e-165">Без обработки ошибок деление на ноль автоматически завершается сбоем.</span><span class="sxs-lookup"><span data-stu-id="f145e-165">Without error handling, the division by zero silently fails.</span></span> <span data-ttu-id="f145e-166">Так как такие ошибки никогда не вызывают ошибки необработанных исключений, важно использовать некоторую форму обработки исключений в обработчиках событий, обрабатывающих события из COM-объектов.</span><span class="sxs-lookup"><span data-stu-id="f145e-166">Because such errors never raise unhandled exception errors, it is important that you use some form of exception handling in event handlers that handle events from COM objects.</span></span>  
  
### <a name="understanding-com-interop-errors"></a><span data-ttu-id="f145e-167">Общие сведения об ошибках COM-взаимодействия</span><span class="sxs-lookup"><span data-stu-id="f145e-167">Understanding COM interop errors</span></span>  
 <span data-ttu-id="f145e-168">Без обработки ошибок вызовы взаимодействия часто создают ошибки, которые предоставляют небольшую информацию.</span><span class="sxs-lookup"><span data-stu-id="f145e-168">Without error handling, interop calls often generate errors that provide little information.</span></span> <span data-ttu-id="f145e-169">По возможности используйте структурированную обработку ошибок, чтобы предоставить дополнительные сведения о проблемах, возникающих при их возникновении.</span><span class="sxs-lookup"><span data-stu-id="f145e-169">Whenever possible, use structured error handling to provide more information about problems when they occur.</span></span> <span data-ttu-id="f145e-170">Это может быть особенно полезно при отладке приложений.</span><span class="sxs-lookup"><span data-stu-id="f145e-170">This can be especially helpful when you debug applications.</span></span> <span data-ttu-id="f145e-171">Пример.</span><span class="sxs-lookup"><span data-stu-id="f145e-171">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#25](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#25)]  
  
 <span data-ttu-id="f145e-172">Вы можете найти такие сведения, как описание ошибки, HRESULT и источник ошибок COM, изучив содержимое объекта исключения.</span><span class="sxs-lookup"><span data-stu-id="f145e-172">You can find information such as the error description, HRESULT, and the source of COM errors by examining the contents of the exception object.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor10"></a><span data-ttu-id="f145e-173">Проблемы с элементом управления ActiveX</span><span class="sxs-lookup"><span data-stu-id="f145e-173">ActiveX Control Issues</span></span>  
 <span data-ttu-id="f145e-174">Большинство элементов управления ActiveX, работающих с Visual Basic 6,0, работают с Visual Basic .NET без проблем.</span><span class="sxs-lookup"><span data-stu-id="f145e-174">Most ActiveX controls that work with Visual Basic 6.0 work with Visual Basic .NET without trouble.</span></span> <span data-ttu-id="f145e-175">Основные исключения — это контейнерные элементы управления или элементы управления, которые визуально содержат другие элементы управления.</span><span class="sxs-lookup"><span data-stu-id="f145e-175">The main exceptions are container controls, or controls that visually contain other controls.</span></span> <span data-ttu-id="f145e-176">Ниже приведены некоторые примеры старых элементов управления, которые неправильно работают с Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f145e-176">Some examples of older controls that do not work correctly with Visual Studio are as follows:</span></span>  
  
- <span data-ttu-id="f145e-177">Элемент управления Frame в Microsoft Forms 2,0</span><span class="sxs-lookup"><span data-stu-id="f145e-177">Microsoft Forms 2.0 Frame control</span></span>  
  
- <span data-ttu-id="f145e-178">Элемент управления "вверх/вниз", также известный как элемент управления "Счетчик"</span><span class="sxs-lookup"><span data-stu-id="f145e-178">Up-Down control, also known as the spin control</span></span>  
  
- <span data-ttu-id="f145e-179">Элемент управления вкладки Шеридан</span><span class="sxs-lookup"><span data-stu-id="f145e-179">Sheridan Tab Control</span></span>  
  
 <span data-ttu-id="f145e-180">Существует несколько обходных путей для неподдерживаемых проблем с элементом управления ActiveX.</span><span class="sxs-lookup"><span data-stu-id="f145e-180">There are only a few workarounds for unsupported ActiveX control problems.</span></span> <span data-ttu-id="f145e-181">Вы можете перенести существующие элементы управления в Visual Studio, если вы владеете исходным исходным кодом.</span><span class="sxs-lookup"><span data-stu-id="f145e-181">You can migrate existing controls to Visual Studio if you own the original source code.</span></span> <span data-ttu-id="f145e-182">В противном случае можно проверить наличие обновленных поставщиков программного обеспечения. NET-совместимые версии элементов управления для замены неподдерживаемых элементов управления ActiveX.</span><span class="sxs-lookup"><span data-stu-id="f145e-182">Otherwise, you can check with software vendors for updated .NET-compatible versions of controls to replace unsupported ActiveX controls.</span></span>  
  
## <a name="vbconinteroperabilitymarshalinganchor11"></a><span data-ttu-id="f145e-183">Передача свойств элементов управления только для чтения по ссылке ByRef</span><span class="sxs-lookup"><span data-stu-id="f145e-183">Passing ReadOnly Properties of Controls ByRef</span></span>  
 <span data-ttu-id="f145e-184">Visual Basic .NET иногда вызывает ошибки COM, такие как "Error 0x800A017F CTL_E_SETNOTSUPPORTED", при передаче `ReadOnly` свойств некоторых старых элементов управления ActiveX в качестве `ByRef` параметров в другие процедуры.</span><span class="sxs-lookup"><span data-stu-id="f145e-184">Visual Basic .NET sometimes raises COM errors such as, "Error 0x800A017F CTL_E_SETNOTSUPPORTED", when you pass `ReadOnly` properties of some older ActiveX controls as `ByRef` parameters to other procedures.</span></span> <span data-ttu-id="f145e-185">Аналогичные вызовы процедур из Visual Basic 6,0 не вызывают ошибку, и параметры обрабатываются так, как если бы они были переданы по значению.</span><span class="sxs-lookup"><span data-stu-id="f145e-185">Similar procedure calls from Visual Basic 6.0 do not raise an error, and the parameters are treated as if you passed them by value.</span></span> <span data-ttu-id="f145e-186">Сообщение об ошибке Visual Basic .NET указывает на то, что вы пытаетесь изменить свойство, которое не содержит процедуру свойства `Set`.</span><span class="sxs-lookup"><span data-stu-id="f145e-186">The Visual Basic .NET error message indicates that you are trying to change a property that does not have a property `Set` procedure.</span></span>  
  
 <span data-ttu-id="f145e-187">Если у вас есть доступ к вызываемой процедуре, эту ошибку можно предотвратить с помощью ключевого слова `ByVal`, чтобы объявить параметры, принимающие `ReadOnly` свойства.</span><span class="sxs-lookup"><span data-stu-id="f145e-187">If you have access to the procedure being called, you can prevent this error by using the `ByVal` keyword to declare parameters that accept `ReadOnly` properties.</span></span> <span data-ttu-id="f145e-188">Пример.</span><span class="sxs-lookup"><span data-stu-id="f145e-188">For example:</span></span>  
  
 [!code-vb[VbVbalrInterop#26](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#26)]  
  
 <span data-ttu-id="f145e-189">Если у вас нет доступа к исходному коду для вызываемой процедуры, можно принудительно передать свойство по значению, добавив дополнительный набор квадратных скобок вокруг вызывающей процедуры.</span><span class="sxs-lookup"><span data-stu-id="f145e-189">If you do not have access to the source code for the procedure being called, you can force the property to be passed by value by adding an extra set of brackets around the calling procedure.</span></span> <span data-ttu-id="f145e-190">Например, в проекте, который содержит ссылку на COM-объект библиотеки Microsoft объекты данных ActiveX 2,8, можно использовать:</span><span class="sxs-lookup"><span data-stu-id="f145e-190">For example, in a project that has a reference to the Microsoft ActiveX Data Objects 2.8 Library COM object, you can use:</span></span>  
  
 [!code-vb[VbVbalrInterop#27](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrInterop/VB/Class1.vb#27)]  
  
## <a name="vbconinteroperabilitymarshalinganchor12"></a><span data-ttu-id="f145e-191">Развертывание сборок, предоставляющих взаимодействие</span><span class="sxs-lookup"><span data-stu-id="f145e-191">Deploying Assemblies That Expose Interop</span></span>  
 <span data-ttu-id="f145e-192">Развертывание сборок, предоставляющих интерфейсы COM, представляет некоторые уникальные проблемы.</span><span class="sxs-lookup"><span data-stu-id="f145e-192">Deploying assemblies that expose COM interfaces presents some unique challenges.</span></span> <span data-ttu-id="f145e-193">Например, потенциальная проблема возникает, когда отдельные приложения ссылаются на одну и ту же сборку COM.</span><span class="sxs-lookup"><span data-stu-id="f145e-193">For example, a potential problem occurs when separate applications reference the same COM assembly.</span></span> <span data-ttu-id="f145e-194">Такая ситуация часто возникает при установке новой версии сборки, если другое приложение по-прежнему использует старую версию сборки.</span><span class="sxs-lookup"><span data-stu-id="f145e-194">This situation is common when a new version of an assembly is installed and another application is still using the old version of the assembly.</span></span> <span data-ttu-id="f145e-195">При удалении сборки, использующей общую библиотеку DLL, можно непреднамеренно сделать ее недоступной для других сборок.</span><span class="sxs-lookup"><span data-stu-id="f145e-195">If you uninstall an assembly that shares a DLL, you can unintentionally make it unavailable to the other assemblies.</span></span>  
  
 <span data-ttu-id="f145e-196">Чтобы избежать этой проблемы, следует установить общие сборки в глобальный кэш сборок (GAC) и использовать установочного модуля для компонента.</span><span class="sxs-lookup"><span data-stu-id="f145e-196">To avoid this problem, you should install shared assemblies to the Global Assembly Cache (GAC) and use a MergeModule for the component.</span></span> <span data-ttu-id="f145e-197">Если вы не можете установить приложение в глобальном кэше сборок, его следует установить на Коммонфилесфолдер в подкаталоге, относящемся к конкретной версии.</span><span class="sxs-lookup"><span data-stu-id="f145e-197">If you cannot install the application in the GAC, it should be installed to CommonFilesFolder in a version-specific subdirectory.</span></span>  
  
 <span data-ttu-id="f145e-198">Сборки, которые не являются общими, должны располагаться рядом в каталоге с вызывающим приложением.</span><span class="sxs-lookup"><span data-stu-id="f145e-198">Assemblies that are not shared should be located side by side in the directory with the calling application.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="f145e-199">См. также</span><span class="sxs-lookup"><span data-stu-id="f145e-199">See also</span></span>

- <xref:System.Runtime.InteropServices.MarshalAsAttribute>
- [<span data-ttu-id="f145e-200">COM-взаимодействие</span><span class="sxs-lookup"><span data-stu-id="f145e-200">COM Interop</span></span>](../../../visual-basic/programming-guide/com-interop/index.md)
- [<span data-ttu-id="f145e-201">Tlbimp.exe (программа экспорта библиотек типов)</span><span class="sxs-lookup"><span data-stu-id="f145e-201">Tlbimp.exe (Type Library Importer)</span></span>](../../../framework/tools/tlbimp-exe-type-library-importer.md)
- [<span data-ttu-id="f145e-202">Tlbexp.exe (программа экспорта библиотек типов)</span><span class="sxs-lookup"><span data-stu-id="f145e-202">Tlbexp.exe (Type Library Exporter)</span></span>](../../../framework/tools/tlbexp-exe-type-library-exporter.md)
- [<span data-ttu-id="f145e-203">Пошаговое руководство. Реализация наследования с использованием COM-объектов</span><span class="sxs-lookup"><span data-stu-id="f145e-203">Walkthrough: Implementing Inheritance with COM Objects</span></span>](../../../visual-basic/programming-guide/com-interop/walkthrough-implementing-inheritance-with-com-objects.md)
- [<span data-ttu-id="f145e-204">Оператор Inherits</span><span class="sxs-lookup"><span data-stu-id="f145e-204">Inherits Statement</span></span>](../../../visual-basic/language-reference/statements/inherits-statement.md)
- [<span data-ttu-id="f145e-205">Глобальный кэш сборок</span><span class="sxs-lookup"><span data-stu-id="f145e-205">Global Assembly Cache</span></span>](../../../framework/app-domains/gac.md)
