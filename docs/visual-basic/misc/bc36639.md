---
title: Параметр «ByRef» '<parametername>' не может использоваться в лямбда-выражения
ms.date: 07/20/2015
f1_keywords:
- bc36639
- vbc36639
helpviewer_keywords:
- BC36639
ms.assetid: 5913f9b6-2929-4c05-8dd1-00b10fcd5a83
ms.openlocfilehash: a8ddab532f2c618f56ec21401503be0b0dbc1ab6
ms.sourcegitcommit: da2dd2772fcf32b44eb18b1cbe8affd17b1753c9
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 09/27/2019
ms.locfileid: "71351980"
---
# <a name="byref-parameter-parametername-cannot-be-used-in-a-lambda-expression"></a><span data-ttu-id="cb888-102">Параметр «ByRef» '\<имя_параметра >' не может использоваться в лямбда-выражения</span><span class="sxs-lookup"><span data-stu-id="cb888-102">'ByRef' parameter '\<parametername>' cannot be used in a lambda expression</span></span>
<span data-ttu-id="cb888-103">Лямбда-выражение, объявленное в `Sub` или функции, не может использовать любые параметры `ByRef` этого `Sub` или функции.</span><span class="sxs-lookup"><span data-stu-id="cb888-103">A lambda expression declared within a `Sub` or function cannot use any `ByRef` parameters of that `Sub` or function.</span></span> <span data-ttu-id="cb888-104">Например, следующий код вызовет эту ошибку, поскольку параметр `ByRef` `n` используется в лямбда-выражении.</span><span class="sxs-lookup"><span data-stu-id="cb888-104">For example, the following code will cause this error because the `ByRef` parameter `n` is used in the lambda expression.</span></span>  
  
```vb  
'' Not valid.   
'Sub ExampleSub(ByRef n As Integer)  
  
'    Dim lambda = Function(p As Integer) p + n  
  
'End Sub  
```  
  
 <span data-ttu-id="cb888-105">**Идентификатор ошибки:** BC36639</span><span class="sxs-lookup"><span data-stu-id="cb888-105">**Error ID:** BC36639</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="cb888-106">Исправление ошибки</span><span class="sxs-lookup"><span data-stu-id="cb888-106">To correct this error</span></span>  
  
- <span data-ttu-id="cb888-107">Назначьте параметр `ByRef` локальной переменной и используйте эту локальную переменную в лямбда-выражении, как показано в следующем коде.</span><span class="sxs-lookup"><span data-stu-id="cb888-107">Assign the `ByRef` parameter to a local variable, and use the local variable in the lambda expression, as shown in the following code:</span></span>  
  
    ```vb  
    Sub ExampleSub(ByRef n As Integer)  
  
        Dim temp = n  
        Dim lambda = Function(p As Integer) p + temp  
  
    End Sub  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="cb888-108">См. также</span><span class="sxs-lookup"><span data-stu-id="cb888-108">See also</span></span>

- [<span data-ttu-id="cb888-109">Лямбда-выражения</span><span class="sxs-lookup"><span data-stu-id="cb888-109">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
