---
title: Для Option Strict On необходимо, чтобы каждый параметр лямбда-выражения объявлялся с конструкцией As, если его тип не удается вывести.
ms.date: 07/20/2015
f1_keywords:
- bc36642
- vbc36642
helpviewer_keywords:
- BC36642
ms.assetid: 2aaa62b8-49c9-4ae8-b0f5-08e3f0b5ad10
ms.openlocfilehash: 6d0d509ba2465b1a9300b759cc258781e2e183a9
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/15/2019
ms.locfileid: "58040791"
---
# <a name="option-strict-on-requires-each-lambda-expression-parameter-to-be-declared-with-an-as-clause-if-its-type-cannot-be-inferred"></a><span data-ttu-id="2e729-102">Для Option Strict On необходимо, чтобы каждый параметр лямбда-выражения объявлялся с конструкцией As, если его тип не удается вывести.</span><span class="sxs-lookup"><span data-stu-id="2e729-102">Option Strict On requires each lambda expression parameter to be declared with an 'As' clause if its type cannot be inferred</span></span>
<span data-ttu-id="2e729-103">Параметр в лямбда-выражении был объявлен без использования предложения `As` , с `Option Strict` on.</span><span class="sxs-lookup"><span data-stu-id="2e729-103">You have declared a parameter in a lambda expression without using an `As` clause, with `Option Strict` on.</span></span>  
  
```  
' Not valid when Option Strict is on.  
' Dim increment1 = Function (n) n + 1  
```  
  
 <span data-ttu-id="2e729-104">Предыдущее объявление допустимо, если тип `n` может быть определен.</span><span class="sxs-lookup"><span data-stu-id="2e729-104">The previous declaration is valid if the type of `n` can be inferred.</span></span> <span data-ttu-id="2e729-105">Например, если предыдущее лямбда-выражение назначается делегату функции `Del`:</span><span class="sxs-lookup"><span data-stu-id="2e729-105">For example, if you are assigning the previous lambda expression to a function delegate, `Del`:</span></span>  
  
```  
Delegate Function Del(ByVal p As Integer) As Integer  
```  
  
 <span data-ttu-id="2e729-106">Теперь тип `n` может быть выведен из параметра `p`:</span><span class="sxs-lookup"><span data-stu-id="2e729-106">Now the type of `n` can be inferred from parameter `p`:</span></span>  
  
```  
Dim increment2 as Del = Function(n) n + 1  
```  
  
 <span data-ttu-id="2e729-107">**Идентификатор ошибки:** BC36642</span><span class="sxs-lookup"><span data-stu-id="2e729-107">**Error ID:** BC36642</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="2e729-108">Исправление ошибки</span><span class="sxs-lookup"><span data-stu-id="2e729-108">To correct this error</span></span>  
  
-   <span data-ttu-id="2e729-109">Добавьте предложение `As` в объявление параметра:</span><span class="sxs-lookup"><span data-stu-id="2e729-109">Add an `As` clause to the parameter declaration:</span></span>  
  
    ```  
    Dim increment3 = Function (n As Integer) n + 1  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="2e729-110">См. также</span><span class="sxs-lookup"><span data-stu-id="2e729-110">See also</span></span>

- [<span data-ttu-id="2e729-111">Лямбда-выражения</span><span class="sxs-lookup"><span data-stu-id="2e729-111">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
