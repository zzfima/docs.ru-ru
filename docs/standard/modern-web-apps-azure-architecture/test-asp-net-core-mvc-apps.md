---
title: "ASP.NET Core тестирования приложения MVC"
description: "Архитектора современных веб-приложений с помощью ASP.NET Core и Azure | ASP.NET Core тестирования приложения MVC"
author: ardalis
ms.author: wiwagn
ms.date: 10/08/2017
ms.prod: .net-core
ms.technology: dotnet-docker
ms.workload:
- dotnet
- dotnetcore
ms.openlocfilehash: d23d0accc33fb8335dff602d6e1d6c8689972906
ms.sourcegitcommit: e7f04439d78909229506b56935a1105a4149ff3d
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 12/23/2017
---
# <a name="test-aspnet-core-mvc-apps"></a><span data-ttu-id="a914f-103">ASP.NET Core тестирования приложения MVC</span><span class="sxs-lookup"><span data-stu-id="a914f-103">Test ASP.NET Core MVC Apps</span></span>

> <span data-ttu-id="a914f-104">_«Если вас не устраивает модульное тестирование продукта, скорее всего клиентов не будет, например для тестирования, либо.»_</span><span class="sxs-lookup"><span data-stu-id="a914f-104">_"If you don't like unit testing your product, most likely your customers won't like to test it, either."_</span></span>
> <span data-ttu-id="a914f-105">_ - Анонимный -</span><span class="sxs-lookup"><span data-stu-id="a914f-105">_- Anonymous-</span></span>

## <a name="summary"></a><span data-ttu-id="a914f-106">Сводка</span><span class="sxs-lookup"><span data-stu-id="a914f-106">Summary</span></span>

<span data-ttu-id="a914f-107">Программное обеспечение любой сложности может произойти непредвиденный сбой в ответ на изменения.</span><span class="sxs-lookup"><span data-stu-id="a914f-107">Software of any complexity can fail in unexpected ways in response to changes.</span></span> <span data-ttu-id="a914f-108">Таким образом проверка после внесения изменений является обязательным для всех, кроме самых стандартных (или наименьшей важности) приложения.</span><span class="sxs-lookup"><span data-stu-id="a914f-108">Thus, testing after making changes is required for all but the most trivial (or least critical) applications.</span></span> <span data-ttu-id="a914f-109">Тестирование вручную является самым медленным, наиболее ресурсоемких бы надежный способ для тестирования программного обеспечения.</span><span class="sxs-lookup"><span data-stu-id="a914f-109">Manual testing is the slowest, least reliable, most expensive way to test software.</span></span> <span data-ttu-id="a914f-110">К сожалению Если приложения не предназначены для тестирования, может быть единственный имеющийся.</span><span class="sxs-lookup"><span data-stu-id="a914f-110">Unfortunately, if applications are not designed to be testable, it can be the only means available.</span></span> <span data-ttu-id="a914f-111">Приложения, написанные следующие архитектурные принципы располагаются в главе X должен модульного тестирования и приложения ASP.NET Core поддерживают автоматизированной интеграции и функционального тестирования также.</span><span class="sxs-lookup"><span data-stu-id="a914f-111">Applications written following the architectural principles laid out in chapter X should be unit testable, and ASP.NET Core applications support automated integration and functional testing as well.</span></span>

## <a name="kinds-of-automated-tests"></a><span data-ttu-id="a914f-112">Виды автоматических тестов</span><span class="sxs-lookup"><span data-stu-id="a914f-112">Kinds of Automated Tests</span></span>

<span data-ttu-id="a914f-113">Существует множество видов автоматических тестов для приложений.</span><span class="sxs-lookup"><span data-stu-id="a914f-113">There are many kinds of automated tests for software applications.</span></span> <span data-ttu-id="a914f-114">Самая простая, нижнего уровня тест — модульный тест.</span><span class="sxs-lookup"><span data-stu-id="a914f-114">The simplest, lowest level test is the unit test.</span></span> <span data-ttu-id="a914f-115">Немного более высокого уровня, тесты интеграции и функциональных тестов.</span><span class="sxs-lookup"><span data-stu-id="a914f-115">At a slightly higher level there are integration tests and functional tests.</span></span> <span data-ttu-id="a914f-116">Другие виды тесты как тесты пользовательского интерфейса, нагрузочные тесты, нагрузочные тесты и тесты состояния выходят за рамки данного документа.</span><span class="sxs-lookup"><span data-stu-id="a914f-116">Other kinds of tests, like UI tests, load tests, stress tests, and smoke tests, are beyond the scope of this document.</span></span>

### <a name="unit-tests"></a><span data-ttu-id="a914f-117">Модульные тесты</span><span class="sxs-lookup"><span data-stu-id="a914f-117">Unit Tests</span></span>

<span data-ttu-id="a914f-118">Модульный тест проверяет определенной части логики приложения.</span><span class="sxs-lookup"><span data-stu-id="a914f-118">A unit test tests a single part of your application's logic.</span></span> <span data-ttu-id="a914f-119">Один дополнительные можно описать, указав ряд особенностей, которые не.</span><span class="sxs-lookup"><span data-stu-id="a914f-119">One can further describe it by listing some of the things that it isn't.</span></span> <span data-ttu-id="a914f-120">Модульный тест не проверяет, как ваш код работает с зависимостей или инфраструктурой — проверяет, какие интеграции предназначены для.</span><span class="sxs-lookup"><span data-stu-id="a914f-120">A unit test doesn't test how your code works with dependencies or infrastructure – that's what integration tests are for.</span></span> <span data-ttu-id="a914f-121">Модульный тест не проверка код написан на инфраструктуре: он работает или, если его найти не, зарегистрировать ошибку и кода для решения этой проблемы следует считать.</span><span class="sxs-lookup"><span data-stu-id="a914f-121">A unit test doesn't test the framework your code is written on – you should assume it works or, if you find it doesn't, file a bug and code a workaround.</span></span> <span data-ttu-id="a914f-122">Модульный тест выполняется полностью в памяти и в процессе.</span><span class="sxs-lookup"><span data-stu-id="a914f-122">A unit test runs completely in memory and in process.</span></span> <span data-ttu-id="a914f-123">Он не взаимодействуют с файловой системы, сети или базы данных.</span><span class="sxs-lookup"><span data-stu-id="a914f-123">It doesn't communicate with the file system, the network, or a database.</span></span> <span data-ttu-id="a914f-124">Модульные тесты следует тестировать только в коде.</span><span class="sxs-lookup"><span data-stu-id="a914f-124">Unit tests should only test your code.</span></span>

<span data-ttu-id="a914f-125">Очень быстро выполнять модульные тесты, благодаря тому, что они проверяют только единый блок кода, без внешних зависимостей.</span><span class="sxs-lookup"><span data-stu-id="a914f-125">Unit tests, by virtue of the fact that they test only a single unit of your code, with no external dependencies, should execute extremely quickly.</span></span> <span data-ttu-id="a914f-126">Таким образом можно выполнять наборы тестов из сотен модульных тестов в течение нескольких секунд.</span><span class="sxs-lookup"><span data-stu-id="a914f-126">Thus, you should be able to run test suites of hundreds of unit tests in a few seconds.</span></span> <span data-ttu-id="a914f-127">Выполните их как правило, в идеале перед каждой отправкой в репозитории управления общим источником, а наверняка каждые автоматизированной сборки на сервере сборки.</span><span class="sxs-lookup"><span data-stu-id="a914f-127">Run them frequently, ideally before every push to a shared source control repository, and certainly with every automated build on your build server.</span></span>

### <a name="integration-tests"></a><span data-ttu-id="a914f-128">Интеграционные тесты</span><span class="sxs-lookup"><span data-stu-id="a914f-128">Integration Tests</span></span>

<span data-ttu-id="a914f-129">Несмотря на то, что это хороший способ инкапсуляции кода, взаимодействующая с инфраструктурой как файловых систем и баз данных, по-прежнему будет иметь некоторый код, и, возможно, необходимо проверить его.</span><span class="sxs-lookup"><span data-stu-id="a914f-129">Although it's a good idea to encapsulate your code that interacts with infrastructure like databases and file systems, you will still have some of that code, and you will probably want to test it.</span></span> <span data-ttu-id="a914f-130">Кроме того необходимо проверить, кода и слои взаимодействующих должным образом при зависимостей приложения будут полностью устранены.</span><span class="sxs-lookup"><span data-stu-id="a914f-130">Additionally, you should verify that your code's layers interact as you expect when your application's dependencies are fully resolved.</span></span> <span data-ttu-id="a914f-131">Это отвечает интеграционные тесты.</span><span class="sxs-lookup"><span data-stu-id="a914f-131">This is the responsibility of integration tests.</span></span> <span data-ttu-id="a914f-132">Интеграционные тесты обычно работать медленнее и сложнее в настройке, чем модульные тесты, так как они часто используют внешние зависимости и инфраструктуры.</span><span class="sxs-lookup"><span data-stu-id="a914f-132">Integration tests tend to be slower and more difficult to set up than unit tests, because they often depend on external dependencies and infrastructure.</span></span> <span data-ttu-id="a914f-133">Таким образом следует избегать тестирования, которые могут тестов с помощью модульных тестов в тесты интеграции.</span><span class="sxs-lookup"><span data-stu-id="a914f-133">Thus, you should avoid testing things that could be tests with unit tests in integration tests.</span></span> <span data-ttu-id="a914f-134">Если данного сценария можно проверить с помощью модульного теста, следует проверить его с модульным тестом.</span><span class="sxs-lookup"><span data-stu-id="a914f-134">If you can test a given scenario with a unit test, you should test it with a unit test.</span></span> <span data-ttu-id="a914f-135">Если это невозможно, рассмотрите возможность использования интеграционного теста.</span><span class="sxs-lookup"><span data-stu-id="a914f-135">If you can't, then consider using an integration test.</span></span>

<span data-ttu-id="a914f-136">Интеграционные тесты часто сталкиваются с более сложные установки и удаления процедуры, чем модульные тесты.</span><span class="sxs-lookup"><span data-stu-id="a914f-136">Integration tests will often have more complex setup and teardown procedures than unit tests.</span></span> <span data-ttu-id="a914f-137">Например интеграционного теста, который переходит к реальной базе данных потребуется способ возвращения базы данных в ее начальное состояние каждого тестового запуска.</span><span class="sxs-lookup"><span data-stu-id="a914f-137">For example, an integration test that goes against an actual database will need a way to return the database to a known state before each test run.</span></span> <span data-ttu-id="a914f-138">Мере добавляются новые тесты схему рабочей базы данных, эти сценарии постепенно увеличиваться в размере и сложности тестирования.</span><span class="sxs-lookup"><span data-stu-id="a914f-138">As new tests are added and the production database schema evolves, these test scripts will tend to grow in size and complexity.</span></span> <span data-ttu-id="a914f-139">Во многих системах больших оказывается нецелесообразным для выполнения полной наборы тестов интеграции на рабочих станциях разработчика перед возвратом изменений в общих систему управления версиями.</span><span class="sxs-lookup"><span data-stu-id="a914f-139">In many large systems, it is impractical to run full suites of integration tests on developer workstations before checking in changes to shared source control.</span></span> <span data-ttu-id="a914f-140">В этих случаях интеграционные тесты могут выполняться на сервере сборки.</span><span class="sxs-lookup"><span data-stu-id="a914f-140">In these cases, integration tests may be run on a build server.</span></span>

<span data-ttu-id="a914f-141">Класс реализации LocalFileImageService реализует логику для получения и возврат байт файла изображения из определенной папке присвоен идентификационный номер:</span><span class="sxs-lookup"><span data-stu-id="a914f-141">The LocalFileImageService implementation class implements the logic for fetching and returning the bytes of an image file from a particular folder given an id:</span></span>

```cs
public class LocalFileImageService : IImageService
{
    private readonly IHostingEnvironment _env;
    public LocalFileImageService(IHostingEnvironment env)
    {
        _env = env;
    }
    public byte[] GetImageBytesById(int id)
    {
        try
        {
            var contentRoot = _env.ContentRootPath + "//Pics";
            var path = Path.Combine(contentRoot, id + ".png");
            return File.ReadAllBytes(path);
```

### <a name="functional-tests"></a><span data-ttu-id="a914f-142">Функциональных тестов</span><span class="sxs-lookup"><span data-stu-id="a914f-142">Functional Tests</span></span>

<span data-ttu-id="a914f-143">Интеграционные тесты записываются с точки зрения разработчика, чтобы убедиться, что некоторые компоненты системы могут работать неправильно вместе.</span><span class="sxs-lookup"><span data-stu-id="a914f-143">Integration tests are written from the perspective of the developer, to verify that some components of the system work correctly together.</span></span> <span data-ttu-id="a914f-144">Функциональных тестов записываются с точки зрения пользователя и проверьте правильность системы, в зависимости от его требованиям.</span><span class="sxs-lookup"><span data-stu-id="a914f-144">Functional tests are written from the perspective of the user, and verify the correctness of the system based on its requirements.</span></span> <span data-ttu-id="a914f-145">Следующий фрагмент предлагает Полезная аналогия для как можно рассматривать функциональных тестов, по сравнению с модульными тестами.</span><span class="sxs-lookup"><span data-stu-id="a914f-145">The following excerpt offers a useful analogy for how to think about functional tests, compared to unit tests:</span></span>

> <span data-ttu-id="a914f-146">«Много раз разработки системы приравнены к построение дома.</span><span class="sxs-lookup"><span data-stu-id="a914f-146">"Many times the development of a system is likened to the building of a house.</span></span> <span data-ttu-id="a914f-147">Хотя эта аналогия не совсем правильно, мы можем расширить для понимания разницу между модульных и функциональных тестов.</span><span class="sxs-lookup"><span data-stu-id="a914f-147">While this analogy isn't quite correct, we can extend it for the purposes of understanding the difference between unit and functional tests.</span></span> <span data-ttu-id="a914f-148">Модульное тестирование является аналогом построение инспектора, узле конструкции дома.</span><span class="sxs-lookup"><span data-stu-id="a914f-148">Unit testing is analogous to a building inspector visiting a house's construction site.</span></span> <span data-ttu-id="a914f-149">Он предназначен для различных внутренних систем из дома, основой кадрирования, тока, коммуникации и т. д.</span><span class="sxs-lookup"><span data-stu-id="a914f-149">He is focused on the various internal systems of the house, the foundation, framing, electrical, plumbing, and so on.</span></span> <span data-ttu-id="a914f-150">Он обеспечивает (тестов), части дома будет работать неправильно и безопасно, то есть, соответствуют построение кода.</span><span class="sxs-lookup"><span data-stu-id="a914f-150">He ensures (tests) that the parts of the house will work correctly and safely, that is, meet the building code.</span></span> <span data-ttu-id="a914f-151">Функциональных тестов в этом сценарии аналогичны подсветкой, этот же узле построения.</span><span class="sxs-lookup"><span data-stu-id="a914f-151">Functional tests in this scenario are analogous to the homeowner visiting this same construction site.</span></span> <span data-ttu-id="a914f-152">Он предполагается, внутренних систем будет работать надлежащим образом, построение инспектор выполняет свою задачу.</span><span class="sxs-lookup"><span data-stu-id="a914f-152">He assumes that the internal systems will behave appropriately, that the building inspector is performing his task.</span></span> <span data-ttu-id="a914f-153">Подсветкой основное внимание уделено каким он будет как находящегося в этом дома.</span><span class="sxs-lookup"><span data-stu-id="a914f-153">The homeowner is focused on what it will be like to live in this house.</span></span> <span data-ttu-id="a914f-154">Он отвечает за внешний вид дома, различных комнаты научились размер, дома под нужды семейства, — это окна, в подходящее место для перехвата sun утра.</span><span class="sxs-lookup"><span data-stu-id="a914f-154">He is concerned with how the house looks, are the various rooms a comfortable size, does the house fit the family's needs, are the windows in a good spot to catch the morning sun.</span></span> <span data-ttu-id="a914f-155">Подсветкой выполняет функциональных тестов на дома.</span><span class="sxs-lookup"><span data-stu-id="a914f-155">The homeowner is performing functional tests on the house.</span></span> <span data-ttu-id="a914f-156">У него есть точки зрения пользователя.</span><span class="sxs-lookup"><span data-stu-id="a914f-156">He has the user's perspective.</span></span> <span data-ttu-id="a914f-157">Построение инспектор выполняет модульные тесты для дома.</span><span class="sxs-lookup"><span data-stu-id="a914f-157">The building inspector is performing unit tests on the house.</span></span> <span data-ttu-id="a914f-158">Он содержит построитель перспективы».</span><span class="sxs-lookup"><span data-stu-id="a914f-158">He has the builder's perspective."</span></span>

<span data-ttu-id="a914f-159">Источник: [модульное тестирование и функциональных тестов](http://www.softwaretestingtricks.com/2007/01/unit-testing-versus-functional-tests.html)</span><span class="sxs-lookup"><span data-stu-id="a914f-159">Source: [Unit Testing versus Functional Tests](http://www.softwaretestingtricks.com/2007/01/unit-testing-versus-functional-tests.html)</span></span>

<span data-ttu-id="a914f-160">Я хочу шрифта говоря» как разработчики отказа двумя способами: мы создаем самое неправильный или мы создаем неверный.»</span><span class="sxs-lookup"><span data-stu-id="a914f-160">I'm fond of saying "As developers, we fail in two ways: we build the thing wrong, or we build the wrong thing."</span></span> <span data-ttu-id="a914f-161">Модульные тесты убедитесь, что вы создаете очередь вправо; функциональных тестов убедитесь, что вы создаете правильно.</span><span class="sxs-lookup"><span data-stu-id="a914f-161">Unit tests ensure you are building the thing right; functional tests ensure you are building the right thing.</span></span>

<span data-ttu-id="a914f-162">Поскольку функциональные тесты работают на уровне системы, могут потребоваться некоторые степень автоматизации пользовательского интерфейса.</span><span class="sxs-lookup"><span data-stu-id="a914f-162">Since functional tests operate at the system level, they may require some degree of UI automation.</span></span> <span data-ttu-id="a914f-163">Как интеграционных тестов они обычно работают с каким-либо инфраструктуры тестирования также.</span><span class="sxs-lookup"><span data-stu-id="a914f-163">Like integration tests, they usually work with some kind of test infrastructure as well.</span></span> <span data-ttu-id="a914f-164">Это делает их медленнее и еще более нестабильным, чем интеграции и модульные тесты.</span><span class="sxs-lookup"><span data-stu-id="a914f-164">This makes them slower and more brittle than unit and integration tests.</span></span> <span data-ttu-id="a914f-165">Необходимо иметь только столько функциональных тестов быть уверенным, как пользователи ожидают, что поведение системы.</span><span class="sxs-lookup"><span data-stu-id="a914f-165">You should have only as many functional tests as you need to be confident the system is behaving as users expect.</span></span>

### <a name="testing-pyramid"></a><span data-ttu-id="a914f-166">Тестирование Пирамидальная</span><span class="sxs-lookup"><span data-stu-id="a914f-166">Testing Pyramid</span></span>

<span data-ttu-id="a914f-167">Martin Fowler написал о тестирования пирамиды, пример которой показан на рисунке 9-1.</span><span class="sxs-lookup"><span data-stu-id="a914f-167">Martin Fowler wrote about the testing pyramid, an example of which is shown in Figure 9-1.</span></span>

![](./media/image9-1.png)

<span data-ttu-id="a914f-168">На рисунке 9-1 тестирование Пирамидальная</span><span class="sxs-lookup"><span data-stu-id="a914f-168">Figure 9-1 Testing Pyramid</span></span>

<span data-ttu-id="a914f-169">Различные уровни пирамиды и их относительные размеры представляют различные виды тестов и сколько необходимо для написания приложения.</span><span class="sxs-lookup"><span data-stu-id="a914f-169">The different layers of the pyramid, and their relative sizes, represent different kinds of tests and how many you should write for your application.</span></span> <span data-ttu-id="a914f-170">Как видите, рекомендуется иметь большой базы модульных тестов, поддерживаемых слой меньшего размера интеграционные тесты с собой еще меньше уровень функциональных тестов.</span><span class="sxs-lookup"><span data-stu-id="a914f-170">As you can see, the recommendation is to have a large base of unit tests, supported by a smaller layer of integration tests, with an even smaller layer of functional tests.</span></span> <span data-ttu-id="a914f-171">Каждый слой следует в идеале только содержать тесты, не может быть выполнена должным образом на нижнем уровне.</span><span class="sxs-lookup"><span data-stu-id="a914f-171">Each layer should ideally only have tests in it that cannot be performed adequately at a lower layer.</span></span> <span data-ttu-id="a914f-172">Имейте в виду тестирования пирамиды, когда вы пытаетесь решить, какая теста нужна для того или иного сценария.</span><span class="sxs-lookup"><span data-stu-id="a914f-172">Keep the testing pyramid in mind when you are trying to decide which kind of test you need for a particular scenario.</span></span>

### <a name="what-to-test"></a><span data-ttu-id="a914f-173">Что следует проверить</span><span class="sxs-lookup"><span data-stu-id="a914f-173">What to Test</span></span>

<span data-ttu-id="a914f-174">Распространенной проблемой для разработчиков, которые имеют достаточного опыта работы с создание автоматических тестов тут какие для проверки.</span><span class="sxs-lookup"><span data-stu-id="a914f-174">A common problem for developers who are inexperienced with writing automated tests is coming up with what to test.</span></span> <span data-ttu-id="a914f-175">Хорошей отправной точкой является тестирование условной логики.</span><span class="sxs-lookup"><span data-stu-id="a914f-175">A good starting point is to test conditional logic.</span></span> <span data-ttu-id="a914f-176">В любом месте, что у вас есть метод с поведением, изменения на основании условный оператор (if-else, переключение, т. д.), можно начать работу по крайней мере несколько тестов, подтверждающие правильное поведение для определенных условий.</span><span class="sxs-lookup"><span data-stu-id="a914f-176">Anywhere you have a method with behavior that changes based on a conditional statement (if-else, switch, etc.), you should be able to come up at least a couple of tests that confirm the correct behavior for certain conditions.</span></span> <span data-ttu-id="a914f-177">Если код содержит ошибки, рекомендуется записи хотя бы один тест для «довольны пути» через код (завершилась без ошибок) и по крайней мере один тест для «жаль, что путь» (с ошибками или нетипичные результаты), чтобы убедиться, что приложение работает должным образом при возникновении ошибки.</span><span class="sxs-lookup"><span data-stu-id="a914f-177">If your code has error conditions, it's good to write at least one test for the "happy path" through the code (with no errors), and at least one test for the "sad path" (with errors or atypical results) to confirm your application behaves as expected in the face of errors.</span></span> <span data-ttu-id="a914f-178">Наконец попробуйте сосредоточиться на тестирование вещей, которые могут вызвать сбой, а не уделять все внимание такие показатели, как объем протестированного кода.</span><span class="sxs-lookup"><span data-stu-id="a914f-178">Finally, try to focus on testing things that can fail, rather than focusing on metrics like code coverage.</span></span> <span data-ttu-id="a914f-179">Обычно лучше, чем меньше, больше покрытия кода.</span><span class="sxs-lookup"><span data-stu-id="a914f-179">More code coverage is better than less, generally.</span></span> <span data-ttu-id="a914f-180">Однако написание несколько дополнительных тестов очень сложными и важных метода обычно является лучше использовать времени, чем написание тестов для автосвойства только для улучшения метрики покрытия кода теста.</span><span class="sxs-lookup"><span data-stu-id="a914f-180">However, writing a few more tests of a very complex and business-critical method is usually a better use of time than writing tests for auto-properties just to improve test code coverage metrics.</span></span>

## <a name="organizing-test-projects"></a><span data-ttu-id="a914f-181">Упорядочение тестовых проектов</span><span class="sxs-lookup"><span data-stu-id="a914f-181">Organizing Test Projects</span></span>

<span data-ttu-id="a914f-182">Тестовые проекты могут быть упорядочены, однако работает наилучшим образом для вас.</span><span class="sxs-lookup"><span data-stu-id="a914f-182">Test projects can be organized however works best for you.</span></span> <span data-ttu-id="a914f-183">Это хороший способ разделения тесты по типу (модульный тест, тест интеграции) и что они предназначены для проверки (по проекту, пространством имен).</span><span class="sxs-lookup"><span data-stu-id="a914f-183">It's a good idea to separate tests by type (unit test, integration test) and by what they are testing (by project, by namespace).</span></span> <span data-ttu-id="a914f-184">Является ли такое разделение состоит из папок, содержащихся в одном проекте тестов или нескольких тестовых проектов является решение.</span><span class="sxs-lookup"><span data-stu-id="a914f-184">Whether this separation consists of folders within a single test project, or multiple test projects, is a design decision.</span></span> <span data-ttu-id="a914f-185">Один проект является простым, но для крупных проектов с помощью много тестов или для упрощения выполнения разных наборах тестов можно иметь несколько различных тестовых проектов.</span><span class="sxs-lookup"><span data-stu-id="a914f-185">One project is simplest, but for large projects with many tests, or in order to more easily run different sets of tests, you might want to have several different test projects.</span></span> <span data-ttu-id="a914f-186">Многие команды Упорядочить тестовые проекты на основе проекта, тестирования, для приложений с более чем несколькими проектами может привести к появлению большого количества тестовых проектов, особенно в том случае, если вы по-прежнему разбить их согласно вида тестов, в каждом проекте.</span><span class="sxs-lookup"><span data-stu-id="a914f-186">Many teams organize test projects based on the project they are testing, which for applications with more than a few projects can result in a large number of test projects, especially if you still break these down according to what kind of tests are in each project.</span></span> <span data-ttu-id="a914f-187">Компрометация подход заключается в один проект каждого вида тестирования каждого приложения с папками в тестовых проектов для указания проекта (и класс) тестируемой.</span><span class="sxs-lookup"><span data-stu-id="a914f-187">A compromise approach is to have one project per kind of test, per application, with folders inside the test projects to indicate the project (and class) being tested.</span></span>

<span data-ttu-id="a914f-188">Наиболее распространенный подход заключается в том, для организации проектов приложений, в папке «src» и тестовые проекты приложения в папке параллельного «тесты».</span><span class="sxs-lookup"><span data-stu-id="a914f-188">A common approach is to organize the application projects under a ‘src' folder, and the application's test projects under a parallel ‘tests' folder.</span></span> <span data-ttu-id="a914f-189">Если вам пригодиться этой организации, можно создать соответствующий папки решений в Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a914f-189">You can create matching solution folders in Visual Studio, if you find this organization useful.</span></span>

![](./media/image9-2.png)

<span data-ttu-id="a914f-190">Рис. 9-2 организации теста в решении</span><span class="sxs-lookup"><span data-stu-id="a914f-190">Figure 9-2 Test organization in your solution</span></span>

<span data-ttu-id="a914f-191">Можно использовать любой платформы тестирования вы предпочитаете.</span><span class="sxs-lookup"><span data-stu-id="a914f-191">You can use whichever test framework you prefer.</span></span> <span data-ttu-id="a914f-192">Платформа xUnit работает хорошо и является то, что все тесты ASP.NET Core и EF Core языке.</span><span class="sxs-lookup"><span data-stu-id="a914f-192">The xUnit framework works well and is what all of the ASP.NET Core and EF Core tests are written in.</span></span> <span data-ttu-id="a914f-193">Можно добавить xUnit тестовый проект в Visual Studio с помощью шаблона, показанные на рисунке 9-3, или из CLI с помощью нового xunit dotnet.</span><span class="sxs-lookup"><span data-stu-id="a914f-193">You can add an xUnit test project in Visual Studio using the template shown in Figure 9-3, or from the CLI using dotnet new xunit.</span></span>

![](./media/image9-3.png)

<span data-ttu-id="a914f-194">Рис. 9 — 3 добавьте xUnit тестовый проект в Visual Studio</span><span class="sxs-lookup"><span data-stu-id="a914f-194">Figure 9-3 Add an xUnit Test Project in Visual Studio</span></span>

### <a name="test-naming"></a><span data-ttu-id="a914f-195">Именование теста</span><span class="sxs-lookup"><span data-stu-id="a914f-195">Test Naming</span></span>

<span data-ttu-id="a914f-196">Имена, указывающие, что делает каждый тест следует присвоить имя тестов согласованным образом.</span><span class="sxs-lookup"><span data-stu-id="a914f-196">You should name your tests in a consistent fashion, with names that indicate what each test does.</span></span> <span data-ttu-id="a914f-197">Является одним из подходов было успешно с тестовых классов имя класса и метод, который они предназначены для проверки.</span><span class="sxs-lookup"><span data-stu-id="a914f-197">One approach I've had great success with is to name test classes according to the class and method they are testing.</span></span> <span data-ttu-id="a914f-198">Это приведет к много небольших тестовых классов, но упрощает очень понять, что каждый тест отвечает за.</span><span class="sxs-lookup"><span data-stu-id="a914f-198">This results in many small test classes, but it makes it extremely clear what each test is responsible for.</span></span> <span data-ttu-id="a914f-199">С именем класса теста, настройка для идентификации класса и метода, которое необходимо проверить чтобы указать поведение тестируемой можно использовать имя метода теста.</span><span class="sxs-lookup"><span data-stu-id="a914f-199">With the test class name set up to identify the class and method to be tested, the test method name can be used to specify the behavior being tested.</span></span> <span data-ttu-id="a914f-200">Это должен быть ожидаемым поведением и ни входов ни предположения, должны возвращать такое поведение.</span><span class="sxs-lookup"><span data-stu-id="a914f-200">This should include the expected behavior and any inputs or assumptions that should yield this behavior.</span></span> <span data-ttu-id="a914f-201">Некоторые примеры имен тестов:</span><span class="sxs-lookup"><span data-stu-id="a914f-201">Some example test names:</span></span>

-   <span data-ttu-id="a914f-202">CatalogControllerGetImage.CallsImageServiceWithId</span><span class="sxs-lookup"><span data-stu-id="a914f-202">CatalogControllerGetImage.CallsImageServiceWithId</span></span>

-   <span data-ttu-id="a914f-203">CatalogControllerGetImage.LogsWarningGivenImageMissingException</span><span class="sxs-lookup"><span data-stu-id="a914f-203">CatalogControllerGetImage.LogsWarningGivenImageMissingException</span></span>

-   <span data-ttu-id="a914f-204">CatalogControllerGetImage.ReturnsFileResultWithBytesGivenSuccess</span><span class="sxs-lookup"><span data-stu-id="a914f-204">CatalogControllerGetImage.ReturnsFileResultWithBytesGivenSuccess</span></span>

-   <span data-ttu-id="a914f-205">CatalogControllerGetImage.ReturnsNotFoundResultGivenImageMissingException</span><span class="sxs-lookup"><span data-stu-id="a914f-205">CatalogControllerGetImage.ReturnsNotFoundResultGivenImageMissingException</span></span>

<span data-ttu-id="a914f-206">Вариация этого подхода завершает каждым именем класса теста с «Должно» и изменяет немного времени:</span><span class="sxs-lookup"><span data-stu-id="a914f-206">A variation of this approach ends each test class name with "Should" and modifies the tense slightly:</span></span>

-   <span data-ttu-id="a914f-207">CatalogControllerGetImage**Should**.**Call**ImageServiceWithId</span><span class="sxs-lookup"><span data-stu-id="a914f-207">CatalogControllerGetImage**Should**.**Call**ImageServiceWithId</span></span>

-   <span data-ttu-id="a914f-208">CatalogControllerGetImage**следует**. **Журнал**WarningGivenImageMissingException</span><span class="sxs-lookup"><span data-stu-id="a914f-208">CatalogControllerGetImage**Should**.**Log**WarningGivenImageMissingException</span></span>

<span data-ttu-id="a914f-209">Некоторые команды Найти второй именования подход яснее, хотя немного более подробных сведений.</span><span class="sxs-lookup"><span data-stu-id="a914f-209">Some teams find the second naming approach clearer, though slightly more verbose.</span></span> <span data-ttu-id="a914f-210">В любом случае попробуйте использовать соглашение об именовании, которое обеспечивает представление о поведении тестового, чтобы в случае сбоя одного или нескольких тестов очевидна из их имен, какие варианты произошел сбой.</span><span class="sxs-lookup"><span data-stu-id="a914f-210">In any case, try to use a naming convention that provides insight into test behavior, so that when one or more tests fail, it's obvious from their names what cases have failed.</span></span> <span data-ttu-id="a914f-211">Избегайте именования вы тесты которое отдаленно, такие как ControllerTests.Test1, как они предлагают нет значения, когда они отображаются в результатах теста.</span><span class="sxs-lookup"><span data-stu-id="a914f-211">Avoid naming you tests vaguely, such as ControllerTests.Test1, as these offer no value when you see them in test results.</span></span>

<span data-ttu-id="a914f-212">Если следовать соглашение об именовании, как приведенная выше, создает множество небольших тестовых классов, рекомендуется для дальнейшей организации тесты с помощью папок и пространств имен.</span><span class="sxs-lookup"><span data-stu-id="a914f-212">If you follow a naming convention like the one above that produces many small test classes, it's a good idea to further organize your tests using folders and namespaces.</span></span> <span data-ttu-id="a914f-213">Рис. 9-4 показывает один из подходов к организация тестов по папкам в нескольких тестовых проектов.</span><span class="sxs-lookup"><span data-stu-id="a914f-213">Figure 9-4 shows one approach to organizing tests by folder within several test projects.</span></span>

![](./media/image9-4.png)

<span data-ttu-id="a914f-214">**Рис. 9-4.**</span><span class="sxs-lookup"><span data-stu-id="a914f-214">**Figure 9-4.**</span></span> <span data-ttu-id="a914f-215">Упорядочение тестовых классов на основе класса тестируемой папке.</span><span class="sxs-lookup"><span data-stu-id="a914f-215">Organizing test classes by folder based on class being tested.</span></span>

<span data-ttu-id="a914f-216">Конечно Если класс конкретного приложения имеет множество методов тестируемой (и тем самым число тестовых классов), смысл поместить их в папку, соответствующую класс приложения.</span><span class="sxs-lookup"><span data-stu-id="a914f-216">Of course, if a particular application class has many methods being tested (and thus many test classes), it may make sense to place these in a folder corresponding to the application class.</span></span> <span data-ttu-id="a914f-217">Эта организация ничем не отличается от как можно организовать файлы по папкам в другом месте.</span><span class="sxs-lookup"><span data-stu-id="a914f-217">This organization is no different than how you might organize files into folders elsewhere.</span></span> <span data-ttu-id="a914f-218">При наличии более чем на три или четыре связанных файлов в папке, содержащей много файлов, часто бывает полезно переместить их в собственной подпапке.</span><span class="sxs-lookup"><span data-stu-id="a914f-218">If you have more than three or four related files in a folder containing many other files, it's often helpful to move them into their own subfolder.</span></span>

## <a name="unit-testing-aspnet-core-apps"></a><span data-ttu-id="a914f-219">Тестирование приложений ASP.NET Core единицы</span><span class="sxs-lookup"><span data-stu-id="a914f-219">Unit Testing ASP.NET Core Apps</span></span>

<span data-ttu-id="a914f-220">В хорошо спроектированные приложения ASP.NET Core большая часть сложности и бизнес-логики, будет инкапсулирован в бизнес-сущности и различных служб.</span><span class="sxs-lookup"><span data-stu-id="a914f-220">In a well-designed ASP.NET Core application, most of the complexity and business logic will be encapsulated in business entities and a variety of services.</span></span> <span data-ttu-id="a914f-221">Приложения ASP.NET MVC ядра, с контроллеров, фильтры, viewmodels и представления, требуется очень мало модульных тестов.</span><span class="sxs-lookup"><span data-stu-id="a914f-221">The ASP.NET Core MVC app itself, with its controllers, filters, viewmodels, and views, should require very few unit tests.</span></span> <span data-ttu-id="a914f-222">Большинство функций данное действие находится за пределами сам метод.</span><span class="sxs-lookup"><span data-stu-id="a914f-222">Much of the functionality of a given action lies outside the action method itself.</span></span> <span data-ttu-id="a914f-223">Тестирования ли Маршрутизация работает правильно и обработка ошибок глобального, не может выполняться эффективно с модульным тестом.</span><span class="sxs-lookup"><span data-stu-id="a914f-223">Testing whether routing works correctly, or global error handling, cannot be done effectively with a unit test.</span></span> <span data-ttu-id="a914f-224">Аналогичным образом любые фильтры, включая проверки модели и проверку подлинности и фильтры авторизации, не может быть модульного тестирования.</span><span class="sxs-lookup"><span data-stu-id="a914f-224">Likewise, any filters, including model validation and authentication and authorization filters, cannot be unit tested.</span></span> <span data-ttu-id="a914f-225">Без этих источников поведение большинство методов действий обычно невелико элементарно, делегирование большая часть работы до служб, которые можно протестировать независимо от контроллера, который использует их.</span><span class="sxs-lookup"><span data-stu-id="a914f-225">Without these sources of behavior, most action methods should be trivially small, delegating the bulk of their work to services that can be tested independent of the controller that uses them.</span></span>

<span data-ttu-id="a914f-226">Иногда необходимо рефакторинга кода в модульный тест его порядок.</span><span class="sxs-lookup"><span data-stu-id="a914f-226">Sometimes you'll need to refactor your code in order to unit test it.</span></span> <span data-ttu-id="a914f-227">Зачастую это включает в себя определение абстракции и с помощью внедрения зависимости, для доступа к абстракции в код, который вы хотите протестировать, а не кодирование непосредственно на инфраструктуру.</span><span class="sxs-lookup"><span data-stu-id="a914f-227">Frequently this involves identifying abstractions and using dependency injection to access the abstraction in the code you'd like to test, rather than coding directly against infrastructure.</span></span> <span data-ttu-id="a914f-228">Например рассмотрим этот метод простое действие для отображения изображения.</span><span class="sxs-lookup"><span data-stu-id="a914f-228">For example, consider this simple action method for displaying images:</span></span>

```cs
[HttpGet("[controller]/pic/{id}")]
public IActionResult GetImage(int id)
{
    var contentRoot = _env.ContentRootPath + "//Pics";
    var path = Path.Combine(contentRoot, id + ".png");
    Byte[] b = System.IO.File.ReadAllBytes(path);
    return File(b, "image/png");
}
```

<span data-ttu-id="a914f-229">Модульное тестирование этого метода усложняла его прямых зависимостей от System.IO.File, который используется для чтения из файловой системы.</span><span class="sxs-lookup"><span data-stu-id="a914f-229">Unit testing this method is made difficult by its direct dependency on System.IO.File, which it uses to read from the file system.</span></span> <span data-ttu-id="a914f-230">Вы можете протестировать таким образом, чтобы он работает нужным образом, но это с реальными файлами относится к интеграционному тесту.</span><span class="sxs-lookup"><span data-stu-id="a914f-230">You can test this behavior to ensure it works as expected, but doing so with real files is an integration test.</span></span> <span data-ttu-id="a914f-231">Стоит отметить невозможно протестировать этот метод маршрута — будет показано, как это сделать с помощью функционального теста чуть ниже.</span><span class="sxs-lookup"><span data-stu-id="a914f-231">It's worth noting you can't test this method's route – you'll see how to do this with a functional test shortly.</span></span>

<span data-ttu-id="a914f-232">Если модульный тест поведение файловой системы невозможно напрямую и не может проверить маршрут, что существует для тестирования?</span><span class="sxs-lookup"><span data-stu-id="a914f-232">If you can't unit test the file system behavior directly, and you can't test the route, what is there to test?</span></span> <span data-ttu-id="a914f-233">Также после рефакторинга, чтобы сделать возможным модульное тестирование, вы можете заметить некоторые тестовые случаи и отсутствует поведения, такие как обработка ошибок.</span><span class="sxs-lookup"><span data-stu-id="a914f-233">Well, after refactoring to make unit testing possible, you may discover some test cases and missing behavior, such as error handling.</span></span> <span data-ttu-id="a914f-234">Что делает метод? Если файл не найден</span><span class="sxs-lookup"><span data-stu-id="a914f-234">What does the method do when a file isn't found?</span></span> <span data-ttu-id="a914f-235">Что следует делать?</span><span class="sxs-lookup"><span data-stu-id="a914f-235">What should it do?</span></span> <span data-ttu-id="a914f-236">В этом примере рефакторингу метод выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="a914f-236">In this example, the refactored method looks like this:</span></span>

```cs
[HttpGet("[controller]/pic/{id}")\]
public IActionResult GetImage(int id)
{
    byte[] imageBytes;
    try
    {
        imageBytes = _imageService.GetImageBytesById(id);
    }
    catch (CatalogImageMissingException ex)
    {
        _logger.LogWarning($"No image found for id: {id}");
        return NotFound();
    }
    return File(imageBytes, "image/png");
}
```

<span data-ttu-id="a914f-237">\_Средства ведения журнала и \_imageService введенного как зависимости.</span><span class="sxs-lookup"><span data-stu-id="a914f-237">The \_logger and \_imageService are both injected as dependencies.</span></span> <span data-ttu-id="a914f-238">Теперь можно проверить, передается таким же идентификатором, который передается в метод действия \_imageService, и что полученные байты возвращаются как часть FileResult.</span><span class="sxs-lookup"><span data-stu-id="a914f-238">Now you can test that the same id that is passed to the action method is passed to the \_imageService, and that the resulting bytes are returned as part of the FileResult.</span></span> <span data-ttu-id="a914f-239">Также можно проверить, происходит ошибка ведения журнала должным образом и, результат не найдено, возвращается, если изображение отсутствует, при условии, что это поведение важные приложения (то есть, не только временные код разработчика, добавлены диагностику проблем).</span><span class="sxs-lookup"><span data-stu-id="a914f-239">You can also test that error logging is happening as expected, and that a NotFound result is returned if the image is missing, assuming this is important application behavior (that is, not just temporary code the developer added to diagnose an issue).</span></span> <span data-ttu-id="a914f-240">Логика фактический файл был перемещен в отдельную реализацию службы и были включены для возврата конкретного приложения исключение в случае отсутствующий файл.</span><span class="sxs-lookup"><span data-stu-id="a914f-240">The actual file logic has moved into a separate implementation service, and has been augmented to return an application-specific exception for the case of a missing file.</span></span> <span data-ttu-id="a914f-241">Можно проверить Эта реализация независимо друг от друга, используя интеграционного теста.</span><span class="sxs-lookup"><span data-stu-id="a914f-241">You can test this implementation independently, using an integration test.</span></span>

## <a name="integration-testing-aspnet-core-apps"></a><span data-ttu-id="a914f-242">Тестирование приложений ASP.NET Core интеграции</span><span class="sxs-lookup"><span data-stu-id="a914f-242">Integration Testing ASP.NET Core Apps</span></span>

```cs
    }
        catch (FileNotFoundException ex)
        {
            throw new CatalogImageMissingException(ex);
        }
    }
}
```

<span data-ttu-id="a914f-243">Эта служба использует IHostingEnvironment так же, как и CatalogController, код был до рефакторинга в отдельной службе.</span><span class="sxs-lookup"><span data-stu-id="a914f-243">This service uses the IHostingEnvironment, just as the CatalogController code did before it was refactored into a separate service.</span></span> <span data-ttu-id="a914f-244">Поскольку она только код в контроллер, который используется IHostingEnvironment зависимость был удален из CatalogController в конструктор.</span><span class="sxs-lookup"><span data-stu-id="a914f-244">Since this was the only code in the controller that used IHostingEnvironment, that dependency was removed from CatalogController's constructor.</span></span>

<span data-ttu-id="a914f-245">Чтобы проверить, что эта служба работает правильно, необходимо создать файл образа известных тестирования и убедитесь, что служба возвращает его с учетом определенных входных данных.</span><span class="sxs-lookup"><span data-stu-id="a914f-245">To test that this service works correctly, you need to create a known test image file and verify that the service returns it given a specific input.</span></span> <span data-ttu-id="a914f-246">Следует позаботиться отказаться от использования макетов объектов на поведение, которое фактически необходимо проверить (в данном случае чтение из файловой системы).</span><span class="sxs-lookup"><span data-stu-id="a914f-246">You should take care not to use mock objects on the behavior you actually want to test (in this case, reading from the file system).</span></span> <span data-ttu-id="a914f-247">Однако макетов объектов, по-прежнему можно использовать для настройки интеграции тестов.</span><span class="sxs-lookup"><span data-stu-id="a914f-247">However, mock objects may still be useful to set up integration tests.</span></span> <span data-ttu-id="a914f-248">В этом случае можно макета IHostingEnvironment, чтобы его ContentRootPath указывал на папку, которую вы собираетесь использовать для тестирования изображения.</span><span class="sxs-lookup"><span data-stu-id="a914f-248">In this case, you can mock IHostingEnvironment so that its ContentRootPath points to the folder you're going to use for your test image.</span></span> <span data-ttu-id="a914f-249">Полный рабочий интеграции тестового класса приведен ниже:</span><span class="sxs-lookup"><span data-stu-id="a914f-249">The complete working integration test class is shown here:</span></span>

```cs
public class LocalFileImageServiceGetImageBytesById
{
    private byte[] _testBytes = new byte[] { 0x01, 0x02, 0x03 };
    private readonly Mock<IHostingEnvironment> _mockEnvironment = new Mock<IHostingEnvironment>();
    private int _testImageId = 123;
    private string _testFileName = "123.png";
    public LocalFileImageServiceGetImageBytesById()
    {
        // create folder if necessary
        Directory.CreateDirectory(Path.Combine(GetFileDirectory(), "Pics"));
        string filePath = GetFilePath(_testFileName);
        System.IO.File.WriteAllBytes(filePath, _testBytes);
        _mockEnvironment.SetupGet<string>(m => m.ContentRootPath).Returns(GetFileDirectory());
    }
    private string GetFilePath(string fileName)
    {
        return Path.Combine(GetFileDirectory(), "Pics", fileName);
        }
            private string GetFileDirectory()
        {
            var location = System.Reflection.Assembly.GetEntryAssembly().Location;
            return Path.GetDirectoryName(location);
        }

        [Fact]
        public void ReturnsFileContentResultGivenValidId()
        {
            var fileService = new LocalFileImageService(_mockEnvironment.Object);
            var result = fileService.GetImageBytesById(_testImageId);
            Assert.Equal(_testBytes, result);
        }
    }
```

> [!NOTE]
> <span data-ttu-id="a914f-250">что сам тест является очень простой большая часть кода необходим для настройки системы и создание инфраструктура тестирования (в данном случае фактический файл для чтения с диска).</span><span class="sxs-lookup"><span data-stu-id="a914f-250">that the test itself is very simple – the bulk of the code is necessary to configure the system and create the testing infrastructure (in this case, an actual file to be read from disk).</span></span> <span data-ttu-id="a914f-251">Это характерно для тестирования интеграции, которые часто требуют установки более сложных действий, чем модульные тесты.</span><span class="sxs-lookup"><span data-stu-id="a914f-251">This is typical for integration tests, which often require more complex setup work than unit tests.</span></span>

## <a name="functional-testing-aspnet-core-apps"></a><span data-ttu-id="a914f-252">Режим работы тестирование приложений ASP.NET Core</span><span class="sxs-lookup"><span data-stu-id="a914f-252">Functional Testing ASP.NET Core Apps</span></span>

<span data-ttu-id="a914f-253">Для приложений ASP.NET Core TestServer класс упрощает функциональных тестов довольно для записи.</span><span class="sxs-lookup"><span data-stu-id="a914f-253">For ASP.NET Core applications, the TestServer class makes functional tests fairly easy to write.</span></span> <span data-ttu-id="a914f-254">TestServer, с помощью WebHostBuilder настраивается так же, как обычно для вашего приложения.</span><span class="sxs-lookup"><span data-stu-id="a914f-254">You configure a TestServer using a WebHostBuilder, just as you normally do for your application.</span></span> <span data-ttu-id="a914f-255">Это WebHostBuilder должен быть настроен так же, как реальные ведущего приложения, но вы можете изменить все аспекты, которые упростить тестирование.</span><span class="sxs-lookup"><span data-stu-id="a914f-255">This WebHostBuilder should be configured just like your application's real host, but you can modify any aspects of it that make testing easier.</span></span> <span data-ttu-id="a914f-256">В большинстве случаев, использовать какой-либо же TestServer для количество тестовых случаев, поэтому он может инкапсулировать в повторно используемый метод (возможно, в базовый класс):</span><span class="sxs-lookup"><span data-stu-id="a914f-256">Most of the time, you'll reuse the same TestServer for many test cases, so you can encapsulate it in a reusable method (perhaps in a base class):</span></span>

```cs
public abstract class BaseWebTest
{
    protected readonly HttpClient _client;
    protected string _contentRoot;

    public BaseWebTest()
    {
        _client = GetClient();
    }
    
    protected HttpClient GetClient()
    {
        var startupAssembly = typeof(Startup).GetTypeInfo().Assembly;
        _contentRoot = GetProjectPath("src", startupAssembly);
        var builder = new WebHostBuilder()
        .UseContentRoot(_contentRoot)
        .UseStartup&lt;Startup&gt;();
        var server = new TestServer(builder);
        var client = server.CreateClient();
        return client;
    }
}
```

<span data-ttu-id="a914f-257">Getprojectpath-метод просто возвращает физический путь в веб-проект (Загрузка примера решения).</span><span class="sxs-lookup"><span data-stu-id="a914f-257">The GetProjectPath method simply returns the physical path to the web project (download sample solution).</span></span> <span data-ttu-id="a914f-258">WebHostBuilder в этом случае просто указывает, где — корневой элемент содержимого для веб-приложения, а ссылается на же класс запуска, используемых в реальных веб-приложения.</span><span class="sxs-lookup"><span data-stu-id="a914f-258">The WebHostBuilder in this case simply specifies where the content root for the web application is, and references the same Startup class the real web application uses.</span></span> <span data-ttu-id="a914f-259">Для работы с TestServer, используется стандартный тип System.Net.HttpClient делать запросы к нему.</span><span class="sxs-lookup"><span data-stu-id="a914f-259">To work with the TestServer, you use the standard System.Net.HttpClient type to make requests to it.</span></span> <span data-ttu-id="a914f-260">TestServer предоставляет полезные метод CreateClient, который предоставляет предварительно настроенный клиент, который будет готов для выполнения запросов для приложения, работающего на TestServer.</span><span class="sxs-lookup"><span data-stu-id="a914f-260">TestServer exposes a helpful CreateClient method that provides a pre-configured client that is ready to make requests to the application running on the TestServer.</span></span> <span data-ttu-id="a914f-261">Используйте этот клиент (присвоено защищенный \_участника клиент на базовый тест выше) при записи функциональных тестов для приложения ASP.NET Core:</span><span class="sxs-lookup"><span data-stu-id="a914f-261">You use this client (set to the protected \_client member on the base test above) when writing functional tests for your ASP.NET Core application:</span></span>

```cs
public class CatalogControllerGetImage : BaseWebTest
{
    [Fact]
    public async Task ReturnsFileContentResultGivenValidId()
    {
        var testFilePath = Path.Combine(_contentRoot, "pics//1.png");
        var expectedFileBytes = File.ReadAllBytes(testFilePath);
        var response = await _client.GetAsync("/catalog/pic/1");
        response.EnsureSuccessStatusCode();
        var streamResponse = await response.Content.ReadAsStreamAsync();
        byte[] byteResult;
        using (var ms = new MemoryStream())
        {
            streamResponse.CopyTo(ms);
            byteResult = ms.ToArray();
        }
        Assert.Equal(expectedFileBytes, byteResult);
    }
}
```

<span data-ttu-id="a914f-262">Это функционального теста выполняет полный стек приложения ASP.NET Core MVC, включая все по промежуточного слоя, фильтры, связыватели, т. д., могут быть выполнены.</span><span class="sxs-lookup"><span data-stu-id="a914f-262">This functional test exercises the full ASP.NET Core MVC application stack, including all middleware, filters, binders, etc. that may be in place.</span></span> <span data-ttu-id="a914f-263">Проверяет, что данный маршрут («/ каталога и pic-1») возвращает массив байтов, ожидаемый для файла в известном расположении.</span><span class="sxs-lookup"><span data-stu-id="a914f-263">It verifies that a given route ("/catalog/pic/1") returns the expected byte array for a file in a known location.</span></span> <span data-ttu-id="a914f-264">Выполняется без настройки реальных веб-сервера и поэтому позволяет избежать большую часть хрупкость, использование реальных веб-сервер для тестирования можно столкнуться (например, проблемы с параметрами брандмауэра).</span><span class="sxs-lookup"><span data-stu-id="a914f-264">It does so without setting up a real web server, and so avoids much of the brittleness that using a real web server for testing can experience (for example, problems with firewall settings).</span></span> <span data-ttu-id="a914f-265">Функциональные тесты, выполняемые к TestServer обычно медленнее, чем интеграции и модульные тесты, но выполняется гораздо быстрее, чем тесты, которые будет выполняться по сети к серверу веб-теста.</span><span class="sxs-lookup"><span data-stu-id="a914f-265">Functional tests that run against TestServer are usually slower than integration and unit tests, but are much faster than tests that would run over the network to a test web server.</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="a914f-266">[Предыдущие] (work-with-data-in-asp-net-core-apps.md) [Далее] (разработки процесс для azure.md)</span><span class="sxs-lookup"><span data-stu-id="a914f-266">[Previous] (work-with-data-in-asp-net-core-apps.md) [Next] (development-process-for-azure.md)</span></span>
