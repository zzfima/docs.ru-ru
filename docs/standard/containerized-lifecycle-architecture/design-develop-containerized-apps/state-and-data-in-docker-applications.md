---
title: Состояние и данные в приложениях Docker
description: Узнайте, доступен параметр для сохранения состояния в контейнерных приложений.
author: CESARDELATORRE
ms.author: wiwagn
ms.date: 02/15/2019
ms.openlocfilehash: 30dde3ce44aa61fff3fad1841ae4a8b941573877
ms.sourcegitcommit: 58fc0e6564a37fa1b9b1b140a637e864c4cf696e
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/08/2019
ms.locfileid: "57678089"
---
# <a name="state-and-data-in-docker-applications"></a><span data-ttu-id="5f2eb-103">Состояние и данные в приложениях Docker</span><span class="sxs-lookup"><span data-stu-id="5f2eb-103">State and data in Docker applications</span></span>

<span data-ttu-id="5f2eb-104">Представьте себе контейнер как экземпляр процесса.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-104">In most cases, you can think of a container as an instance of a process.</span></span> <span data-ttu-id="5f2eb-105">Процесс не находится в неизменном состоянии.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-105">A process does not maintain persistent state.</span></span> <span data-ttu-id="5f2eb-106">Хотя контейнер может записывать данные в локальное хранилище, при условии, что экземпляр всегда будет на месте — например, предположив, что одного расположения в памяти будет устойчивой.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-106">While a container can write to its local storage, assuming that an instance will be around indefinitely is like assuming that a single location in memory will be durable.</span></span> <span data-ttu-id="5f2eb-107">Образы контейнеров, как и процессы, должно быть принято несколько экземпляров, и они конечном итоге будут завершены; Если они управляются оркестратором контейнеров, следует полагать, что они могут перемещаться из одного узла или виртуальной Машины на другой.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-107">Container images, like processes, should be assumed to have multiple instances and that they will eventually be killed; if they're managed with a container orchestrator, it should be assumed that they might get moved from one node or VM to another.</span></span>

<span data-ttu-id="5f2eb-108">Для управления постоянными данными в приложениях Docker используются следующие решения:</span><span class="sxs-lookup"><span data-stu-id="5f2eb-108">The following solutions are used to manage persistent data in Docker applications:</span></span>

<span data-ttu-id="5f2eb-109">Из узла Docker в качестве [томов Docker](https://docs.docker.com/engine/admin/volumes/):</span><span class="sxs-lookup"><span data-stu-id="5f2eb-109">From the Docker host, as [Docker Volumes](https://docs.docker.com/engine/admin/volumes/):</span></span>

- <span data-ttu-id="5f2eb-110">**Тома** хранятся в той части файловой системы узла, которая находится под управлением Docker.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-110">**Volumes** are stored in an area of the host filesystem that's managed by Docker.</span></span>

- <span data-ttu-id="5f2eb-111">**Привязать подключение** можно сопоставить с любой папки в файловой системе узла, поэтому доступ не может управляться из процесса Docker и может представлять угрозу для безопасности, а контейнер может получить доступ к конфиденциальным папки операционной системы.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-111">**Bind mounts** can map to any folder in the host filesystem, so access can't be controlled from a Docker process and can pose a security risk as a container could access sensitive OS folders.</span></span>

- <span data-ttu-id="5f2eb-112">**Подключения tmpfs** аналогичны виртуальным папкам, которые существуют только в памяти узла и никогда не записываются в файловую систему.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-112">**tmpfs mounts** are like virtual folders that only exist in the host's memory and are never written to the filesystem.</span></span>

<span data-ttu-id="5f2eb-113">Из удаленного хранилища:</span><span class="sxs-lookup"><span data-stu-id="5f2eb-113">From remote storage:</span></span>

- <span data-ttu-id="5f2eb-114">[Служба хранилища Azure](https://azure.microsoft.com/documentation/services/storage/) предоставляет геораспределенное хранилище, предоставляя неплохим долговременным решением сохраняемости для контейнеров.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-114">[Azure Storage](https://azure.microsoft.com/documentation/services/storage/) provides geo-distributable storage, providing a good long-term persistence solution for containers.</span></span>

- <span data-ttu-id="5f2eb-115">Удаленные реляционные базы данных, такие как [базы данных SQL Azure](https://azure.microsoft.com/services/sql-database/), базы данных NoSQL [Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), или кэшировать служб, таких как [Redis](https://redis.io/).</span><span class="sxs-lookup"><span data-stu-id="5f2eb-115">Remote relational databases like [Azure SQL Database](https://azure.microsoft.com/services/sql-database/), NoSQL databases like [Azure Cosmos DB](https://docs.microsoft.com/azure/cosmos-db/introduction), or cache services like [Redis](https://redis.io/).</span></span>

<span data-ttu-id="5f2eb-116">Из контейнера Docker:</span><span class="sxs-lookup"><span data-stu-id="5f2eb-116">From the Docker container:</span></span>

- <span data-ttu-id="5f2eb-117">Docker имеет функцию под названием *оверлейная файловая система*.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-117">Docker provides a feature named the *overlay file system*.</span></span> <span data-ttu-id="5f2eb-118">Эта функция реализует задачу копирования при записи, что хранилищ обновленные сведения о корневой файловой системы контейнера.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-118">This feature implements a copy-on-write task that stores updated information to the root file system of the container.</span></span> <span data-ttu-id="5f2eb-119">Эти сведения «закладывает в верхней части» исходное изображение, на котором базируется контейнер.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-119">That information "lays on top of" the original image on which the container is based.</span></span> <span data-ttu-id="5f2eb-120">Если удалить контейнер из системы, эти изменения будут утеряны.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-120">If the container is deleted from the system, those changes are lost.</span></span> <span data-ttu-id="5f2eb-121">Таким образом хотя можно сохранить состояние контейнера в его локальном хранилище, проектирование системы на основе этой функции будет противоречить конструкции контейнера, который по умолчанию без отслеживания состояния.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-121">Therefore, while it's possible to save the state of a container within its local storage, designing a system based on this feature would conflict with the premise of container design, which by default is stateless.</span></span>

- <span data-ttu-id="5f2eb-122">Однако тома Docker теперь является предпочтительным способом для обработки локальных данных в Docker.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-122">However, Docker Volumes is now the preferred way to handle local data in Docker.</span></span> <span data-ttu-id="5f2eb-123">Дополнительные сведения о хранилище в контейнерах, см. на [драйверы Docker](https://docs.docker.com/engine/userguide/storagedriver/) и [об образах, контейнеры и драйверы запоминающих устройств](https://docs.docker.com/engine/userguide/storagedriver/imagesandcontainers/).</span><span class="sxs-lookup"><span data-stu-id="5f2eb-123">If you need more information about storage in containers, check on [Docker storage drivers](https://docs.docker.com/engine/userguide/storagedriver/) and [About images, containers, and storage drivers](https://docs.docker.com/engine/userguide/storagedriver/imagesandcontainers/).</span></span>

<span data-ttu-id="5f2eb-124">Ниже приведены дополнительные сведения об этих параметрах.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-124">The following provides additional detail about these options.</span></span>

<span data-ttu-id="5f2eb-125">**Тома** — это каталоги из ОС узла, сопоставленные с каталогами в контейнерах.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-125">**Volumes** are directories mapped from the host OS to directories in containers.</span></span> <span data-ttu-id="5f2eb-126">Если код в контейнере имеет доступ к каталогу, на самом деле он обращается к каталогу на ОС узла.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-126">When code in the container has access to the directory, that access is actually to a directory on the host OS.</span></span> <span data-ttu-id="5f2eb-127">Этот каталог не привязан к времени существования самого контейнера, находится под управлением Docker и изолирован от основных функциональных возможностей хост-компьютера.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-127">This directory is not tied to the lifetime of the container itself, and the directory is managed by Docker and isolated from the core functionality of the host machine.</span></span> <span data-ttu-id="5f2eb-128">Поэтому тома данных хранят данные независимо от контейнера.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-128">Thus, data volumes are designed to persist data independently of the life of the container.</span></span> <span data-ttu-id="5f2eb-129">Если удалить контейнер или образ из узла Docker, данные из томов данных не будут удалены.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-129">If you delete a container or an image from the Docker host, the data persisted in the data volume is not deleted.</span></span>

<span data-ttu-id="5f2eb-130">Тома могут быть именованными или анонимными (по умолчанию).</span><span class="sxs-lookup"><span data-stu-id="5f2eb-130">Volumes can be named or anonymous (the default).</span></span> <span data-ttu-id="5f2eb-131">Именованные тома — это следующий этап развития **контейнеров томов данных**. Они упрощают обмен данными между контейнерами.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-131">Named volumes are the evolution of **Data Volume Containers** and make it easy to share data between containers.</span></span> <span data-ttu-id="5f2eb-132">Тома также поддерживает драйверы томов, которые позволяют хранить данные на удаленных узлах, т. д.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-132">Volumes also support volume drivers that allow you to store data on remote hosts, among other options.</span></span>

<span data-ttu-id="5f2eb-133">**Привязать подключение** были доступны в течение длительного времени, а также разрешить сопоставление любой папки в точку подключения в контейнере.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-133">**Bind mounts** have been available for a long time and allow the mapping of any folder to a mount point in a container.</span></span> <span data-ttu-id="5f2eb-134">Подключения привязки имеют больше ограничений, чем тома, и с ними связаны некоторые проблемы безопасности, поэтому тома являются рекомендуемым способом.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-134">Bind mounts have more limitations than volumes and some important security issues, so volumes are the recommended option.</span></span>

<span data-ttu-id="5f2eb-135">**`tmpfs` Подключает** — это виртуальные папки, которые находятся только в оперативной памяти на узле и никогда не записываются в файловую систему.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-135">**`tmpfs` mounts** are virtual folders that live only in the host's memory and are never written to the filesystem.</span></span> <span data-ttu-id="5f2eb-136">Они быстрые и безопасные, но используют ресурсы памяти и предназначены только для временных данных.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-136">They are fast and secure but use memory and are only meant for non-persistent data.</span></span>

<span data-ttu-id="5f2eb-137">Как показано на рисунке 4-5, обычные тома Docker могут храниться за пределами самих контейнерами, но в физических границах сервера узла или виртуальной машины.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-137">As shown in Figure 4-5, regular Docker volumes can be stored outside of the containers themselves but within the physical boundaries of the host server or VM.</span></span> <span data-ttu-id="5f2eb-138">Тем не менее контейнеры Docker с одного сервера узла или виртуальной машины не могут обращаться к тому на другом сервере узла или виртуальной машине.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-138">However, Docker containers cannot access a volume from one host server or VM to another.</span></span> <span data-ttu-id="5f2eb-139">Другими словами, с помощью этих томов невозможно управлять данными контейнеров, которые выполняются на разных узлах Docker, хотя такой режим работы можно реализовать с помощью драйвера томов, который поддерживает удаленные узлы.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-139">In other words, with these volumes, it isn't possible to manage data shared between containers that run on different Docker hosts, although it could be achieved with a volume driver that supports remote hosts.</span></span>

![<span data-ttu-id="5f2eb-140">Тома могут совместно использоваться несколькими контейнерами, но только на одном узле, если вы не используете удаленный драйвер с поддержкой удаленных узлов.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-140">Volumes can be shared between containers, but only in the same host, unless you use a remote driver that supports remote hosts.</span></span> ](./media/image5.png)

<span data-ttu-id="5f2eb-141">**Рис. 4-5**.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-141">**Figure 4-5**.</span></span> <span data-ttu-id="5f2eb-142">Тома и внешние источники данных для приложений на основе контейнера</span><span class="sxs-lookup"><span data-stu-id="5f2eb-142">Volumes and external data sources for container-based applications</span></span>

<span data-ttu-id="5f2eb-143">Кроме того, когда контейнеры Docker управляются оркестратором, контейнеры могут "перемещаться" между узлами в рамках оптимизации, выполняемой кластером.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-143">In addition, when Docker containers are managed by an orchestrator, containers might "move" between hosts, depending on the optimizations performed by the cluster.</span></span> <span data-ttu-id="5f2eb-144">Поэтому тома данных не рекомендуется использовать для бизнес-данных.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-144">Therefore, it isn't recommended that you use data volumes for business data.</span></span> <span data-ttu-id="5f2eb-145">Но они являются прекрасным механизмом для работы с файлами трассировки, временными файлами или аналогичные, которая не влияет на целостность бизнес-данных.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-145">But they are a good mechanism to work with trace files, temporal files, or similar, that will not impact business data consistency.</span></span>

<span data-ttu-id="5f2eb-146">**Удаленные источники данных и инструменты кэширования**, такие как база данных Azure SQL, Azure Cosmos DB или служба кэша, например Redis, можно использовать в упакованных в контейнеры приложениях так же, как они используются при разработке без контейнеров.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-146">**Remote data sources and cache** tools like Azure SQL Database, Azure Cosmos DB, or a remote cache like Redis can be used in containerized applications the same way they are used when developing without containers.</span></span> <span data-ttu-id="5f2eb-147">Это проверенный способ хранения данных бизнес-приложений.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-147">This is a proven way to store business application data.</span></span>

<span data-ttu-id="5f2eb-148">**Служба хранилища Azure**</span><span class="sxs-lookup"><span data-stu-id="5f2eb-148">**Azure Storage.**</span></span> <span data-ttu-id="5f2eb-149">Обычно бизнес-данных должна находиться во внешних ресурсах или базах данных, например службы хранилища Azure.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-149">Business data usually needs to be placed in external resources or databases, like Azure Storage.</span></span> <span data-ttu-id="5f2eb-150">Хранилище Azure предоставляет следующие службы в облаке.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-150">Azure Storage provides the following services in the cloud:</span></span>

- <span data-ttu-id="5f2eb-151">Хранилище BLOB-объектов хранит неструктурированные данные объектов.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-151">Blob storage stores unstructured object data.</span></span> <span data-ttu-id="5f2eb-152">Большой двоичный объект может представлять собой текстовые или двоичные данные любого типа, например документ или файл мультимедиа (файлы с изображением, аудио и видео).</span><span class="sxs-lookup"><span data-stu-id="5f2eb-152">A blob can be any type of text or binary data, such as document or media files (images, audio, and video files).</span></span> <span data-ttu-id="5f2eb-153">Хранилище BLOB-объектов также называют хранилищем объектов.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-153">Blob storage is also referred to as Object storage.</span></span>

- <span data-ttu-id="5f2eb-154">Хранилище файлов предоставляет общее хранилище для приложений прежних версий, с помощью стандартного протокола SMB.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-154">File storage offers shared storage for legacy applications using the standard SMB protocol.</span></span> <span data-ttu-id="5f2eb-155">Виртуальные машины Azure и облачные службы могут совместно использовать данные файлов в компонентах приложений через подключенный общий ресурс.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-155">Azure virtual machines and cloud services can share file data across application components via mounted shares.</span></span> <span data-ttu-id="5f2eb-156">Локальные приложения могут обращаться к данным файлов совместно с помощью API REST службы файлов.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-156">On-premises applications can access file data in a share via the File Service REST API.</span></span>

- <span data-ttu-id="5f2eb-157">Хранилище таблиц содержит структурированные наборы данных.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-157">Table storage stores structured datasets.</span></span> <span data-ttu-id="5f2eb-158">Хранилище таблиц — это хранилище данных NoSQL типа "ключ-значение", обеспечивающее быструю разработку и доступ к большим объемам данных.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-158">Table storage is a NoSQL key-attribute data store, which allows rapid development and fast access to large quantities of data.</span></span>

<span data-ttu-id="5f2eb-159">**Реляционные базы данных и базы данных NoSQL.**</span><span class="sxs-lookup"><span data-stu-id="5f2eb-159">**Relational databases and NoSQL databases.**</span></span> <span data-ttu-id="5f2eb-160">Существует множество вариантов внешних баз данных — реляционные базы данных, такие как SQL Server, PostgreSQL, Oracle или базы данных NoSQL, например Azure Cosmos DB, MongoDB и т. д. Эти базы данных не будут рассматриваться как части этого руководства, так как они являются полностью посвящен новой теме.</span><span class="sxs-lookup"><span data-stu-id="5f2eb-160">There are many choices for external databases, from relational databases like SQL Server, PostgreSQL, Oracle, or NoSQL databases like Azure Cosmos DB, MongoDB, etc. These databases are not going to be explained as part of this guide since they are a different topic altogether.</span></span>

>[!div class="step-by-step"]
><span data-ttu-id="5f2eb-161">[Назад](monolithic-applications.md)
>[Вперед](soa-applications.md)</span><span class="sxs-lookup"><span data-stu-id="5f2eb-161">[Previous](monolithic-applications.md)
[Next](soa-applications.md)</span></span>
