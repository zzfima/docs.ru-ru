---
title: Миграция с Newtonsoft.Json на System.Text.Json — .NET
author: tdykstra
ms.author: tdykstra
no-loc:
- System.Text.Json
- Newtonsoft.Json
ms.date: 01/10/2020
helpviewer_keywords:
- JSON serialization
- serializing objects
- serialization
- objects, serializing
ms.openlocfilehash: d84b6d16d529914c87d42bf12ce17dc7093fe9ee
ms.sourcegitcommit: 09b4090b78f52fd09b0e430cd4b26576f1fdf96e
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/17/2020
ms.locfileid: "76211971"
---
# <a name="how-to-migrate-from-opno-locnewtonsoftjson-to-opno-locsystemtextjson"></a><span data-ttu-id="e511a-102">Переход с Newtonsoft.Json на System.Text.Json</span><span class="sxs-lookup"><span data-stu-id="e511a-102">How to migrate from Newtonsoft.Json to System.Text.Json</span></span>

<span data-ttu-id="e511a-103">В этой статье показано, как выполнить миграцию с [Newtonsoft.Json](https://www.newtonsoft.com/json) на <xref:System.Text.Json>.</span><span class="sxs-lookup"><span data-stu-id="e511a-103">This article shows how to migrate from [Newtonsoft.Json](https://www.newtonsoft.com/json) to <xref:System.Text.Json>.</span></span>

<span data-ttu-id="e511a-104">`System.Text.Json` фокусируется в первую очередь на производительность, безопасность и соответствие стандартам.</span><span class="sxs-lookup"><span data-stu-id="e511a-104">`System.Text.Json` focuses primarily on performance, security, and standards compliance.</span></span> <span data-ttu-id="e511a-105">Он имеет некоторые ключевые отличия в поведении по умолчанию и не имеет функции соответствия `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-105">It has some key differences in default behavior and doesn't aim to have feature parity with `Newtonsoft.Json`.</span></span> <span data-ttu-id="e511a-106">В некоторых сценариях `System.Text.Json` не имеет встроенных функций, но рекомендуется использовать их.</span><span class="sxs-lookup"><span data-stu-id="e511a-106">For some scenarios, `System.Text.Json` has no built-in functionality, but there are recommended workarounds.</span></span> <span data-ttu-id="e511a-107">В других сценариях обходные пути нецелесообразны.</span><span class="sxs-lookup"><span data-stu-id="e511a-107">For other scenarios, workarounds are impractical.</span></span> <span data-ttu-id="e511a-108">Если приложение зависит от отсутствующей функции, попробуйте добавить [проблему](https://github.com/dotnet/runtime/issues/new) , чтобы узнать, можно ли добавлять поддержку для вашего сценария.</span><span class="sxs-lookup"><span data-stu-id="e511a-108">If your application depends on a missing feature, consider [filing an issue](https://github.com/dotnet/runtime/issues/new) to find out if support for your scenario can be added.</span></span>

<!-- For information about which features might be added in future releases, see the [Roadmap](https://github.com/dotnet/runtime/tree/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/roadmap/README.md). [Restore this when the roadmap is updated.]-->

<span data-ttu-id="e511a-109">Большая часть этой статьи посвящена использованию <xref:System.Text.Json.JsonSerializer> API, но также содержит рекомендации по использованию <xref:System.Text.Json.JsonDocument> (который представляет модель DOM или модель DOM), <xref:System.Text.Json.Utf8JsonReader>и типы <xref:System.Text.Json.Utf8JsonWriter>.</span><span class="sxs-lookup"><span data-stu-id="e511a-109">Most of this article is about how to use the <xref:System.Text.Json.JsonSerializer> API, but it also includes guidance on how to use the <xref:System.Text.Json.JsonDocument> (which represents the Document Object Model or DOM), <xref:System.Text.Json.Utf8JsonReader>, and <xref:System.Text.Json.Utf8JsonWriter> types.</span></span>

## <a name="table-of-differences-between-opno-locnewtonsoftjson-and-opno-locsystemtextjson"></a><span data-ttu-id="e511a-110">Таблица различий между Newtonsoft.Json и System.Text.Json</span><span class="sxs-lookup"><span data-stu-id="e511a-110">Table of differences between Newtonsoft.Json and System.Text.Json</span></span>

<span data-ttu-id="e511a-111">В следующей таблице перечислены `Newtonsoft.Json` функции и `System.Text.Json` эквиваленты.</span><span class="sxs-lookup"><span data-stu-id="e511a-111">The following table lists `Newtonsoft.Json` features and `System.Text.Json` equivalents.</span></span> <span data-ttu-id="e511a-112">Эквиваленты делятся на следующие категории:</span><span class="sxs-lookup"><span data-stu-id="e511a-112">The equivalents fall into the following categories:</span></span>

* <span data-ttu-id="e511a-113">Поддерживается встроенными функциональными возможностями.</span><span class="sxs-lookup"><span data-stu-id="e511a-113">Supported by built-in functionality.</span></span> <span data-ttu-id="e511a-114">Для получения подобного поведения от `System.Text.Json` может потребоваться использование атрибута или глобального параметра.</span><span class="sxs-lookup"><span data-stu-id="e511a-114">Getting similar behavior from `System.Text.Json` may require the use of an attribute or global option.</span></span>
* <span data-ttu-id="e511a-115">Не поддерживается, решение можно устранить.</span><span class="sxs-lookup"><span data-stu-id="e511a-115">Not supported, workaround is possible.</span></span> <span data-ttu-id="e511a-116">Обходные пути являются [пользовательскими преобразователями](system-text-json-converters-how-to.md), которые могут не обеспечивать полную четность с помощью функций `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-116">The workarounds are [custom converters](system-text-json-converters-how-to.md), which may not provide complete parity with `Newtonsoft.Json` functionality.</span></span> <span data-ttu-id="e511a-117">Некоторые из этих примеров кода приведены в качестве примеров.</span><span class="sxs-lookup"><span data-stu-id="e511a-117">For some of these, sample code is provided as examples.</span></span> <span data-ttu-id="e511a-118">Если вы полагаетесь на эти `Newtonsoft.Json` функции, то миграция потребует внесения изменений в объектные модели .NET или другие изменения кода.</span><span class="sxs-lookup"><span data-stu-id="e511a-118">If you rely on these `Newtonsoft.Json` features, migration will require modifications to your .NET object models or other code changes.</span></span>
* <span data-ttu-id="e511a-119">Не поддерживается, обходной путь не является практичным и невозможным.</span><span class="sxs-lookup"><span data-stu-id="e511a-119">Not supported, workaround is not practical or possible.</span></span> <span data-ttu-id="e511a-120">Если вы полагаетесь на эти `Newtonsoft.Json` функции, миграция будет невозможна без существенных изменений.</span><span class="sxs-lookup"><span data-stu-id="e511a-120">If you rely on these `Newtonsoft.Json` features, migration will not be possible without significant changes.</span></span>

| <span data-ttu-id="e511a-121">Функция в Newtonsoft.Json</span><span class="sxs-lookup"><span data-stu-id="e511a-121">Newtonsoft.Json feature</span></span>                               | <span data-ttu-id="e511a-122">эквивалент System.Text.Json</span><span class="sxs-lookup"><span data-stu-id="e511a-122">System.Text.Json equivalent</span></span> |
|-------------------------------------------------------|-----------------------------|
| <span data-ttu-id="e511a-123">Десериализация без учета регистра по умолчанию</span><span class="sxs-lookup"><span data-stu-id="e511a-123">Case-insensitive deserialization by default</span></span>           | <span data-ttu-id="e511a-124">[глобальный параметр ✔️ пропертинамекасеинсенситиве](#case-insensitive-deserialization)</span><span class="sxs-lookup"><span data-stu-id="e511a-124">✔️ [PropertyNameCaseInsensitive global setting](#case-insensitive-deserialization)</span></span> |
| <span data-ttu-id="e511a-125">Имена свойств в стиле Camel</span><span class="sxs-lookup"><span data-stu-id="e511a-125">Camel-case property names</span></span>                             | <span data-ttu-id="e511a-126">[глобальный параметр ✔️ пропертинамингполици](system-text-json-how-to.md#use-camel-case-for-all-json-property-names)</span><span class="sxs-lookup"><span data-stu-id="e511a-126">✔️ [PropertyNamingPolicy global setting](system-text-json-how-to.md#use-camel-case-for-all-json-property-names)</span></span> |
| <span data-ttu-id="e511a-127">Минимальное экранирование символов</span><span class="sxs-lookup"><span data-stu-id="e511a-127">Minimal character escaping</span></span>                            | <span data-ttu-id="e511a-128">✔️е [запрещенного экранирования символов, настраиваемого](#minimal-character-escaping)</span><span class="sxs-lookup"><span data-stu-id="e511a-128">✔️ [Strict character escaping, configurable](#minimal-character-escaping)</span></span> |
| <span data-ttu-id="e511a-129">Глобальный параметр `NullValueHandling.Ignore`</span><span class="sxs-lookup"><span data-stu-id="e511a-129">`NullValueHandling.Ignore` global setting</span></span>             | <span data-ttu-id="e511a-130">✔️ [глобальный параметр игноренуллвалуес](system-text-json-how-to.md#exclude-all-null-value-properties)</span><span class="sxs-lookup"><span data-stu-id="e511a-130">✔️ [IgnoreNullValues global option](system-text-json-how-to.md#exclude-all-null-value-properties)</span></span> |
| <span data-ttu-id="e511a-131">Разрешить комментарии</span><span class="sxs-lookup"><span data-stu-id="e511a-131">Allow comments</span></span>                                        | <span data-ttu-id="e511a-132">[глобальный параметр ✔️ реадкомменсандлинг](#comments)</span><span class="sxs-lookup"><span data-stu-id="e511a-132">✔️ [ReadCommentHandling global setting](#comments)</span></span> |
| <span data-ttu-id="e511a-133">Разрешить конечные запятые</span><span class="sxs-lookup"><span data-stu-id="e511a-133">Allow trailing commas</span></span>                                 | <span data-ttu-id="e511a-134">[глобальный параметр ✔️ алловтраилингкоммас](#trailing-commas)</span><span class="sxs-lookup"><span data-stu-id="e511a-134">✔️ [AllowTrailingCommas global setting](#trailing-commas)</span></span> |
| <span data-ttu-id="e511a-135">Регистрация пользовательского преобразователя</span><span class="sxs-lookup"><span data-stu-id="e511a-135">Custom converter registration</span></span>                         | <span data-ttu-id="e511a-136">✔️ [Порядок приоритета отличается](#converter-registration-precedence)</span><span class="sxs-lookup"><span data-stu-id="e511a-136">✔️ [Order of precedence differs](#converter-registration-precedence)</span></span> |
| <span data-ttu-id="e511a-137">По умолчанию отсутствует максимальная глубина</span><span class="sxs-lookup"><span data-stu-id="e511a-137">No maximum depth by default</span></span>                           | <span data-ttu-id="e511a-138">[Максимальная глубина ✔️ по умолчанию 64, настраиваемая](#maximum-depth)</span><span class="sxs-lookup"><span data-stu-id="e511a-138">✔️ [Default maximum depth 64, configurable](#maximum-depth)</span></span> |
| <span data-ttu-id="e511a-139">Поддержка широкого спектра типов</span><span class="sxs-lookup"><span data-stu-id="e511a-139">Support for a broad range of types</span></span>                    | <span data-ttu-id="e511a-140">⚠️ для [некоторых типов требуются пользовательские преобразователи](#types-without-built-in-support)</span><span class="sxs-lookup"><span data-stu-id="e511a-140">⚠️ [Some types require custom converters](#types-without-built-in-support)</span></span> |
| <span data-ttu-id="e511a-141">Десериализация строк как чисел</span><span class="sxs-lookup"><span data-stu-id="e511a-141">Deserialize strings as numbers</span></span>                        | <span data-ttu-id="e511a-142">⚠️ [не поддерживается, обходной путь, пример](#quoted-numbers)</span><span class="sxs-lookup"><span data-stu-id="e511a-142">⚠️ [Not supported, workaround, sample](#quoted-numbers)</span></span> |
| <span data-ttu-id="e511a-143">Десериализация `Dictionary` с ключом, не являющимся строкой</span><span class="sxs-lookup"><span data-stu-id="e511a-143">Deserialize `Dictionary` with non-string key</span></span>          | <span data-ttu-id="e511a-144">⚠️ [не поддерживается, обходной путь, пример](#dictionary-with-non-string-key)</span><span class="sxs-lookup"><span data-stu-id="e511a-144">⚠️ [Not supported, workaround, sample](#dictionary-with-non-string-key)</span></span> |
| <span data-ttu-id="e511a-145">Пресериализация в полиморфизм</span><span class="sxs-lookup"><span data-stu-id="e511a-145">Polymorphic serialization</span></span>                             | <span data-ttu-id="e511a-146">⚠️ [не поддерживается, обходной путь, пример](#polymorphic-serialization)</span><span class="sxs-lookup"><span data-stu-id="e511a-146">⚠️ [Not supported, workaround, sample](#polymorphic-serialization)</span></span> |
| <span data-ttu-id="e511a-147">Пресериализация в полиморфизм</span><span class="sxs-lookup"><span data-stu-id="e511a-147">Polymorphic deserialization</span></span>                           | <span data-ttu-id="e511a-148">⚠️ [не поддерживается, обходной путь, пример](#polymorphic-deserialization)</span><span class="sxs-lookup"><span data-stu-id="e511a-148">⚠️ [Not supported, workaround, sample](#polymorphic-deserialization)</span></span> |
| <span data-ttu-id="e511a-149">Десериализация выводимого типа для `object` свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-149">Deserialize inferred type to `object` properties</span></span>      | <span data-ttu-id="e511a-150">⚠️ [не поддерживается, обходной путь, пример](#deserialization-of-object-properties)</span><span class="sxs-lookup"><span data-stu-id="e511a-150">⚠️ [Not supported, workaround, sample](#deserialization-of-object-properties)</span></span> |
| <span data-ttu-id="e511a-151">Десериализация JSON `null` литерала в типы, не допускающие значение null</span><span class="sxs-lookup"><span data-stu-id="e511a-151">Deserialize JSON `null` literal to non-nullable types</span></span> | <span data-ttu-id="e511a-152">⚠️ [не поддерживается, обходной путь, пример](#deserialize-null-to-non-nullable-type)</span><span class="sxs-lookup"><span data-stu-id="e511a-152">⚠️ [Not supported, workaround, sample](#deserialize-null-to-non-nullable-type)</span></span> |
| <span data-ttu-id="e511a-153">Десериализация в неизменяемые классы и структуры</span><span class="sxs-lookup"><span data-stu-id="e511a-153">Deserialize to immutable classes and structs</span></span>          | <span data-ttu-id="e511a-154">⚠️ [не поддерживается, обходной путь, пример](#deserialize-to-immutable-classes-and-structs)</span><span class="sxs-lookup"><span data-stu-id="e511a-154">⚠️ [Not supported, workaround, sample](#deserialize-to-immutable-classes-and-structs)</span></span> |
| <span data-ttu-id="e511a-155">Атрибут `[JsonConstructor]`</span><span class="sxs-lookup"><span data-stu-id="e511a-155">`[JsonConstructor]` attribute</span></span>                         | <span data-ttu-id="e511a-156">⚠️ [не поддерживается, обходной путь, пример](#specify-constructor-to-use)</span><span class="sxs-lookup"><span data-stu-id="e511a-156">⚠️ [Not supported, workaround, sample](#specify-constructor-to-use)</span></span> |
| <span data-ttu-id="e511a-157">параметр `Required` для атрибута `[JsonProperty]`</span><span class="sxs-lookup"><span data-stu-id="e511a-157">`Required` setting on `[JsonProperty]` attribute</span></span>        | <span data-ttu-id="e511a-158">⚠️ [не поддерживается, обходной путь, пример](#required-properties)</span><span class="sxs-lookup"><span data-stu-id="e511a-158">⚠️ [Not supported, workaround, sample](#required-properties)</span></span> |
| <span data-ttu-id="e511a-159">параметр `NullValueHandling` для атрибута `[JsonProperty]`</span><span class="sxs-lookup"><span data-stu-id="e511a-159">`NullValueHandling` setting on `[JsonProperty]` attribute</span></span> | <span data-ttu-id="e511a-160">⚠️ [не поддерживается, обходной путь, пример](#conditionally-ignore-a-property)</span><span class="sxs-lookup"><span data-stu-id="e511a-160">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span>  |
| <span data-ttu-id="e511a-161">параметр `DefaultValueHandling` для атрибута `[JsonProperty]`</span><span class="sxs-lookup"><span data-stu-id="e511a-161">`DefaultValueHandling` setting on `[JsonProperty]` attribute</span></span> | <span data-ttu-id="e511a-162">⚠️ [не поддерживается, обходной путь, пример](#conditionally-ignore-a-property)</span><span class="sxs-lookup"><span data-stu-id="e511a-162">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span>  |
| <span data-ttu-id="e511a-163">Глобальный параметр `DefaultValueHandling`</span><span class="sxs-lookup"><span data-stu-id="e511a-163">`DefaultValueHandling` global setting</span></span>                 | <span data-ttu-id="e511a-164">⚠️ [не поддерживается, обходной путь, пример](#conditionally-ignore-a-property)</span><span class="sxs-lookup"><span data-stu-id="e511a-164">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span> |
| <span data-ttu-id="e511a-165">`DefaultContractResolver` для исключения свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-165">`DefaultContractResolver` to exclude properties</span></span>       | <span data-ttu-id="e511a-166">⚠️ [не поддерживается, обходной путь, пример](#conditionally-ignore-a-property)</span><span class="sxs-lookup"><span data-stu-id="e511a-166">⚠️ [Not supported, workaround, sample](#conditionally-ignore-a-property)</span></span> |
| <span data-ttu-id="e511a-167">`DateTimeZoneHandling`, параметры `DateFormatString`</span><span class="sxs-lookup"><span data-stu-id="e511a-167">`DateTimeZoneHandling`, `DateFormatString` settings</span></span>   | <span data-ttu-id="e511a-168">⚠️ [не поддерживается, обходной путь, пример](#specify-date-format)</span><span class="sxs-lookup"><span data-stu-id="e511a-168">⚠️ [Not supported, workaround, sample](#specify-date-format)</span></span> |
| <span data-ttu-id="e511a-169">Обратные вызовы</span><span class="sxs-lookup"><span data-stu-id="e511a-169">Callbacks</span></span>                                             | <span data-ttu-id="e511a-170">⚠️ [не поддерживается, обходной путь, пример](#callbacks)</span><span class="sxs-lookup"><span data-stu-id="e511a-170">⚠️ [Not supported, workaround, sample](#callbacks)</span></span> |
| <span data-ttu-id="e511a-171">Поддержка открытых и не открытых полей</span><span class="sxs-lookup"><span data-stu-id="e511a-171">Support for public and non-public fields</span></span>              | <span data-ttu-id="e511a-172">⚠️ [не поддерживается, обходной путь](#public-and-non-public-fields)</span><span class="sxs-lookup"><span data-stu-id="e511a-172">⚠️ [Not supported, workaround](#public-and-non-public-fields)</span></span> |
| <span data-ttu-id="e511a-173">Поддержка методов и методов доступа для внутренних и закрытых свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-173">Support for internal and private property setters and getters</span></span> | <span data-ttu-id="e511a-174">⚠️ [не поддерживается, обходной путь](#internal-and-private-property-setters-and-getters)</span><span class="sxs-lookup"><span data-stu-id="e511a-174">⚠️ [Not supported, workaround](#internal-and-private-property-setters-and-getters)</span></span> |
| <span data-ttu-id="e511a-175">Метод`JsonConvert.PopulateObject`</span><span class="sxs-lookup"><span data-stu-id="e511a-175">`JsonConvert.PopulateObject` method</span></span>                   | <span data-ttu-id="e511a-176">⚠️ [не поддерживается, обходной путь](#populate-existing-objects)</span><span class="sxs-lookup"><span data-stu-id="e511a-176">⚠️ [Not supported, workaround](#populate-existing-objects)</span></span> |
| <span data-ttu-id="e511a-177">Глобальный параметр `ObjectCreationHandling`</span><span class="sxs-lookup"><span data-stu-id="e511a-177">`ObjectCreationHandling` global setting</span></span>               | <span data-ttu-id="e511a-178">⚠️ [не поддерживается, обходной путь](#reuse-rather-than-replace-properties)</span><span class="sxs-lookup"><span data-stu-id="e511a-178">⚠️ [Not supported, workaround](#reuse-rather-than-replace-properties)</span></span> |
| <span data-ttu-id="e511a-179">Добавить в коллекции без методов задания</span><span class="sxs-lookup"><span data-stu-id="e511a-179">Add to collections without setters</span></span>                    | <span data-ttu-id="e511a-180">⚠️ [не поддерживается, обходной путь](#add-to-collections-without-setters)</span><span class="sxs-lookup"><span data-stu-id="e511a-180">⚠️ [Not supported, workaround](#add-to-collections-without-setters)</span></span> |
| <span data-ttu-id="e511a-181">Глобальный параметр `PreserveReferencesHandling`</span><span class="sxs-lookup"><span data-stu-id="e511a-181">`PreserveReferencesHandling` global setting</span></span>           | <span data-ttu-id="e511a-182">❌ [не поддерживается](#preserve-object-references-and-handle-loops)</span><span class="sxs-lookup"><span data-stu-id="e511a-182">❌ [Not supported](#preserve-object-references-and-handle-loops)</span></span> |
| <span data-ttu-id="e511a-183">Глобальный параметр `ReferenceLoopHandling`</span><span class="sxs-lookup"><span data-stu-id="e511a-183">`ReferenceLoopHandling` global setting</span></span>                | <span data-ttu-id="e511a-184">❌ [не поддерживается](#preserve-object-references-and-handle-loops)</span><span class="sxs-lookup"><span data-stu-id="e511a-184">❌ [Not supported](#preserve-object-references-and-handle-loops)</span></span> |
| <span data-ttu-id="e511a-185">Поддержка атрибутов `System.Runtime.Serialization`</span><span class="sxs-lookup"><span data-stu-id="e511a-185">Support for `System.Runtime.Serialization` attributes</span></span> | <span data-ttu-id="e511a-186">❌ [не поддерживается](#systemruntimeserialization-attributes)</span><span class="sxs-lookup"><span data-stu-id="e511a-186">❌ [Not supported](#systemruntimeserialization-attributes)</span></span> |
| <span data-ttu-id="e511a-187">Глобальный параметр `MissingMemberHandling`</span><span class="sxs-lookup"><span data-stu-id="e511a-187">`MissingMemberHandling` global setting</span></span>                | <span data-ttu-id="e511a-188">❌ [не поддерживается](#missingmemberhandling)</span><span class="sxs-lookup"><span data-stu-id="e511a-188">❌ [Not supported](#missingmemberhandling)</span></span> |
| <span data-ttu-id="e511a-189">Разрешить имена свойств без кавычек</span><span class="sxs-lookup"><span data-stu-id="e511a-189">Allow property names without quotes</span></span>                   | <span data-ttu-id="e511a-190">❌ [не поддерживается](#json-strings-property-names-and-string-values)</span><span class="sxs-lookup"><span data-stu-id="e511a-190">❌ [Not supported](#json-strings-property-names-and-string-values)</span></span> |
| <span data-ttu-id="e511a-191">Разрешить одиночные кавычки вокруг строковых значений</span><span class="sxs-lookup"><span data-stu-id="e511a-191">Allow single quotes around string values</span></span>              | <span data-ttu-id="e511a-192">❌ [не поддерживается](#json-strings-property-names-and-string-values)</span><span class="sxs-lookup"><span data-stu-id="e511a-192">❌ [Not supported](#json-strings-property-names-and-string-values)</span></span> |
| <span data-ttu-id="e511a-193">Разрешить значения, отличные от строкового JSON, для строковых свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-193">Allow non-string JSON values for string properties</span></span>    | <span data-ttu-id="e511a-194">❌ [не поддерживается](#non-string-values-for-string-properties)</span><span class="sxs-lookup"><span data-stu-id="e511a-194">❌ [Not supported](#non-string-values-for-string-properties)</span></span> |

<span data-ttu-id="e511a-195">Это не исчерпывающий список функций `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-195">This is not an exhaustive list of `Newtonsoft.Json` features.</span></span> <span data-ttu-id="e511a-196">Список включает многие сценарии, которые были запрошены в [проблемах GitHub](https://github.com/dotnet/runtime/issues?q=is%3Aopen+is%3Aissue+label%3Aarea-System.Text.Json) или [StackOverflow](https://stackoverflow.com/questions/tagged/system.text.json) .</span><span class="sxs-lookup"><span data-stu-id="e511a-196">The list includes many of the scenarios that have been requested in [GitHub issues](https://github.com/dotnet/runtime/issues?q=is%3Aopen+is%3Aissue+label%3Aarea-System.Text.Json) or [StackOverflow](https://stackoverflow.com/questions/tagged/system.text.json) posts.</span></span> <span data-ttu-id="e511a-197">Если вы реализуете обходной путь для одного из сценариев, перечисленных здесь без примера кода, и если вы хотите поделиться своим решением, нажмите кнопку "**Эта страница**" в нижней части страницы.</span><span class="sxs-lookup"><span data-stu-id="e511a-197">If you implement a workaround for one of the scenarios listed here without sample code, and if you want to share your solution, select the "**This page**" button at the bottom of the page.</span></span> <span data-ttu-id="e511a-198">Это создает ошибку GitHub и добавляет ее к проблемам, перечисленным в нижней части страницы.</span><span class="sxs-lookup"><span data-stu-id="e511a-198">That creates a GitHub issue and adds it to the issues that are listed at the bottom of the page.</span></span>

## <a name="differences-in-default-jsonserializer-behavior-compared-to-opno-locnewtonsoftjson"></a><span data-ttu-id="e511a-199">Различия в поведении JsonSerializer по умолчанию по сравнению с Newtonsoft.Json</span><span class="sxs-lookup"><span data-stu-id="e511a-199">Differences in default JsonSerializer behavior compared to Newtonsoft.Json</span></span>

<span data-ttu-id="e511a-200"><xref:System.Text.Json> по умолчанию является строгой и позволяет избежать подбора или интерпретации от имени вызывающего объекта, подчеркивая детерминированное поведение.</span><span class="sxs-lookup"><span data-stu-id="e511a-200"><xref:System.Text.Json> is strict by default and avoids any guessing or interpretation on the caller's behalf, emphasizing deterministic behavior.</span></span> <span data-ttu-id="e511a-201">Библиотека преднамеренно разработана таким образом для повышения производительности и безопасности.</span><span class="sxs-lookup"><span data-stu-id="e511a-201">The library is intentionally designed this way for performance and security.</span></span> <span data-ttu-id="e511a-202">`Newtonsoft.Json` по умолчанию является гибкой.</span><span class="sxs-lookup"><span data-stu-id="e511a-202">`Newtonsoft.Json` is flexible by default.</span></span> <span data-ttu-id="e511a-203">Это фундаментальное различие в проектировании – это за многие из следующих различий в поведении по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="e511a-203">This fundamental difference in design is behind many of the following specific differences in default behavior.</span></span>

### <a name="case-insensitive-deserialization"></a><span data-ttu-id="e511a-204">Десериализация без учета регистра</span><span class="sxs-lookup"><span data-stu-id="e511a-204">Case-insensitive deserialization</span></span> 

<span data-ttu-id="e511a-205">Во время десериализации `Newtonsoft.Json` имя свойства, по умолчанию совпадающее с учетом регистра.</span><span class="sxs-lookup"><span data-stu-id="e511a-205">During deserialization, `Newtonsoft.Json` does case-insensitive property name matching by default.</span></span> <span data-ttu-id="e511a-206"><xref:System.Text.Json> умолчанию учитывается регистр, что обеспечивает лучшую производительность, поскольку выполняет точное соответствие.</span><span class="sxs-lookup"><span data-stu-id="e511a-206">The <xref:System.Text.Json> default is case-sensitive, which gives better performance since it's doing an exact match.</span></span> <span data-ttu-id="e511a-207">Сведения о том, как выполнять сопоставление без учета регистра, см. в разделе [сопоставление свойств без учета регистра](system-text-json-how-to.md#case-insensitive-property-matching).</span><span class="sxs-lookup"><span data-stu-id="e511a-207">For information about how to do case-insensitive matching, see [Case-insensitive property matching](system-text-json-how-to.md#case-insensitive-property-matching).</span></span>

<span data-ttu-id="e511a-208">Если вы используете `System.Text.Json` косвенно с помощью ASP.NET Core, вам не нужно ничего делать для получения такого поведения, как `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-208">If you're using `System.Text.Json` indirectly by using ASP.NET Core, you don't need to do anything to get behavior like `Newtonsoft.Json`.</span></span> <span data-ttu-id="e511a-209">ASP.NET Core задает параметры для [имен свойств в неоднородном регистре](system-text-json-how-to.md#use-camel-case-for-all-json-property-names) и сопоставления без учета регистра при использовании `System.Text.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-209">ASP.NET Core specifies the settings for [camel-casing property names](system-text-json-how-to.md#use-camel-case-for-all-json-property-names) and case-insensitive matching when it uses `System.Text.Json`.</span></span>

### <a name="minimal-character-escaping"></a><span data-ttu-id="e511a-210">Минимальное экранирование символов</span><span class="sxs-lookup"><span data-stu-id="e511a-210">Minimal character escaping</span></span>

<span data-ttu-id="e511a-211">Во время сериализации `Newtonsoft.Json` имеет относительно недостаточное разрешение на использование символов без экранирования их.</span><span class="sxs-lookup"><span data-stu-id="e511a-211">During serialization, `Newtonsoft.Json` is relatively permissive about letting characters through without escaping them.</span></span> <span data-ttu-id="e511a-212">То есть он не заменяет их `\uxxxx`, где `xxxx` является кодовой точкой символа.</span><span class="sxs-lookup"><span data-stu-id="e511a-212">That is, it doesn't replace them with `\uxxxx` where `xxxx` is the character's code point.</span></span> <span data-ttu-id="e511a-213">Там они помещаются в escape-последовательность, выпуская `\` перед символом (например, `"` преобразуется в `\"`).</span><span class="sxs-lookup"><span data-stu-id="e511a-213">Where it does escape them, it does so by emitting a `\` before the character (for example, `"` becomes `\"`).</span></span> <span data-ttu-id="e511a-214">по умолчанию <xref:System.Text.Json> содержит больше символов, чтобы обеспечить защиту от несанкционированного использования межсайтовых сценариев (XSS) или атак с раскрытием информации и делает это с помощью последовательности из шести символов.</span><span class="sxs-lookup"><span data-stu-id="e511a-214"><xref:System.Text.Json> escapes more characters by default to provide defense-in-depth protections against cross-site scripting (XSS) or information-disclosure attacks and does so by using the six-character sequence.</span></span> <span data-ttu-id="e511a-215">`System.Text.Json` отменяет все символы, отличные от ASCII, по умолчанию, поэтому вам не нужно ничего делать, если вы используете `StringEscapeHandling.EscapeNonAscii` в `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-215">`System.Text.Json` escapes all non-ASCII characters by default, so you don't need to do anything if you're using `StringEscapeHandling.EscapeNonAscii` in `Newtonsoft.Json`.</span></span> <span data-ttu-id="e511a-216">`System.Text.Json` также по умолчанию обходит символы, зависящие от HTML.</span><span class="sxs-lookup"><span data-stu-id="e511a-216">`System.Text.Json` also escapes HTML-sensitive characters, by default.</span></span> <span data-ttu-id="e511a-217">Сведения о том, как переопределить поведение `System.Text.Json` по умолчанию, см. в разделе [Настройка кодировки символов](system-text-json-how-to.md#customize-character-encoding).</span><span class="sxs-lookup"><span data-stu-id="e511a-217">For information about how to override the default `System.Text.Json` behavior, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="comments"></a><span data-ttu-id="e511a-218">Comments</span><span class="sxs-lookup"><span data-stu-id="e511a-218">Comments</span></span>

<span data-ttu-id="e511a-219">Во время десериализации `Newtonsoft.Json` по умолчанию игнорирует комментарии в JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-219">During deserialization, `Newtonsoft.Json` ignores comments in the JSON by default.</span></span> <span data-ttu-id="e511a-220"><xref:System.Text.Json> по умолчанию — создавать исключения для комментариев, так как спецификация [RFC 8259](https://tools.ietf.org/html/rfc8259) не включает их.</span><span class="sxs-lookup"><span data-stu-id="e511a-220">The <xref:System.Text.Json> default is to throw exceptions for comments because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't include them.</span></span> <span data-ttu-id="e511a-221">Сведения о том, как разрешить комментарии, см. в разделе [allow Comments and замыкающие запятые](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span><span class="sxs-lookup"><span data-stu-id="e511a-221">For information about how to allow comments, see [Allow comments and trailing commas](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span></span>

### <a name="trailing-commas"></a><span data-ttu-id="e511a-222">Конечные запятые</span><span class="sxs-lookup"><span data-stu-id="e511a-222">Trailing commas</span></span>

<span data-ttu-id="e511a-223">Во время десериализации `Newtonsoft.Json` по умолчанию игнорирует конечные запятые.</span><span class="sxs-lookup"><span data-stu-id="e511a-223">During deserialization, `Newtonsoft.Json` ignores trailing commas by default.</span></span> <span data-ttu-id="e511a-224">Он также игнорирует несколько конечных запятых (например, `[{"Color":"Red"},{"Color":"Green"},,]`).</span><span class="sxs-lookup"><span data-stu-id="e511a-224">It also ignores multiple trailing commas (for example, `[{"Color":"Red"},{"Color":"Green"},,]`).</span></span> <span data-ttu-id="e511a-225"><xref:System.Text.Json> по умолчанию — создавать исключения для конечных запятых, так как спецификация [RFC 8259](https://tools.ietf.org/html/rfc8259) не допускает их.</span><span class="sxs-lookup"><span data-stu-id="e511a-225">The <xref:System.Text.Json> default is to throw exceptions for trailing commas because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't allow them.</span></span> <span data-ttu-id="e511a-226">Сведения о том, как сделать `System.Text.Json` допустимыми, см. в разделе [allow Comments and замыкающие запятые](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span><span class="sxs-lookup"><span data-stu-id="e511a-226">For information about how to make `System.Text.Json` accept them, see [Allow comments and trailing commas](system-text-json-how-to.md#allow-comments-and-trailing-commas).</span></span> <span data-ttu-id="e511a-227">Невозможно разрешить несколько конечных запятых.</span><span class="sxs-lookup"><span data-stu-id="e511a-227">There's no way to allow multiple trailing commas.</span></span>

### <a name="converter-registration-precedence"></a><span data-ttu-id="e511a-228">Приоритет регистрации преобразователя</span><span class="sxs-lookup"><span data-stu-id="e511a-228">Converter registration precedence</span></span>

<span data-ttu-id="e511a-229">Приоритет регистрации `Newtonsoft.Json` для пользовательских преобразователей выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="e511a-229">The `Newtonsoft.Json` registration precedence for custom converters is as follows:</span></span>

* <span data-ttu-id="e511a-230">Атрибут для свойства</span><span class="sxs-lookup"><span data-stu-id="e511a-230">Attribute on property</span></span>
* <span data-ttu-id="e511a-231">Атрибут для типа</span><span class="sxs-lookup"><span data-stu-id="e511a-231">Attribute on type</span></span>
* <span data-ttu-id="e511a-232">Коллекция [преобразователей](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonSerializerSettings_Converters.htm)</span><span class="sxs-lookup"><span data-stu-id="e511a-232">[Converters](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonSerializerSettings_Converters.htm) collection</span></span>

<span data-ttu-id="e511a-233">Этот порядок означает, что пользовательский преобразователь в коллекции `Converters` переопределяется преобразователем, зарегистрированным путем применения атрибута на уровне типа.</span><span class="sxs-lookup"><span data-stu-id="e511a-233">This order means that a custom converter in the `Converters` collection is overridden by a converter that is registered by applying an attribute at the type level.</span></span> <span data-ttu-id="e511a-234">Обе эти регистрации переопределяются атрибутом на уровне свойства.</span><span class="sxs-lookup"><span data-stu-id="e511a-234">Both of those registrations are overridden by an attribute at the property level.</span></span>

<span data-ttu-id="e511a-235">Приоритет регистрации <xref:System.Text.Json> для пользовательских преобразователей отличается:</span><span class="sxs-lookup"><span data-stu-id="e511a-235">The <xref:System.Text.Json> registration precedence for custom converters is different:</span></span>

* <span data-ttu-id="e511a-236">Атрибут для свойства</span><span class="sxs-lookup"><span data-stu-id="e511a-236">Attribute on property</span></span>
* <span data-ttu-id="e511a-237">Коллекция <xref:System.Text.Json.JsonSerializerOptions.Converters></span><span class="sxs-lookup"><span data-stu-id="e511a-237"><xref:System.Text.Json.JsonSerializerOptions.Converters> collection</span></span>
* <span data-ttu-id="e511a-238">Атрибут для типа</span><span class="sxs-lookup"><span data-stu-id="e511a-238">Attribute on type</span></span>

<span data-ttu-id="e511a-239">Разница заключается в том, что пользовательский преобразователь в коллекции `Converters` переопределяет атрибут на уровне типа.</span><span class="sxs-lookup"><span data-stu-id="e511a-239">The difference here is that a custom converter in the `Converters` collection overrides an attribute at the type level.</span></span> <span data-ttu-id="e511a-240">Цель этого порядка приоритета заключается в том, чтобы изменения во время выполнения переопределяли варианты времени разработки.</span><span class="sxs-lookup"><span data-stu-id="e511a-240">The intention behind this order of precedence is to make run-time changes override design-time choices.</span></span> <span data-ttu-id="e511a-241">Изменить приоритет невозможно.</span><span class="sxs-lookup"><span data-stu-id="e511a-241">There's no way to change the precedence.</span></span>

<span data-ttu-id="e511a-242">Дополнительные сведения о регистрации пользовательских преобразователей см. [в разделе Регистрация пользовательского преобразователя](system-text-json-converters-how-to.md#register-a-custom-converter).</span><span class="sxs-lookup"><span data-stu-id="e511a-242">For more information about custom converter registration, see [Register a custom converter](system-text-json-converters-how-to.md#register-a-custom-converter).</span></span>

### <a name="maximum-depth"></a><span data-ttu-id="e511a-243">Максимальная глубина</span><span class="sxs-lookup"><span data-stu-id="e511a-243">Maximum depth</span></span>

<span data-ttu-id="e511a-244">значение по умолчанию для `Newtonsoft.Json` не равно максимальной глубины.</span><span class="sxs-lookup"><span data-stu-id="e511a-244">`Newtonsoft.Json` doesn't have a maximum depth limit by default.</span></span> <span data-ttu-id="e511a-245">Для <xref:System.Text.Json> существует ограничение по умолчанию 64, которое можно настроить, установив <xref:System.Text.Json.JsonSerializerOptions.MaxDepth?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e511a-245">For <xref:System.Text.Json> there's a default limit  of 64, and it's configurable by setting <xref:System.Text.Json.JsonSerializerOptions.MaxDepth?displayProperty=nameWithType>.</span></span>

### <a name="json-strings-property-names-and-string-values"></a><span data-ttu-id="e511a-246">Строки JSON (имена свойств и строковые значения)</span><span class="sxs-lookup"><span data-stu-id="e511a-246">JSON strings (property names and string values)</span></span>

<span data-ttu-id="e511a-247">Во время десериализации `Newtonsoft.Json` принимает имена свойств, заключенные в двойные кавычки, одинарные кавычки или без кавычек.</span><span class="sxs-lookup"><span data-stu-id="e511a-247">During deserialization, `Newtonsoft.Json` accepts property names surrounded by double quotes, single quotes, or without quotes.</span></span> <span data-ttu-id="e511a-248">Он принимает строковые значения, заключенные в двойные кавычки или одинарные кавычки.</span><span class="sxs-lookup"><span data-stu-id="e511a-248">It accepts string values surrounded by double quotes or single quotes.</span></span> <span data-ttu-id="e511a-249">Например, `Newtonsoft.Json` принимает следующий код JSON:</span><span class="sxs-lookup"><span data-stu-id="e511a-249">For example, `Newtonsoft.Json` accepts the following JSON:</span></span>

```json
{
  "name1": "value",
  'name2': "value",
  name3: 'value'
}
```

<span data-ttu-id="e511a-250">`System.Text.Json` принимает только имена свойств и строковые значения в двойных кавычках, поскольку этот формат является обязательным для спецификации [RFC 8259](https://tools.ietf.org/html/rfc8259) и является единственным форматом, который считается допустимым JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-250">`System.Text.Json` only accepts property names and string values in double quotes because that format is required by the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification and is the only format considered valid JSON.</span></span>

<span data-ttu-id="e511a-251">Значение, заключенное в одинарные кавычки, приводит к [жсонексцептион](xref:System.Text.Json.JsonException) со следующим сообщением:</span><span class="sxs-lookup"><span data-stu-id="e511a-251">A value enclosed in single quotes results in a [JsonException](xref:System.Text.Json.JsonException) with the following message:</span></span>

```
''' is an invalid start of a value.
```

### <a name="non-string-values-for-string-properties"></a><span data-ttu-id="e511a-252">Нестроковые значения для строковых свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-252">Non-string values for string properties</span></span>

<span data-ttu-id="e511a-253">`Newtonsoft.Json` принимает нестроковые значения, такие как число или литералы `true` и `false`, для десериализации свойств типа String.</span><span class="sxs-lookup"><span data-stu-id="e511a-253">`Newtonsoft.Json` accepts non-string values, such as a number or the literals `true` and `false`, for deserialization to properties of type string.</span></span> <span data-ttu-id="e511a-254">Ниже приведен пример JSON, который `Newtonsoft.Json` успешно десериализуется в следующий класс:</span><span class="sxs-lookup"><span data-stu-id="e511a-254">Here's an example of JSON that `Newtonsoft.Json` successfully deserializes to the following class:</span></span>

```json
{
  "String1": 1,
  "String2": true,
  "String3": false
}
```

```csharp
public class ExampleClass
{
    public string String1 { get; set; }
    public string String2 { get; set; }
    public string String3 { get; set; }
}
```

<span data-ttu-id="e511a-255">`System.Text.Json` не выполняет десериализацию нестроковых значений в свойства строк.</span><span class="sxs-lookup"><span data-stu-id="e511a-255">`System.Text.Json` doesn't deserialize non-string values into string properties.</span></span> <span data-ttu-id="e511a-256">Нестроковое значение, полученное для строкового поля, приводит к [жсонексцептион](xref:System.Text.Json.JsonException) со следующим сообщением:</span><span class="sxs-lookup"><span data-stu-id="e511a-256">A non-string value received for a string field results in a [JsonException](xref:System.Text.Json.JsonException) with the following message:</span></span>

```
The JSON value could not be converted to System.String.
```

## <a name="scenarios-using-jsonserializer-that-require-workarounds"></a><span data-ttu-id="e511a-257">Сценарии с использованием JsonSerializer, для которых требуются обходные пути</span><span class="sxs-lookup"><span data-stu-id="e511a-257">Scenarios using JsonSerializer that require workarounds</span></span>

<span data-ttu-id="e511a-258">Следующие сценарии не поддерживаются встроенными функциями, но возможны обходные пути.</span><span class="sxs-lookup"><span data-stu-id="e511a-258">The following scenarios aren't supported by built-in functionality, but workarounds are possible.</span></span> <span data-ttu-id="e511a-259">Обходные пути являются [пользовательскими преобразователями](system-text-json-converters-how-to.md), которые могут не обеспечивать полную четность с помощью функций `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-259">The workarounds are [custom converters](system-text-json-converters-how-to.md), which may not provide complete parity with `Newtonsoft.Json` functionality.</span></span> <span data-ttu-id="e511a-260">Некоторые из этих примеров кода приведены в качестве примеров.</span><span class="sxs-lookup"><span data-stu-id="e511a-260">For some of these, sample code is provided as examples.</span></span> <span data-ttu-id="e511a-261">Если вы полагаетесь на эти `Newtonsoft.Json` функции, то миграция потребует внесения изменений в объектные модели .NET или другие изменения кода.</span><span class="sxs-lookup"><span data-stu-id="e511a-261">If you rely on these `Newtonsoft.Json` features, migration will require modifications to your .NET object models or other code changes.</span></span>

### <a name="types-without-built-in-support"></a><span data-ttu-id="e511a-262">Типы без встроенной поддержки</span><span class="sxs-lookup"><span data-stu-id="e511a-262">Types without built-in support</span></span>

<span data-ttu-id="e511a-263"><xref:System.Text.Json> не предоставляет встроенную поддержку для следующих типов:</span><span class="sxs-lookup"><span data-stu-id="e511a-263"><xref:System.Text.Json> doesn't provide built-in support for the following types:</span></span>

* <span data-ttu-id="e511a-264"><xref:System.Data.DataTable> и связанные типы</span><span class="sxs-lookup"><span data-stu-id="e511a-264"><xref:System.Data.DataTable> and related types</span></span>
* <span data-ttu-id="e511a-265">F#типы, такие как [Размеченные объединения](../../fsharp/language-reference/discriminated-unions.md), [типы записей](../../fsharp/language-reference/records.md)и [анонимные типы записей](../../fsharp/language-reference/anonymous-records.md).</span><span class="sxs-lookup"><span data-stu-id="e511a-265">F# types, such as [discriminated unions](../../fsharp/language-reference/discriminated-unions.md), [record types](../../fsharp/language-reference/records.md), and [anonymous record types](../../fsharp/language-reference/anonymous-records.md).</span></span>
* <xref:System.Dynamic.ExpandoObject>
* <xref:System.TimeZoneInfo>
* <xref:System.Numerics.BigInteger>
* <xref:System.TimeSpan>
* <xref:System.DBNull>
* <xref:System.Type>
* <span data-ttu-id="e511a-266"><xref:System.ValueTuple> и связанные с ними универсальные типы</span><span class="sxs-lookup"><span data-stu-id="e511a-266"><xref:System.ValueTuple> and its associated generic types</span></span>

<span data-ttu-id="e511a-267">Пользовательские преобразователи можно реализовать для типов, у которых нет встроенной поддержки.</span><span class="sxs-lookup"><span data-stu-id="e511a-267">Custom converters can be implemented for types that don't have built-in support.</span></span>

### <a name="quoted-numbers"></a><span data-ttu-id="e511a-268">Заключенные в кавычки числа</span><span class="sxs-lookup"><span data-stu-id="e511a-268">Quoted numbers</span></span>

<span data-ttu-id="e511a-269">`Newtonsoft.Json` может сериализовать или десериализовать числа, представленные строками JSON (заключенные в кавычки).</span><span class="sxs-lookup"><span data-stu-id="e511a-269">`Newtonsoft.Json` can serialize or deserialize numbers represented by JSON strings (surrounded by quotes).</span></span> <span data-ttu-id="e511a-270">Например, он может принимать: `{"DegreesCelsius":"23"}` вместо `{"DegreesCelsius":23}`.</span><span class="sxs-lookup"><span data-stu-id="e511a-270">For example, it can accept: `{"DegreesCelsius":"23"}` instead of `{"DegreesCelsius":23}`.</span></span> <span data-ttu-id="e511a-271">Чтобы включить это поведение в <xref:System.Text.Json>, реализуйте пользовательский преобразователь, как показано в следующем примере.</span><span class="sxs-lookup"><span data-stu-id="e511a-271">To enable that behavior in <xref:System.Text.Json>, implement a custom converter like the following example.</span></span> <span data-ttu-id="e511a-272">Преобразователь обрабатывает свойства, определенные как `long`:</span><span class="sxs-lookup"><span data-stu-id="e511a-272">The converter handles properties defined as `long`:</span></span>

* <span data-ttu-id="e511a-273">Он сериализует их как строки JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-273">It serializes them as JSON strings.</span></span> 
* <span data-ttu-id="e511a-274">Он принимает числа и числа JSON в кавычках при десериализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-274">It accepts JSON numbers and numbers within quotes while deserializing.</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/LongToStringConverter.cs)]

<span data-ttu-id="e511a-275">Зарегистрируйте этот пользовательский преобразователь, [используя атрибут](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) для отдельных свойств `long` или [добавив преобразователь](system-text-json-converters-how-to.md#registration-sample---converters-collection) в коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="e511a-275">Register this custom converter by [using an attribute](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) on individual `long` properties or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

### <a name="dictionary-with-non-string-key"></a><span data-ttu-id="e511a-276">Словарь с ключом, не являющимся строкой</span><span class="sxs-lookup"><span data-stu-id="e511a-276">Dictionary with non-string key</span></span>

<span data-ttu-id="e511a-277">`Newtonsoft.Json` поддерживает коллекции типа `Dictionary<TKey, TValue>`.</span><span class="sxs-lookup"><span data-stu-id="e511a-277">`Newtonsoft.Json` supports collections of type `Dictionary<TKey, TValue>`.</span></span> <span data-ttu-id="e511a-278">Встроенная поддержка коллекций словарей в <xref:System.Text.Json> ограничена `Dictionary<string, TValue>`.</span><span class="sxs-lookup"><span data-stu-id="e511a-278">The built-in support for dictionary collections in <xref:System.Text.Json> is limited to `Dictionary<string, TValue>`.</span></span> <span data-ttu-id="e511a-279">То есть ключ должен быть строкой.</span><span class="sxs-lookup"><span data-stu-id="e511a-279">That is, the key must be a string.</span></span>

<span data-ttu-id="e511a-280">Чтобы обеспечить поддержку словаря с целым числом или другим типом в качестве ключа, создайте такой преобразователь, как в примере, в котором [описывается написание пользовательских преобразователей](system-text-json-converters-how-to.md#support-dictionary-with-non-string-key).</span><span class="sxs-lookup"><span data-stu-id="e511a-280">To support a dictionary with an integer or some other type as the key, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-dictionary-with-non-string-key).</span></span>

### <a name="polymorphic-serialization"></a><span data-ttu-id="e511a-281">Пресериализация в полиморфизм</span><span class="sxs-lookup"><span data-stu-id="e511a-281">Polymorphic serialization</span></span>

<span data-ttu-id="e511a-282">`Newtonsoft.Json` автоматически выполняет сериализацию.</span><span class="sxs-lookup"><span data-stu-id="e511a-282">`Newtonsoft.Json` automatically does polymorphic serialization.</span></span> <span data-ttu-id="e511a-283">Сведения о возможностях ограниченного преобразования полиморфизма <xref:System.Text.Json>см. в разделе [сериализация свойств производных классов](system-text-json-how-to.md#serialize-properties-of-derived-classes).</span><span class="sxs-lookup"><span data-stu-id="e511a-283">For information about the limited polymorphic serialization capabilities of <xref:System.Text.Json>, see [Serialize properties of derived classes](system-text-json-how-to.md#serialize-properties-of-derived-classes).</span></span>

<span data-ttu-id="e511a-284">Описанный обходной путь состоит в определении свойств, которые могут содержать производные классы в качестве типа `object`.</span><span class="sxs-lookup"><span data-stu-id="e511a-284">The workaround described there is to define properties that may contain derived classes as type `object`.</span></span> <span data-ttu-id="e511a-285">Если это невозможно, можно создать преобразователь с `Write`ным методом для всей иерархии типов наследования, как в примере [записи пользовательских преобразователей](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span><span class="sxs-lookup"><span data-stu-id="e511a-285">If that isn't possible, another option is to create a converter with a `Write` method for the whole inheritance type hierarchy like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span></span>

### <a name="polymorphic-deserialization"></a><span data-ttu-id="e511a-286">Пресериализация в полиморфизм</span><span class="sxs-lookup"><span data-stu-id="e511a-286">Polymorphic deserialization</span></span>

<span data-ttu-id="e511a-287">`Newtonsoft.Json` имеет параметр `TypeNameHandling`, который добавляет метаданные имени типа в JSON во время сериализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-287">`Newtonsoft.Json` has a `TypeNameHandling` setting that adds type name metadata to the JSON while serializing.</span></span> <span data-ttu-id="e511a-288">Он использует метаданные при десериализации для выполнения полиморфизма.</span><span class="sxs-lookup"><span data-stu-id="e511a-288">It uses the metadata while deserializing to do polymorphic deserialization.</span></span> <span data-ttu-id="e511a-289"><xref:System.Text.Json> может выполнять ограниченный диапазон [полиморфизма](system-text-json-how-to.md#serialize-properties-of-derived-classes) , но не является полиморфизмом.</span><span class="sxs-lookup"><span data-stu-id="e511a-289"><xref:System.Text.Json> can do a limited range of [polymorphic serialization](system-text-json-how-to.md#serialize-properties-of-derived-classes) but not polymorphic deserialization.</span></span>

<span data-ttu-id="e511a-290">Чтобы обеспечить поддержку полиморфизма, создайте такой же конвертер, как и в примере, в котором [описывается написание пользовательских преобразователей](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span><span class="sxs-lookup"><span data-stu-id="e511a-290">To support polymorphic deserialization, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#support-polymorphic-deserialization).</span></span>

### <a name="deserialization-of-object-properties"></a><span data-ttu-id="e511a-291">Десериализация свойств объекта</span><span class="sxs-lookup"><span data-stu-id="e511a-291">Deserialization of object properties</span></span>

<span data-ttu-id="e511a-292">При `Newtonsoft.Json` десериализации для `object` свойств в POCO или в словарях типа `Dictionary<string, object>`это:</span><span class="sxs-lookup"><span data-stu-id="e511a-292">When `Newtonsoft.Json` deserializes to `object` properties in POCOs or in dictionaries of type `Dictionary<string, object>`, it:</span></span>

* <span data-ttu-id="e511a-293">Выводит типы примитивных значений в полезных данных JSON (кроме `null`) и возвращает хранимые `string`, `long`, `double`, `boolean`или `DateTime` в виде упакованного объекта.</span><span class="sxs-lookup"><span data-stu-id="e511a-293">Infers the type of primitive values in the JSON payload (other than `null`) and returns the stored `string`, `long`, `double`, `boolean`, or `DateTime` as a boxed object.</span></span> <span data-ttu-id="e511a-294">*Примитивные значения* — это одиночные значения JSON, такие как номер JSON, строка, `true`, `false`или `null`.</span><span class="sxs-lookup"><span data-stu-id="e511a-294">*Primitive values* are single JSON values such as a JSON number, string, `true`, `false`, or `null`.</span></span>
* <span data-ttu-id="e511a-295">Возвращает `JObject` или `JArray` для сложных значений в полезных данных JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-295">Returns a `JObject` or `JArray` for complex values in the JSON payload.</span></span> <span data-ttu-id="e511a-296">*Сложные значения* — это коллекции пар "ключ-значение" JSON в фигурных скобках (`{}`) или списках значений в квадратных скобках (`[]`).</span><span class="sxs-lookup"><span data-stu-id="e511a-296">*Complex values* are collections of JSON key-value pairs within braces (`{}`) or lists of values within brackets (`[]`).</span></span> <span data-ttu-id="e511a-297">Свойства и значения в фигурных скобках или квадратных скобках могут иметь дополнительные свойства или значения.</span><span class="sxs-lookup"><span data-stu-id="e511a-297">The properties and values within the braces or brackets can have additional properties or values.</span></span>
* <span data-ttu-id="e511a-298">Возвращает пустую ссылку, если полезная нагрузка содержит литерал `null` JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-298">Returns a null reference when the payload has the `null` JSON literal.</span></span>

<span data-ttu-id="e511a-299"><xref:System.Text.Json> сохраняет упакованную `JsonElement` как для простых, так и для сложных значений в свойстве `System.Object` или словаре.</span><span class="sxs-lookup"><span data-stu-id="e511a-299"><xref:System.Text.Json> stores a boxed `JsonElement` for both primitive and complex values within the `System.Object` property or dictionary value.</span></span> <span data-ttu-id="e511a-300">Однако он обрабатывает `null` так же, как `Newtonsoft.Json`, и возвращает пустую ссылку, если полезная нагрузка содержит `null` литерал JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-300">However, it treats `null` the same as `Newtonsoft.Json` and returns a null reference when the payload has the `null` JSON literal in it.</span></span>

<span data-ttu-id="e511a-301">Чтобы реализовать определение типа для `object` свойств, создайте преобразователь, как в примере, в котором [описывается написание пользовательских преобразователей](system-text-json-converters-how-to.md#deserialize-inferred-types-to-object-properties).</span><span class="sxs-lookup"><span data-stu-id="e511a-301">To implement type inference for `object` properties, create a converter like the example in [How to write custom converters](system-text-json-converters-how-to.md#deserialize-inferred-types-to-object-properties).</span></span>

### <a name="deserialize-null-to-non-nullable-type"></a><span data-ttu-id="e511a-302">Десериализация значения NULL в тип, не допускающий значения NULL</span><span class="sxs-lookup"><span data-stu-id="e511a-302">Deserialize null to non-nullable type</span></span> 

<span data-ttu-id="e511a-303">`Newtonsoft.Json` не создает исключение в следующем сценарии:</span><span class="sxs-lookup"><span data-stu-id="e511a-303">`Newtonsoft.Json` doesn't throw an exception in the following scenario:</span></span>

* <span data-ttu-id="e511a-304">для `NullValueHandling` задано значение `Ignore`, а</span><span class="sxs-lookup"><span data-stu-id="e511a-304">`NullValueHandling` is set to `Ignore`, and</span></span>
* <span data-ttu-id="e511a-305">Во время десериализации JSON содержит значение NULL для типа, не допускающего значения NULL.</span><span class="sxs-lookup"><span data-stu-id="e511a-305">During deserialization, the JSON contains a null value for a non-nullable type.</span></span>

<span data-ttu-id="e511a-306">В том же сценарии <xref:System.Text.Json> создает исключение.</span><span class="sxs-lookup"><span data-stu-id="e511a-306">In the same scenario, <xref:System.Text.Json> does throw an exception.</span></span> <span data-ttu-id="e511a-307">(Соответствующий параметр обработки значений NULL — <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues?displayProperty=nameWithType>.)</span><span class="sxs-lookup"><span data-stu-id="e511a-307">(The corresponding null handling setting is <xref:System.Text.Json.JsonSerializerOptions.IgnoreNullValues?displayProperty=nameWithType>.)</span></span>

<span data-ttu-id="e511a-308">Если вы владеете типом целевого объекта, лучшим решением будет сделать свойство в вопросе null (например, изменить `int` на `int?`).</span><span class="sxs-lookup"><span data-stu-id="e511a-308">If you own the target type, the best workaround is to make the property in question nullable (for example, change `int` to `int?`).</span></span>

<span data-ttu-id="e511a-309">Еще один обходной путь — сделать преобразователь для типа, например следующий пример, который обрабатывает значения NULL для типов `DateTimeOffset`:</span><span class="sxs-lookup"><span data-stu-id="e511a-309">Another workaround is to make a converter for the type, such as the following example that handles null values for `DateTimeOffset` types:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/DateTimeOffsetNullHandlingConverter.cs)]

<span data-ttu-id="e511a-310">Зарегистрируйте этот пользовательский преобразователь с [помощью атрибута в свойстве](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) или путем [добавления преобразователя](system-text-json-converters-how-to.md#registration-sample---converters-collection) в коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="e511a-310">Register this custom converter by [using an attribute on the property](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-property) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="e511a-311">**Примечание.** Предыдущий преобразователь **обрабатывает значения NULL иначе** , чем `Newtonsoft.Json` ВЫПОЛНЯЕТ для POCO, задающих значения по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="e511a-311">**Note:** The preceding converter **handles null values differently** than `Newtonsoft.Json` does for POCOs that specify default values.</span></span> <span data-ttu-id="e511a-312">Например, предположим, что целевой объект представляет следующий код:</span><span class="sxs-lookup"><span data-stu-id="e511a-312">For example, suppose the following code represents your target object:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWFWithDefault)]

<span data-ttu-id="e511a-313">Предположим, что следующий JSON десериализуется с помощью предыдущего конвертера:</span><span class="sxs-lookup"><span data-stu-id="e511a-313">And suppose the following JSON is deserialized by using the preceding converter:</span></span>

```json
{
  "Date": null,
  "TemperatureCelsius": 25,
  "Summary": null
}
```

<span data-ttu-id="e511a-314">После десериализации свойство `Date` имеет 1/1/0001 (`default(DateTimeOffset)`), то есть значение, заданное в конструкторе, перезаписывается.</span><span class="sxs-lookup"><span data-stu-id="e511a-314">After deserialization, the `Date` property has 1/1/0001 (`default(DateTimeOffset)`), that is, the value set in the constructor is overwritten.</span></span> <span data-ttu-id="e511a-315">При наличии одних и тех же POCO и JSON `Newtonsoft.Json` десериализация оставляет 1/1/2001 в свойстве `Date`.</span><span class="sxs-lookup"><span data-stu-id="e511a-315">Given the same POCO and JSON, `Newtonsoft.Json` deserialization would leave 1/1/2001 in the `Date` property.</span></span>

### <a name="deserialize-to-immutable-classes-and-structs"></a><span data-ttu-id="e511a-316">Десериализация в неизменяемые классы и структуры</span><span class="sxs-lookup"><span data-stu-id="e511a-316">Deserialize to immutable classes and structs</span></span>

<span data-ttu-id="e511a-317">`Newtonsoft.Json` можно десериализовать в неизменяемые классы и структуры, так как он может использовать конструкторы с параметрами.</span><span class="sxs-lookup"><span data-stu-id="e511a-317">`Newtonsoft.Json` can deserialize to immutable classes and structs because it can use constructors that have parameters.</span></span> <span data-ttu-id="e511a-318"><xref:System.Text.Json> поддерживает только открытые конструкторы без параметров.</span><span class="sxs-lookup"><span data-stu-id="e511a-318"><xref:System.Text.Json> supports only public parameterless constructors.</span></span> <span data-ttu-id="e511a-319">В качестве обходного решения можно вызвать конструктор с параметрами в пользовательском преобразователе.</span><span class="sxs-lookup"><span data-stu-id="e511a-319">As a workaround, you can call a constructor with parameters in a custom converter.</span></span>

<span data-ttu-id="e511a-320">Ниже приведена неизменяемая структура с несколькими параметрами конструктора:</span><span class="sxs-lookup"><span data-stu-id="e511a-320">Here's an immutable struct with multiple constructor parameters:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ImmutablePoint.cs#ImmutablePoint)]

<span data-ttu-id="e511a-321">А вот преобразователь, который сериализует и десериализует эту структуру:</span><span class="sxs-lookup"><span data-stu-id="e511a-321">And here's a converter that serializes and deserializes this struct:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ImmutablePointConverter.cs)]

<span data-ttu-id="e511a-322">Зарегистрируйте этот пользовательский преобразователь, [добавив преобразователь](system-text-json-converters-how-to.md#registration-sample---converters-collection) в коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="e511a-322">Register this custom converter by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="e511a-323">Пример подобного преобразователя, обрабатывающего открытые универсальные свойства, см. в разделе [встроенный преобразователь для пар "ключ-значение"](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/src/System/Text/Json/Serialization/Converters/JsonValueConverterKeyValuePair.cs).</span><span class="sxs-lookup"><span data-stu-id="e511a-323">For an example of a similar converter that handles open generic properties, see the [built-in converter for key-value pairs](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/src/System/Text/Json/Serialization/Converters/JsonValueConverterKeyValuePair.cs).</span></span>

### <a name="specify-constructor-to-use"></a><span data-ttu-id="e511a-324">Укажите используемый конструктор</span><span class="sxs-lookup"><span data-stu-id="e511a-324">Specify constructor to use</span></span>

<span data-ttu-id="e511a-325">Атрибут `Newtonsoft.Json` `[JsonConstructor]` позволяет указать, какой конструктор вызывать при десериализации POCO.</span><span class="sxs-lookup"><span data-stu-id="e511a-325">The `Newtonsoft.Json` `[JsonConstructor]` attribute lets you specify which constructor to call when deserializing to a POCO.</span></span> <span data-ttu-id="e511a-326"><xref:System.Text.Json> поддерживает только конструкторы без параметров.</span><span class="sxs-lookup"><span data-stu-id="e511a-326"><xref:System.Text.Json> supports only parameterless constructors.</span></span> <span data-ttu-id="e511a-327">В качестве обходного решения можно вызвать любой конструктор, необходимый в пользовательском преобразователе.</span><span class="sxs-lookup"><span data-stu-id="e511a-327">As a workaround, you can call whichever constructor you need in a custom converter.</span></span> <span data-ttu-id="e511a-328">См. пример для [десериализации неизменяемых классов и структур](#deserialize-to-immutable-classes-and-structs).</span><span class="sxs-lookup"><span data-stu-id="e511a-328">See the example for [Deserialize to immutable classes and structs](#deserialize-to-immutable-classes-and-structs).</span></span>

### <a name="required-properties"></a><span data-ttu-id="e511a-329">Требуемые свойства</span><span class="sxs-lookup"><span data-stu-id="e511a-329">Required properties</span></span>

<span data-ttu-id="e511a-330">В `Newtonsoft.Json`указать, что свойство требуется, задав `Required` атрибута `[JsonProperty]`.</span><span class="sxs-lookup"><span data-stu-id="e511a-330">In `Newtonsoft.Json`, you specify that a property is required by setting `Required` on the `[JsonProperty]` attribute.</span></span> <span data-ttu-id="e511a-331">`Newtonsoft.Json` создает исключение, если в JSON не получено значение для свойства, помеченного как обязательное.</span><span class="sxs-lookup"><span data-stu-id="e511a-331">`Newtonsoft.Json` throws an exception if no value is received in the JSON for a property marked as required.</span></span>

<span data-ttu-id="e511a-332"><xref:System.Text.Json> не создает исключение, если для одного из свойств целевого типа не получено никакого значения.</span><span class="sxs-lookup"><span data-stu-id="e511a-332"><xref:System.Text.Json> doesn't throw an exception if no value is received for one of the properties of the target type.</span></span> <span data-ttu-id="e511a-333">Например, если имеется класс `WeatherForecast`:</span><span class="sxs-lookup"><span data-stu-id="e511a-333">For example, if you have a `WeatherForecast` class:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWF)]

<span data-ttu-id="e511a-334">Следующий JSON десериализуется без ошибки:</span><span class="sxs-lookup"><span data-stu-id="e511a-334">The following JSON is deserialized without error:</span></span>

```json
{
    "TemperatureCelsius": 25,
    "Summary": "Hot"
}
```

<span data-ttu-id="e511a-335">Чтобы выполнить десериализацию не удалось, если в JSON нет `Date` свойства, реализуйте пользовательский преобразователь.</span><span class="sxs-lookup"><span data-stu-id="e511a-335">To make deserialization fail if no `Date` property is in the JSON, implement a custom converter.</span></span> <span data-ttu-id="e511a-336">Следующий пример кода преобразователя создает исключение, если после десериализации свойство `Date` не задано:</span><span class="sxs-lookup"><span data-stu-id="e511a-336">The following sample converter code throws an exception if the `Date` property isn't set after deserialization is complete:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastRequiredPropertyConverter.cs)]

<span data-ttu-id="e511a-337">Зарегистрируйте этот пользовательский преобразователь с [помощью атрибута в классе POCO](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) или путем [добавления преобразователя](system-text-json-converters-how-to.md#registration-sample---converters-collection) в коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="e511a-337">Register this custom converter by [using an attribute on the POCO class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="e511a-338">Если следовать этому шаблону, не передавайте объект Options при рекурсивном вызове <xref:System.Text.Json.JsonSerializer.Serialize%2A> или <xref:System.Text.Json.JsonSerializer.Deserialize%2A>.</span><span class="sxs-lookup"><span data-stu-id="e511a-338">If you follow this pattern, don't pass in the options object when recursively calling <xref:System.Text.Json.JsonSerializer.Serialize%2A> or <xref:System.Text.Json.JsonSerializer.Deserialize%2A>.</span></span> <span data-ttu-id="e511a-339">Объект options содержит коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters%2A>.</span><span class="sxs-lookup"><span data-stu-id="e511a-339">The options object contains the <xref:System.Text.Json.JsonSerializerOptions.Converters%2A> collection.</span></span> <span data-ttu-id="e511a-340">Если передать его в `Serialize` или `Deserialize`, Пользовательский преобразователь вызывает сам себя и делает бесконечный цикл, который приводит к возникновению исключения переполнения стека.</span><span class="sxs-lookup"><span data-stu-id="e511a-340">If you pass it in to `Serialize` or `Deserialize`, the custom converter calls into itself, making an infinite loop that results in a stack overflow exception.</span></span> <span data-ttu-id="e511a-341">Если параметры по умолчанию нецелесообразны, создайте новый экземпляр параметров с нужными параметрами.</span><span class="sxs-lookup"><span data-stu-id="e511a-341">If the default options are not feasible, create a new instance of the options with the settings that you need.</span></span> <span data-ttu-id="e511a-342">Этот подход будет замедляться, так как каждый новый экземпляр кэшируется независимо друг от друга.</span><span class="sxs-lookup"><span data-stu-id="e511a-342">This approach will be slow since each new instance caches independently.</span></span>

<span data-ttu-id="e511a-343">Предыдущий код преобразователя является упрощенным примером.</span><span class="sxs-lookup"><span data-stu-id="e511a-343">The preceding converter code is a simplified example.</span></span> <span data-ttu-id="e511a-344">Если необходимо обрабатывать атрибуты (например, [[жсонигноре]](xref:System.Text.Json.Serialization.JsonIgnoreAttribute) или различные параметры (например, пользовательские кодировщики), потребуется дополнительная логика.</span><span class="sxs-lookup"><span data-stu-id="e511a-344">Additional logic would be required if you need to handle attributes (such as [[JsonIgnore]](xref:System.Text.Json.Serialization.JsonIgnoreAttribute) or different options (such as custom encoders).</span></span> <span data-ttu-id="e511a-345">Кроме того, пример кода не обрабатывает свойства, для которых в конструкторе задано значение по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="e511a-345">Also, the example code doesn't handle properties for which a default value is set in the constructor.</span></span> <span data-ttu-id="e511a-346">И этот подход не различает следующие сценарии:</span><span class="sxs-lookup"><span data-stu-id="e511a-346">And this approach doesn't differentiate between the following scenarios:</span></span>

* <span data-ttu-id="e511a-347">В JSON отсутствует свойство.</span><span class="sxs-lookup"><span data-stu-id="e511a-347">A property is missing from the JSON.</span></span>
* <span data-ttu-id="e511a-348">Свойство для типа, не допускающего значения NULL, содержится в JSON, но значение является значением по умолчанию для типа, например нулевым для `int`.</span><span class="sxs-lookup"><span data-stu-id="e511a-348">A property for a non-nullable type is present in the JSON, but the value is the default for the type, such as zero for an `int`.</span></span>
* <span data-ttu-id="e511a-349">Свойство для типа, допускающего значение null, содержится в JSON, но значение равно null.</span><span class="sxs-lookup"><span data-stu-id="e511a-349">A property for a nullable type is present in the JSON, but the value is null.</span></span>

### <a name="conditionally-ignore-a-property"></a><span data-ttu-id="e511a-350">Условно игнорировать свойство</span><span class="sxs-lookup"><span data-stu-id="e511a-350">Conditionally ignore a property</span></span>

<span data-ttu-id="e511a-351">`Newtonsoft.Json` имеет несколько способов условно игнорировать свойство при сериализации или десериализации:</span><span class="sxs-lookup"><span data-stu-id="e511a-351">`Newtonsoft.Json` has several ways to conditionally ignore a property on serialization or deserialization:</span></span>

* <span data-ttu-id="e511a-352">`DefaultContractResolver` позволяет выбрать свойства для включения или исключения на основе произвольных критериев.</span><span class="sxs-lookup"><span data-stu-id="e511a-352">`DefaultContractResolver` lets you select properties to include or exclude, based on arbitrary criteria.</span></span> 
* <span data-ttu-id="e511a-353">Параметры `NullValueHandling` и `DefaultValueHandling` на `JsonSerializerSettings` позволяют указать, что все свойства со значением NULL или значениями по умолчанию должны игнорироваться.</span><span class="sxs-lookup"><span data-stu-id="e511a-353">The `NullValueHandling` and `DefaultValueHandling` settings on `JsonSerializerSettings` let you specify that all null-value or default-value properties should be ignored.</span></span>
* <span data-ttu-id="e511a-354">Параметры `NullValueHandling` и `DefaultValueHandling` атрибута `[JsonProperty]` позволяют указать отдельные свойства, которые должны игнорироваться при установке значения NULL или по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="e511a-354">The `NullValueHandling` and `DefaultValueHandling` settings on the `[JsonProperty]` attribute let you specify individual properties that should be ignored when set to null or the default value.</span></span>

<span data-ttu-id="e511a-355"><xref:System.Text.Json> предоставляет следующие способы опустить свойства при сериализации:</span><span class="sxs-lookup"><span data-stu-id="e511a-355"><xref:System.Text.Json> provides the following ways to omit properties while serializing:</span></span>

* <span data-ttu-id="e511a-356">Атрибут [[жсонигноре]](system-text-json-how-to.md#exclude-individual-properties) в свойстве приводит к исключению свойства из JSON во время сериализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-356">The [[JsonIgnore]](system-text-json-how-to.md#exclude-individual-properties) attribute on a property causes the property to be omitted from the JSON during serialization.</span></span>
* <span data-ttu-id="e511a-357">Параметр [игноренуллвалуес](system-text-json-how-to.md#exclude-all-null-value-properties) Global позволяет исключить все свойства со значением NULL.</span><span class="sxs-lookup"><span data-stu-id="e511a-357">The [IgnoreNullValues](system-text-json-how-to.md#exclude-all-null-value-properties) global option lets you exclude all null-value properties.</span></span>
* <span data-ttu-id="e511a-358">Параметр [игноререадонлипропертиес](system-text-json-how-to.md#exclude-all-read-only-properties) Global позволяет исключить все свойства только для чтения.</span><span class="sxs-lookup"><span data-stu-id="e511a-358">The [IgnoreReadOnlyProperties](system-text-json-how-to.md#exclude-all-read-only-properties) global option lets you exclude all read-only properties.</span></span>

<span data-ttu-id="e511a-359">Эти параметры **не** позволяют:</span><span class="sxs-lookup"><span data-stu-id="e511a-359">These options **don't** let you:</span></span>

* <span data-ttu-id="e511a-360">Игнорировать все свойства, имеющие значение по умолчанию для типа.</span><span class="sxs-lookup"><span data-stu-id="e511a-360">Ignore all properties that have the default value for the type.</span></span>
* <span data-ttu-id="e511a-361">Игнорировать выбранные свойства, которые имеют значение по умолчанию для типа.</span><span class="sxs-lookup"><span data-stu-id="e511a-361">Ignore selected properties that have the default value for the type.</span></span>
* <span data-ttu-id="e511a-362">Игнорировать выбранные свойства, если их значение равно null.</span><span class="sxs-lookup"><span data-stu-id="e511a-362">Ignore selected properties if their value is null.</span></span>
* <span data-ttu-id="e511a-363">Игнорировать выбранные свойства на основе произвольных критериев, вычисляемых во время выполнения.</span><span class="sxs-lookup"><span data-stu-id="e511a-363">Ignore selected properties based on arbitrary criteria evaluated at run time.</span></span> 

<span data-ttu-id="e511a-364">Для этой функции можно написать пользовательский преобразователь.</span><span class="sxs-lookup"><span data-stu-id="e511a-364">For that functionality, you can write a custom converter.</span></span> <span data-ttu-id="e511a-365">Ниже приведен пример POCO и пользовательский конвертер, демонстрирующий этот подход:</span><span class="sxs-lookup"><span data-stu-id="e511a-365">Here's a sample POCO and a custom converter for it that illustrates this approach:</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecast.cs?name=SnippetWF)]

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastRuntimeIgnoreConverter.cs)]

<span data-ttu-id="e511a-366">Преобразователь вызывает пропуск свойства `Summary` из сериализации, если его значение равно null, пустая строка или "н/д".</span><span class="sxs-lookup"><span data-stu-id="e511a-366">The converter causes the `Summary` property to be omitted from serialization if its value is null, an empty string, or "N/A".</span></span> 

<span data-ttu-id="e511a-367">Зарегистрируйте этот пользовательский преобразователь с [помощью атрибута в классе](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) или путем [добавления преобразователя](system-text-json-converters-how-to.md#registration-sample---converters-collection) в коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="e511a-367">Register this custom converter by [using an attribute on the class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="e511a-368">Этот подход требует дополнительной логики, если:</span><span class="sxs-lookup"><span data-stu-id="e511a-368">This approach requires additional logic if:</span></span>

* <span data-ttu-id="e511a-369">POCO включает сложные свойства.</span><span class="sxs-lookup"><span data-stu-id="e511a-369">The POCO includes complex properties.</span></span>
* <span data-ttu-id="e511a-370">Необходимо выполнять обработку таких атрибутов, как `[JsonIgnore]` или таких параметров, как пользовательские кодировщики.</span><span class="sxs-lookup"><span data-stu-id="e511a-370">You need to handle attributes such as `[JsonIgnore]` or options such as custom encoders.</span></span>

### <a name="specify-date-format"></a><span data-ttu-id="e511a-371">Укажите формат даты</span><span class="sxs-lookup"><span data-stu-id="e511a-371">Specify date format</span></span>

<span data-ttu-id="e511a-372">`Newtonsoft.Json` предоставляет несколько способов управления сериализацией и десериализации свойств `DateTime` и типов `DateTimeOffset`:</span><span class="sxs-lookup"><span data-stu-id="e511a-372">`Newtonsoft.Json` provides several ways to control how properties of `DateTime` and `DateTimeOffset` types are serialized and deserialized:</span></span>

* <span data-ttu-id="e511a-373">Параметр `DateTimeZoneHandling` можно использовать для сериализации всех значений `DateTime` как даты в формате UTC.</span><span class="sxs-lookup"><span data-stu-id="e511a-373">The `DateTimeZoneHandling` setting can be used to serialize all `DateTime` values as UTC dates.</span></span>
* <span data-ttu-id="e511a-374">Для настройки формата строк даты можно использовать параметры `DateFormatString` и `DateTime` преобразователей.</span><span class="sxs-lookup"><span data-stu-id="e511a-374">The `DateFormatString` setting and `DateTime` converters can be used to customize the format of date strings.</span></span>

<span data-ttu-id="e511a-375">В <xref:System.Text.Json>единственным форматом, поддерживающим встроенную поддержку, является ISO 8601-1:2019, так как он широко используется, однозначно и делает круговые циклы.</span><span class="sxs-lookup"><span data-stu-id="e511a-375">In <xref:System.Text.Json>, the only format that has built-in support is ISO 8601-1:2019 since it's widely adopted, unambiguous, and makes round trips precisely.</span></span> <span data-ttu-id="e511a-376">Чтобы использовать любой другой формат, создайте пользовательский преобразователь.</span><span class="sxs-lookup"><span data-stu-id="e511a-376">To use any other format, create a custom converter.</span></span> <span data-ttu-id="e511a-377">Дополнительные сведения см. [в разделе Поддержка DateTime и DateTimeOffset в System.Text.Json](../datetime/system-text-json-support.md).</span><span class="sxs-lookup"><span data-stu-id="e511a-377">For more information, see [DateTime and DateTimeOffset support in System.Text.Json](../datetime/system-text-json-support.md).</span></span>

### <a name="callbacks"></a><span data-ttu-id="e511a-378">Обратные вызовы</span><span class="sxs-lookup"><span data-stu-id="e511a-378">Callbacks</span></span>

<span data-ttu-id="e511a-379">`Newtonsoft.Json` позволяет выполнять пользовательский код в нескольких точках процесса сериализации или десериализации:</span><span class="sxs-lookup"><span data-stu-id="e511a-379">`Newtonsoft.Json` lets you execute custom code at several points in the serialization or deserialization process:</span></span>

* <span data-ttu-id="e511a-380">OnDeserializing (при начале десериализации объекта)</span><span class="sxs-lookup"><span data-stu-id="e511a-380">OnDeserializing (when beginning to deserialize an object)</span></span>
* <span data-ttu-id="e511a-381">Десериализация (после завершения десериализации объекта)</span><span class="sxs-lookup"><span data-stu-id="e511a-381">OnDeserialized (when finished deserializing an object)</span></span>
* <span data-ttu-id="e511a-382">Onserializing (при начале сериализации объекта)</span><span class="sxs-lookup"><span data-stu-id="e511a-382">OnSerializing (when beginning to serialize an object)</span></span>
* <span data-ttu-id="e511a-383">Onseriald (после завершения сериализации объекта)</span><span class="sxs-lookup"><span data-stu-id="e511a-383">OnSerialized (when finished serializing an object)</span></span>

<span data-ttu-id="e511a-384">В <xref:System.Text.Json>можно имитировать обратные вызовы, написав пользовательский преобразователь.</span><span class="sxs-lookup"><span data-stu-id="e511a-384">In <xref:System.Text.Json>, you can simulate callbacks by writing a custom converter.</span></span> <span data-ttu-id="e511a-385">В следующем примере показан пользовательский преобразователь для POCO.</span><span class="sxs-lookup"><span data-stu-id="e511a-385">The following example shows a custom converter for a POCO.</span></span> <span data-ttu-id="e511a-386">Этот преобразователь включает код, отображающий сообщение в каждой точке, которая соответствует обратному вызову `Newtonsoft.Json`.</span><span class="sxs-lookup"><span data-stu-id="e511a-386">The converter includes code that displays a message at each point that corresponds to a `Newtonsoft.Json` callback.</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/WeatherForecastCallbacksConverter.cs)]

<span data-ttu-id="e511a-387">Зарегистрируйте этот пользовательский преобразователь с [помощью атрибута в классе](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) или путем [добавления преобразователя](system-text-json-converters-how-to.md#registration-sample---converters-collection) в коллекцию <xref:System.Text.Json.JsonSerializerOptions.Converters>.</span><span class="sxs-lookup"><span data-stu-id="e511a-387">Register this custom converter by [using an attribute on the class](system-text-json-converters-how-to.md#registration-sample---jsonconverter-on-a-type) or by [adding the converter](system-text-json-converters-how-to.md#registration-sample---converters-collection) to the <xref:System.Text.Json.JsonSerializerOptions.Converters> collection.</span></span>

<span data-ttu-id="e511a-388">Если вы используете пользовательский преобразователь, следующий за предыдущим образцом:</span><span class="sxs-lookup"><span data-stu-id="e511a-388">If you use a custom converter that follows the preceding sample:</span></span>

* <span data-ttu-id="e511a-389">Код `OnDeserializing` не имеет доступа к новому экземпляру POCO.</span><span class="sxs-lookup"><span data-stu-id="e511a-389">The `OnDeserializing` code doesn't have access to the new POCO instance.</span></span> <span data-ttu-id="e511a-390">Чтобы управлять новым экземпляром POCO в начале десериализации, вставьте этот код в конструктор POCO.</span><span class="sxs-lookup"><span data-stu-id="e511a-390">To manipulate the new POCO instance at the start of deserialization, put that code in the POCO constructor.</span></span>
* <span data-ttu-id="e511a-391">Не передавайте объект Options при рекурсивном вызове `Serialize` или `Deserialize`.</span><span class="sxs-lookup"><span data-stu-id="e511a-391">Don't pass in the options object when recursively calling `Serialize` or `Deserialize`.</span></span> <span data-ttu-id="e511a-392">Объект options содержит коллекцию `Converters`.</span><span class="sxs-lookup"><span data-stu-id="e511a-392">The options object contains the `Converters` collection.</span></span> <span data-ttu-id="e511a-393">Если передать его в `Serialize` или `Deserialize`, будет использоваться преобразователь, что приведет к возникновению исключения переполнения стека.</span><span class="sxs-lookup"><span data-stu-id="e511a-393">If you pass it in to `Serialize` or `Deserialize`, the converter will be used, making an infinite loop that results in a stack overflow exception.</span></span>

### <a name="public-and-non-public-fields"></a><span data-ttu-id="e511a-394">Открытые и не являющиеся открытыми поля</span><span class="sxs-lookup"><span data-stu-id="e511a-394">Public and non-public fields</span></span>

<span data-ttu-id="e511a-395">`Newtonsoft.Json` могут сериализовать и десериализовать поля, а также свойства.</span><span class="sxs-lookup"><span data-stu-id="e511a-395">`Newtonsoft.Json` can serialize and deserialize fields as well as properties.</span></span> <span data-ttu-id="e511a-396"><xref:System.Text.Json> работает только с общими свойствами.</span><span class="sxs-lookup"><span data-stu-id="e511a-396"><xref:System.Text.Json> only works with public properties.</span></span> <span data-ttu-id="e511a-397">Эти функции могут быть предоставлены пользовательскими преобразователями.</span><span class="sxs-lookup"><span data-stu-id="e511a-397">Custom converters can provide this functionality.</span></span>

### <a name="internal-and-private-property-setters-and-getters"></a><span data-ttu-id="e511a-398">Методы и методы доступа для внутренних и закрытых свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-398">Internal and private property setters and getters</span></span>

<span data-ttu-id="e511a-399">`Newtonsoft.Json` может использовать частные и внутренние методы задания свойств и методы получения с помощью атрибута `JsonProperty`.</span><span class="sxs-lookup"><span data-stu-id="e511a-399">`Newtonsoft.Json` can use private and internal property setters and getters via the `JsonProperty` attribute.</span></span> <span data-ttu-id="e511a-400"><xref:System.Text.Json> поддерживает только открытые методы задания.</span><span class="sxs-lookup"><span data-stu-id="e511a-400"><xref:System.Text.Json> supports only public setters.</span></span> <span data-ttu-id="e511a-401">Эти функции могут быть предоставлены пользовательскими преобразователями.</span><span class="sxs-lookup"><span data-stu-id="e511a-401">Custom converters can provide this functionality.</span></span>

### <a name="populate-existing-objects"></a><span data-ttu-id="e511a-402">Заполнение существующих объектов</span><span class="sxs-lookup"><span data-stu-id="e511a-402">Populate existing objects</span></span>

<span data-ttu-id="e511a-403">Метод `JsonConvert.PopulateObject` в `Newtonsoft.Json` десериализует документ JSON в существующий экземпляр класса вместо создания нового экземпляра.</span><span class="sxs-lookup"><span data-stu-id="e511a-403">The `JsonConvert.PopulateObject` method in `Newtonsoft.Json` deserializes a JSON document to an existing instance of a class, instead of creating a new instance.</span></span> <span data-ttu-id="e511a-404"><xref:System.Text.Json> всегда создает новый экземпляр целевого типа с помощью открытого конструктора без параметров по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="e511a-404"><xref:System.Text.Json> always creates a new instance of the target type by using the default public parameterless constructor.</span></span> <span data-ttu-id="e511a-405">Пользовательские преобразователи можно десериализовать в существующий экземпляр.</span><span class="sxs-lookup"><span data-stu-id="e511a-405">Custom converters can deserialize to an existing instance.</span></span>

### <a name="reuse-rather-than-replace-properties"></a><span data-ttu-id="e511a-406">Повторное использование вместо замены свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-406">Reuse rather than replace properties</span></span>

<span data-ttu-id="e511a-407">Параметр `Newtonsoft.Json` `ObjectCreationHandling` позволяет указать, что объекты в свойствах следует использовать повторно, а не заменять во время десериализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-407">The `Newtonsoft.Json` `ObjectCreationHandling` setting lets you specify that objects in properties should be reused rather than replaced during deserialization.</span></span> <span data-ttu-id="e511a-408"><xref:System.Text.Json> всегда заменяет объекты в свойствах.</span><span class="sxs-lookup"><span data-stu-id="e511a-408"><xref:System.Text.Json> always replaces objects in properties.</span></span>  <span data-ttu-id="e511a-409">Эти функции могут быть предоставлены пользовательскими преобразователями.</span><span class="sxs-lookup"><span data-stu-id="e511a-409">Custom converters can provide this functionality.</span></span>

### <a name="add-to-collections-without-setters"></a><span data-ttu-id="e511a-410">Добавить в коллекции без методов задания</span><span class="sxs-lookup"><span data-stu-id="e511a-410">Add to collections without setters</span></span>

<span data-ttu-id="e511a-411">Во время десериализации `Newtonsoft.Json` добавляет объекты в коллекцию, даже если свойство не имеет метода задания.</span><span class="sxs-lookup"><span data-stu-id="e511a-411">During deserialization, `Newtonsoft.Json` adds objects to a collection even if the property has no setter.</span></span> <span data-ttu-id="e511a-412"><xref:System.Text.Json> не учитывает свойства, у которых нет методов задания.</span><span class="sxs-lookup"><span data-stu-id="e511a-412"><xref:System.Text.Json> ignores properties that don't have setters.</span></span> <span data-ttu-id="e511a-413">Эти функции могут быть предоставлены пользовательскими преобразователями.</span><span class="sxs-lookup"><span data-stu-id="e511a-413">Custom converters can provide this functionality.</span></span>

## <a name="scenarios-that-jsonserializer-currently-doesnt-support"></a><span data-ttu-id="e511a-414">Сценарии, JsonSerializer в настоящее время не поддерживают</span><span class="sxs-lookup"><span data-stu-id="e511a-414">Scenarios that JsonSerializer currently doesn't support</span></span>

<span data-ttu-id="e511a-415">В следующих сценариях обходные пути не являются практичными и невозможными.</span><span class="sxs-lookup"><span data-stu-id="e511a-415">For the following scenarios, workarounds are not practical or possible.</span></span> <span data-ttu-id="e511a-416">Если вы полагаетесь на эти `Newtonsoft.Json` функции, миграция будет невозможна без существенных изменений.</span><span class="sxs-lookup"><span data-stu-id="e511a-416">If you rely on these `Newtonsoft.Json` features, migration will not be possible without significant changes.</span></span>

### <a name="preserve-object-references-and-handle-loops"></a><span data-ttu-id="e511a-417">Сохранять ссылки на объекты и циклы обработки</span><span class="sxs-lookup"><span data-stu-id="e511a-417">Preserve object references and handle loops</span></span>

<span data-ttu-id="e511a-418">По умолчанию `Newtonsoft.Json` сериализуется по значению.</span><span class="sxs-lookup"><span data-stu-id="e511a-418">By default, `Newtonsoft.Json` serializes by value.</span></span> <span data-ttu-id="e511a-419">Например, если объект содержит два свойства, которые содержат ссылку на один и тот же объект `Person`, значения свойств этого объекта `Person` дублируются в JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-419">For example, if an object contains two properties that contain a reference to the same `Person` object, the values of that `Person` object's properties are duplicated in the JSON.</span></span>

<span data-ttu-id="e511a-420">`Newtonsoft.Json` имеет параметр `PreserveReferencesHandling` на `JsonSerializerSettings`, который позволяет выполнять сериализацию по ссылке:</span><span class="sxs-lookup"><span data-stu-id="e511a-420">`Newtonsoft.Json` has a `PreserveReferencesHandling` setting on `JsonSerializerSettings` that lets you serialize by reference:</span></span>

* <span data-ttu-id="e511a-421">Метаданные идентификатора добавляются в JSON, созданный для первого объекта `Person`.</span><span class="sxs-lookup"><span data-stu-id="e511a-421">An identifier metadata is added to the JSON created for the first `Person` object.</span></span>
* <span data-ttu-id="e511a-422">JSON, созданный для второго объекта `Person`, содержит ссылку на этот идентификатор вместо значений свойств.</span><span class="sxs-lookup"><span data-stu-id="e511a-422">The JSON that is created for the second `Person` object contains a reference to that identifier instead of property values.</span></span>

<span data-ttu-id="e511a-423">`Newtonsoft.Json` также имеет параметр `ReferenceLoopHandling`, который позволяет игнорировать циклические ссылки, а не создавать исключение.</span><span class="sxs-lookup"><span data-stu-id="e511a-423">`Newtonsoft.Json` also has a `ReferenceLoopHandling` setting that lets you ignore circular references rather than throw an exception.</span></span>

<span data-ttu-id="e511a-424"><xref:System.Text.Json> поддерживает только сериализацию по значению и создает исключение для циклических ссылок.</span><span class="sxs-lookup"><span data-stu-id="e511a-424"><xref:System.Text.Json> only supports serialization by value and throws an exception for circular references.</span></span>

### <a name="systemruntimeserialization-attributes"></a><span data-ttu-id="e511a-425">Атрибуты System. Runtime. Serialization</span><span class="sxs-lookup"><span data-stu-id="e511a-425">System.Runtime.Serialization attributes</span></span>

<span data-ttu-id="e511a-426"><xref:System.Text.Json> не поддерживает атрибуты из пространства имен `System.Runtime.Serialization`, например `DataMemberAttribute` и `IgnoreDataMemberAttribute`.</span><span class="sxs-lookup"><span data-stu-id="e511a-426"><xref:System.Text.Json> doesn't support attributes from the `System.Runtime.Serialization` namespace, such as `DataMemberAttribute` and `IgnoreDataMemberAttribute`.</span></span>

### <a name="octal-numbers"></a><span data-ttu-id="e511a-427">Восьмеричные числа</span><span class="sxs-lookup"><span data-stu-id="e511a-427">Octal numbers</span></span>

<span data-ttu-id="e511a-428">`Newtonsoft.Json` обрабатывает числа с ведущим нулем в виде восьмеричных чисел.</span><span class="sxs-lookup"><span data-stu-id="e511a-428">`Newtonsoft.Json` treats numbers with a leading zero as octal numbers.</span></span> <span data-ttu-id="e511a-429"><xref:System.Text.Json> не допускают начальных нулей, так как спецификация [RFC 8259](https://tools.ietf.org/html/rfc8259) не разрешает их.</span><span class="sxs-lookup"><span data-stu-id="e511a-429"><xref:System.Text.Json> doesn't allow leading zeroes because the [RFC 8259](https://tools.ietf.org/html/rfc8259) specification doesn't allow them.</span></span>

### <a name="missingmemberhandling"></a><span data-ttu-id="e511a-430">миссингмемберхандлинг</span><span class="sxs-lookup"><span data-stu-id="e511a-430">MissingMemberHandling</span></span>

<span data-ttu-id="e511a-431">`Newtonsoft.Json` можно настроить для создания исключений во время десериализации, если в JSON имеются свойства, отсутствующие в целевом типе.</span><span class="sxs-lookup"><span data-stu-id="e511a-431">`Newtonsoft.Json` can be configured to throw exceptions during deserialization if the JSON includes properties that are missing in the target type.</span></span> <span data-ttu-id="e511a-432"><xref:System.Text.Json> игнорирует дополнительные свойства в JSON, за исключением случаев, когда используется [атрибут [жсонекстенсиондата]](system-text-json-how-to.md#handle-overflow-json).</span><span class="sxs-lookup"><span data-stu-id="e511a-432"><xref:System.Text.Json> ignores extra properties in the JSON, except when you use the [[JsonExtensionData] attribute](system-text-json-how-to.md#handle-overflow-json).</span></span> <span data-ttu-id="e511a-433">Для функции отсутствующих членов не существует решения.</span><span class="sxs-lookup"><span data-stu-id="e511a-433">There's no workaround for the missing member feature.</span></span>

### <a name="tracewriter"></a><span data-ttu-id="e511a-434">TraceWriter</span><span class="sxs-lookup"><span data-stu-id="e511a-434">TraceWriter</span></span>

<span data-ttu-id="e511a-435">`Newtonsoft.Json` позволяет выполнять отладку с помощью `TraceWriter` для просмотра журналов, созданных при сериализации или десериализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-435">`Newtonsoft.Json` lets you debug by using a `TraceWriter` to view logs that are generated by serialization or deserialization.</span></span> <span data-ttu-id="e511a-436"><xref:System.Text.Json> не выполняет ведение журнала.</span><span class="sxs-lookup"><span data-stu-id="e511a-436"><xref:System.Text.Json> doesn't do logging.</span></span>

## <a name="jsondocument-and-jsonelement-compared-to-jtoken-like-jobject-jarray"></a><span data-ttu-id="e511a-437">Жсондокумент и Жсонелемент по сравнению с JToken (например, JObject, Жаррай)</span><span class="sxs-lookup"><span data-stu-id="e511a-437">JsonDocument and JsonElement compared to JToken (like JObject, JArray)</span></span>

<span data-ttu-id="e511a-438"><xref:System.Text.Json.JsonDocument?displayProperty=fullName> предоставляет возможность выполнять синтаксический анализ и сборку модель DOM (DOM) **только для чтения** из существующих полезных данных JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-438"><xref:System.Text.Json.JsonDocument?displayProperty=fullName> provides the ability to parse and build a **read-only** Document Object Model (DOM) from existing JSON payloads.</span></span> <span data-ttu-id="e511a-439">Модель DOM предоставляет произвольный доступ к данным в полезных данных JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-439">The DOM provides random access to data in a JSON payload.</span></span> <span data-ttu-id="e511a-440">Доступ к элементам JSON, составляющим полезные данные, можно получить с помощью типа <xref:System.Text.Json.JsonElement>.</span><span class="sxs-lookup"><span data-stu-id="e511a-440">The JSON elements that compose the payload can be accessed via the <xref:System.Text.Json.JsonElement> type.</span></span> <span data-ttu-id="e511a-441">Тип `JsonElement` предоставляет интерфейсы API для преобразования текста JSON в общие типы .NET.</span><span class="sxs-lookup"><span data-stu-id="e511a-441">The `JsonElement` type provides APIs to convert JSON text to common .NET types.</span></span> <span data-ttu-id="e511a-442">`JsonDocument` предоставляет свойство <xref:System.Text.Json.JsonDocument.RootElement>.</span><span class="sxs-lookup"><span data-stu-id="e511a-442">`JsonDocument` exposes a <xref:System.Text.Json.JsonDocument.RootElement> property.</span></span>

### <a name="jsondocument-is-idisposable"></a><span data-ttu-id="e511a-443">Жсондокумент является IDisposable</span><span class="sxs-lookup"><span data-stu-id="e511a-443">JsonDocument is IDisposable</span></span>

<span data-ttu-id="e511a-444">`JsonDocument` создает в памяти представление данных в буфере в пуле.</span><span class="sxs-lookup"><span data-stu-id="e511a-444">`JsonDocument` builds an in-memory view of the data into a pooled buffer.</span></span> <span data-ttu-id="e511a-445">Таким образом, в отличие от `JObject` или `JArray` от `Newtonsoft.Json`тип `JsonDocument` реализует `IDisposable` и должен использоваться внутри блока using.</span><span class="sxs-lookup"><span data-stu-id="e511a-445">Therefore, unlike `JObject` or `JArray` from `Newtonsoft.Json`, the `JsonDocument` type implements `IDisposable` and needs to be used inside a using block.</span></span> 

<span data-ttu-id="e511a-446">Возвращать `JsonDocument` из API, если вы хотите передать владение временем существования и освободить ответственность вызывающему.</span><span class="sxs-lookup"><span data-stu-id="e511a-446">Only return a `JsonDocument` from your API if you want to transfer lifetime ownership and dispose responsibility to the caller.</span></span> <span data-ttu-id="e511a-447">В большинстве случаев это необязательно.</span><span class="sxs-lookup"><span data-stu-id="e511a-447">In most scenarios, that isn't necessary.</span></span> <span data-ttu-id="e511a-448">Если вызывающему объекту необходимо работать с документом JSON целиком, возвращайте <xref:System.Text.Json.JsonElement.Clone%2A> <xref:System.Text.Json.JsonDocument.RootElement%2A>, то есть <xref:System.Text.Json.JsonElement>.</span><span class="sxs-lookup"><span data-stu-id="e511a-448">If the caller needs to work with the entire JSON document, return the <xref:System.Text.Json.JsonElement.Clone%2A> of the <xref:System.Text.Json.JsonDocument.RootElement%2A>, which is a <xref:System.Text.Json.JsonElement>.</span></span> <span data-ttu-id="e511a-449">Если вызывающему объекту необходимо работать с определенным элементом в документе JSON, возвращайте <xref:System.Text.Json.JsonElement.Clone%2A> этого <xref:System.Text.Json.JsonElement>.</span><span class="sxs-lookup"><span data-stu-id="e511a-449">If the caller needs to work with a particular element within the JSON document, return the <xref:System.Text.Json.JsonElement.Clone%2A> of that <xref:System.Text.Json.JsonElement>.</span></span> <span data-ttu-id="e511a-450">Если вы возвращаете `RootElement` или вложенный элемент напрямую, не выполняя `Clone`, вызывающий объект не сможет получить доступ к возвращаемому `JsonElement` после того, как `JsonDocument`, которому он принадлежит, будет удален.</span><span class="sxs-lookup"><span data-stu-id="e511a-450">If you return the `RootElement` or a sub-element directly without making a `Clone`, the caller won't be able to access the returned `JsonElement` after the `JsonDocument` that owns it is disposed.</span></span>

<span data-ttu-id="e511a-451">Ниже приведен пример, в котором необходимо сделать `Clone`:</span><span class="sxs-lookup"><span data-stu-id="e511a-451">Here's an example that requires you to make a `Clone`:</span></span>

```csharp
public JsonElement LookAndLoad(JsonElement source)
{
    string json = File.ReadAllText(source.GetProperty("fileName").GetString());
   
    using (JsonDocument doc = JsonDocument.Parse(json))
    {
        return doc.RootElement.Clone();
    }
}
```

<span data-ttu-id="e511a-452">Приведенный выше код ждет `JsonElement`, содержащий свойство `fileName`.</span><span class="sxs-lookup"><span data-stu-id="e511a-452">The preceding code expects a `JsonElement` that contains a `fileName` property.</span></span> <span data-ttu-id="e511a-453">Он открывает JSON-файл и создает `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="e511a-453">It opens the JSON file and creates a `JsonDocument`.</span></span> <span data-ttu-id="e511a-454">Метод предполагает, что вызывающий объект хочет работать со всем документом, поэтому он возвращает `Clone` `RootElement`.</span><span class="sxs-lookup"><span data-stu-id="e511a-454">The method assumes that the caller wants to work with the entire document, so it returns the `Clone` of the `RootElement`.</span></span> 

<span data-ttu-id="e511a-455">Если вы получаете `JsonElement` и возвращаете вложенный элемент, нет необходимости возвращать `Clone` вложенного элемента.</span><span class="sxs-lookup"><span data-stu-id="e511a-455">If you receive a `JsonElement` and are returning a sub-element, it's not necessary to return a `Clone` of the sub-element.</span></span> <span data-ttu-id="e511a-456">Вызывающий объект отвечает за поддержание активности `JsonDocument`, к которой принадлежит переданный `JsonElement`.</span><span class="sxs-lookup"><span data-stu-id="e511a-456">The caller is responsible for keeping alive the `JsonDocument` that the passed-in `JsonElement` belongs to.</span></span> <span data-ttu-id="e511a-457">Например:</span><span class="sxs-lookup"><span data-stu-id="e511a-457">For example:</span></span>

```csharp
public JsonElement ReturnFileName(JsonElement source)
{
   return source.GetProperty("fileName");
}
```

### <a name="jsondocument-is-read-only"></a><span data-ttu-id="e511a-458">Жсондокумент доступен только для чтения</span><span class="sxs-lookup"><span data-stu-id="e511a-458">JsonDocument is read-only</span></span>

<span data-ttu-id="e511a-459"><xref:System.Text.Json> DOM не удается добавить, удалить или изменить элементы JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-459">The <xref:System.Text.Json> DOM can't add, remove, or modify JSON elements.</span></span> <span data-ttu-id="e511a-460">Он разработан таким образом для повышения производительности и сокращения количества выделений для анализа общих размеров полезных данных JSON (то есть < 1 МБ).</span><span class="sxs-lookup"><span data-stu-id="e511a-460">It's designed this way for performance and to reduce allocations for parsing common JSON payload sizes (that is, < 1 MB).</span></span> <span data-ttu-id="e511a-461">Если в вашем сценарии используется изменяемая модель DOM, возможны следующие обходные пути.</span><span class="sxs-lookup"><span data-stu-id="e511a-461">If your scenario currently uses a modifiable DOM, one of the following workarounds might be feasible:</span></span>

* <span data-ttu-id="e511a-462">Чтобы создать `JsonDocument` с нуля (то есть без передачи существующих полезных данных JSON в метод `Parse`), напишите текст JSON с помощью `Utf8JsonWriter` и Проанализируйте выходные данные, чтобы создать новый `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="e511a-462">To build a `JsonDocument` from scratch (that is, without passing in an existing JSON payload to the `Parse` method), write the JSON text by using the `Utf8JsonWriter` and parse the output from that to make a new `JsonDocument`.</span></span>
* <span data-ttu-id="e511a-463">Чтобы изменить существующий `JsonDocument`, используйте его для записи текста JSON, внесения изменений во время записи и синтаксического анализа выходных данных для создания нового `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="e511a-463">To modify an existing `JsonDocument`, use it to write JSON text, making changes while you write, and parse the output from that to make a new `JsonDocument`.</span></span>
* <span data-ttu-id="e511a-464">Сведения о слиянии существующих документов JSON, эквивалентных API `JObject.Merge` или `JContainer.Merge` из `Newtonsoft.Json`, см. в [этой статье о проблемах GitHub](https://github.com/dotnet/corefx/issues/42466#issuecomment-570475853).</span><span class="sxs-lookup"><span data-stu-id="e511a-464">To merge existing JSON documents, equivalent to the `JObject.Merge` or `JContainer.Merge` APIs from `Newtonsoft.Json`, see [this GitHub issue](https://github.com/dotnet/corefx/issues/42466#issuecomment-570475853).</span></span>

### <a name="jsonelement-is-a-union-struct"></a><span data-ttu-id="e511a-465">Жсонелемент является структурой объединения</span><span class="sxs-lookup"><span data-stu-id="e511a-465">JsonElement is a union struct</span></span>

<span data-ttu-id="e511a-466">`JsonDocument` предоставляет `RootElement` как свойство типа <xref:System.Text.Json.JsonElement>— объединение, тип структуры, охватывающий любой элемент JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-466">`JsonDocument` exposes the `RootElement` as a property of type <xref:System.Text.Json.JsonElement>, which is a union, struct type that encompasses any JSON element.</span></span> <span data-ttu-id="e511a-467">`Newtonsoft.Json` использует выделенные иерархические типы, такие как `JObject`,`JArray`, `JToken`и т. д.</span><span class="sxs-lookup"><span data-stu-id="e511a-467">`Newtonsoft.Json` uses dedicated hierarchical types like `JObject`,`JArray`, `JToken`, and so forth.</span></span> <span data-ttu-id="e511a-468">`JsonElement` — это то, что можно искать и перечислять, и можно использовать `JsonElement` для материализации элементов JSON в типы .NET.</span><span class="sxs-lookup"><span data-stu-id="e511a-468">`JsonElement` is what you can search and enumerate over, and you can use `JsonElement` to materialize JSON elements into .NET types.</span></span>

### <a name="how-to-search-a-jsondocument-and-jsonelement-for-sub-elements"></a><span data-ttu-id="e511a-469">Поиск вложенных элементов в Жсондокумент и Жсонелемент</span><span class="sxs-lookup"><span data-stu-id="e511a-469">How to search a JsonDocument and JsonElement for sub-elements</span></span>

<span data-ttu-id="e511a-470">Выполняет поиск токенов JSON с помощью `JObject` или `JArray` от `Newtonsoft.Json`, как правило, выполняется относительно быстро, так как они уточняются в каком-либо словаре.</span><span class="sxs-lookup"><span data-stu-id="e511a-470">Searches for JSON tokens using `JObject` or `JArray` from `Newtonsoft.Json` tend to be relatively fast because they're lookups in some dictionary.</span></span> <span data-ttu-id="e511a-471">По сравнению, поиск по `JsonElement` требует последовательного поиска свойств и, следовательно, довольно медленный (например, при использовании `TryGetProperty`).</span><span class="sxs-lookup"><span data-stu-id="e511a-471">By comparison, searches on `JsonElement` require a sequential search of the properties and hence is relatively slow (for example when using `TryGetProperty`).</span></span> <span data-ttu-id="e511a-472"><xref:System.Text.Json> предназначено для снижения начального времени анализа, а не времени поиска.</span><span class="sxs-lookup"><span data-stu-id="e511a-472"><xref:System.Text.Json> is designed to minimize initial parse time rather than lookup time.</span></span> <span data-ttu-id="e511a-473">Поэтому используйте следующие подходы для оптимизации производительности при поиске по объекту `JsonDocument`.</span><span class="sxs-lookup"><span data-stu-id="e511a-473">Therefore, use the following approaches to optimize performance when searching through a `JsonDocument` object:</span></span>

* <span data-ttu-id="e511a-474">Используйте встроенные перечислители (<xref:System.Text.Json.JsonElement.EnumerateArray%2A> и <xref:System.Text.Json.JsonElement.EnumerateObject%2A>) вместо выполнения собственных индексов или циклов.</span><span class="sxs-lookup"><span data-stu-id="e511a-474">Use the built-in enumerators (<xref:System.Text.Json.JsonElement.EnumerateArray%2A> and <xref:System.Text.Json.JsonElement.EnumerateObject%2A>) rather than doing your own indexing or loops.</span></span>
* <span data-ttu-id="e511a-475">Не выполняйте последовательный поиск по всему `JsonDocument` через каждое свойство с помощью `RootElement`.</span><span class="sxs-lookup"><span data-stu-id="e511a-475">Don't do a sequential search on the whole `JsonDocument` through every property by using `RootElement`.</span></span> <span data-ttu-id="e511a-476">Вместо этого выполните поиск по вложенным объектам JSON на основе известной структуры данных JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-476">Instead, search on nested JSON objects based on the known structure of the JSON data.</span></span> <span data-ttu-id="e511a-477">Например, если вы ищете свойство `Grade` в `Student` объектов, пройдите по `Student`ным объектам и получите значение `Grade` для каждого из них, вместо того чтобы выполнять поиск по всем объектам `JsonElement`, которые ищут свойства `Grade`.</span><span class="sxs-lookup"><span data-stu-id="e511a-477">For example, if you're looking for a `Grade` property in `Student` objects, loop through the `Student` objects and get the value of `Grade` for each, rather than searching through all `JsonElement` objects looking for `Grade` properties.</span></span> <span data-ttu-id="e511a-478">Выполнение второго действия приведет к ненужным проходам по тем же данным.</span><span class="sxs-lookup"><span data-stu-id="e511a-478">Doing the latter will result in unnecessary passes over the same data.</span></span>

<span data-ttu-id="e511a-479">Пример кода см. в разделе [Использование жсондокумент для доступа к данным](system-text-json-how-to.md#use-jsondocument-for-access-to-data).</span><span class="sxs-lookup"><span data-stu-id="e511a-479">For a code example, see [Use JsonDocument for access to data](system-text-json-how-to.md#use-jsondocument-for-access-to-data).</span></span>

## <a name="utf8jsonreader-compared-to-jsontextreader"></a><span data-ttu-id="e511a-480">Utf8JsonReader по сравнению с Жсонтекстреадер</span><span class="sxs-lookup"><span data-stu-id="e511a-480">Utf8JsonReader compared to JsonTextReader</span></span>

<span data-ttu-id="e511a-481"><xref:System.Text.Json.Utf8JsonReader?displayProperty=fullName> — это высокопроизводительное, медленное средство чтения, доступное только для прямого просмотра для текста JSON в кодировке UTF-8, чтение из [реадонлиспан\<байт >](xref:System.ReadOnlySpan%601) или [реадонлисекуенце\<байт >](xref:System.Buffers.ReadOnlySequence%601).</span><span class="sxs-lookup"><span data-stu-id="e511a-481"><xref:System.Text.Json.Utf8JsonReader?displayProperty=fullName> is a high-performance, low allocation, forward-only reader for UTF-8 encoded JSON text, read from a [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601) or [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601).</span></span> <span data-ttu-id="e511a-482">`Utf8JsonReader` — это тип низкого уровня, который можно использовать для создания пользовательских средств синтаксического анализа и десериализаторов.</span><span class="sxs-lookup"><span data-stu-id="e511a-482">The `Utf8JsonReader` is a low-level type that can be used to build custom parsers and deserializers.</span></span>

<span data-ttu-id="e511a-483">В следующих разделах описываются рекомендуемые шаблоны программирования для использования `Utf8JsonReader`.</span><span class="sxs-lookup"><span data-stu-id="e511a-483">The following sections explain recommended programming patterns for using `Utf8JsonReader`.</span></span>

### <a name="utf8jsonreader-is-a-ref-struct"></a><span data-ttu-id="e511a-484">Utf8JsonReader является структурой ссылок</span><span class="sxs-lookup"><span data-stu-id="e511a-484">Utf8JsonReader is a ref struct</span></span>

<span data-ttu-id="e511a-485">Поскольку тип `Utf8JsonReader` является *структурой ссылок*, он имеет [определенные ограничения](../../csharp/language-reference/keywords/ref.md#ref-struct-types).</span><span class="sxs-lookup"><span data-stu-id="e511a-485">Because the `Utf8JsonReader` type is a *ref struct*, it has [certain limitations](../../csharp/language-reference/keywords/ref.md#ref-struct-types).</span></span> <span data-ttu-id="e511a-486">Например, он не может храниться как поле в классе или структуре, отличной от структуры ссылки.</span><span class="sxs-lookup"><span data-stu-id="e511a-486">For example, it can't be stored as a field on a class or struct other than a ref struct.</span></span> <span data-ttu-id="e511a-487">Для достижения высокой производительности этот тип должен быть `ref struct` так как он должен кэшировать входные [реадонлиспан\<byte >](xref:System.ReadOnlySpan%601), что является структурой ссылок.</span><span class="sxs-lookup"><span data-stu-id="e511a-487">To achieve high performance, this type must be a `ref struct` since it needs to cache the input [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601), which itself is a ref struct.</span></span> <span data-ttu-id="e511a-488">Кроме того, этот тип является изменяемым, так как он содержит состояние.</span><span class="sxs-lookup"><span data-stu-id="e511a-488">In addition, this type is mutable since it holds state.</span></span> <span data-ttu-id="e511a-489">Поэтому **передайте его по ссылке** , а не по значению.</span><span class="sxs-lookup"><span data-stu-id="e511a-489">Therefore, **pass it by ref** rather than by value.</span></span> <span data-ttu-id="e511a-490">Передача его по значению приведет к копированию структуры, и изменения состояния не будут видны вызывающему объекту.</span><span class="sxs-lookup"><span data-stu-id="e511a-490">Passing it by value would result in a struct copy and the state changes would not be visible to the caller.</span></span> <span data-ttu-id="e511a-491">Это отличается от `Newtonsoft.Json`, так как `Newtonsoft.Json` `JsonTextReader` является классом.</span><span class="sxs-lookup"><span data-stu-id="e511a-491">This differs from `Newtonsoft.Json` since the `Newtonsoft.Json` `JsonTextReader` is a class.</span></span> <span data-ttu-id="e511a-492">Дополнительные сведения об использовании структур ref см. в разделе [написание безопасного и C# эффективного кода](../../csharp/write-safe-efficient-code.md).</span><span class="sxs-lookup"><span data-stu-id="e511a-492">For more information about how to use ref structs, see [Write safe and efficient C# code](../../csharp/write-safe-efficient-code.md).</span></span>

### <a name="read-utf-8-text"></a><span data-ttu-id="e511a-493">Чтение текста UTF-8</span><span class="sxs-lookup"><span data-stu-id="e511a-493">Read UTF-8 text</span></span>

<span data-ttu-id="e511a-494">Для достижения наилучшей производительности при использовании `Utf8JsonReader`прочтите полезные данные JSON, уже закодированные как текст UTF-8, а не как строки UTF-16.</span><span class="sxs-lookup"><span data-stu-id="e511a-494">To achieve the best possible performance while using the `Utf8JsonReader`, read JSON payloads already encoded as UTF-8 text rather than as UTF-16 strings.</span></span> <span data-ttu-id="e511a-495">Пример кода см. в разделе [Фильтрация данных с помощью Utf8JsonReader](system-text-json-how-to.md#filter-data-using-utf8jsonreader).</span><span class="sxs-lookup"><span data-stu-id="e511a-495">For a code example, see [Filter data using Utf8JsonReader](system-text-json-how-to.md#filter-data-using-utf8jsonreader).</span></span>

### <a name="read-with-a-stream-or-pipereader"></a><span data-ttu-id="e511a-496">Чтение с помощью потока или Пипереадер</span><span class="sxs-lookup"><span data-stu-id="e511a-496">Read with a Stream or PipeReader</span></span>

<span data-ttu-id="e511a-497">`Utf8JsonReader` поддерживает чтение из кодировки UTF-8 [реадонлиспан\<byte >](xref:System.ReadOnlySpan%601) или [реадонлисекуенце\<Byte >](xref:System.Buffers.ReadOnlySequence%601) (что является результатом чтения из <xref:System.IO.Pipelines.PipeReader>).</span><span class="sxs-lookup"><span data-stu-id="e511a-497">The `Utf8JsonReader` supports reading from a UTF-8 encoded [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601) or [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601) (which is the result of reading from a <xref:System.IO.Pipelines.PipeReader>).</span></span>

<span data-ttu-id="e511a-498">Для синхронного чтения можно считать полезные данные JSON до конца потока в массив байтов и передать его в модуль чтения.</span><span class="sxs-lookup"><span data-stu-id="e511a-498">For synchronous reading, you could read the JSON payload until the end of the stream into a byte array and pass that into the reader.</span></span> <span data-ttu-id="e511a-499">Для чтения из строки (которая кодируется в кодировке UTF-16) вызовите <xref:System.Text.Encoding.UTF8>.<xref:System.Text.Encoding.GetBytes%2A></span><span class="sxs-lookup"><span data-stu-id="e511a-499">For reading from a string (which is encoded as UTF-16), call <xref:System.Text.Encoding.UTF8>.<xref:System.Text.Encoding.GetBytes%2A></span></span> <span data-ttu-id="e511a-500">Сначала перекодирует строку в массив байтов в кодировке UTF-8.</span><span class="sxs-lookup"><span data-stu-id="e511a-500">to first transcode the string to a UTF-8 encoded byte array.</span></span> <span data-ttu-id="e511a-501">Затем передайте его в `Utf8JsonReader`.</span><span class="sxs-lookup"><span data-stu-id="e511a-501">Then pass that to the `Utf8JsonReader`.</span></span> 

<span data-ttu-id="e511a-502">Поскольку `Utf8JsonReader` считает входные данные текстом JSON, отметка порядка байтов UTF-8 (BOM) считается недопустимым JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-502">Since the `Utf8JsonReader` considers the input to be JSON text, a UTF-8 byte order mark (BOM) is considered invalid JSON.</span></span> <span data-ttu-id="e511a-503">Вызывающий объект должен отфильтровать это, прежде чем передавать данные в модуль чтения.</span><span class="sxs-lookup"><span data-stu-id="e511a-503">The caller needs to filter that out before passing the data to the reader.</span></span>

<span data-ttu-id="e511a-504">Примеры кода см. в разделе [use Utf8JsonReader](system-text-json-how-to.md#use-utf8jsonreader).</span><span class="sxs-lookup"><span data-stu-id="e511a-504">For code examples, see [Use Utf8JsonReader](system-text-json-how-to.md#use-utf8jsonreader).</span></span>

### <a name="read-with-multi-segment-readonlysequence"></a><span data-ttu-id="e511a-505">Чтение с помощью Реадонлисекуенце нескольких сегментов</span><span class="sxs-lookup"><span data-stu-id="e511a-505">Read with multi-segment ReadOnlySequence</span></span>

<span data-ttu-id="e511a-506">Если входные данные JSON являются [реадонлиспан\<](xref:System.ReadOnlySpan%601), к каждому элементу JSON можно получить доступ из свойства `ValueSpan` в модуле чтения по мере прохождения цикла чтения.</span><span class="sxs-lookup"><span data-stu-id="e511a-506">If your JSON input is a [ReadOnlySpan\<byte>](xref:System.ReadOnlySpan%601), each JSON element can be accessed from the `ValueSpan` property on the reader as you go through the read loop.</span></span> <span data-ttu-id="e511a-507">Однако если входные данные являются [реадонлисекуенце\<](xref:System.Buffers.ReadOnlySequence%601) (что является результатом чтения из <xref:System.IO.Pipelines.PipeReader>), некоторые элементы JSON могут повлечь за собой несколько сегментов объекта `ReadOnlySequence<byte>`.</span><span class="sxs-lookup"><span data-stu-id="e511a-507">However, if your input is a [ReadOnlySequence\<byte>](xref:System.Buffers.ReadOnlySequence%601) (which is the result of reading from a <xref:System.IO.Pipelines.PipeReader>), some JSON elements might straddle multiple segments of the `ReadOnlySequence<byte>` object.</span></span> <span data-ttu-id="e511a-508">Эти элементы не будут доступны из <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> в непрерывном блоке памяти.</span><span class="sxs-lookup"><span data-stu-id="e511a-508">These elements would not be accessible from <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> in a contiguous memory block.</span></span> <span data-ttu-id="e511a-509">Вместо этого каждый раз, когда у вас есть несколько сегментов `ReadOnlySequence<byte>` в качестве входных данных, следует опросить свойство <xref:System.Text.Json.Utf8JsonReader.HasValueSequence%2A> в модуле чтения, чтобы выяснить, как получить доступ к текущему элементу JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-509">Instead, whenever you have a multi-segment `ReadOnlySequence<byte>` as input, poll the <xref:System.Text.Json.Utf8JsonReader.HasValueSequence%2A> property on the reader to figure out how to access the current JSON element.</span></span> <span data-ttu-id="e511a-510">Вот рекомендуемый шаблон:</span><span class="sxs-lookup"><span data-stu-id="e511a-510">Here's a recommended pattern:</span></span>

```csharp
while (reader.Read())
{
    switch (reader.TokenType)
    {
        // ...
        ReadOnlySpan<byte> jsonElement = reader.HasValueSequence ?
            reader.ValueSequence.ToArray() :
            reader.ValueSpan;
        // ...
    }
}
```

### <a name="use-valuetextequals-for-property-name-lookups"></a><span data-ttu-id="e511a-511">Использование Валуетекстекуалс для поиска имен свойств</span><span class="sxs-lookup"><span data-stu-id="e511a-511">Use ValueTextEquals for property name lookups</span></span>

<span data-ttu-id="e511a-512">Не используйте <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> для сравнения по байтам путем вызова <xref:System.MemoryExtensions.SequenceEqual%2A> для уточняющих запросов имени свойства.</span><span class="sxs-lookup"><span data-stu-id="e511a-512">Don't use <xref:System.Text.Json.Utf8JsonReader.ValueSpan%2A> to do byte-by-byte comparisons by calling <xref:System.MemoryExtensions.SequenceEqual%2A> for property name lookups.</span></span> <span data-ttu-id="e511a-513">Вместо этого вызовите <xref:System.Text.Json.Utf8JsonReader.ValueTextEquals%2A>, так как этот метод преобразует все символы, которые экранированы в JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-513">Call <xref:System.Text.Json.Utf8JsonReader.ValueTextEquals%2A> instead, because that method unescapes any characters that are escaped in the JSON.</span></span> <span data-ttu-id="e511a-514">Ниже приведен пример, демонстрирующий Поиск свойства с именем Name.</span><span class="sxs-lookup"><span data-stu-id="e511a-514">Here's an example that shows how to search for a property that is named "name":</span></span>

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ValueTextEqualsExample.cs?name=SnippetDefineUtf8Var)]

[!code-csharp[](~/samples/snippets/core/system-text-json/csharp/ValueTextEqualsExample.cs?name=SnippetUseUtf8Var&highlight=11)]

### <a name="read-null-values-into-nullable-value-types"></a><span data-ttu-id="e511a-515">Считывание значений NULL в типы значений, допускающие значения NULL</span><span class="sxs-lookup"><span data-stu-id="e511a-515">Read null values into nullable value types</span></span>

<span data-ttu-id="e511a-516">`Newtonsoft.Json` предоставляет интерфейсы API, которые возвращают <xref:System.Nullable%601>, такие как `ReadAsBoolean`, который обрабатывает `Null` `TokenType`, возвращая `bool?`.</span><span class="sxs-lookup"><span data-stu-id="e511a-516">`Newtonsoft.Json` provides APIs that return <xref:System.Nullable%601>, such as `ReadAsBoolean`, which handles a `Null` `TokenType` for you by returning a `bool?`.</span></span> <span data-ttu-id="e511a-517">Встроенные API-интерфейсы `System.Text.Json` возвращают только типы значений, не допускающие значения NULL.</span><span class="sxs-lookup"><span data-stu-id="e511a-517">The built-in `System.Text.Json` APIs return only non-nullable value types.</span></span> <span data-ttu-id="e511a-518">Например, <xref:System.Text.Json.Utf8JsonReader.GetBoolean%2A?displayProperty=nameWithType> возвращает `bool`.</span><span class="sxs-lookup"><span data-stu-id="e511a-518">For example, <xref:System.Text.Json.Utf8JsonReader.GetBoolean%2A?displayProperty=nameWithType> returns a `bool`.</span></span> <span data-ttu-id="e511a-519">Он вызывает исключение, если обнаруживает `Null` в JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-519">It throws an exception if it finds `Null` in the JSON.</span></span> <span data-ttu-id="e511a-520">В следующих примерах показаны два способа обработки значений NULL, один из которых возвращает тип значения, допускающего значение null, и один, возвращая значение по умолчанию:</span><span class="sxs-lookup"><span data-stu-id="e511a-520">The following examples show two ways to handle nulls, one by returning a nullable value type and one by returning the default value:</span></span>

```csharp
public bool? ReadAsNullableBoolean()
{
    _reader.Read();
    if (_reader.TokenType == JsonTokenType.Null)
    {
        return null;
    }
    if (_reader.TokenType != JsonTokenType.True && _reader.TokenType != JsonTokenType.False)
    {
        throw new JsonException();
    }
    return _reader.GetBoolean();
}
```

```csharp
public bool ReadAsBoolean(bool defaultValue)
{
    _reader.Read();
    if (_reader.TokenType == JsonTokenType.Null)
    {
        return defaultValue;
    }
    if (_reader.TokenType != JsonTokenType.True && _reader.TokenType != JsonTokenType.False)
    {
        throw new JsonException();
    }
    return _reader.GetBoolean();
}
```

### <a name="multi-targeting"></a><span data-ttu-id="e511a-521">Настройка для различных версий</span><span class="sxs-lookup"><span data-stu-id="e511a-521">Multi-targeting</span></span>

<span data-ttu-id="e511a-522">Если необходимо продолжить использование `Newtonsoft.Json` для определенных целевых платформ, можно использовать несколько целевых версий и две реализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-522">If you need to continue to use `Newtonsoft.Json` for certain target frameworks, you can multi-target and have two implementations.</span></span> <span data-ttu-id="e511a-523">Однако это нетривиальный, и потребуется некоторое `#ifdefs` и дублирование источника.</span><span class="sxs-lookup"><span data-stu-id="e511a-523">However, this is not trivial and would require some `#ifdefs` and source duplication.</span></span> <span data-ttu-id="e511a-524">Одним из способов совместного использования максимально возможного объема кода является создание `ref struct`ной оболочки для `Utf8JsonReader` и `Newtonsoft.Json` `JsonTextReader`.</span><span class="sxs-lookup"><span data-stu-id="e511a-524">One way to share as much code as possible is to create a `ref struct` wrapper around `Utf8JsonReader` and `Newtonsoft.Json` `JsonTextReader`.</span></span> <span data-ttu-id="e511a-525">Эта оболочка будет объединять общедоступную контактную зону при изоляции различий в поведении.</span><span class="sxs-lookup"><span data-stu-id="e511a-525">This wrapper would unify the public surface area while isolating the behavioral differences.</span></span> <span data-ttu-id="e511a-526">Это позволяет изолировать изменения в основном с созданием типа, а также передавать новый тип по ссылке.</span><span class="sxs-lookup"><span data-stu-id="e511a-526">This lets you isolate the changes mainly to the construction of the type, along with passing the new type around by reference.</span></span> <span data-ttu-id="e511a-527">Это шаблон, который следует библиотеке [Microsoft. Extensions. депенденцимодел](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) :</span><span class="sxs-lookup"><span data-stu-id="e511a-527">This is the pattern that the [Microsoft.Extensions.DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) library follows:</span></span>

* [<span data-ttu-id="e511a-528">UnifiedJsonReader.JsonTextReader.cs</span><span class="sxs-lookup"><span data-stu-id="e511a-528">UnifiedJsonReader.JsonTextReader.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonReader.JsonTextReader.cs)
* [<span data-ttu-id="e511a-529">UnifiedJsonReader.Utf8JsonReader.cs</span><span class="sxs-lookup"><span data-stu-id="e511a-529">UnifiedJsonReader.Utf8JsonReader.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonReader.Utf8JsonReader.cs)

## <a name="utf8jsonwriter-compared-to-jsontextwriter"></a><span data-ttu-id="e511a-530">Utf8JsonWriter по сравнению с JsonTextWriter</span><span class="sxs-lookup"><span data-stu-id="e511a-530">Utf8JsonWriter compared to JsonTextWriter</span></span>

<span data-ttu-id="e511a-531"><xref:System.Text.Json.Utf8JsonWriter?displayProperty=fullName> — это высокопроизводительный способ записи текста JSON в кодировке UTF-8 из распространенных типов .NET, таких как `String`, `Int32`и `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="e511a-531"><xref:System.Text.Json.Utf8JsonWriter?displayProperty=fullName> is a high-performance way to write UTF-8 encoded JSON text from common .NET types like `String`, `Int32`, and `DateTime`.</span></span> <span data-ttu-id="e511a-532">Модуль записи — это тип низкого уровня, который можно использовать для создания пользовательских сериализаторов.</span><span class="sxs-lookup"><span data-stu-id="e511a-532">The writer is a low-level type that can be used to build custom serializers.</span></span>

<span data-ttu-id="e511a-533">В следующих разделах описываются рекомендуемые шаблоны программирования для использования `Utf8JsonWriter`.</span><span class="sxs-lookup"><span data-stu-id="e511a-533">The following sections explain recommended programming patterns for using `Utf8JsonWriter`.</span></span>

### <a name="write-with-utf-8-text"></a><span data-ttu-id="e511a-534">Запись с помощью текста UTF-8</span><span class="sxs-lookup"><span data-stu-id="e511a-534">Write with UTF-8 text</span></span>

<span data-ttu-id="e511a-535">Чтобы достичь максимальной производительности при использовании `Utf8JsonWriter`, напишите полезные данные JSON, уже закодированные как текст UTF-8, а не как строки UTF-16.</span><span class="sxs-lookup"><span data-stu-id="e511a-535">To achieve the best possible performance while using the `Utf8JsonWriter`, write JSON payloads already encoded as UTF-8 text rather than as UTF-16 strings.</span></span> <span data-ttu-id="e511a-536">Используйте <xref:System.Text.Json.JsonEncodedText> для кэширования и предварительно закодировать имена и значения именованных свойств строки как статические и передать их в модуль записи вместо использования строковых литералов UTF-16.</span><span class="sxs-lookup"><span data-stu-id="e511a-536">Use <xref:System.Text.Json.JsonEncodedText> to cache and pre-encode known string property names and values as statics, and pass those to the writer, rather than using UTF-16 string literals.</span></span> <span data-ttu-id="e511a-537">Это быстрее, чем кэширование и использование байтовых массивов UTF-8.</span><span class="sxs-lookup"><span data-stu-id="e511a-537">This is faster than caching and using UTF-8 byte arrays.</span></span>

<span data-ttu-id="e511a-538">Этот подход также работает, если необходимо выполнить пользовательское экранирование.</span><span class="sxs-lookup"><span data-stu-id="e511a-538">This approach also works if you need to do custom escaping.</span></span> <span data-ttu-id="e511a-539">`System.Text.Json` не позволяет отключить экранирование при записи строки.</span><span class="sxs-lookup"><span data-stu-id="e511a-539">`System.Text.Json` doesn't let you disable escaping while writing a string.</span></span> <span data-ttu-id="e511a-540">Однако можно передать собственный пользовательский <xref:System.Text.Encodings.Web.JavaScriptEncoder> в качестве параметра для модуля записи или создать собственную `JsonEncodedText`, которая использует `JavascriptEncoder` для выполнения экранирования, а затем написать `JsonEncodedText` вместо строки.</span><span class="sxs-lookup"><span data-stu-id="e511a-540">However, you could pass in your own custom <xref:System.Text.Encodings.Web.JavaScriptEncoder> as an option to the writer, or create your own `JsonEncodedText` that uses your `JavascriptEncoder` to do the escaping, and then write the `JsonEncodedText` instead of the string.</span></span> <span data-ttu-id="e511a-541">Дополнительные сведения см. в разделе [Настройка кодировки символов](system-text-json-how-to.md#customize-character-encoding).</span><span class="sxs-lookup"><span data-stu-id="e511a-541">For more information, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="write-raw-values"></a><span data-ttu-id="e511a-542">Запись необработанных значений</span><span class="sxs-lookup"><span data-stu-id="e511a-542">Write raw values</span></span>

<span data-ttu-id="e511a-543">Метод `Newtonsoft.Json` `WriteRawValue` записывает необработанный код JSON, где ожидается значение.</span><span class="sxs-lookup"><span data-stu-id="e511a-543">The `Newtonsoft.Json` `WriteRawValue` method writes raw JSON where a value is expected.</span></span> <span data-ttu-id="e511a-544"><xref:System.Text.Json> не имеет прямого эквивалента, но ниже приведено решение, обеспечивающее написание только допустимого JSON:</span><span class="sxs-lookup"><span data-stu-id="e511a-544"><xref:System.Text.Json> has no direct equivalent, but here's a workaround that ensures only valid JSON is written:</span></span>

```csharp
using JsonDocument doc = JsonDocument.Parse(string);
doc.WriteTo(writer);
```

### <a name="customize-character-escaping"></a><span data-ttu-id="e511a-545">Настройка экранирования символов</span><span class="sxs-lookup"><span data-stu-id="e511a-545">Customize character escaping</span></span>

<span data-ttu-id="e511a-546">Параметр [стринжескапехандлинг](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_StringEscapeHandling.htm) `JsonTextWriter` предлагает параметры для экранирования всех символов, не являющихся символами ASCII **, или** символов HTML.</span><span class="sxs-lookup"><span data-stu-id="e511a-546">The [StringEscapeHandling](https://www.newtonsoft.com/json/help/html/T_Newtonsoft_Json_StringEscapeHandling.htm) setting of `JsonTextWriter` offers options to escape all non-ASCII characters **or** HTML characters.</span></span> <span data-ttu-id="e511a-547">По умолчанию `Utf8JsonWriter` обходит все символы, отличные от ASCII **и** HTML.</span><span class="sxs-lookup"><span data-stu-id="e511a-547">By default, `Utf8JsonWriter` escapes all non-ASCII **and** HTML characters.</span></span> <span data-ttu-id="e511a-548">Такое экранирование выполняется в целях глубокой защиты.</span><span class="sxs-lookup"><span data-stu-id="e511a-548">This escaping is done for defense-in-depth security reasons.</span></span> <span data-ttu-id="e511a-549">Чтобы указать другую политику экранирования, создайте <xref:System.Text.Encodings.Web.JavaScriptEncoder> и задайте <xref:System.Text.Json.JsonWriterOptions.Encoder?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e511a-549">To specify a different escaping policy, create a <xref:System.Text.Encodings.Web.JavaScriptEncoder> and set <xref:System.Text.Json.JsonWriterOptions.Encoder?displayProperty=nameWithType>.</span></span> <span data-ttu-id="e511a-550">Дополнительные сведения см. в разделе [Настройка кодировки символов](system-text-json-how-to.md#customize-character-encoding).</span><span class="sxs-lookup"><span data-stu-id="e511a-550">For more information, see [Customize character encoding](system-text-json-how-to.md#customize-character-encoding).</span></span>

### <a name="customize-json-format"></a><span data-ttu-id="e511a-551">Настройка формата JSON</span><span class="sxs-lookup"><span data-stu-id="e511a-551">Customize JSON format</span></span>

<span data-ttu-id="e511a-552">`JsonTextWriter` включает следующие параметры, для которых `Utf8JsonWriter` не имеет эквивалента.</span><span class="sxs-lookup"><span data-stu-id="e511a-552">`JsonTextWriter` includes the following settings, for which `Utf8JsonWriter` has no equivalent:</span></span>

* <span data-ttu-id="e511a-553">[Отступ](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_Indentation.htm) — задает количество символов для отступа.</span><span class="sxs-lookup"><span data-stu-id="e511a-553">[Indentation](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_Indentation.htm) - Specifies how many characters to indent.</span></span> <span data-ttu-id="e511a-554">`Utf8JsonWriter` всегда имеет 2-значный отступ.</span><span class="sxs-lookup"><span data-stu-id="e511a-554">`Utf8JsonWriter` always does 2-character indentation.</span></span>
* <span data-ttu-id="e511a-555">[Индентчар](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_IndentChar.htm) — указывает символ, используемый для отступа.</span><span class="sxs-lookup"><span data-stu-id="e511a-555">[IndentChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_IndentChar.htm) - Specifies the character to use for indentation.</span></span>  <span data-ttu-id="e511a-556">`Utf8JsonWriter` всегда использует пробелы.</span><span class="sxs-lookup"><span data-stu-id="e511a-556">`Utf8JsonWriter` always uses whitespace.</span></span>
* <span data-ttu-id="e511a-557">[QuoteChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteChar.htm) — указывает символ, используемый для окружить строковых значений.</span><span class="sxs-lookup"><span data-stu-id="e511a-557">[QuoteChar](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteChar.htm) - Specifies the character to use to surround string values.</span></span>  <span data-ttu-id="e511a-558">`Utf8JsonWriter` всегда использует двойные кавычки.</span><span class="sxs-lookup"><span data-stu-id="e511a-558">`Utf8JsonWriter` always uses double quotes.</span></span>
* <span data-ttu-id="e511a-559">[QuoteName](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteName.htm) — указывает, следует ли заключать имена свойств в кавычки.</span><span class="sxs-lookup"><span data-stu-id="e511a-559">[QuoteName](https://www.newtonsoft.com/json/help/html/P_Newtonsoft_Json_JsonTextWriter_QuoteName.htm) - Specifies whether or not to surround property names with quotes.</span></span>  <span data-ttu-id="e511a-560">`Utf8JsonWriter` всегда заключаются в кавычки.</span><span class="sxs-lookup"><span data-stu-id="e511a-560">`Utf8JsonWriter` always surrounds them with quotes.</span></span>

<span data-ttu-id="e511a-561">Не существует решений, которые позволяют настроить JSON, созданный `Utf8JsonWriter`.</span><span class="sxs-lookup"><span data-stu-id="e511a-561">There are no workarounds that would let you customize the JSON produced by `Utf8JsonWriter` in these ways.</span></span>

### <a name="write-null-values"></a><span data-ttu-id="e511a-562">Записать значения NULL</span><span class="sxs-lookup"><span data-stu-id="e511a-562">Write null values</span></span>

<span data-ttu-id="e511a-563">Чтобы записать значения NULL с помощью `Utf8JsonWriter`, вызовите:</span><span class="sxs-lookup"><span data-stu-id="e511a-563">To write null values by using `Utf8JsonWriter`, call:</span></span>

* <span data-ttu-id="e511a-564"><xref:System.Text.Json.Utf8JsonWriter.WriteNull%2A> записать пару "ключ-значение" со значением NULL в качестве значения.</span><span class="sxs-lookup"><span data-stu-id="e511a-564"><xref:System.Text.Json.Utf8JsonWriter.WriteNull%2A> to write a key-value pair with null as the value.</span></span>
* <span data-ttu-id="e511a-565"><xref:System.Text.Json.Utf8JsonWriter.WriteNullValue%2A> записать значение null как элемент массива JSON.</span><span class="sxs-lookup"><span data-stu-id="e511a-565"><xref:System.Text.Json.Utf8JsonWriter.WriteNullValue%2A> to write null as an element of a JSON array.</span></span>

<span data-ttu-id="e511a-566">Для строкового свойства, если строка имеет значение null, <xref:System.Text.Json.Utf8JsonWriter.WriteString%2A> и <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A> эквивалентны `WriteNull` и `WriteNullValue`.</span><span class="sxs-lookup"><span data-stu-id="e511a-566">For a string property, if the string is null, <xref:System.Text.Json.Utf8JsonWriter.WriteString%2A> and <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A> are equivalent to `WriteNull` and `WriteNullValue`.</span></span>

### <a name="write-timespan-uri-or-char-values"></a><span data-ttu-id="e511a-567">Запись значений TimeSpan, URI или char</span><span class="sxs-lookup"><span data-stu-id="e511a-567">Write Timespan, Uri, or char values</span></span>

<span data-ttu-id="e511a-568">`JsonTextWriter` предоставляет методы `WriteValue` для значений [TimeSpan](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_18.htm), [URI](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_22.htm)и [char](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_3.htm) .</span><span class="sxs-lookup"><span data-stu-id="e511a-568">`JsonTextWriter` provides `WriteValue` methods for [TimeSpan](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_18.htm), [Uri](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_22.htm), and [char](https://www.newtonsoft.com/json/help/html/M_Newtonsoft_Json_JsonTextWriter_WriteValue_3.htm) values.</span></span> <span data-ttu-id="e511a-569">`Utf8JsonWriter` не имеет эквивалентных методов.</span><span class="sxs-lookup"><span data-stu-id="e511a-569">`Utf8JsonWriter` doesn't have equivalent methods.</span></span> <span data-ttu-id="e511a-570">Вместо этого следует отформатировать эти значения как строки (например, вызвав `ToString()`) и вызвать <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A>.</span><span class="sxs-lookup"><span data-stu-id="e511a-570">Instead, format these values as strings (by calling `ToString()`, for example) and call <xref:System.Text.Json.Utf8JsonWriter.WriteStringValue%2A>.</span></span>

### <a name="multi-targeting"></a><span data-ttu-id="e511a-571">Настройка для различных версий</span><span class="sxs-lookup"><span data-stu-id="e511a-571">Multi-targeting</span></span>

<span data-ttu-id="e511a-572">Если необходимо продолжить использование `Newtonsoft.Json` для определенных целевых платформ, можно использовать несколько целевых версий и две реализации.</span><span class="sxs-lookup"><span data-stu-id="e511a-572">If you need to continue to use `Newtonsoft.Json` for certain target frameworks, you can multi-target and have two implementations.</span></span> <span data-ttu-id="e511a-573">Однако это нетривиальный, и потребуется некоторое `#ifdefs` и дублирование источника.</span><span class="sxs-lookup"><span data-stu-id="e511a-573">However, this is not trivial and would require some `#ifdefs` and source duplication.</span></span> <span data-ttu-id="e511a-574">Одним из способов совместного использования максимально возможного объема кода является создание оболочки для `Utf8JsonWriter` и `Newtonsoft` `JsonTextWriter`.</span><span class="sxs-lookup"><span data-stu-id="e511a-574">One way to share as much code as possible is to create a wrapper around `Utf8JsonWriter` and `Newtonsoft` `JsonTextWriter`.</span></span> <span data-ttu-id="e511a-575">Эта оболочка будет объединять общедоступную контактную зону при изоляции различий в поведении.</span><span class="sxs-lookup"><span data-stu-id="e511a-575">This wrapper would unify the public surface area while isolating the behavioral differences.</span></span> <span data-ttu-id="e511a-576">Это позволяет изолировать изменения в основном с созданием типа.</span><span class="sxs-lookup"><span data-stu-id="e511a-576">This lets you isolate the changes mainly to the construction of the type.</span></span> <span data-ttu-id="e511a-577">Библиотека [Microsoft. Extensions. депенденцимодел](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="e511a-577">[Microsoft.Extensions.DependencyModel](https://www.nuget.org/packages/Microsoft.Extensions.DependencyModel/3.1.0/) library follows:</span></span>

* [<span data-ttu-id="e511a-578">UnifiedJsonWriter.JsonTextWriter.cs</span><span class="sxs-lookup"><span data-stu-id="e511a-578">UnifiedJsonWriter.JsonTextWriter.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonWriter.JsonTextWriter.cs)
* [<span data-ttu-id="e511a-579">UnifiedJsonWriter.Utf8JsonWriter.cs</span><span class="sxs-lookup"><span data-stu-id="e511a-579">UnifiedJsonWriter.Utf8JsonWriter.cs</span></span>](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/installer/managed/Microsoft.Extensions.DependencyModel/UnifiedJsonWriter.Utf8JsonWriter.cs)

## <a name="additional-resources"></a><span data-ttu-id="e511a-580">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="e511a-580">Additional resources</span></span>

<!-- * [System.Text.Json roadmap](https://github.com/dotnet/runtime/blob/81bf79fd9aa75305e55abe2f7e9ef3f60624a3a1/src/libraries/System.Text.Json/roadmap/README.md)[Restore this when the roadmap is updated.]-->
* <span data-ttu-id="e511a-581">[Обзор System.Text.Json](system-text-json-overview.md)</span><span class="sxs-lookup"><span data-stu-id="e511a-581">[System.Text.Json overview](system-text-json-overview.md)</span></span>
* <span data-ttu-id="e511a-582">[Использование System.Text.Json](system-text-json-how-to.md)</span><span class="sxs-lookup"><span data-stu-id="e511a-582">[How to use System.Text.Json](system-text-json-how-to.md)</span></span>
* [<span data-ttu-id="e511a-583">Написание пользовательских преобразователей</span><span class="sxs-lookup"><span data-stu-id="e511a-583">How to write custom converters</span></span>](system-text-json-converters-how-to.md)
* <span data-ttu-id="e511a-584">[Поддержка DateTime и DateTimeOffset в System.Text.Json](../datetime/system-text-json-support.md)</span><span class="sxs-lookup"><span data-stu-id="e511a-584">[DateTime and DateTimeOffset support in System.Text.Json](../datetime/system-text-json-support.md)</span></span>
* <span data-ttu-id="e511a-585">[Справочник по System.Text.Json API](xref:System.Text.Json)</span><span class="sxs-lookup"><span data-stu-id="e511a-585">[System.Text.Json API reference](xref:System.Text.Json)</span></span>
* <span data-ttu-id="e511a-586">[System.Text.Json. Справочник по API сериализации](xref:System.Text.Json.Serialization)</span><span class="sxs-lookup"><span data-stu-id="e511a-586">[System.Text.Json.Serialization API reference](xref:System.Text.Json.Serialization)</span></span>
