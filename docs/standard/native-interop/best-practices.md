---
title: Рекомендации по использованию взаимодействия на уровне машинного кода для .NET
description: Ознакомьтесь с рекомендациями по взаимодействию с компонентами на уровне машинного кода в .NET.
ms.date: 01/18/2019
ms.openlocfilehash: e5d96471e796dca712d25d2d9e2609508180d83f
ms.sourcegitcommit: a9b8945630426a575ab0a332e568edc807666d1b
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/30/2020
ms.locfileid: "80391225"
---
# <a name="native-interoperability-best-practices"></a><span data-ttu-id="78a1c-103">Рекомендации по использованию взаимодействия на уровне машинного кода</span><span class="sxs-lookup"><span data-stu-id="78a1c-103">Native interoperability best practices</span></span>

<span data-ttu-id="78a1c-104">Платформа .NET предоставляет несколько способов настройки машинного кода для взаимодействия.</span><span class="sxs-lookup"><span data-stu-id="78a1c-104">.NET gives you a variety of ways to customize your native interoperability code.</span></span> <span data-ttu-id="78a1c-105">В этой статье приводятся рекомендации для команд .NET-разработчиков, которые сталкиваются с проблемами взаимодействия на уровне машинного кода.</span><span class="sxs-lookup"><span data-stu-id="78a1c-105">This article includes the guidance that Microsoft's .NET teams follow for native interoperability.</span></span>

## <a name="general-guidance"></a><span data-ttu-id="78a1c-106">Общие рекомендации</span><span class="sxs-lookup"><span data-stu-id="78a1c-106">General guidance</span></span>

<span data-ttu-id="78a1c-107">Рекомендации в этом разделе относятся ко всем сценариям взаимодействия.</span><span class="sxs-lookup"><span data-stu-id="78a1c-107">The guidance in this section applies to all interop scenarios.</span></span>

- <span data-ttu-id="78a1c-108">✔️ РЕКОМЕНДУЕТСЯ использовать такие же имена и написание для методов и параметров, как и для метода машинного кода, который вы хотите вызвать.</span><span class="sxs-lookup"><span data-stu-id="78a1c-108">✔️ DO use the same naming and capitalization for your methods and parameters as the native method you want to call.</span></span>
- <span data-ttu-id="78a1c-109">✔️ ДОПУСТИМО использовать такое же именование и написание для констант.</span><span class="sxs-lookup"><span data-stu-id="78a1c-109">✔️ CONSIDER using the same naming and capitalization for constant values.</span></span>
- <span data-ttu-id="78a1c-110">✔️ РЕКОМЕНДУЕТСЯ использовать типы .NET, которые наиболее сопоставимы с собственным типом.</span><span class="sxs-lookup"><span data-stu-id="78a1c-110">✔️ DO use .NET types that map closest to the native type.</span></span> <span data-ttu-id="78a1c-111">Например, если собственный тип в C# — `unsigned int`, используйте `uint`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-111">For example, in C#, use `uint` when the native type is `unsigned int`.</span></span>
- <span data-ttu-id="78a1c-112">✔️ РЕКОМЕНДУЕТСЯ использовать только атрибуты `[In]` и `[Out]`, если реакция на событие, которую нужно реализовать, отличается от реакции по умолчанию.</span><span class="sxs-lookup"><span data-stu-id="78a1c-112">✔️ DO only use `[In]` and `[Out]` attributes when the behavior you want differs from the default behavior.</span></span>
- <span data-ttu-id="78a1c-113">✔️ ДОПУСТИМО использовать <xref:System.Buffers.ArrayPool%601?displayProperty=nameWithType> для заполнения буферов собственного массива.</span><span class="sxs-lookup"><span data-stu-id="78a1c-113">✔️ CONSIDER using <xref:System.Buffers.ArrayPool%601?displayProperty=nameWithType> to pool your native array buffers.</span></span>
- <span data-ttu-id="78a1c-114">✔️ ДОПУСТИМО создавать оболочку для объявлений P/Invoke в классе с таким же именем и написанием, как в собственной библиотеке.</span><span class="sxs-lookup"><span data-stu-id="78a1c-114">✔️ CONSIDER wrapping your P/Invoke declarations in a class with the same name and capitalization as your native library.</span></span>
  - <span data-ttu-id="78a1c-115">Это позволяет использовать возможность языка C# `nameof` для атрибутов `[DllImport]`, чтобы передать имя собственной библиотеки и проверить правильность его написания.</span><span class="sxs-lookup"><span data-stu-id="78a1c-115">This allows your `[DllImport]` attributes to use the C# `nameof` language feature to pass in the name of the native library and ensure that you didn't misspell the name of the native library.</span></span>

## <a name="dllimport-attribute-settings"></a><span data-ttu-id="78a1c-116">Параметры атрибута DllImport</span><span class="sxs-lookup"><span data-stu-id="78a1c-116">DllImport attribute settings</span></span>

| <span data-ttu-id="78a1c-117">Параметр</span><span class="sxs-lookup"><span data-stu-id="78a1c-117">Setting</span></span> | <span data-ttu-id="78a1c-118">Значение по умолчанию</span><span class="sxs-lookup"><span data-stu-id="78a1c-118">Default</span></span> | <span data-ttu-id="78a1c-119">Рекомендация</span><span class="sxs-lookup"><span data-stu-id="78a1c-119">Recommendation</span></span> | <span data-ttu-id="78a1c-120">Сведения</span><span class="sxs-lookup"><span data-stu-id="78a1c-120">Details</span></span> |
|---------|---------|----------------|---------|
| <xref:System.Runtime.InteropServices.DllImportAttribute.PreserveSig>   | `true` |  <span data-ttu-id="78a1c-121">Оставьте значение по умолчанию</span><span class="sxs-lookup"><span data-stu-id="78a1c-121">keep default</span></span>  | <span data-ttu-id="78a1c-122">Если для параметра явно задано значение false, в результате сбоя запроса HRESULT возвращаются значения, которые вызывают исключения (и в результате возвращаемое значение в месте определения становится пустым).</span><span class="sxs-lookup"><span data-stu-id="78a1c-122">When this is explicitly set to false, failed HRESULT return values will be turned into exceptions (and the return value in the definition becomes null as a result).</span></span>|
| <xref:System.Runtime.InteropServices.DllImportAttribute.SetLastError> | `false`  | <span data-ttu-id="78a1c-123">в зависимости от API</span><span class="sxs-lookup"><span data-stu-id="78a1c-123">depends on the API</span></span>  | <span data-ttu-id="78a1c-124">Присвойте этому параметру значение true, если для получения значения в API используется GetLastError и Marshal.GetLastWin32Error.</span><span class="sxs-lookup"><span data-stu-id="78a1c-124">Set this to true if the API uses GetLastError and use Marshal.GetLastWin32Error to get the value.</span></span> <span data-ttu-id="78a1c-125">Если API устанавливает условие, указывающее на ошибку, перед тем как выполнить другие вызовы, получите информацию об ошибке, чтобы избежать ее непреднамеренной перезаписи.</span><span class="sxs-lookup"><span data-stu-id="78a1c-125">If the API sets a condition that says it has an error, get the error before making other calls to avoid inadvertently having it overwritten.</span></span>|
| <xref:System.Runtime.InteropServices.DllImportAttribute.CharSet> | <span data-ttu-id="78a1c-126">`CharSet.None` с откатом к реакции на событие `CharSet.Ansi`</span><span class="sxs-lookup"><span data-stu-id="78a1c-126">`CharSet.None`, which falls back to `CharSet.Ansi` behavior</span></span>  | <span data-ttu-id="78a1c-127">Если в определении есть строки или символы, используйте `CharSet.Unicode` или `CharSet.Ansi` в явном виде</span><span class="sxs-lookup"><span data-stu-id="78a1c-127">Explicitly  use `CharSet.Unicode` or `CharSet.Ansi` when strings or characters are present in the definition</span></span> | <span data-ttu-id="78a1c-128">Указывает реакцию на событие при маршалинге строк и то, как работает `ExactSpelling`, если задано значение `false`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-128">This specifies marshaling behavior of strings and what `ExactSpelling` does when `false`.</span></span> <span data-ttu-id="78a1c-129">Обратите внимание, что в Unix `CharSet.Ansi` имеет кодировку UTF8.</span><span class="sxs-lookup"><span data-stu-id="78a1c-129">Note that `CharSet.Ansi` is actually UTF8 on Unix.</span></span> <span data-ttu-id="78a1c-130">_Обычно_ в Windows используется Юникод, а в Unix — UTF8.</span><span class="sxs-lookup"><span data-stu-id="78a1c-130">_Most_ of the time Windows uses Unicode while Unix uses UTF8.</span></span> <span data-ttu-id="78a1c-131">См. дополнительные сведения в [документации по кодировке](./charset.md).</span><span class="sxs-lookup"><span data-stu-id="78a1c-131">See more information on the [documentation on charsets](./charset.md).</span></span> |
| <xref:System.Runtime.InteropServices.DllImportAttribute.ExactSpelling> | `false` | `true`             | <span data-ttu-id="78a1c-132">Если присвоить этому параметру значение true, можно немного повысить производительность — среда выполнения не будет искать другие имена функций с суффиксом "A" или "W" в зависимости от значения параметра `CharSet` ("A" для `CharSet.Ansi` и "W" для `CharSet.Unicode`).</span><span class="sxs-lookup"><span data-stu-id="78a1c-132">Set this to true and gain a slight perf benefit as the runtime will not look for alternate function names with either an "A" or "W" suffix depending on the value of the `CharSet` setting ("A" for `CharSet.Ansi` and "W" for `CharSet.Unicode`).</span></span> |

## <a name="string-parameters"></a><span data-ttu-id="78a1c-133">Параметры строки</span><span class="sxs-lookup"><span data-stu-id="78a1c-133">String parameters</span></span>

<span data-ttu-id="78a1c-134">Если выбрана кодировка Юникод или аргумент явно обозначен как `[MarshalAs(UnmanagedType.LPWSTR)]` _и_ строка передается по значению (не по `ref` или `out`), строка будет закреплена, чтобы использоваться непосредственно в машинном коде (вместо копирования).</span><span class="sxs-lookup"><span data-stu-id="78a1c-134">When the CharSet is Unicode or the argument is explicitly marked as `[MarshalAs(UnmanagedType.LPWSTR)]` _and_ the string is passed by value (not `ref` or `out`), the string will be pinned and used directly by native code (rather than copied).</span></span>

<span data-ttu-id="78a1c-135">Если требуется обработка строк по стандарту Американского национального института стандартов (ANSI) в явном виде, не забудьте отметить параметр `[DllImport]` как `Charset.Unicode`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-135">Remember to mark the `[DllImport]` as `Charset.Unicode` unless you explicitly want ANSI treatment of your strings.</span></span>

<span data-ttu-id="78a1c-136">❌НЕ используйте `[Out] string` параметры.</span><span class="sxs-lookup"><span data-stu-id="78a1c-136">❌ DO NOT use `[Out] string` parameters.</span></span> <span data-ttu-id="78a1c-137">Если эта строка интернирована, строковые параметры, передаваемые по значению с атрибутом `[Out]`, могут дестабилизировать среду выполнения.</span><span class="sxs-lookup"><span data-stu-id="78a1c-137">String parameters passed by value with the `[Out]` attribute can destabilize the runtime if the string is an interned string.</span></span> <span data-ttu-id="78a1c-138">См. дополнительные сведения в документации по методу интернирования строк <xref:System.String.Intern%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="78a1c-138">See more information about string interning in the documentation for <xref:System.String.Intern%2A?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="78a1c-139">❌ПАРАМЕТРЫ `StringBuilder` AVOID.</span><span class="sxs-lookup"><span data-stu-id="78a1c-139">❌ AVOID `StringBuilder` parameters.</span></span> <span data-ttu-id="78a1c-140">`StringBuilder` во время маршалинга *всегда* создает копию собственного буфера.</span><span class="sxs-lookup"><span data-stu-id="78a1c-140">`StringBuilder` marshaling *always* creates a native buffer copy.</span></span> <span data-ttu-id="78a1c-141">Таким образом, маршалинг может оказаться крайне неэффективным.</span><span class="sxs-lookup"><span data-stu-id="78a1c-141">As such, it can be extremely inefficient.</span></span> <span data-ttu-id="78a1c-142">Выполните обычный сценарий вызова API Windows, который принимает строку:</span><span class="sxs-lookup"><span data-stu-id="78a1c-142">Take the typical scenario of calling a Windows API that takes a string:</span></span>

1. <span data-ttu-id="78a1c-143">Создание SB нужной емкости (распределяет управляемую емкость)**{1}**</span><span class="sxs-lookup"><span data-stu-id="78a1c-143">Create a SB of the desired capacity (allocates managed capacity) **{1}**</span></span>
2. <span data-ttu-id="78a1c-144">Invoke</span><span class="sxs-lookup"><span data-stu-id="78a1c-144">Invoke</span></span>
   1. <span data-ttu-id="78a1c-145">Выделяет родной буфер**{2}**</span><span class="sxs-lookup"><span data-stu-id="78a1c-145">Allocates a native buffer **{2}**</span></span>
   2. <span data-ttu-id="78a1c-146">Копируется содержимое, если задано значение `[In]` _(значение по умолчанию для параметра `StringBuilder`)_.</span><span class="sxs-lookup"><span data-stu-id="78a1c-146">Copies the contents if `[In]` _(the default for a `StringBuilder` parameter)_</span></span>
   3. <span data-ttu-id="78a1c-147">В только что выделенный управляемый массив копируется собственный буфер, если задано значение `[Out]` **{3}** _(также устанавливается по умолчанию для `StringBuilder`)_.</span><span class="sxs-lookup"><span data-stu-id="78a1c-147">Copies the native buffer into a newly allocated managed array if `[Out]` **{3}** _(also the default for `StringBuilder`)_</span></span>
3. <span data-ttu-id="78a1c-148">`ToString()`выделяет еще один управляемый массив**{4}**</span><span class="sxs-lookup"><span data-stu-id="78a1c-148">`ToString()` allocates yet another managed array **{4}**</span></span>

<span data-ttu-id="78a1c-149">Это *{4}* выделение средств для получения строки из родного кода.</span><span class="sxs-lookup"><span data-stu-id="78a1c-149">That is *{4}* allocations to get a string out of native code.</span></span> <span data-ttu-id="78a1c-150">Чтобы уменьшить их количество, повторно используйте `StringBuilder` в другом вызове. Но это позволяет сэкономить только *одно* распределение.</span><span class="sxs-lookup"><span data-stu-id="78a1c-150">The best you can do to limit this is to reuse the `StringBuilder` in another call but this still only saves *1* allocation.</span></span> <span data-ttu-id="78a1c-151">Гораздо лучше использовать и кэшировать буфер символов из `ArrayPool` — так вы сможете ограничиться распределением для `ToString()` при последующих вызовах.</span><span class="sxs-lookup"><span data-stu-id="78a1c-151">It's much better to use and cache a character buffer from `ArrayPool`- you can then get down to just the allocation for the `ToString()` on subsequent calls.</span></span>

<span data-ttu-id="78a1c-152">Еще одна проблема, связанная с `StringBuilder`, заключается в том, что этот атрибут всегда создает резервную копию буфера возврата к первому значению NULL.</span><span class="sxs-lookup"><span data-stu-id="78a1c-152">The other issue with `StringBuilder` is that it always copies the return buffer back up to the first null.</span></span> <span data-ttu-id="78a1c-153">Если возвращенная строка не завершена или завершается двумя символами NULL, атрибут P/Invoke задан неправильно (в лучшем случае).</span><span class="sxs-lookup"><span data-stu-id="78a1c-153">If the passed back string isn't terminated or is a double-null-terminated string, your P/Invoke is incorrect at best.</span></span>

<span data-ttu-id="78a1c-154">Если вы все же *используете*`StringBuilder`, возникает еще одна проблема — емкость **не** включает скрытого значения NULL, которое всегда учитывается при взаимодействии.</span><span class="sxs-lookup"><span data-stu-id="78a1c-154">If you *do* use `StringBuilder`, one last gotcha is that the capacity does **not** include a hidden null, which is always accounted for in interop.</span></span> <span data-ttu-id="78a1c-155">Пользователи часто ошибаются, так как для многих API требуется, чтобы в размер буфера *включалось* значение NULL.</span><span class="sxs-lookup"><span data-stu-id="78a1c-155">It's common for people to get this wrong as most APIs want the size of the buffer *including* the null.</span></span> <span data-ttu-id="78a1c-156">Это может привести к чрезмерному или ненужному распределению.</span><span class="sxs-lookup"><span data-stu-id="78a1c-156">This can result in wasted/unnecessary allocations.</span></span> <span data-ttu-id="78a1c-157">Кроме того, эта ошибка не позволяет среде выполнения оптимизировать маршалинг `StringBuilder` для минимизации количества копий.</span><span class="sxs-lookup"><span data-stu-id="78a1c-157">Additionally, this gotcha prevents the runtime from optimizing `StringBuilder` marshaling to minimize copies.</span></span>

<span data-ttu-id="78a1c-158">✔️ ДОПУСТИМО использовать массивы `char[]` из `ArrayPool`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-158">✔️ CONSIDER using `char[]`s from an `ArrayPool`.</span></span>

<span data-ttu-id="78a1c-159">Дополнительные сведения см. в статье [Маршалинг по умолчанию для строк](../../framework/interop/default-marshaling-for-strings.md) и разделе [Customizing string parameters](customize-parameter-marshaling.md#customizing-string-parameters) (Настройка строковых параметров).</span><span class="sxs-lookup"><span data-stu-id="78a1c-159">For more information on string marshaling, see [Default Marshaling for Strings](../../framework/interop/default-marshaling-for-strings.md) and [Customizing string marshaling](customize-parameter-marshaling.md#customizing-string-parameters).</span></span>

> <span data-ttu-id="78a1c-160">__Специфические Windows__ Для `[Out]` строк CLR будет `CoTaskMemFree` использоватьпо умолчанию `SysStringFree` свободные строки или `UnmanagedType.BSTR`для строк, которые помечены как.</span><span class="sxs-lookup"><span data-stu-id="78a1c-160">__Windows Specific__ For `[Out]` strings the CLR will use `CoTaskMemFree` by default to free strings or `SysStringFree` for strings that are marked as `UnmanagedType.BSTR`.</span></span>
> <span data-ttu-id="78a1c-161">**Для большинства AA с буфером строки вывода:** Пройденное в коле символов должно включать null.</span><span class="sxs-lookup"><span data-stu-id="78a1c-161">**For most APIs with an output string buffer:** The passed in character count must include the null.</span></span> <span data-ttu-id="78a1c-162">Если возвращаемое значение меньше, чем количество переданных символов, значит, вызов завершился успешно. В таком случае значение — это количество символов *без* NULL в конце.</span><span class="sxs-lookup"><span data-stu-id="78a1c-162">If the returned value is less than the passed in character count the call has succeeded and the value is the number of characters *without* the trailing null.</span></span> <span data-ttu-id="78a1c-163">В противном случае это требуемый размер буфера, *включая* нуль-символ.</span><span class="sxs-lookup"><span data-stu-id="78a1c-163">Otherwise the count is the required size of the buffer *including* the null character.</span></span>
>
> - <span data-ttu-id="78a1c-164">Передайте в 5, получить 4: строка 4 символов длиной с задней нулю.</span><span class="sxs-lookup"><span data-stu-id="78a1c-164">Pass in 5, get 4: The string is 4 characters long with a trailing null.</span></span>
> - <span data-ttu-id="78a1c-165">Передайте в 5, получить 6: строка 5 символов длиной, необходимо 6 символов буфера провести null.</span><span class="sxs-lookup"><span data-stu-id="78a1c-165">Pass in 5, get 6: The string is 5 characters long, need a 6 character buffer to hold the null.</span></span>
> <span data-ttu-id="78a1c-166">[Windows Data Types for Strings](/windows/desktop/Intl/windows-data-types-for-strings) (Типы данных Windows для работы со строками)</span><span class="sxs-lookup"><span data-stu-id="78a1c-166">[Windows Data Types for Strings](/windows/desktop/Intl/windows-data-types-for-strings)</span></span>

## <a name="boolean-parameters-and-fields"></a><span data-ttu-id="78a1c-167">Логические параметры и поля</span><span class="sxs-lookup"><span data-stu-id="78a1c-167">Boolean parameters and fields</span></span>

<span data-ttu-id="78a1c-168">Логические значения можно легко спутать.</span><span class="sxs-lookup"><span data-stu-id="78a1c-168">Booleans are easy to mess up.</span></span> <span data-ttu-id="78a1c-169">По умолчанию тип .NET `bool` маршалируется в тип Windows `BOOL`, где имеет 4-байтовое значение.</span><span class="sxs-lookup"><span data-stu-id="78a1c-169">By default, a .NET `bool` is marshaled to a Windows `BOOL`, where it's a 4-byte value.</span></span> <span data-ttu-id="78a1c-170">Но типы `_Bool` и `bool` в C и C++ имеют размер *один* байт.</span><span class="sxs-lookup"><span data-stu-id="78a1c-170">However, the `_Bool`, and `bool` types in C and C++ are a *single* byte.</span></span> <span data-ttu-id="78a1c-171">Это может усложнить поиск ошибок, так как половина возвращаемого значения может не учитываться, что, *возможно*, изменит результат.</span><span class="sxs-lookup"><span data-stu-id="78a1c-171">This can lead to hard to track down bugs as half the return value will be discarded, which will only *potentially* change the result.</span></span> <span data-ttu-id="78a1c-172">См. дополнительные сведения о маршалинге значений .NET `bool` для типов C или C++ `bool` в документации по [настройке маршалинга логического поля](customize-struct-marshaling.md#customizing-boolean-field-marshaling).</span><span class="sxs-lookup"><span data-stu-id="78a1c-172">For more for information on marshaling .NET `bool` values to C or C++ `bool` types, see the documentation on [customizing boolean field marshaling](customize-struct-marshaling.md#customizing-boolean-field-marshaling).</span></span>

## <a name="guids"></a><span data-ttu-id="78a1c-173">Идентификаторы GUID</span><span class="sxs-lookup"><span data-stu-id="78a1c-173">GUIDs</span></span>

<span data-ttu-id="78a1c-174">Идентификаторы GUID используются непосредственно в сигнатурах.</span><span class="sxs-lookup"><span data-stu-id="78a1c-174">GUIDs are usable directly in signatures.</span></span> <span data-ttu-id="78a1c-175">Многие API Windows принимают такие псевдонимы типа `GUID&`, как `REFIID`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-175">Many Windows APIs take `GUID&` type aliases like `REFIID`.</span></span> <span data-ttu-id="78a1c-176">При их передаче по ссылке можно воспользоваться атрибутом `ref` или `[MarshalAs(UnmanagedType.LPStruct)]`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-176">When passed by ref, they can either be passed by `ref` or with the `[MarshalAs(UnmanagedType.LPStruct)]` attribute.</span></span>

| <span data-ttu-id="78a1c-177">GUID</span><span class="sxs-lookup"><span data-stu-id="78a1c-177">GUID</span></span> | <span data-ttu-id="78a1c-178">Идентификатор GUID для передачи по ссылке</span><span class="sxs-lookup"><span data-stu-id="78a1c-178">By-ref GUID</span></span> |
|------|-------------|
| `KNOWNFOLDERID` | `REFKNOWNFOLDERID` |

<span data-ttu-id="78a1c-179">❌НЕ используйте `[MarshalAs(UnmanagedType.LPStruct)]` ни `ref` для чего, кроме параметров GUID.</span><span class="sxs-lookup"><span data-stu-id="78a1c-179">❌ DO NOT Use `[MarshalAs(UnmanagedType.LPStruct)]` for anything other than `ref` GUID parameters.</span></span>

## <a name="blittable-types"></a><span data-ttu-id="78a1c-180">Непреобразуемые типы данных</span><span class="sxs-lookup"><span data-stu-id="78a1c-180">Blittable types</span></span>

<span data-ttu-id="78a1c-181">Непреобразуемые типы — это типы данных с одинаковым представлением на битовом уровне в управляемом и машинном коде.</span><span class="sxs-lookup"><span data-stu-id="78a1c-181">Blittable types are types that have the same bit-level representation in managed and native code.</span></span> <span data-ttu-id="78a1c-182">Для маршалинга в машинный код и из него эти типы не обязательно преобразовывать в другой формат, что повышает производительность. Поэтому им следует отдавать предпочтение.</span><span class="sxs-lookup"><span data-stu-id="78a1c-182">As such they do not need to be converted to another format to be marshaled to and from native code, and as this improves performance they should be preferred.</span></span>

<span data-ttu-id="78a1c-183">**Непреобразуемые типы данных:**</span><span class="sxs-lookup"><span data-stu-id="78a1c-183">**Blittable types:**</span></span>

- <span data-ttu-id="78a1c-184">`byte`, `sbyte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `single`, `double`</span><span class="sxs-lookup"><span data-stu-id="78a1c-184">`byte`, `sbyte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `single`, `double`</span></span>
- <span data-ttu-id="78a1c-185">невложенные одномерные массивы непреобразуемых типов (например, `int[]`);</span><span class="sxs-lookup"><span data-stu-id="78a1c-185">non-nested one-dimensional arrays of blittable types (for example, `int[]`)</span></span>
- <span data-ttu-id="78a1c-186">структуры и классы с фиксированной структурой, содержащие только непреобразуемые значения, например типы полей;</span><span class="sxs-lookup"><span data-stu-id="78a1c-186">structs and classes with fixed layout that only have blittable value types for instance fields</span></span>
  - <span data-ttu-id="78a1c-187">чтобы обеспечить фиксированную структуру, необходимо указать `[StructLayout(LayoutKind.Sequential)]` или `[StructLayout(LayoutKind.Explicit)]`;</span><span class="sxs-lookup"><span data-stu-id="78a1c-187">fixed layout requires `[StructLayout(LayoutKind.Sequential)]` or `[StructLayout(LayoutKind.Explicit)]`</span></span>
  - <span data-ttu-id="78a1c-188">по умолчанию для структур указывается `LayoutKind.Sequential`, а для классов — `LayoutKind.Auto`</span><span class="sxs-lookup"><span data-stu-id="78a1c-188">structs are `LayoutKind.Sequential` by default, classes are `LayoutKind.Auto`</span></span>

<span data-ttu-id="78a1c-189">**Преобразуемые типы данных:**</span><span class="sxs-lookup"><span data-stu-id="78a1c-189">**NOT blittable:**</span></span>

- `bool`

<span data-ttu-id="78a1c-190">**ПЕРИОДИЧЕСКИ преобразуемые типы данных:**</span><span class="sxs-lookup"><span data-stu-id="78a1c-190">**SOMETIMES blittable:**</span></span>

- <span data-ttu-id="78a1c-191">`char`, `string`</span><span class="sxs-lookup"><span data-stu-id="78a1c-191">`char`, `string`</span></span>

<span data-ttu-id="78a1c-192">Если непреобразуемые типы передаются по ссылке, маршалер просто закрепляет их вместо копирования в промежуточный буфер</span><span class="sxs-lookup"><span data-stu-id="78a1c-192">When blittable types are passed by reference, they're simply pinned by the marshaller instead of being copied to an intermediate buffer.</span></span> <span data-ttu-id="78a1c-193">(классы изначально передаются по ссылке, структуры данных передаются по ссылке при использовании атрибутов `ref` или `out`).</span><span class="sxs-lookup"><span data-stu-id="78a1c-193">(Classes are inherently passed by reference, structs are passed by reference when used with `ref` or `out`.)</span></span>

<span data-ttu-id="78a1c-194">Тип `char` является непреобразуемым в одномерном массиве **или**, если он является частью другого типа, явным образом помечается `[StructLayout]` с `CharSet = CharSet.Unicode`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-194">`char` is blittable in a one-dimensional array **or** if it's part of a type that contains it's explicitly marked with `[StructLayout]` with `CharSet = CharSet.Unicode`.</span></span>

```csharp
[StructLayout(LayoutKind.Sequential, CharSet = CharSet.Unicode)]
public struct UnicodeCharStruct
{
    public char c;
}
```

<span data-ttu-id="78a1c-195">Тип `string` является непреобразуемым, если не содержится в другом типе, и передается как аргумент с атрибутом `[MarshalAs(UnmanagedType.LPWStr)]` или `[DllImport]` имеет набор `CharSet = CharSet.Unicode`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-195">`string` is blittable if it isn't contained in another type and it's being passed as an argument that is marked with `[MarshalAs(UnmanagedType.LPWStr)]` or the `[DllImport]` has `CharSet = CharSet.Unicode` set.</span></span>

<span data-ttu-id="78a1c-196">Чтобы проверить, является ли тип непреобразуемым, попытайтесь создать закрепленный атрибут `GCHandle`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-196">You can see if a type is blittable by attempting to create a pinned `GCHandle`.</span></span> <span data-ttu-id="78a1c-197">Если тип не является строкой или считается непреобразуемым, `GCHandle.Alloc` вызовет `ArgumentException`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-197">If the type isn't a string or considered blittable, `GCHandle.Alloc` will throw an `ArgumentException`.</span></span>

<span data-ttu-id="78a1c-198">✔️ РЕКОМЕНДУЕТСЯ по возможности сделать свои структуры данных непреобразуемыми.</span><span class="sxs-lookup"><span data-stu-id="78a1c-198">✔️ DO make your structures blittable when possible.</span></span>

<span data-ttu-id="78a1c-199">Дополнительные сведения см. в разделе:</span><span class="sxs-lookup"><span data-stu-id="78a1c-199">For more information, see:</span></span>

- [<span data-ttu-id="78a1c-200">Преобразуемые и непреобразуемые типы</span><span class="sxs-lookup"><span data-stu-id="78a1c-200">Blittable and Non-Blittable Types</span></span>](../../framework/interop/blittable-and-non-blittable-types.md)
- [<span data-ttu-id="78a1c-201">Тип Маршалинга</span><span class="sxs-lookup"><span data-stu-id="78a1c-201">Type Marshaling</span></span>](type-marshaling.md)

## <a name="keeping-managed-objects-alive"></a><span data-ttu-id="78a1c-202">Сохранение управляемых объектов активными</span><span class="sxs-lookup"><span data-stu-id="78a1c-202">Keeping managed objects alive</span></span>

<span data-ttu-id="78a1c-203">`GC.KeepAlive()` гарантирует, что объект остается в области видимости, пока не будет завершен метод проверки активности KeepAlive.</span><span class="sxs-lookup"><span data-stu-id="78a1c-203">`GC.KeepAlive()` will ensure an object stays in scope until the KeepAlive method is hit.</span></span>

<span data-ttu-id="78a1c-204">[`HandleRef`](xref:System.Runtime.InteropServices.HandleRef)позволяет маршалу сохранять объект живым в течение всего срока действия P/Invoke.</span><span class="sxs-lookup"><span data-stu-id="78a1c-204">[`HandleRef`](xref:System.Runtime.InteropServices.HandleRef) allows the marshaller to keep an object alive for the duration of a P/Invoke.</span></span> <span data-ttu-id="78a1c-205">Его можно использовать вместо `IntPtr` в сигнатурах метода.</span><span class="sxs-lookup"><span data-stu-id="78a1c-205">It can be used instead of `IntPtr` in method signatures.</span></span> <span data-ttu-id="78a1c-206">`SafeHandle` фактически заменяет этот класс и должен использоваться вместо него.</span><span class="sxs-lookup"><span data-stu-id="78a1c-206">`SafeHandle` effectively replaces this class and should be used instead.</span></span>

<span data-ttu-id="78a1c-207">[`GCHandle`](xref:System.Runtime.InteropServices.GCHandle)позволяет прикрепить управляемый объект и получить родной указатель на него.</span><span class="sxs-lookup"><span data-stu-id="78a1c-207">[`GCHandle`](xref:System.Runtime.InteropServices.GCHandle) allows pinning a managed object and getting the native pointer to it.</span></span> <span data-ttu-id="78a1c-208">Базовый шаблон приведен ниже:</span><span class="sxs-lookup"><span data-stu-id="78a1c-208">The basic pattern is:</span></span>

```csharp
GCHandle handle = GCHandle.Alloc(obj, GCHandleType.Pinned);
IntPtr ptr = handle.AddrOfPinnedObject();
handle.Free();
```

<span data-ttu-id="78a1c-209">Для `GCHandle` по умолчанию не задано закрепление.</span><span class="sxs-lookup"><span data-stu-id="78a1c-209">Pinning isn't the default for `GCHandle`.</span></span> <span data-ttu-id="78a1c-210">Еще один основной шаблон предназначен для передачи ссылки на управляемый объект через машинный код и обратно на управляемый код, обычно с обратным вызовом.</span><span class="sxs-lookup"><span data-stu-id="78a1c-210">The other major pattern is for passing a reference to a managed object through native code and back to managed code, usually with a callback.</span></span> <span data-ttu-id="78a1c-211">Вот этот шаблон:</span><span class="sxs-lookup"><span data-stu-id="78a1c-211">Here is the pattern:</span></span>

```csharp
GCHandle handle = GCHandle.Alloc(obj);
SomeNativeEnumerator(callbackDelegate, GCHandle.ToIntPtr(handle));

// In the callback
GCHandle handle = GCHandle.FromIntPtr(param);
object managedObject = handle.Target;

// After the last callback
handle.Free();
```

<span data-ttu-id="78a1c-212">Не забывайте, что `GCHandle` необходимо явно освобождать, чтобы избежать утечек памяти.</span><span class="sxs-lookup"><span data-stu-id="78a1c-212">Don't forget that `GCHandle` needs to be explicitly freed to avoid memory leaks.</span></span>

## <a name="common-windows-data-types"></a><span data-ttu-id="78a1c-213">Общие типы данных Windows</span><span class="sxs-lookup"><span data-stu-id="78a1c-213">Common Windows data types</span></span>

<span data-ttu-id="78a1c-214">Ниже приведен список типов данных, часто используемых в API Windows, и типов данных C#, используемых при вызове в виде кода Windows.</span><span class="sxs-lookup"><span data-stu-id="78a1c-214">Here is a list of data types commonly used in Windows APIs and which C# types to use when calling into the Windows code.</span></span>

<span data-ttu-id="78a1c-215">Указанные ниже типы имеют одинаковый размер в 32-разрядной и 64-разрядной версиях Windows независимо от их имен.</span><span class="sxs-lookup"><span data-stu-id="78a1c-215">The following types are the same size on 32-bit and 64-bit Windows, despite their names.</span></span>

| <span data-ttu-id="78a1c-216">Ширина</span><span class="sxs-lookup"><span data-stu-id="78a1c-216">Width</span></span> | <span data-ttu-id="78a1c-217">Windows</span><span class="sxs-lookup"><span data-stu-id="78a1c-217">Windows</span></span>          | <span data-ttu-id="78a1c-218">C (Windows)</span><span class="sxs-lookup"><span data-stu-id="78a1c-218">C (Windows)</span></span>          | <span data-ttu-id="78a1c-219">C#</span><span class="sxs-lookup"><span data-stu-id="78a1c-219">C#</span></span>       | <span data-ttu-id="78a1c-220">Альтернатива</span><span class="sxs-lookup"><span data-stu-id="78a1c-220">Alternative</span></span>                          |
|:------|:-----------------|:---------------------|:---------|:-------------------------------------|
| <span data-ttu-id="78a1c-221">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-221">32</span></span>    | `BOOL`           | `int`                | `int`    | `bool`                               |
| <span data-ttu-id="78a1c-222">8</span><span class="sxs-lookup"><span data-stu-id="78a1c-222">8</span></span>     | `BOOLEAN`        | `unsigned char`      | `byte`   | `[MarshalAs(UnmanagedType.U1)] bool` |
| <span data-ttu-id="78a1c-223">8</span><span class="sxs-lookup"><span data-stu-id="78a1c-223">8</span></span>     | `BYTE`           | `unsigned char`      | `byte`   |                                      |
| <span data-ttu-id="78a1c-224">8</span><span class="sxs-lookup"><span data-stu-id="78a1c-224">8</span></span>     | `CHAR`           | `char`               | `sbyte`  |                                      |
| <span data-ttu-id="78a1c-225">8</span><span class="sxs-lookup"><span data-stu-id="78a1c-225">8</span></span>     | `UCHAR`          | `unsigned char`      | `byte`   |                                      |
| <span data-ttu-id="78a1c-226">16</span><span class="sxs-lookup"><span data-stu-id="78a1c-226">16</span></span>    | `SHORT`          | `short`              | `short`  |                                      |
| <span data-ttu-id="78a1c-227">16</span><span class="sxs-lookup"><span data-stu-id="78a1c-227">16</span></span>    | `CSHORT`         | `short`              | `short`  |                                      |
| <span data-ttu-id="78a1c-228">16</span><span class="sxs-lookup"><span data-stu-id="78a1c-228">16</span></span>    | `USHORT`         | `unsigned short`     | `ushort` |                                      |
| <span data-ttu-id="78a1c-229">16</span><span class="sxs-lookup"><span data-stu-id="78a1c-229">16</span></span>    | `WORD`           | `unsigned short`     | `ushort` |                                      |
| <span data-ttu-id="78a1c-230">16</span><span class="sxs-lookup"><span data-stu-id="78a1c-230">16</span></span>    | `ATOM`           | `unsigned short`     | `ushort` |                                      |
| <span data-ttu-id="78a1c-231">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-231">32</span></span>    | `INT`            | `int`                | `int`    |                                      |
| <span data-ttu-id="78a1c-232">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-232">32</span></span>    | `LONG`           | `long`               | `int`    |                                      |
| <span data-ttu-id="78a1c-233">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-233">32</span></span>    | `ULONG`          | `unsigned long`      | `uint`   |                                      |
| <span data-ttu-id="78a1c-234">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-234">32</span></span>    | `DWORD`          | `unsigned long`      | `uint`   |                                      |
| <span data-ttu-id="78a1c-235">64</span><span class="sxs-lookup"><span data-stu-id="78a1c-235">64</span></span>    | `QWORD`          | `long long`          | `long`   |                                      |
| <span data-ttu-id="78a1c-236">64</span><span class="sxs-lookup"><span data-stu-id="78a1c-236">64</span></span>    | `LARGE_INTEGER`  | `long long`          | `long`   |                                      |
| <span data-ttu-id="78a1c-237">64</span><span class="sxs-lookup"><span data-stu-id="78a1c-237">64</span></span>    | `LONGLONG`       | `long long`          | `long`   |                                      |
| <span data-ttu-id="78a1c-238">64</span><span class="sxs-lookup"><span data-stu-id="78a1c-238">64</span></span>    | `ULONGLONG`      | `unsigned long long` | `ulong`  |                                      |
| <span data-ttu-id="78a1c-239">64</span><span class="sxs-lookup"><span data-stu-id="78a1c-239">64</span></span>    | `ULARGE_INTEGER` | `unsigned long long` | `ulong`  |                                      |
| <span data-ttu-id="78a1c-240">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-240">32</span></span>    | `HRESULT`        | `long`               | `int`    |                                      |
| <span data-ttu-id="78a1c-241">32</span><span class="sxs-lookup"><span data-stu-id="78a1c-241">32</span></span>    | `NTSTATUS`       | `long`               | `int`    |                                      |

<span data-ttu-id="78a1c-242">Следующие типы являются указателями и зависят от ширины платформы.</span><span class="sxs-lookup"><span data-stu-id="78a1c-242">The following types, being pointers, do follow the width of the platform.</span></span> <span data-ttu-id="78a1c-243">Для них используйте `IntPtr`/`UIntPtr`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-243">Use `IntPtr`/`UIntPtr` for these.</span></span>

| <span data-ttu-id="78a1c-244">Типы указателей со знаком (используйте `IntPtr`)</span><span class="sxs-lookup"><span data-stu-id="78a1c-244">Signed Pointer Types (use `IntPtr`)</span></span> | <span data-ttu-id="78a1c-245">Типы указателей без знака (используйте `UIntPtr`)</span><span class="sxs-lookup"><span data-stu-id="78a1c-245">Unsigned Pointer Types (use `UIntPtr`)</span></span> |
|:------------------------------------|:---------------------------------------|
| `HANDLE`                            | `WPARAM`                               |
| `HWND`                              | `UINT_PTR`                             |
| `HINSTANCE`                         | `ULONG_PTR`                            |
| `LPARAM`                            | `SIZE_T`                               |
| `LRESULT`                           |                                        |
| `LONG_PTR`                          |                                        |
| `INT_PTR`                           |                                        |

<span data-ttu-id="78a1c-246">`PVOID` в Windows — это аналог `void*` в C, который можно маршалировать как `IntPtr` или `UIntPtr`, но рекомендуется по возможности выбирать `void*`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-246">A Windows `PVOID` which is a C `void*` can be marshaled as either `IntPtr` or `UIntPtr`, but prefer `void*` when possible.</span></span>

[<span data-ttu-id="78a1c-247">Типы данных Windows</span><span class="sxs-lookup"><span data-stu-id="78a1c-247">Windows Data Types</span></span>](/windows/desktop/WinProg/windows-data-types)

[<span data-ttu-id="78a1c-248">Диапазоны типов данных</span><span class="sxs-lookup"><span data-stu-id="78a1c-248">Data Type Ranges</span></span>](/cpp/cpp/data-type-ranges)

## <a name="structs"></a><span data-ttu-id="78a1c-249">Структуры</span><span class="sxs-lookup"><span data-stu-id="78a1c-249">Structs</span></span>

<span data-ttu-id="78a1c-250">Управляемые структуры создаются в стеке и не удаляются, пока метод не вернет значение.</span><span class="sxs-lookup"><span data-stu-id="78a1c-250">Managed structs are created on the stack and aren't removed until the method returns.</span></span> <span data-ttu-id="78a1c-251">Таким образом, по определению, эти структуры "закреплены" (не перемещаются службой сборки мусора).</span><span class="sxs-lookup"><span data-stu-id="78a1c-251">By definition then, they are "pinned" (it won't get moved by the GC).</span></span> <span data-ttu-id="78a1c-252">Кроме того, если в машинном коде не используется указатель, по завершении этого метода вы можете получить адрес из блоков неуправляемого кода.</span><span class="sxs-lookup"><span data-stu-id="78a1c-252">You can also simply take the address in unsafe code blocks if native code won't use the pointer past the end of the current method.</span></span>

<span data-ttu-id="78a1c-253">Непреобразуемые структуры обеспечивают высокую производительность, так как их можно использовать непосредственно на слое маршалинга.</span><span class="sxs-lookup"><span data-stu-id="78a1c-253">Blittable structs are much more performant as they can simply be used directly by the marshaling layer.</span></span> <span data-ttu-id="78a1c-254">Попробуйте сделать структуры непреобразуемыми (например, не используйте атрибут `bool`).</span><span class="sxs-lookup"><span data-stu-id="78a1c-254">Try to make structs blittable (for example, avoid `bool`).</span></span> <span data-ttu-id="78a1c-255">См. дополнительные сведения в разделе [Непреобразуемые типы данных](#blittable-types).</span><span class="sxs-lookup"><span data-stu-id="78a1c-255">For more information, see the [Blittable Types](#blittable-types) section.</span></span>

<span data-ttu-id="78a1c-256">*Если* структура является непреобразуемой, используйте атрибут `sizeof()` вместо `Marshal.SizeOf<MyStruct>()`, чтобы повысить производительность.</span><span class="sxs-lookup"><span data-stu-id="78a1c-256">*If* the struct is blittable, use `sizeof()` instead of `Marshal.SizeOf<MyStruct>()` for better performance.</span></span> <span data-ttu-id="78a1c-257">Как описано выше, чтобы проверить, является ли тип непреобразуемым, попытайтесь создать закрепленный атрибут `GCHandle`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-257">As mentioned above, you can validate that the type is blittable by attempting to create a pinned `GCHandle`.</span></span> <span data-ttu-id="78a1c-258">Если тип не является строкой или считается непреобразуемым, атрибут `GCHandle.Alloc` вызовет `ArgumentException`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-258">If the type is not a string or considered blittable, `GCHandle.Alloc` will throw an `ArgumentException`.</span></span>

<span data-ttu-id="78a1c-259">Указатели структур в определениях необходимо передавать в `ref` или с помощью `unsafe` и `*`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-259">Pointers to structs in definitions must either be passed by `ref` or use `unsafe` and `*`.</span></span>

<span data-ttu-id="78a1c-260">✔️ РЕКОМЕНДУЕТСЯ сопоставить управляемую структуру как можно точнее по форме и именам, которые используются в официальной документации платформы или заголовке.</span><span class="sxs-lookup"><span data-stu-id="78a1c-260">✔️ DO match the managed struct as closely as possible to the shape and names that are used in the official platform documentation or header.</span></span>

<span data-ttu-id="78a1c-261">✔️ РЕКОМЕНДУЕТСЯ использовать C# `sizeof()` вместо `Marshal.SizeOf<MyStruct>()` для непреобразуемых структур, чтобы повысить производительность.</span><span class="sxs-lookup"><span data-stu-id="78a1c-261">✔️ DO use the C# `sizeof()` instead of `Marshal.SizeOf<MyStruct>()` for blittable structures to improve performance.</span></span>

<span data-ttu-id="78a1c-262">❌AVOID `System.Delegate` использует `System.MulticastDelegate` или поля для представления полей указателей функций в структурах.</span><span class="sxs-lookup"><span data-stu-id="78a1c-262">❌ AVOID using `System.Delegate` or `System.MulticastDelegate` fields to represent function pointer fields in structures.</span></span>

<span data-ttu-id="78a1c-263"><xref:System.Delegate?displayProperty=fullName> Поскольку <xref:System.MulticastDelegate?displayProperty=fullName> и не имеют необходимой подписи, они не гарантируют, что делегат, сданный в будет соответствовать подписи родной код ожидает.</span><span class="sxs-lookup"><span data-stu-id="78a1c-263">Since <xref:System.Delegate?displayProperty=fullName> and <xref:System.MulticastDelegate?displayProperty=fullName> don't have a required signature, they don't guarantee that the delegate passed in will match the signature the native code expects.</span></span> <span data-ttu-id="78a1c-264">Кроме того, в .NET Framework и .NET Core, `System.Delegate` `System.MulticastDelegate` перестраивание структуры, содержащей или от его родного представления к управляемому объекту, может дестабилизировать время выполнения, если значение поля в родном представлении не является указателем функции, который обертывает управляемого делегата.</span><span class="sxs-lookup"><span data-stu-id="78a1c-264">Additionally, in .NET Framework and .NET Core, marshaling a struct containing a `System.Delegate` or `System.MulticastDelegate` from its native representation to a managed object can destabilize the runtime if the value of the field in the native representation isn't a function pointer that wraps a managed delegate.</span></span> <span data-ttu-id="78a1c-265">В .NET 5 и более `System.Delegate` поздних версиях не поддерживается перенаправление или `System.MulticastDelegate` поля от родного представления к управляемому объекту.</span><span class="sxs-lookup"><span data-stu-id="78a1c-265">In .NET 5 and later versions, marshaling a `System.Delegate` or `System.MulticastDelegate` field from a native representation to a managed object is not supported.</span></span> <span data-ttu-id="78a1c-266">Используйте определенный тип `System.Delegate` `System.MulticastDelegate`делегата вместо или .</span><span class="sxs-lookup"><span data-stu-id="78a1c-266">Use a specific delegate type instead of `System.Delegate` or `System.MulticastDelegate`.</span></span>

### <a name="fixed-buffers"></a><span data-ttu-id="78a1c-267">Фиксированные буферы</span><span class="sxs-lookup"><span data-stu-id="78a1c-267">Fixed Buffers</span></span>

<span data-ttu-id="78a1c-268">Массив `INT_PTR Reserved1[2]` необходимо маршалировать в два поля `IntPtr`: `Reserved1a` и `Reserved1b`.</span><span class="sxs-lookup"><span data-stu-id="78a1c-268">An array like `INT_PTR Reserved1[2]` has to be marshaled to two `IntPtr` fields, `Reserved1a` and `Reserved1b`.</span></span> <span data-ttu-id="78a1c-269">Если собственный массив имеет простой тип, используйте ключевое слово `fixed`, чтобы записать его более точно.</span><span class="sxs-lookup"><span data-stu-id="78a1c-269">When the native array is a primitive type, we can use the `fixed` keyword to write it a little more cleanly.</span></span> <span data-ttu-id="78a1c-270">Например, `SYSTEM_PROCESS_INFORMATION` в заголовке машинного кода выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="78a1c-270">For example, `SYSTEM_PROCESS_INFORMATION` looks like this in the native header:</span></span>

```c
typedef struct _SYSTEM_PROCESS_INFORMATION {
    ULONG NextEntryOffset;
    ULONG NumberOfThreads;
    BYTE Reserved1[48];
    UNICODE_STRING ImageName;
...
} SYSTEM_PROCESS_INFORMATION
```

<span data-ttu-id="78a1c-271">В C# можно написать такой код:</span><span class="sxs-lookup"><span data-stu-id="78a1c-271">In C#, we can write it like this:</span></span>

```csharp
internal unsafe struct SYSTEM_PROCESS_INFORMATION
{
    internal uint NextEntryOffset;
    internal uint NumberOfThreads;
    private fixed byte Reserved1[48];
    internal Interop.UNICODE_STRING ImageName;
    ...
}
```

<span data-ttu-id="78a1c-272">Но с буферами фиксированного размера возникают некоторые ошибки.</span><span class="sxs-lookup"><span data-stu-id="78a1c-272">However, there are some gotchas with fixed buffers.</span></span> <span data-ttu-id="78a1c-273">Буферы фиксированного размера преобразуемых типов не маршалируются правильно, поэтому локальный массив следует развернуть до нескольких отдельных полей.</span><span class="sxs-lookup"><span data-stu-id="78a1c-273">Fixed buffers of non-blittable types won't be correctly marshaled, so the in-place array needs to be expanded out to multiple individual fields.</span></span> <span data-ttu-id="78a1c-274">Кроме того, в .NET Framework и .NET Core версий, предшествующих 3.0, если структура содержит поле буфера фиксированного размера и вложена в преобразуемую структуру, поле буфера фиксированного размера не маршалируется правильно в машинный код.</span><span class="sxs-lookup"><span data-stu-id="78a1c-274">Additionally, in .NET Framework and .NET Core before 3.0, if a struct containing a fixed buffer field is nested within a non-blittable struct, the fixed buffer field won't be correctly marshaled to native code.</span></span>
